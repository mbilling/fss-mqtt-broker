---
adr: "0006"
title: Consensus & replication for durable sessions
adr_status: Accepted
tasks:
  - id: 0006-P1
    title: Spike + ratify the engine (openraft chosen; engine-agnostic lease/fencing prototype)
    status: done
    date: 2026-06-15
    evidence: lease::overlapping_quorums_cannot_both_commit / superseded_holder_is_fenced; deny.toml cargo-deny gate
  - id: 0006-P2
    title: SessionStore over ReplicatedLog (ReplicatedSessionStore holds no durable state)
    status: done
    date: 2026-06-13
    evidence: logged::state_lives_in_the_log_not_the_store
  - id: 0006-P3a
    title: Epoch-fenced quorum-append core (ClusterLog + ReplicaState over a ReplicaTransport seam)
    status: done
    date: 2026-06-13
    evidence: cluster_log::append_is_quorum_durable_and_assigns_offsets / append_below_quorum_is_rejected_and_leaves_no_committed_hole / stale_leader_is_fenced
  - id: 0006-P3b-i
    title: Networked transport (PeerReplicaTransport over the peer mesh, req_id ack correlation, fail_node)
    status: done
    date: 2026-06-13
    evidence: repl_net::deliver_round_trips_and_applies_on_the_follower / stale_epoch_is_fenced_over_the_wire / fail_node_resolves_in_flight_requests
  - id: 0006-P3b-ii-1
    title: openraft lease state machine + type binding (LeaseMap, declare_raft_types!(LeaseConfig))
    status: done
    date: 2026-06-15
    evidence: lease_raft.rs LeaseMap + declare_raft_types!; assign-monotonic-epoch tests
  - id: 0006-P3b-ii-2
    title: openraft storage (LeaseStore RaftStorage over LeaseMap), passes openraft conformance Suite
    status: done
    date: 2026-06-15
    evidence: lease_store::passes_openraft_conformance_suite_in_memory
  - id: 0006-P3b-ii-3
    title: openraft network + in-memory bring-up (RaftNetwork; single + three-node group commit a lease)
    status: done
    date: 2026-06-15
    evidence: lease_group::single_node_group_elects_and_commits_a_lease / three_node_group_replicates_a_committed_lease
  - id: 0006-P3b-ii-4
    title: Mesh network (raft_mesh carries RaftRpc over the peer bus; leader elected + lease replicated over the wire)
    status: done
    date: 2026-06-15
    evidence: raft_mesh::two_nodes_elect_and_replicate_over_the_wire
  - id: 0006-P3c
    title: Replicated exactly-once state (QoS-2 dedup window + outbound packet-id counter in the replicated snapshot)
    status: done
    date: 2026-06-15
    evidence: logged::qos2_state_replicates_through_the_log; SessionStore record_received/clear_received/received/next_packet_id
  - id: 0006-P4
    title: Wire it in - swap MemorySessionStore for the durable backend; relocated-session owners write through it
    status: done
    date: 2026-06-22
    evidence: main.rs build_durable_node wiring (MQTTD_DATA_DIR); durable_sessions::a_persistent_client_resumes_its_session_on_the_new_owner_after_takeover / enqueue_is_durable_across_a_three_node_cluster
  - id: 0006-P3c-i
    title: Replace in-memory backend O(n) cap count with a rebuildable per-key index
    status: done
    date: 2026-06-24
    evidence: "Already satisfied: ReplicatedLog::live_range is overridden by InMemoryReplicatedLog (repl.rs) with an O(1) VecDeque front/back offset lookup, and logged.rs enqueue computes the cap count as high-low+1 over the contiguous retained range — no whole-log scan. The offset watermarks (maintained on push_back/pop_front, rebuilt from disk on the persistent backend) are the per-key index the note asked for. Introduced with the live_range watermark design (ADR 0017/0018)."
  - id: 0006-T5
    title: "Under-replication is visible and gateable — replication-health gauges + /statusz block, and the opt-in min-replicas write floor (issue #167)"
    status: done
    date: 2026-08-12
    evidence: "The §4 contract ('R=3, quorum=2') silently degraded with membership: replica sets truncate to min(R, members) (placement.rs), quorum is len/2+1 (cluster_log.rs), so a lone node acked durable writes at quorum-of-1 while the operator believed R=3 — confirmed by the 2026-08-11 review-panel audit. Visibility: Placement::replication_health() (shipped earlier, PR #177) now feeds the replication_desired / replication_min_actual gauges (Prometheus + OTLP, set from the hub metrics tick) and a /statusz 'replication' block {desired, min_actual, under_replicated, write_floor} beside the members list; the reconcile driver's edge-triggered WARN/recovery log stands. Gating: durable.min_replicas (MQTTD_MIN_REPLICAS, default 1 = off — a lone node stays fully operational, the standing requirement): GroupRoutedLog::append refuses with the new ReplError::UnderReplicated{actual, floor} when the key's group replica set — the same set the append would replicate over — is below the floor; mapped to StorageError::Unavailable so every retry/queue path behaves exactly as for NoQuorum (QoS>=1 acks withheld, sources redeliver; retained mutations queue until capacity returns), while reads/truncate/remove stay ungated (serve what was already promised). Config validate rejects 0 (the meaningful 'no floor' is 1); a floor above the replication factor is rejected at startup as unsatisfiable; startup logs the active floor. Tests: appends_below_the_min_replicas_floor_are_refused_until_capacity_returns (lone node with floor 2 refuses with actual=1; a follower joining lifts it and the append commits at quorum 2/2; the set shrinking re-arms the refusal), statusz_reports... extended with the exact replication block (2 members under R=3 -> under_replicated:true), config default/TOML/env/reject-0 tests. ADR 0006 §4 'As delivered' amendment records both halves and the availability trade."
  - id: 0006-T6
    title: "The min-replicas write floor is ON by default, derived from known membership (issue #239)"
    status: done
    date: 2026-08-13
    evidence: "0006-T5 shipped the gate but left it opt-in, so the SHIPPED posture was still the audited defect: a group degraded to one live member acked durable writes on ONE copy while the operator believed R=3 (docs/COMPARISON.md admitted it in-line). Config: durable.min_replicas becomes MinReplicas::{Count(u32), Majority} (serde over an untagged integer-or-word raw shape; only the exact word 'majority' is accepted, 0 and any other word are loud errors), defaulting to Majority; MQTTD_MIN_REPLICAS was wired in overlay_from but MISSING from ENV_VARS, so the totality sweep had never exercised it — added (curated count 72 -> 73). Resolution: the new placement::WriteFloor{Fixed(usize), Majority{declared}} is resolved by Placement::min_replicas() — the SAME single point cluster_store.rs's gate already read, so no parallel mechanism — as min(R, witness)/2+1, witness = the quorum-committed durable roster (#229) when pushed AND NON-EMPTY (an empty roster is an uninitialised raft group, never a one-member cluster: it must not disarm the floor or discard the fallback witness), else the high-water observed membership (peak_members, latched in observe() so it cannot follow liveness down), never below declared = runtime.ready_min_members. That is 1 alone-and-never-peered (the standing lone-node requirement) and 2 once the cluster is known; capped at R, so it is satisfiable by construction, costs ZERO availability at 2 or 3 live members (the write quorum is already len/2+1, so the one-at-a-time roll is untouched), and stays 2 on 5 or 7 nodes — the cap is stated in every place the rule is summarised (README headline + Limitations + env table, COMPARISON durability cell, the startup INFO, the reference TOML, the ADR). Placement::new keeps Fixed(1), so every existing unit harness keeps its meaning; the policy is injected only at assembly. What the gate enforces is stated exactly rather than over-promised: it bounds the replica-set SIZE, not the number of acks the commit waits for (quorum is len/2+1), so an explicit min_replicas = 3 commits on 2 of a 3-member set. The derived default has no such gap (a set of 2 or 3 needs 2 acks and the floor is 2); the config doc comment, the reference TOML, the startup log and ADR 0006 all say so. Gate placement: the floor moved INSIDE GroupRoutedLog::log_for_key (a new gate_write_floor flag, set only by append) so it is judged against the replica set the ClusterLog is actually built over, under a SINGLE placement-lock acquisition — the previous shape checked actual/floor under one guard, dropped it, and rebuilt the set under another, so a membership shrink landing between them could commit the append over the smaller set at quorum-of-1. Assembly: main.rs resolve_write_floor() — called from startup, from check_config_inner (so --check-config rejects an unsatisfiable explicit floor BEFORE a rollout) and from runtime_precheck (the ADR 0046 T4 live-reload acceptance gate; durable is restart-scoped, so a reload that accepted an unsatisfiable floor would brick the NEXT boot hours later, detached from the reload). It returns Fixed(1) when durable.enabled = false — the placement ring is still built and still reported there, so without this /statusz and the new PAGE rule would fire on a non-durable cluster that refuses nothing — while still enforcing the <= R bound in both modes. Plus an unconditional startup INFO stating the posture and a WARN when the floor is explicitly disabled on a node declaring ready_min_members >= 2. Operator surface: the new replication_write_floor gauge (Prometheus + OTLP) beside replication_desired/min_actual, set from the same hub metrics tick under the same guard, and /statusz's replication block gains write_floor_source (derived|configured) so a floor of 1 reads unambiguously ('alone' vs 'disabled'); min_actual < write_floor is the documented PAGE condition (OPERATIONS), min_actual < desired stays the warn. Tests: placement::the_derived_write_floor_is_one_alone_and_two_once_the_cluster_is_known, the_durable_roster_is_the_authority_for_the_derived_floor, a_declared_member_count_arms_the_write_floor_before_gossip_or_the_roster, an_explicit_floor_overrides_the_derived_one_in_both_directions, an_empty_durable_roster_is_not_a_membership_witness; cluster_store::a_lone_survivor_of_a_known_three_node_group_refuses_appends_by_default (re-stamps the shrunken replica sets to model the CONVERGED post-shrink state — without that the append fails NoQuorum whatever the floor says; with it, reverting the resolver yields Ok(2), the defect itself) and a_node_that_never_knew_a_cluster_acks_durable_writes_under_the_default_floor; metrics::counters_and_gauges_move_and_render extended to drive set_replication_health and assert all three rendered replication gauges (the write-floor gauge is the only alertable surface for the refusal, so a rename or a dropped .set() must fail a test); health::statusz_reports_the_derived_write_floor_and_its_source; main::a_non_durable_node_resolves_no_write_floor and runtime_precheck_rejects_what_startup_would_reject extended with the unsatisfiable floor; check_config::check_config_rejects_a_min_replicas_floor_above_the_replication_factor. END TO END (the correction that matters most): the refusal IS reachable at R=3, and cluster_proc::the_default_write_floor_arms_itself_and_refuses_the_single_copy_promise now asserts it on real spawned binaries under the shipped default. An earlier draft of this record, of ADR 0006 residual gap (iii) and of a comment in that test claimed the opposite — 'with R=3 every state whose replica set is 1 also has no lease quorum, so the refusal is not reachable end-to-end at this cluster size'. That claim was FALSE and is withdrawn: LocalLeaseSource::epoch_for (cluster_store.rs:79-100) is a purely local read of the applied lease map with no leadership, quorum or TTL check, and reassignment is what needs a quorum, so a lone survivor keeps appending to the groups it already leased. The earlier 'unreachable' observation was a scenario artifact — the one subscriber probed happened to be owned by a killed node, so the publish was acked-and-dropped by the no-known-subscriber path. The test now picks ONE durable client id per node from the broker's own HRW hash (placement::group_of + Placement::hrw_owner) before anything spawns and connects each subscriber to ITS owner (session hosted there, no ADR 0005 relocation, socket survives the other kills); chooses the survivor by raft role from /statusz; and gates the probe on TWO polled convergence conditions on the operator's own surface: membership/floor via /statusz replication, and /statusz replica_groups.current == tracked — the ADR 0043 P1 re-stamp, which is REQUIRED because enqueue_with_expiry does live_range (ungated) before append (gated), so before the re-stamp the enqueue fails EARLIER with NoQuorum and the floor is masked. It then asserts: no PUBACK across a 45s budget of fresh-connection attempts; the survivor's log carrying 'withholding the publisher's ack' together with 'replica set holds 1 of the configured floor of 2 copies' for the survivor-owned client id (a withheld ack alone cannot distinguish the floor from NotOwner/NoQuorum, so the REASON is the load-bearing assertion); and mqttd_durable_append_failures_total{reason=\"unavailable\"} moving. Falsification is one env var: with MQTTD_MIN_REPLICAS=1 the same scenario ACKS a durable promise held on ONE copy at ~8s with zero refusal lines — issue #239 reproduced on spawned binaries. Parts 2 and 4 were also de-vacuified: they had asserted only that a PUBACK arrived, which hub.rs also does for a publish with no known subscriber, so 'costs no availability' could pass on an ack that promised nothing; they now assert the payload is actually RECEIVED by the durable subscriber, and part 3 additionally pins the page rule's gauges (replication_write_floor = 2, replication_min_actual = 1). Genuinely uncovered and now recorded instead of implied (ADR 0006 residual gap (iv) + README Limitations + the README headline sentence, which is scoped rather than left absolute): a publish for a durable session whose OWNER is gone is still acked and dropped by the pre-existing no-known-subscriber path, with no refusal logged and no publish_dropped{reason} distinguishing it. The floor covers writes that reach a group this node leases. Docs: the operator remedy is now ONE consistent story in three places — README Limitations, OPERATIONS 'Down and the write floor' and ADR 0006 residual gap (ii) — namely durable.min_replicas = 1, restart-scoped (requires_restart), with the UNCONSENTED-loss / DR-restore case named explicitly: the persisted roster arms the floor by itself there, so lowering ready_min_members does NOTHING (the earlier text prescribed exactly that). docs/TROUBLESHOOTING.md gains a 'QoS>=1 publishers stop getting PUBACKs (and get disconnected)' section — the new client-visible symptom, with the warn line, the /statusz block, the two metrics and the remedy — and its 'UNDER-REPLICATED warnings' section no longer says under-replicated appends 'commit on a smaller quorum' and are 'still quorum-durable at the reduced size', which is false under the shipped default. ADR 0006's #167 'As delivered' note carries a partly-superseded marker (the repo's convention) so a reader does not hit the old default first. Rollback (ADR 0058 §E), recorded in the ADR note and in the reference TOML: this widened an existing key's value shape from integer to integer-or-'majority' and the example ships the word form, so a config carrying the word CANNOT be read by the previous release (a type mismatch, which runtime.config_unknown_keys = 'warn' does not rescue). Pre-1.0 that is allowed; the remedy for a roll-back-safe config is to spell the floor as an integer or omit the key. Deferred to a follow-up: StorageError::UnderReplicated + reason='under-replicated' counter and a v5 DISCONNECT 0x89 reason string — the new gauge and the 'unavailable' reason already support the alert rules. ADR 0006 §4 carries a second dated 'As delivered' note with the roster-not-liveness argument, the zero-availability-cost argument, what the gate enforces exactly, and residual gaps (i)-(iv)."
---

# Delivery — ADR 0006: Consensus & replication for durable sessions

Decision: [docs/adr/0006-consensus-and-replication.md](../adr/0006-consensus-and-replication.md).

## Plan

The decision's "Phasing (workstream E)" (spike, SessionStore-over-log, the consensus-backed
log in sub-steps 3a/3b/3c, then wire-in) decomposes into these tasks. The engine selection
is ratified (openraft); realization tasks are marked done only where concrete code/tests
exist. Each carries a stable id used by commits, tests, and the dashboard.

| Task | Acceptance criterion |
|------|----------------------|
| **0006-P1** Spike + engine | openraft is ratified against `deny.toml` (raft-rs rejected for an active DoS); the engine-agnostic ownership-lease/fencing prototype (`mqtt-cluster::lease`) pins split-brain safety (two epochs can never both reach quorum; a superseded holder is fenced). |
| **0006-P2** Store over log | `ReplicatedSessionStore` implements the full `SessionStore` over a `ReplicatedLog`, holding no durable state of its own; a second store over the same log sees the first's sessions in full. |
| **0006-P3a** Quorum-append core | `ClusterLog` implements `ReplicatedLog` by epoch-fenced quorum replication behind a `ReplicaTransport` seam; sans-I/O simulation pins quorum-durable append, single-replica-loss survival (R=3/q=2), below-quorum rejection with no committed hole, stale-leader fencing, and lazy local truncation. |
| **0006-P3b-i** Networked transport | `PeerReplicaTransport` realizes the `ReplicaTransport` seam over the peer mesh (`Replicate`/`ReplicateAck`, `req_id` correlation, `fail_node` on drop); pinned over real framed streams including fencing and disconnect. |
| **0006-P3b-ii-1** Raft types | `lease_raft` defines the replicated `LeaseMap` (`group -> (holder, epoch)`, monotonic epoch) and binds it to openraft via `declare_raft_types!(LeaseConfig)` with a compile-assert it is a valid `RaftTypeConfig`. |
| **0006-P3b-ii-2** Raft storage | `LeaseStore` implements openraft's `RaftStorage` over `LeaseMap` and passes openraft's conformance `Suite`. |
| **0006-P3b-ii-3** Raft network + bring-up | `lease_group` implements `RaftNetwork` and brings up a real group; a single-node group elects and commits a lease, and a three-node group elects a leader and replicates a committed lease to every replica. |
| **0006-P3b-ii-4** Mesh network | `raft_mesh` carries the same RPCs over the peer bus (`RaftRpc`/`RaftRpcReply`, `req_id`-correlated); a leader is elected and a committed lease replicated across nodes over the wire. |
| **0006-P3c** Exactly-once state | `SessionStore` gains `record_received`/`clear_received`/`received`/`next_packet_id`; `ReplicatedSessionStore` stores the QoS-2 dedup window and outbound packet-id counter in the replicated snapshot, so exactly-once survives failover. |
| **0006-P4** Wire it in | `mqttd` swaps `MemorySessionStore` for the durable backend so relocated-session owners write through it; ephemeral sessions become durable. |
| **0006-P3c-i** Cap-count index | The in-memory backend's O(n) cap count is replaced by a rebuildable per-key index. |

## Progress

<!-- status-table:0006 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0006-P1 | ✅ done | 2026-06-15 | lease::overlapping_quorums_cannot_both_commit / superseded_holder_is_fenced; deny.toml cargo-deny gate |
| 0006-P2 | ✅ done | 2026-06-13 | logged::state_lives_in_the_log_not_the_store |
| 0006-P3a | ✅ done | 2026-06-13 | cluster_log::append_is_quorum_durable_and_assigns_offsets / append_below_quorum_is_rejected_and_leaves_no_committed_hole / stale_leader_is_fenced |
| 0006-P3b-i | ✅ done | 2026-06-13 | repl_net::deliver_round_trips_and_applies_on_the_follower / stale_epoch_is_fenced_over_the_wire / fail_node_resolves_in_flight_requests |
| 0006-P3b-ii-1 | ✅ done | 2026-06-15 | lease_raft.rs LeaseMap + declare_raft_types!; assign-monotonic-epoch tests |
| 0006-P3b-ii-2 | ✅ done | 2026-06-15 | lease_store::passes_openraft_conformance_suite_in_memory |
| 0006-P3b-ii-3 | ✅ done | 2026-06-15 | lease_group::single_node_group_elects_and_commits_a_lease / three_node_group_replicates_a_committed_lease |
| 0006-P3b-ii-4 | ✅ done | 2026-06-15 | raft_mesh::two_nodes_elect_and_replicate_over_the_wire |
| 0006-P3c | ✅ done | 2026-06-15 | logged::qos2_state_replicates_through_the_log; SessionStore record_received/clear_received/received/next_packet_id |
| 0006-P4 | ✅ done | 2026-06-22 | main.rs build_durable_node wiring (MQTTD_DATA_DIR); durable_sessions::a_persistent_client_resumes_its_session_on_the_new_owner_after_takeover / enqueue_is_durable_across_a_three_node_cluster |
| 0006-P3c-i | ✅ done | 2026-06-24 | "Already satisfied: ReplicatedLog::live_range is overridden by InMemoryReplicatedLog (repl.rs) with an O(1) VecDeque front/back offset lookup, and logged.rs enqueue computes the cap count as high-low+1 over the contiguous retained range — no whole-log scan. The offset watermarks (maintained on push_back/pop_front, rebuilt from disk on the persistent backend) are the per-key index the note asked for. Introduced with the live_range watermark design (ADR 0017/0018)." |
| 0006-T5 | ✅ done | 2026-08-12 | "The §4 contract ('R=3, quorum=2') silently degraded with membership: replica sets truncate to min(R, members) (placement.rs), quorum is len/2+1 (cluster_log.rs), so a lone node acked durable writes at quorum-of-1 while the operator believed R=3 — confirmed by the 2026-08-11 review-panel audit. Visibility: Placement::replication_health() (shipped earlier, PR #177) now feeds the replication_desired / replication_min_actual gauges (Prometheus + OTLP, set from the hub metrics tick) and a /statusz 'replication' block {desired, min_actual, under_replicated, write_floor} beside the members list; the reconcile driver's edge-triggered WARN/recovery log stands. Gating: durable.min_replicas (MQTTD_MIN_REPLICAS, default 1 = off — a lone node stays fully operational, the standing requirement): GroupRoutedLog::append refuses with the new ReplError::UnderReplicated{actual, floor} when the key's group replica set — the same set the append would replicate over — is below the floor; mapped to StorageError::Unavailable so every retry/queue path behaves exactly as for NoQuorum (QoS>=1 acks withheld, sources redeliver; retained mutations queue until capacity returns), while reads/truncate/remove stay ungated (serve what was already promised). Config validate rejects 0 (the meaningful 'no floor' is 1); a floor above the replication factor is rejected at startup as unsatisfiable; startup logs the active floor. Tests: appends_below_the_min_replicas_floor_are_refused_until_capacity_returns (lone node with floor 2 refuses with actual=1; a follower joining lifts it and the append commits at quorum 2/2; the set shrinking re-arms the refusal), statusz_reports... extended with the exact replication block (2 members under R=3 -> under_replicated:true), config default/TOML/env/reject-0 tests. ADR 0006 §4 'As delivered' amendment records both halves and the availability trade." |
| 0006-T6 | ✅ done | 2026-08-13 | "0006-T5 shipped the gate but left it opt-in, so the SHIPPED posture was still the audited defect: a group degraded to one live member acked durable writes on ONE copy while the operator believed R=3 (docs/COMPARISON.md admitted it in-line). Config: durable.min_replicas becomes MinReplicas::{Count(u32), Majority} (serde over an untagged integer-or-word raw shape; only the exact word 'majority' is accepted, 0 and any other word are loud errors), defaulting to Majority; MQTTD_MIN_REPLICAS was wired in overlay_from but MISSING from ENV_VARS, so the totality sweep had never exercised it — added (curated count 72 -> 73). Resolution: the new placement::WriteFloor{Fixed(usize), Majority{declared}} is resolved by Placement::min_replicas() — the SAME single point cluster_store.rs's gate already read, so no parallel mechanism — as min(R, witness)/2+1, witness = the quorum-committed durable roster (#229) when pushed AND NON-EMPTY (an empty roster is an uninitialised raft group, never a one-member cluster: it must not disarm the floor or discard the fallback witness), else the high-water observed membership (peak_members, latched in observe() so it cannot follow liveness down), never below declared = runtime.ready_min_members. That is 1 alone-and-never-peered (the standing lone-node requirement) and 2 once the cluster is known; capped at R, so it is satisfiable by construction, costs ZERO availability at 2 or 3 live members (the write quorum is already len/2+1, so the one-at-a-time roll is untouched), and stays 2 on 5 or 7 nodes — the cap is stated in every place the rule is summarised (README headline + Limitations + env table, COMPARISON durability cell, the startup INFO, the reference TOML, the ADR). Placement::new keeps Fixed(1), so every existing unit harness keeps its meaning; the policy is injected only at assembly. What the gate enforces is stated exactly rather than over-promised: it bounds the replica-set SIZE, not the number of acks the commit waits for (quorum is len/2+1), so an explicit min_replicas = 3 commits on 2 of a 3-member set. The derived default has no such gap (a set of 2 or 3 needs 2 acks and the floor is 2); the config doc comment, the reference TOML, the startup log and ADR 0006 all say so. Gate placement: the floor moved INSIDE GroupRoutedLog::log_for_key (a new gate_write_floor flag, set only by append) so it is judged against the replica set the ClusterLog is actually built over, under a SINGLE placement-lock acquisition — the previous shape checked actual/floor under one guard, dropped it, and rebuilt the set under another, so a membership shrink landing between them could commit the append over the smaller set at quorum-of-1. Assembly: main.rs resolve_write_floor() — called from startup, from check_config_inner (so --check-config rejects an unsatisfiable explicit floor BEFORE a rollout) and from runtime_precheck (the ADR 0046 T4 live-reload acceptance gate; durable is restart-scoped, so a reload that accepted an unsatisfiable floor would brick the NEXT boot hours later, detached from the reload). It returns Fixed(1) when durable.enabled = false — the placement ring is still built and still reported there, so without this /statusz and the new PAGE rule would fire on a non-durable cluster that refuses nothing — while still enforcing the <= R bound in both modes. Plus an unconditional startup INFO stating the posture and a WARN when the floor is explicitly disabled on a node declaring ready_min_members >= 2. Operator surface: the new replication_write_floor gauge (Prometheus + OTLP) beside replication_desired/min_actual, set from the same hub metrics tick under the same guard, and /statusz's replication block gains write_floor_source (derived|configured) so a floor of 1 reads unambiguously ('alone' vs 'disabled'); min_actual < write_floor is the documented PAGE condition (OPERATIONS), min_actual < desired stays the warn. Tests: placement::the_derived_write_floor_is_one_alone_and_two_once_the_cluster_is_known, the_durable_roster_is_the_authority_for_the_derived_floor, a_declared_member_count_arms_the_write_floor_before_gossip_or_the_roster, an_explicit_floor_overrides_the_derived_one_in_both_directions, an_empty_durable_roster_is_not_a_membership_witness; cluster_store::a_lone_survivor_of_a_known_three_node_group_refuses_appends_by_default (re-stamps the shrunken replica sets to model the CONVERGED post-shrink state — without that the append fails NoQuorum whatever the floor says; with it, reverting the resolver yields Ok(2), the defect itself) and a_node_that_never_knew_a_cluster_acks_durable_writes_under_the_default_floor; metrics::counters_and_gauges_move_and_render extended to drive set_replication_health and assert all three rendered replication gauges (the write-floor gauge is the only alertable surface for the refusal, so a rename or a dropped .set() must fail a test); health::statusz_reports_the_derived_write_floor_and_its_source; main::a_non_durable_node_resolves_no_write_floor and runtime_precheck_rejects_what_startup_would_reject extended with the unsatisfiable floor; check_config::check_config_rejects_a_min_replicas_floor_above_the_replication_factor. END TO END (the correction that matters most): the refusal IS reachable at R=3, and cluster_proc::the_default_write_floor_arms_itself_and_refuses_the_single_copy_promise now asserts it on real spawned binaries under the shipped default. An earlier draft of this record, of ADR 0006 residual gap (iii) and of a comment in that test claimed the opposite — 'with R=3 every state whose replica set is 1 also has no lease quorum, so the refusal is not reachable end-to-end at this cluster size'. That claim was FALSE and is withdrawn: LocalLeaseSource::epoch_for (cluster_store.rs:79-100) is a purely local read of the applied lease map with no leadership, quorum or TTL check, and reassignment is what needs a quorum, so a lone survivor keeps appending to the groups it already leased. The earlier 'unreachable' observation was a scenario artifact — the one subscriber probed happened to be owned by a killed node, so the publish was acked-and-dropped by the no-known-subscriber path. The test now picks ONE durable client id per node from the broker's own HRW hash (placement::group_of + Placement::hrw_owner) before anything spawns and connects each subscriber to ITS owner (session hosted there, no ADR 0005 relocation, socket survives the other kills); chooses the survivor by raft role from /statusz; and gates the probe on TWO polled convergence conditions on the operator's own surface: membership/floor via /statusz replication, and /statusz replica_groups.current == tracked — the ADR 0043 P1 re-stamp, which is REQUIRED because enqueue_with_expiry does live_range (ungated) before append (gated), so before the re-stamp the enqueue fails EARLIER with NoQuorum and the floor is masked. It then asserts: no PUBACK across a 45s budget of fresh-connection attempts; the survivor's log carrying 'withholding the publisher's ack' together with 'replica set holds 1 of the configured floor of 2 copies' for the survivor-owned client id (a withheld ack alone cannot distinguish the floor from NotOwner/NoQuorum, so the REASON is the load-bearing assertion); and mqttd_durable_append_failures_total{reason=\"unavailable\"} moving. Falsification is one env var: with MQTTD_MIN_REPLICAS=1 the same scenario ACKS a durable promise held on ONE copy at ~8s with zero refusal lines — issue #239 reproduced on spawned binaries. Parts 2 and 4 were also de-vacuified: they had asserted only that a PUBACK arrived, which hub.rs also does for a publish with no known subscriber, so 'costs no availability' could pass on an ack that promised nothing; they now assert the payload is actually RECEIVED by the durable subscriber, and part 3 additionally pins the page rule's gauges (replication_write_floor = 2, replication_min_actual = 1). Genuinely uncovered and now recorded instead of implied (ADR 0006 residual gap (iv) + README Limitations + the README headline sentence, which is scoped rather than left absolute): a publish for a durable session whose OWNER is gone is still acked and dropped by the pre-existing no-known-subscriber path, with no refusal logged and no publish_dropped{reason} distinguishing it. The floor covers writes that reach a group this node leases. Docs: the operator remedy is now ONE consistent story in three places — README Limitations, OPERATIONS 'Down and the write floor' and ADR 0006 residual gap (ii) — namely durable.min_replicas = 1, restart-scoped (requires_restart), with the UNCONSENTED-loss / DR-restore case named explicitly: the persisted roster arms the floor by itself there, so lowering ready_min_members does NOTHING (the earlier text prescribed exactly that). docs/TROUBLESHOOTING.md gains a 'QoS>=1 publishers stop getting PUBACKs (and get disconnected)' section — the new client-visible symptom, with the warn line, the /statusz block, the two metrics and the remedy — and its 'UNDER-REPLICATED warnings' section no longer says under-replicated appends 'commit on a smaller quorum' and are 'still quorum-durable at the reduced size', which is false under the shipped default. ADR 0006's #167 'As delivered' note carries a partly-superseded marker (the repo's convention) so a reader does not hit the old default first. Rollback (ADR 0058 §E), recorded in the ADR note and in the reference TOML: this widened an existing key's value shape from integer to integer-or-'majority' and the example ships the word form, so a config carrying the word CANNOT be read by the previous release (a type mismatch, which runtime.config_unknown_keys = 'warn' does not rescue). Pre-1.0 that is allowed; the remedy for a roll-back-safe config is to spell the floor as an integer or omit the key. Deferred to a follow-up: StorageError::UnderReplicated + reason='under-replicated' counter and a v5 DISCONNECT 0x89 reason string — the new gauge and the 'unavailable' reason already support the alert rules. ADR 0006 §4 carries a second dated 'As delivered' note with the roster-not-liveness argument, the zero-availability-cost argument, what the gate enforces exactly, and residual gaps (i)-(iv)." |
<!-- /status-table:0006 -->

**Note carried from the ADR:** the engine selection (P1) is the *ratified* decision —
openraft chosen, ADR unchanged. The durable store itself is realized by these phases and
its on-disk form by ADR 0018; the `ReplicatedLog` interface is the v1 seam and may evolve.

## Changelog

- **2026-06-22** — P4 wire-in landed: `mqttd` builds the durable node (`build_durable_node`,
  `MQTTD_DATA_DIR`) so relocated-session owners write through the durable backend; cluster
  takeover and durable-enqueue proven in `durable_sessions`.
- **2026-06-15** — P1 engine ratified (openraft over raft-rs); P3b-ii openraft lease manager
  landed in four parts (types, storage + conformance Suite, in-memory bring-up, mesh network);
  P3c replicated exactly-once state. Cap-count index split out as P3c-i (deferred).
- **2026-06-13** — P2 SessionStore-over-log layering proof; P3a epoch-fenced quorum-append
  core; P3b-i networked replica transport.
