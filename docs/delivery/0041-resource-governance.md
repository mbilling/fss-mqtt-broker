---
adr: "0041"
title: Resource governance (admission caps, per-client quotas, bounded state)
adr_status: Accepted
tasks:
  - id: 0041-T1
    title: Admission caps — global + per-IP connection limits enforced at accept (pre-TLS close), bounded per-IP accounting, metrics; env-tunable with generous defaults
    status: done
    date: 2026-07-06
    evidence: "New mqttd::admission module: AdmissionGate hands out RAII AdmissionPermits — the accept loop asks try_admit(ip) BEFORE spawning any per-connection work (in particular before the TLS handshake; QUIC refuses via Incoming::refuse before the handshake future), moves the permit into the connection task, and the slot frees itself however the task ends. All five listeners gated (TCP-TLS, plaintext, WS, WSS, QUIC). One mutex over both counts (accept-rate, not per-message); the per-IP table removes entries at zero, so it is bounded by live connections — each entry is backed by a real socket, so the accounting can never outgrow the resource it guards. MQTTD_MAX_CONNECTIONS / MQTTD_MAX_CONNECTIONS_PER_IP (admission_gate_from_env): unset = uncapped (today's behavior), a non-positive or unparseable value is a startup error. Rejections counted (admission_rejected_total{reason=max-connections|per-ip}, Prometheus + OTel — bounded reasons, never per-address) and debug-logged. README config table documents both vars. Tests: admission unit (global cap refuses at bound and a freed slot readmits; per-IP independent across addresses and the table shrinks to empty; unconfigured admits 1000 with an empty table) and two process-level acceptance tests through the real binary (binary_smoke): max_connections_cap_refuses_at_accept_and_recovers (third concurrent connection closed with no CONNACK, admitted clients keep a pub/sub round-trip working, a dropped client's slot recycles) and per_ip_cap_refuses_a_second_connection_from_the_same_address. Workspace green (785 tests), clippy zero warnings. Noted: one unrelated pre-existing load-dependent flake observed once (mqtt-cluster a_takeover_recovers_the_retained_value_and_its_token; 5/5 green isolated, 3/3 green per-crate, full-workspace rerun green) — a candidate exhibit for pre-release area 4's stress harness.
  - id: 0041-T2
    title: Auth-failure pushback — per-source-IP decaying penalty box; penalized addresses closed at accept before any Argon2 work; audited + counted; bounded table
    status: done
    date: 2026-07-06
    evidence: "The AdmissionGate gains the penalty box: conn::handle_stream now returns a ConnOutcome whose auth_failed flag is set ONLY by an authenticator rejection (authorization denials never set it; a proxied session's flag is deliberately dropped — its peer address is the RELAYING NODE, which must never be penalized for a client's bad credentials). Every accept-loop wrapper (TCP-TLS, plaintext, WS, WSS, QUIC) reports the failure to gate.record_auth_failure(ip): strikes are integral additions on a CONTINUOUS decay (Strikes { level, at }, recomputed lazily), an address is penalized once it crosses MQTTD_AUTH_PENALTY_THRESHOLD and stays refused until a full strike decays (> threshold-1 — comparing >= threshold would un-penalize an epsilon after crossing), with MQTTD_AUTH_PENALTY_DECAY_SECS (default 60) per strike. While penalized, try_admit refuses BEFORE the global/per-IP checks and before any TLS or Argon2 work (admission_rejected_total{reason=penalty}); crossing the threshold is audited (security.penalty) and warn-logged. The table is hard-bounded (PENALTY_TABLE_MAX 4096, evict-oldest) so address spraying cycles it instead of growing it; the penalty keys on the source address, never a username (a username-keyed lockout would be a DoS lever aimed at victims — ADR alternative). Unset threshold = disabled (today's behavior); values validated at startup. README documents both vars. Tests: admission unit (threshold crossing refuses while a different address is unaffected and the penalty decays back; 8192-address spray never exceeds the bound) and a process-level acceptance test through the real binary (repeated_auth_failures_penalize_the_source_address_then_decay): two bad passwords from 127.0.0.2 penalize it — its next connection with CORRECT credentials is closed with no CONNACK — while 127.0.0.1 authenticates normally throughout, and polling shows the decay readmitting it. Workspace green (788 tests), clippy zero warnings.
  - id: 0041-T3
    title: Per-client quotas — max subscriptions per client (per-filter 0x97/0x80 SUBACK), publish-rate token bucket with read-pause throttling, inbound QoS 1 Receive Maximum enforcement (closes the ADR 0012 §3 deferral)
    status: done
    date: 2026-07-06
    evidence: "Three quotas. SUBSCRIPTIONS (MQTTD_MAX_SUBSCRIPTIONS_PER_CLIENT -> hub Quotas via HubCommand::SetQuotas, sent before any listener accepts): HubCommand::Subscribe gains a reply channel — the hub computes one verdict per ACL-granted filter (an already-held filter replaces, never consumes quota) and answers BEFORE any retained replay, so the connection's SUBACK still precedes replayed publishes on the wire; a denied filter answers 0x97 Quota exceeded (v5) / 0x80 (v3.1.1) in its SUBACK slot while in-cap filters in the same packet are granted; denials counted (quota_rejections_total{kind=subscriptions}). PUBLISH RATE (MQTTD_MAX_PUBLISH_RATE msgs/s -> WireLimits.publish_rate): a per-connection token bucket with a one-second burst, taken in the serve loop BEFORE processing an inbound PUBLISH — an empty bucket sleeps there, with the socket unread, so the over-rate publisher backs up its own TCP window (transport-native backpressure); nothing dropped, nothing disconnected; acks/pings/subscribes flow freely. RECEIVE MAXIMUM QoS 1 (closing ADR 0012 §3): the advertised window counts QoS 1 and QoS 2 publications TOGETHER per spec — a QoS 1 publish arriving with qos2_inflight already at the maximum is a flow-control breach, DISCONNECT 0x93, exactly as QoS 2 already behaved (the purely-serial QoS 1 path alone can never overrun, which is why the deferral looked unobservable until counted jointly). Tests (tests/quotas.rs, own process for the WireLimits global): the_subscription_quota_denies_excess_filters_per_suback_slot (v5 [0,0,0x97] in one packet, re-subscribe at cap granted, new filter denied, granted subscriptions still deliver, v3.1.1 gets 0x80), an_over_rate_publisher_is_throttled_without_loss_or_disconnect (12 QoS1 messages all acked+delivered, elapsed proves the slowdown, a second client's publish interleaves mid-throttle rather than queueing behind it, PING still answers), a_qos1_publish_beyond_the_shared_receive_maximum_gets_0x93 (open QoS2 window + QoS1 publish -> 0x93). The conn stub hub grants verdicts like an uncapped hub, keeping unit tests faithful. README documents both vars. Workspace green (791 tests), clippy zero warnings.
  - id: 0041-T4
    title: Global state caps — retained-topic cap (growth refused, maintenance always allowed), max-sessions cap (resume always allowed), MQTT 5 Maximum Packet Size negotiated from the transport cap, QueueLimits env wiring
    status: done
    date: 2026-07-06
    evidence: "Growth is a grant; maintenance never is. RETAINED (MQTTD_MAX_RETAINED_MESSAGES -> Quotas.max_retained_messages): the hub's publish dispatch refuses a retained publish that would CREATE a new topic beyond the cap — the ack-gate channel now carries a PublishOutcome (Accepted | RetainedQuotaExceeded) and the Publish command a v5 flag: a v5 publisher gets PUBACK/PUBREC 0x97 with nothing delivered or retained (a refused PUBREC ends the QoS2 flow: no Receive-Maximum slot consumed, dedup record released, id reusable), while v3.1.1 (no reason codes) is delivered live with retention stripped (retain=false also on the peer forward, so remote nodes do not retain what the origin refused), warn-logged and counted (quota_rejections_total{kind=retained}); overwrite and clear always work at the cap and a clear frees a slot. SESSIONS (MQTTD_MAX_SESSIONS -> Quotas.max_sessions): finish_attach refuses only a NEW session — resume (session_present) and locally-known ids (takeover, clean-start replacement) always pass — with AttachOutcome::QuotaExceeded -> CONNACK 0x97 (v5) / Server unavailable 0x03 (v3.1.1: no quota code; 'try elsewhere' is the honest translation); count = online ∪ session-expiry (no double-count). MAX PACKET SIZE (MQTTD_MAX_PACKET_SIZE, default 1 MiB, floor 1 KiB): the mqtt-net frame cap becomes a OnceLock the binary sets from the SAME value the CONNACK advertises as the MQTT 5 Maximum Packet Size (the placeholder const the frame module carried since ADR 0002 is paid off — cap and contract cannot drift); outbound, the connection records the CLIENT's advertised maximum and drops a larger publish for that subscriber alone (measured on the encoded frame after the alias rewrite, publish_dropped{too-large}), per spec. QUEUE LIMITS: already operator-wired (MQTTD_MAX_QUEUED_MESSAGES / MQTTD_QUEUE_OVERFLOW existed in main.rs + README — the ADR survey's 'test-only constructor' finding was stale; recorded here as verified rather than re-built). Tests (tests/quotas.rs): the_retained_quota_refuses_growth_but_allows_maintenance (0x97 on third topic; overwrite + clear at cap; clear frees a slot; v3.1.1 delivered-live-not-retained proven via live subscriber + fresh-subscriber replay), the_session_quota_refuses_new_sessions_but_never_resumes (0x97 v5 / refused v3.1.1 stranger; resume through the full cap; clean-session slot recycles), an_oversized_outbound_publish_is_dropped_for_that_subscriber_alone (CONNACK advertises the ceiling; 4 KiB publish reaches the roomy subscriber, silent for the 128-byte one; a small publish reaches both — degraded, not broken). Workspace green (794 tests), clippy zero warnings.
  - id: 0041-T5
    title: Disk watermark + closure — per-store size gauges, soft high-water brownout (growth writes refused, maintenance continues), uniform fail-closed disk-full paths, README config table, ADR acceptance
    status: done
    date: 2026-07-10
    evidence: "Disk is the last ungoverned axis. VISIBILITY (new mqttd::store_watch module): with MQTTD_DATA_DIR set, a 10s poller stats each redb store file (sessions/retained/replicas/lease; absent = 0) and exports store_bytes{store} (Prometheus + OTel gauge family) — always on, watermark or not. WATERMARK (MQTTD_STORE_MAX_BYTES, positive integer else startup error, needs MQTTD_DATA_DIR): when the stores' total crosses it the watcher sends an edge-triggered HubCommand::SetBrownout (transitions only, logged loudly both ways). Under brownout, growth is refused with the T4 behaviors — a NEW retained topic 0x97/delivered-not-retained, a NEW session CONNACK 0x97/0x03 (quota_rejections_total{kind=brownout}), an offline enqueue skipped and counted (publish_dropped{brownout}) — while maintenance continues untouched: retained overwrite/clear, session resume, acks, deletes, expiry. Read-mostly, never the cliff where redb commits start failing mid-write; dropping back under the mark restores everything. Two warts found and fixed on the way: (1) a refused stranger's recovery had already run claim_session, CREATING the durable record the refusal then abandoned — the refusal path now reaps that just-created record off-loop and gates the QuotaExceeded reply on the reap, so a refused grant leaves no growth behind; (2) FAIL-CLOSED cross-node/offline enqueue — the deliver chain now reports durable success, and a failed offline enqueue (e.g. NoQuorum) withholds the publisher's ack (the done gate is dropped, the PUBACK/PUBREC never sent) so a QoS>=1 publisher retries instead of believing a lost message was stored, exactly like the local ack path. Tests: store_watch unit (scan sizes per store + total; the watcher is edge-triggered across cross/steady/recover), hub brownout_refuses_growth_and_recovery_restores_it (growth refused / maintenance continues / recovery restores / the browned-out enqueue never replays while the post-recovery one does), hub a_failed_offline_enqueue_withholds_the_publishers_ack (FlakyStore no-quorum enqueue -> no ack). README: MQTTD_STORE_MAX_BYTES row added; every MQTTD_* cap from this ADR is now in the config table. ADR 0041 flipped to Accepted. Workspace green (798 tests), clippy zero warnings."
  - id: 0041-T6
    title: Per-session byte bound on the offline queue — MQTTD_MAX_QUEUED_BYTES beside the count bound, first-reached wins, same queue_overflow semantics, counted; SIZING.md updated (2026-08-04 amendment)
    status: planned
  - id: 0041-T7
    title: Bridge-spool byte bound — max_bytes joins max_messages in the mqtt-bridge spool, drop-oldest, counted (2026-08-04 amendment)
    status: planned
  - id: 0041-T8
    title: Process-memory watermark — MQTTD_MEMORY_MAX_BYTES, RSS poller + edge-triggered brownout reusing the T5 refusal paths; growth refused, maintenance continues, recovery restores; explicitly not allocation-denial or per-connection force-shutdown (2026-08-04 amendment)
    status: done
    date: 2026-08-10
    evidence: "crates/mqttd/src/memory_watch.rs — RSS from /proc/self/status (no new dependency, no libc call), edge-triggered SetBrownout on the Memory axis, gauges process_resident_bytes + memory_max_bytes. Brownout became PER-AXIS (hub::BrownoutAxis, Hub::set_brownout_axis): the effective state is the OR, so the disk poller reporting 'fine' cannot lift a brownout memory pressure is still asking for — one_axis_recovering_does_not_lift_another_axis_brownout is the test for exactly that, and a_single_axis_still_toggles_brownout_on_its_own proves the T5 contract is unchanged. Unit tests: vm_rss_is_parsed_in_bytes_from_the_right_line, a_similar_looking_line_is_not_mistaken_for_vm_rss (VmSize/VmHWM sit above VmRSS and are larger — picking either would brown out early), a_status_without_vm_rss_yields_none_not_zero, the_watcher_drives_brownout_on_watermark_transitions, without_a_watermark_nothing_is_ever_browned_out, an_unreadable_rss_stops_the_watcher_instead_of_reporting_zero, the_real_sampler_reports_a_plausible_rss_on_linux. Integration (Linux-gated) tests/memory_watermark.rs against the REAL binary: a 1 KiB watermark browns out, exports brownout{axis=\"memory\"} 1 and a plausible RSS, and refuses a new v5 session with 0x97 — with an unset-watermark control so it cannot pass against a broker that refuses everything."
    notes: "A watermark, NOT a ceiling, and said so everywhere it is documented: nothing here stops RSS rising, so a burst that outruns the 10s poll can still OOM and the container/cgroup limit remains the hard bound. RSS needs /proc, so on non-Linux the watcher logs at WARN that it is NOT enforcing (loudly, because the operator believes they have a bound) and exits, rather than sampling zero — a zero would sit under every watermark and never fire. Unreadable RSS is treated the same way. SIZING.md, COMPARISON.md and the README previously said 'there is no total-memory knob'; all three now say what is actually true, including what it cannot do."
  - id: 0041-T9
    title: Per-store disk bound — a share of MQTTD_STORE_MAX_BYTES per redb store (sessions/retained/replicas/lease) so one store cannot consume the whole watermark and brown out the others; same T4 refusal behaviors, counted per store (2026-08-07 amendment)
    status: planned
  - id: 0041-T11
    title: "Brownout refuses the publisher's ack for a QoS>=1 publish it will not durably enqueue — v5 0x97, v3.1.1 no ack + close — for online and offline persistent subscribers alike (issue #238)"
    status: done
    date: 2026-08-13
    evidence: "The ack-gate seam generalised, not reinvented: PublishOutcome becomes {Accepted, Refused(PublishRefusal)} where PublishRefusal {Brownout, RetainedQuota} carries TWO per-version answers — v5_reason() -> 0x97 and v311() -> Refusal311 {PlainAck, CloseNoAck} — because v3.1.1 has no reason byte and each refusal must state whether it is sayable as a plain ack (retained quota: delivered live, not retained, a retry changes nothing) or only as no-ack-and-close (brownout: nothing was stored, so an ack would be a lie). In the hub, the overloaded Appended::Skipped splits into Refused(PublishRefusal) and Dropped (the reject-newest queue cap, behaviour unchanged), durable_append's brownout arm returns Refused(Brownout), and the bool durability chain becomes DurableOutcome {Ok, Refused, Failed} with and() precedence Failed > Refused > Ok — MQTT's ack is per-publish, not per-subscriber, so one refused subscriber refuses the publish, and Failed dominates because no reason code honestly covers it. deliver_to_client refuses the ONLINE persistent branch too, returning BEFORE send_to_client exactly as Appended::Failed does: a live delivery with no durable record promises a redelivery the store cannot honour. New refuse_pending(id, r) sits beside drop_pending (a reason the publisher is told, versus silence-and-close). The load-bearing conn.rs decision is QoS 2: the dedup record is written BEFORE the hub is consulted, so a close without a PUBREC that also erased the record would make the client's mandatory resend of the same id look like a first sighting — and a record answered from the window with nothing stored would be the same lie re-created at QoS 2. (As FIRST delivered, every non-PUBREC exit released the record; the #238 verification proved that releasing it on the CLOSE exits re-creates a duplicate fan-out across the client's mandatory resend, so the delivered shape is T12's three-state record: only the v5 error-PUBREC releases the id, the close exits hold it unacked.) Metric: no new family — quota_rejections_total gains the bounded kind brownout-publish, while publish_dropped{reason=brownout} narrows to the QoS 0 offline enqueue (nothing owed, nothing acked); the per-message warn! drops to debug! so a sustained brownout cannot flood the operator's own diagnosis path. Red-first tests: hub brownout_refuses_the_publishers_ack_for_an_offline_persistent_subscriber (the #238 acceptance criterion), brownout_refuses_the_ack_and_the_live_send_for_an_online_persistent_subscriber, a_publish_owing_no_durability_is_still_acked_under_brownout (the anti-over-refusal guard: clean session and QoS 0 still acked), a_brownout_refusal_is_counted_as_a_quota_rejection_not_a_drop, and brownout_refuses_growth_and_recovery_restores_it amended from a blind fire-and-forget publish into a GATED witness; conn a_v5_publisher_is_answered_0x97_when_the_hub_refuses_the_publish, a_v311_publisher_is_closed_without_a_puback_when_the_hub_refuses_a_brownout_publish (with the RetainedQuota PlainAck sibling, so the two dispositions cannot be conflated); new integration tests/brownout_ack.rs over real TCP + a real Hub. (The first cut's a_refused_qos2_publish_releases_the_packet_id_for_both_protocol_versions codified the duplicate-fan-out defect and was REPLACED under T12 by tests asserting the opposite, correct held-unacked rule.) cluster_stress.rs's waiver is DELETED — brownout acks are hard obligations again, observed rather than excused. (The claim first recorded here that the harness's own v3.1.1 publisher hits the CloseNoAck path was DISPROVEN during the #238 verification — a fresh clean-session publisher can never get a PUBLISH refused by a browned-out node, whose CONNECT refusal comes first — and the harness was made honest under T12.) fmt + clippy (-D warnings) clean; workspace green."
    notes: "Deliberate residuals. BOTH offline-queue overflow policies KEEP ack-and-drop (Appended::Dropped) — the default drop-oldest truncates already-acked entries at the cap and the opt-in reject-newest sheds the newest; shedding is that cap's stated purpose, and #238 is about brownout; the README caveat survives, now enumerating all three arms. As FIRST delivered, a refusal on a PEER collapsed to withhold+close at the origin and was recorded here as out of scope; the #238 verification showed that geometry is the DEFAULT deployment's common case (quorum-replicated durable sessions), so T12 carries the refusal across the peer bus as a proto-7 verdict. No v5 Reason String property is attached — nothing in conn.rs tracks Request Problem Information, and sending one unconditionally would violate [MQTT-3.1.2-29]. Availability trade named in the ADR, not just in code: because the ack is per-publish, a topic with ANY persistent subscriber becomes a partial publish outage while browned out. Metric-semantics change for operators: publish_dropped{reason=brownout} goes quiet for QoS>=1; alert on quota_rejections_total{reason=brownout-publish} instead (both Grafana panels aggregate by (reason), so no dashboard edit and no mqttui re-vendor). Issue #246 (denied publish -> 0x87) plugs into this seam as one variant plus one line in conn.rs's ACL branch."
  - id: 0041-T12
    title: "Issue #238 remediation: the brownout refusal is effect-free and travels — peer-bus verdicts (proto 7) carry it cross-node, shared deliveries are answerable with in-group re-selection, the refusal close fires the Will, QoS 2 dedup survives the close held-unacked, Wills keep their live send"
    status: done
    date: 2026-08-14
    evidence: "Three independent verifiers audited T11's first cut and filed 18 findings — four NEW regressions, two claim defects false in the DEFAULT deployment, plus coverage/claims gaps; all remediated in the same change set. THE REFUSAL IS EFFECT-FREE: a PLAN pass in deliver decides the refusal BEFORE retained.set, before any live send, before forward_to_peers/route_retained_commit, and before a shared group's round-robin turn is consumed — a refused retained publish leaves the previous retained value intact and a brownout refusal reaches NOBODY. THE CLOSE IS HONEST: handle_packet returns a 3-valued PacketOutcome {Continue, ClientDisconnect, BrokerClose}; only a client DISCONNECT maps to a graceful detach, so the v3.1.1 CloseNoAck exit detaches ungracefully and the Will fires ([MQTT-3.14.4-3]). WILLS KEEP THEIR LIVE SEND: durable_append/deliver_to_client carry answerable: bool — suppressing a live delivery is only justified when a publisher is being told — so an ungated refused durable copy (a Will, a settle-window back-fill) keeps the live delivery and is counted publish_dropped{reason=brownout} (a loss, nobody told), never quota_rejections_total{reason=brownout-publish} (an answered refusal; counts attempts). QOS 2 SURVIVES THE CLOSE: SessionStore::record_received returns InboundSighting {Fresh, HeldUnacked, HeldAcked}; ack_received is durable BEFORE the PUBREC reaches the wire; both close exits (v3.1.1 CloseNoAck, hub-gone Err) leave the record HELD-UNACKED so the client's mandatory resend is re-decided rather than answered from the window (only a HeldAcked flow answers a DUP); the v5 reason>=0x80 arm keeps the full release — the flow ends by spec. Persisted as a strictly-trailing unacked_received_qos2 block in SessionMeta, emitted only when non-empty (byte-identical when empty; an older binary ignores it, so rollback degrades to the pre-#238 QoS 2 behaviour, never corruption; costs one extra durable session-meta write per inbound QoS 2 exchange). THE REFUSAL TRAVELS (durable sessions are quorum-replicated by default, so the refusing node is usually a PEER): peer proto 7 — PROTO_MIN stays 6, BASELINE_REF untouched, PublishVerdict{seq,verdict} and SharedDeliverAcked appended at postcard indices 24/25 (pinned-index test), wire codes 1=brownout 2=retained-quota, exhaustive both ways; the negotiated proto is plumbed per link and answer_forward picks the frame by the LINK's proto — a proto-6 link gets today's PublishAck{ok} byte-for-byte plus a once-per-link warn; forward_answered maps Stored->complete, Refused->refuse_pending, Failed/unknown-code/undecodable->WITHHOLD (never ack, never a fabricated refusal); zero-match while routing_unsettled maps to Failed, never Refused. SHARED DELIVERIES ARE ANSWERABLE: a gated QoS>=1 cross-node shared delivery on a proto>=7 link sends SharedDeliverAcked and registers a ForwardKind::Shared obligation; a Refused verdict re-selects within the group (each candidate at most once, local or remote); the publisher is refused only when the pass exhausts (withheld if the last answer was Failed); unanswered obligations hold the ack and the sweep retransmits the SAME frame kind under the same seq; peer_dead re-selects immediately; QoS 0 and proto-6 links keep fire-and-forget SharedDeliver. FALSE REFUSALS PREVENTED: PendingPublish tracks stored-anywhere (mark_stored_since); refuse_pending WITHHOLDS with a warn for any publish already durably stored — only a publish stored nowhere is answered Refused. HARNESS MADE HONEST: publish_step dials a HEALTHY node so the publish is FORWARDED to the browned-out session owner and the refusal actually crosses the bus (a fresh clean-session publisher can never get a PUBLISH refused by a browned-out node — its CONNECT is refused first); per-seed summaries count refusals distinctly from toggles. The DETERMINISTIC guards are three real-mesh cluster tests — a_browned_out_session_owner_refuses_the_publisher_rather_than_owing_a_lost_message (v3.1.1 close + recovery replay), a_v5_publisher_on_a_healthy_node_is_told_0x97_when_the_session_owning_peer_refuses (PUBACK 0x97, connection survives), a_cross_node_shared_subscriber_is_never_bypassed_by_an_ack — the seeded sweep is sampling on top, never the guard. Every fix mutation-proven: 16 single-line production mutations each observed failing its named test, then reverted (one weak pin found and strengthened: the proto negotiation asserts now go through (PROTO_MIN, PROTO_MAX)). NOTE: the pre-#238 state is the reversion of TWO layers (plan_refusal AND durable_append's brownout arm); the arm-only mutation is absorbed by the plan pass by design — any evidence sentence claiming otherwise is false. Gates: fmt clean, clippy -D warnings clean; mqttd lib 238 passed, brownout_ack 5, cluster_stress 11, mqtt-cluster/mqtt-observability/mqtt-storage suites green."
    notes: "Residuals named where they live: post-hoist, durable_append's brownout arm's VARIANT (Refused vs Dropped) is observably inert on answerable paths — defense-in-depth whose tripwire is a debug_assert, verified firing under the invariant-breaking two-layer mutation. A re-selected shared member can deliver a duplicate at QoS 2 cross-node if a verdict is lost. 'A stored, B failed' store-failure duplicates remain (refuse_pending rustdoc). A v3.1.1 retained-value publish under brownout with nobody owed durability still gets a plain PUBACK (delivered live, not retained) — the TROUBLESHOOTING discrimination rule is scoped accordingly. The proto-6 skew window collapses a verdict to withhold+close, so a v5 publisher can see a close mid-rolling-upgrade — every doc site carries that qualifying clause rather than deleting the caveat."
  - id: 0041-T13
    title: "Issue #265 close-out: a v5 DISCONNECT with a non-zero reason fires the Will — the last will-suppressing close in the T12 PacketOutcome seam"
    status: done
    date: 2026-08-14
    evidence: "conn.rs: PacketOutcome grows ClientDisconnectWithWill; the Packet::Disconnect arm branches on the reason byte (0x00 -> ClientDisconnect, graceful; non-zero -> ClientDisconnectWithWill, un-graceful detach so the Will fires per [MQTT-3.1.2-10] — 0x04 Disconnect with Will Message explicitly, any error reason implicitly); serve maps the new variant with BrokerClose to the ungraceful return. v3.1.1 untouched (its DISCONNECT has no reason byte, always decodes 0). Tests (conn::tests), one per close class, each asserting the Detach graceful flag at the hub seam, each mutation-proven: a_v5_disconnect_with_a_non_zero_reason_detaches_ungracefully_so_the_will_fires (0x04 and 0x81; the ONLY failure when the reason branch is reverted to unconditional ClientDisconnect), a_protocol_violation_close_detaches_ungracefully_so_the_will_fires (wildcard PUBLISH topic), an_eof_without_disconnect_detaches_ungracefully_so_the_will_fires (the only failure when the EOF arm is flipped to Ok(true)), a_keepalive_expiry_detaches_ungracefully_so_the_will_fires (paused virtual time; the only failure when the keepalive arm is flipped); flipping serve's ClientDisconnectWithWill|BrokerClose arm to Ok(true) fails the v5-non-zero, protocol-violation, and #238 refusal tests together. Controls already in place: a_client_disconnect_is_still_reported_as_graceful (reason 0x00 stays graceful) and the ADR 0019 graceful-drain tests (server-initiated drain stays will-withholding by design)."
    notes: "Completes the close-honesty work T12 started for issue #238: T12 made every BROKER-initiated close will-firing; #265's residue was the CLIENT-initiated will-firing close — v5 DISCONNECT reason != 0x00, which the old arm collapsed into the graceful case. The graceful-shutdown drain remains the one deliberate non-DISCONNECT graceful close (ADR 0019: the server is going away, not the client; session retained, Will withheld). See the ADR 0041 amendment (2026-08-14, issue #265)."
  - id: 0041-T10
    title: Per-connection write-buffer bound — MAX_BACKLOG (hub.rs, 10 000 messages per stalled subscriber) becomes byte-aware and configurable; today it is a hard-coded count, so a slow consumer's backlog is bounded in messages but unbounded in bytes and cannot be tuned (2026-08-07 amendment)
    status: planned
---

# Delivery — ADR 0041: Resource governance

Decision: [docs/adr/0041-resource-governance.md](../adr/0041-resource-governance.md).

Pre-release area ③ (see ADR 0038's changelog for the four-area plan). The broker bounds
what one frame or session object can cost but not what a client can have *many* of:
connections, auth attempts, subscriptions, publish rate, retained topics, sessions, and
disk are all ungoverned. This delivery caps each at its cheapest enforcement point, with
spec-shaped at-bound behavior (reason codes, TCP backpressure), generous env-tunable
defaults, and a metric per cap.

## Plan

| Task | Acceptance criterion |
|------|----------------------|
| **0041-T1** Admission caps | With `MQTTD_MAX_CONNECTIONS=N`: the N+1-th concurrent connection is closed at accept **before any TLS handshake work**, counted (`admission_rejected_total{reason="max-connections"}`), and a slot freed by a disconnect is reusable. Same per source IP via `MQTTD_MAX_CONNECTIONS_PER_IP`; the per-IP table is bounded. Unset = today's behavior. |
| **0041-T2** Auth pushback | After K failed CONNECTs from one address, the next connection from it is closed at accept (no auth work) while a *different* address still authenticates normally; the penalty decays (a later attempt succeeds); audited (`security.penalty`) and counted. The table never grows past its bound under an address-spraying attack. |
| **0041-T3** Client quotas | A SUBSCRIBE filter beyond `MQTTD_MAX_SUBSCRIPTIONS_PER_CLIENT` gets `0x97` (v5) / `0x80` (v3.1.1) in its SUBACK slot while in-cap filters in the same packet are granted; a publisher exceeding `MQTTD_MAX_PUBLISH_RATE` is slowed to the configured rate by read-pause (no drops, no disconnect, session stays healthy) while a second client is unaffected; a v5 QoS 1 overrun of the advertised Receive Maximum gets `DISCONNECT 0x93` (finishing ADR 0012 §3). |
| **0041-T4** State caps | A retained publish creating a new topic beyond `MQTTD_MAX_RETAINED_MESSAGES` is refused (v5 `0x97`; v3.1.1 delivered-not-retained, counted) while overwrite/clear of existing topics still works at the cap; a CONNECT creating a new session beyond `MQTTD_MAX_SESSIONS` is refused (v5 `0x97`, v3.1.1 `0x03`) while a resume succeeds at the cap; the CONNACK advertises `MQTTD_MAX_PACKET_SIZE` as Maximum Packet Size and an outbound message larger than the *client's* advertised maximum is dropped for that subscriber only, counted; `MQTTD_MAX_QUEUED_MESSAGES` wires `QueueLimits`. |
| **0041-T5** Disk + closure | Per-store redb size gauges exported; with `MQTTD_STORE_MAX_BYTES` set and exceeded, growth writes are refused with the T4 behaviors while acks/deletes/expiry/resume continue (proven by test), and recovery below the mark restores writes; the cross-node offline enqueue failure path is fail-closed like the local ack path; README documents every `MQTTD_*` cap; ADR 0041 flips to Accepted. |

## Progress

<!-- status-table:0041 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0041-T1 | ✅ done | 2026-07-06 | "New mqttd::admission module: AdmissionGate hands out RAII AdmissionPermits — the accept loop asks try_admit(ip) BEFORE spawning any per-connection work (in particular before the TLS handshake; QUIC refuses via Incoming::refuse before the handshake future), moves the permit into the connection task, and the slot frees itself however the task ends. All five listeners gated (TCP-TLS, plaintext, WS, WSS, QUIC). One mutex over both counts (accept-rate, not per-message); the per-IP table removes entries at zero, so it is bounded by live connections — each entry is backed by a real socket, so the accounting can never outgrow the resource it guards. MQTTD_MAX_CONNECTIONS / MQTTD_MAX_CONNECTIONS_PER_IP (admission_gate_from_env): unset = uncapped (today's behavior), a non-positive or unparseable value is a startup error. Rejections counted (admission_rejected_total{reason=max-connections|per-ip}, Prometheus + OTel — bounded reasons, never per-address) and debug-logged. README config table documents both vars. Tests: admission unit (global cap refuses at bound and a freed slot readmits; per-IP independent across addresses and the table shrinks to empty; unconfigured admits 1000 with an empty table) and two process-level acceptance tests through the real binary (binary_smoke): max_connections_cap_refuses_at_accept_and_recovers (third concurrent connection closed with no CONNACK, admitted clients keep a pub/sub round-trip working, a dropped client's slot recycles) and per_ip_cap_refuses_a_second_connection_from_the_same_address. Workspace green (785 tests), clippy zero warnings. Noted: one unrelated pre-existing load-dependent flake observed once (mqtt-cluster a_takeover_recovers_the_retained_value_and_its_token; 5/5 green isolated, 3/3 green per-crate, full-workspace rerun green) — a candidate exhibit for pre-release area 4's stress harness. |
| 0041-T2 | ✅ done | 2026-07-06 | "The AdmissionGate gains the penalty box: conn::handle_stream now returns a ConnOutcome whose auth_failed flag is set ONLY by an authenticator rejection (authorization denials never set it; a proxied session's flag is deliberately dropped — its peer address is the RELAYING NODE, which must never be penalized for a client's bad credentials). Every accept-loop wrapper (TCP-TLS, plaintext, WS, WSS, QUIC) reports the failure to gate.record_auth_failure(ip): strikes are integral additions on a CONTINUOUS decay (Strikes { level, at }, recomputed lazily), an address is penalized once it crosses MQTTD_AUTH_PENALTY_THRESHOLD and stays refused until a full strike decays (> threshold-1 — comparing >= threshold would un-penalize an epsilon after crossing), with MQTTD_AUTH_PENALTY_DECAY_SECS (default 60) per strike. While penalized, try_admit refuses BEFORE the global/per-IP checks and before any TLS or Argon2 work (admission_rejected_total{reason=penalty}); crossing the threshold is audited (security.penalty) and warn-logged. The table is hard-bounded (PENALTY_TABLE_MAX 4096, evict-oldest) so address spraying cycles it instead of growing it; the penalty keys on the source address, never a username (a username-keyed lockout would be a DoS lever aimed at victims — ADR alternative). Unset threshold = disabled (today's behavior); values validated at startup. README documents both vars. Tests: admission unit (threshold crossing refuses while a different address is unaffected and the penalty decays back; 8192-address spray never exceeds the bound) and a process-level acceptance test through the real binary (repeated_auth_failures_penalize_the_source_address_then_decay): two bad passwords from 127.0.0.2 penalize it — its next connection with CORRECT credentials is closed with no CONNACK — while 127.0.0.1 authenticates normally throughout, and polling shows the decay readmitting it. Workspace green (788 tests), clippy zero warnings. |
| 0041-T3 | ✅ done | 2026-07-06 | "Three quotas. SUBSCRIPTIONS (MQTTD_MAX_SUBSCRIPTIONS_PER_CLIENT -> hub Quotas via HubCommand::SetQuotas, sent before any listener accepts): HubCommand::Subscribe gains a reply channel — the hub computes one verdict per ACL-granted filter (an already-held filter replaces, never consumes quota) and answers BEFORE any retained replay, so the connection's SUBACK still precedes replayed publishes on the wire; a denied filter answers 0x97 Quota exceeded (v5) / 0x80 (v3.1.1) in its SUBACK slot while in-cap filters in the same packet are granted; denials counted (quota_rejections_total{kind=subscriptions}). PUBLISH RATE (MQTTD_MAX_PUBLISH_RATE msgs/s -> WireLimits.publish_rate): a per-connection token bucket with a one-second burst, taken in the serve loop BEFORE processing an inbound PUBLISH — an empty bucket sleeps there, with the socket unread, so the over-rate publisher backs up its own TCP window (transport-native backpressure); nothing dropped, nothing disconnected; acks/pings/subscribes flow freely. RECEIVE MAXIMUM QoS 1 (closing ADR 0012 §3): the advertised window counts QoS 1 and QoS 2 publications TOGETHER per spec — a QoS 1 publish arriving with qos2_inflight already at the maximum is a flow-control breach, DISCONNECT 0x93, exactly as QoS 2 already behaved (the purely-serial QoS 1 path alone can never overrun, which is why the deferral looked unobservable until counted jointly). Tests (tests/quotas.rs, own process for the WireLimits global): the_subscription_quota_denies_excess_filters_per_suback_slot (v5 [0,0,0x97] in one packet, re-subscribe at cap granted, new filter denied, granted subscriptions still deliver, v3.1.1 gets 0x80), an_over_rate_publisher_is_throttled_without_loss_or_disconnect (12 QoS1 messages all acked+delivered, elapsed proves the slowdown, a second client's publish interleaves mid-throttle rather than queueing behind it, PING still answers), a_qos1_publish_beyond_the_shared_receive_maximum_gets_0x93 (open QoS2 window + QoS1 publish -> 0x93). The conn stub hub grants verdicts like an uncapped hub, keeping unit tests faithful. README documents both vars. Workspace green (791 tests), clippy zero warnings. |
| 0041-T4 | ✅ done | 2026-07-06 | "Growth is a grant; maintenance never is. RETAINED (MQTTD_MAX_RETAINED_MESSAGES -> Quotas.max_retained_messages): the hub's publish dispatch refuses a retained publish that would CREATE a new topic beyond the cap — the ack-gate channel now carries a PublishOutcome (Accepted | RetainedQuotaExceeded) and the Publish command a v5 flag: a v5 publisher gets PUBACK/PUBREC 0x97 with nothing delivered or retained (a refused PUBREC ends the QoS2 flow: no Receive-Maximum slot consumed, dedup record released, id reusable), while v3.1.1 (no reason codes) is delivered live with retention stripped (retain=false also on the peer forward, so remote nodes do not retain what the origin refused), warn-logged and counted (quota_rejections_total{kind=retained}); overwrite and clear always work at the cap and a clear frees a slot. SESSIONS (MQTTD_MAX_SESSIONS -> Quotas.max_sessions): finish_attach refuses only a NEW session — resume (session_present) and locally-known ids (takeover, clean-start replacement) always pass — with AttachOutcome::QuotaExceeded -> CONNACK 0x97 (v5) / Server unavailable 0x03 (v3.1.1: no quota code; 'try elsewhere' is the honest translation); count = online ∪ session-expiry (no double-count). MAX PACKET SIZE (MQTTD_MAX_PACKET_SIZE, default 1 MiB, floor 1 KiB): the mqtt-net frame cap becomes a OnceLock the binary sets from the SAME value the CONNACK advertises as the MQTT 5 Maximum Packet Size (the placeholder const the frame module carried since ADR 0002 is paid off — cap and contract cannot drift); outbound, the connection records the CLIENT's advertised maximum and drops a larger publish for that subscriber alone (measured on the encoded frame after the alias rewrite, publish_dropped{too-large}), per spec. QUEUE LIMITS: already operator-wired (MQTTD_MAX_QUEUED_MESSAGES / MQTTD_QUEUE_OVERFLOW existed in main.rs + README — the ADR survey's 'test-only constructor' finding was stale; recorded here as verified rather than re-built). Tests (tests/quotas.rs): the_retained_quota_refuses_growth_but_allows_maintenance (0x97 on third topic; overwrite + clear at cap; clear frees a slot; v3.1.1 delivered-live-not-retained proven via live subscriber + fresh-subscriber replay), the_session_quota_refuses_new_sessions_but_never_resumes (0x97 v5 / refused v3.1.1 stranger; resume through the full cap; clean-session slot recycles), an_oversized_outbound_publish_is_dropped_for_that_subscriber_alone (CONNACK advertises the ceiling; 4 KiB publish reaches the roomy subscriber, silent for the 128-byte one; a small publish reaches both — degraded, not broken). Workspace green (794 tests), clippy zero warnings. |
| 0041-T5 | ✅ done | 2026-07-10 | "Disk is the last ungoverned axis. VISIBILITY (new mqttd::store_watch module): with MQTTD_DATA_DIR set, a 10s poller stats each redb store file (sessions/retained/replicas/lease; absent = 0) and exports store_bytes{store} (Prometheus + OTel gauge family) — always on, watermark or not. WATERMARK (MQTTD_STORE_MAX_BYTES, positive integer else startup error, needs MQTTD_DATA_DIR): when the stores' total crosses it the watcher sends an edge-triggered HubCommand::SetBrownout (transitions only, logged loudly both ways). Under brownout, growth is refused with the T4 behaviors — a NEW retained topic 0x97/delivered-not-retained, a NEW session CONNACK 0x97/0x03 (quota_rejections_total{kind=brownout}), an offline enqueue skipped and counted (publish_dropped{brownout}) — while maintenance continues untouched: retained overwrite/clear, session resume, acks, deletes, expiry. Read-mostly, never the cliff where redb commits start failing mid-write; dropping back under the mark restores everything. Two warts found and fixed on the way: (1) a refused stranger's recovery had already run claim_session, CREATING the durable record the refusal then abandoned — the refusal path now reaps that just-created record off-loop and gates the QuotaExceeded reply on the reap, so a refused grant leaves no growth behind; (2) FAIL-CLOSED cross-node/offline enqueue — the deliver chain now reports durable success, and a failed offline enqueue (e.g. NoQuorum) withholds the publisher's ack (the done gate is dropped, the PUBACK/PUBREC never sent) so a QoS>=1 publisher retries instead of believing a lost message was stored, exactly like the local ack path. Tests: store_watch unit (scan sizes per store + total; the watcher is edge-triggered across cross/steady/recover), hub brownout_refuses_growth_and_recovery_restores_it (growth refused / maintenance continues / recovery restores / the browned-out enqueue never replays while the post-recovery one does), hub a_failed_offline_enqueue_withholds_the_publishers_ack (FlakyStore no-quorum enqueue -> no ack). README: MQTTD_STORE_MAX_BYTES row added; every MQTTD_* cap from this ADR is now in the config table. ADR 0041 flipped to Accepted. Workspace green (798 tests), clippy zero warnings." |
| 0041-T6 | ⬜ planned | — |  |
| 0041-T7 | ⬜ planned | — |  |
| 0041-T8 | ✅ done | 2026-08-10 | "crates/mqttd/src/memory_watch.rs — RSS from /proc/self/status (no new dependency, no libc call), edge-triggered SetBrownout on the Memory axis, gauges process_resident_bytes + memory_max_bytes. Brownout became PER-AXIS (hub::BrownoutAxis, Hub::set_brownout_axis): the effective state is the OR, so the disk poller reporting 'fine' cannot lift a brownout memory pressure is still asking for — one_axis_recovering_does_not_lift_another_axis_brownout is the test for exactly that, and a_single_axis_still_toggles_brownout_on_its_own proves the T5 contract is unchanged. Unit tests: vm_rss_is_parsed_in_bytes_from_the_right_line, a_similar_looking_line_is_not_mistaken_for_vm_rss (VmSize/VmHWM sit above VmRSS and are larger — picking either would brown out early), a_status_without_vm_rss_yields_none_not_zero, the_watcher_drives_brownout_on_watermark_transitions, without_a_watermark_nothing_is_ever_browned_out, an_unreadable_rss_stops_the_watcher_instead_of_reporting_zero, the_real_sampler_reports_a_plausible_rss_on_linux. Integration (Linux-gated) tests/memory_watermark.rs against the REAL binary: a 1 KiB watermark browns out, exports brownout{axis=\"memory\"} 1 and a plausible RSS, and refuses a new v5 session with 0x97 — with an unset-watermark control so it cannot pass against a broker that refuses everything." |
| 0041-T9 | ⬜ planned | — |  |
| 0041-T11 | ✅ done | 2026-08-13 | "The ack-gate seam generalised, not reinvented: PublishOutcome becomes {Accepted, Refused(PublishRefusal)} where PublishRefusal {Brownout, RetainedQuota} carries TWO per-version answers — v5_reason() -> 0x97 and v311() -> Refusal311 {PlainAck, CloseNoAck} — because v3.1.1 has no reason byte and each refusal must state whether it is sayable as a plain ack (retained quota: delivered live, not retained, a retry changes nothing) or only as no-ack-and-close (brownout: nothing was stored, so an ack would be a lie). In the hub, the overloaded Appended::Skipped splits into Refused(PublishRefusal) and Dropped (the reject-newest queue cap, behaviour unchanged), durable_append's brownout arm returns Refused(Brownout), and the bool durability chain becomes DurableOutcome {Ok, Refused, Failed} with and() precedence Failed > Refused > Ok — MQTT's ack is per-publish, not per-subscriber, so one refused subscriber refuses the publish, and Failed dominates because no reason code honestly covers it. deliver_to_client refuses the ONLINE persistent branch too, returning BEFORE send_to_client exactly as Appended::Failed does: a live delivery with no durable record promises a redelivery the store cannot honour. New refuse_pending(id, r) sits beside drop_pending (a reason the publisher is told, versus silence-and-close). The load-bearing conn.rs decision is QoS 2: the dedup record is written BEFORE the hub is consulted, so a close without a PUBREC that also erased the record would make the client's mandatory resend of the same id look like a first sighting — and a record answered from the window with nothing stored would be the same lie re-created at QoS 2. (As FIRST delivered, every non-PUBREC exit released the record; the #238 verification proved that releasing it on the CLOSE exits re-creates a duplicate fan-out across the client's mandatory resend, so the delivered shape is T12's three-state record: only the v5 error-PUBREC releases the id, the close exits hold it unacked.) Metric: no new family — quota_rejections_total gains the bounded kind brownout-publish, while publish_dropped{reason=brownout} narrows to the QoS 0 offline enqueue (nothing owed, nothing acked); the per-message warn! drops to debug! so a sustained brownout cannot flood the operator's own diagnosis path. Red-first tests: hub brownout_refuses_the_publishers_ack_for_an_offline_persistent_subscriber (the #238 acceptance criterion), brownout_refuses_the_ack_and_the_live_send_for_an_online_persistent_subscriber, a_publish_owing_no_durability_is_still_acked_under_brownout (the anti-over-refusal guard: clean session and QoS 0 still acked), a_brownout_refusal_is_counted_as_a_quota_rejection_not_a_drop, and brownout_refuses_growth_and_recovery_restores_it amended from a blind fire-and-forget publish into a GATED witness; conn a_v5_publisher_is_answered_0x97_when_the_hub_refuses_the_publish, a_v311_publisher_is_closed_without_a_puback_when_the_hub_refuses_a_brownout_publish (with the RetainedQuota PlainAck sibling, so the two dispositions cannot be conflated); new integration tests/brownout_ack.rs over real TCP + a real Hub. (The first cut's a_refused_qos2_publish_releases_the_packet_id_for_both_protocol_versions codified the duplicate-fan-out defect and was REPLACED under T12 by tests asserting the opposite, correct held-unacked rule.) cluster_stress.rs's waiver is DELETED — brownout acks are hard obligations again, observed rather than excused. (The claim first recorded here that the harness's own v3.1.1 publisher hits the CloseNoAck path was DISPROVEN during the #238 verification — a fresh clean-session publisher can never get a PUBLISH refused by a browned-out node, whose CONNECT refusal comes first — and the harness was made honest under T12.) fmt + clippy (-D warnings) clean; workspace green." |
| 0041-T12 | ✅ done | 2026-08-14 | "Three independent verifiers audited T11's first cut and filed 18 findings — four NEW regressions, two claim defects false in the DEFAULT deployment, plus coverage/claims gaps; all remediated in the same change set. THE REFUSAL IS EFFECT-FREE: a PLAN pass in deliver decides the refusal BEFORE retained.set, before any live send, before forward_to_peers/route_retained_commit, and before a shared group's round-robin turn is consumed — a refused retained publish leaves the previous retained value intact and a brownout refusal reaches NOBODY. THE CLOSE IS HONEST: handle_packet returns a 3-valued PacketOutcome {Continue, ClientDisconnect, BrokerClose}; only a client DISCONNECT maps to a graceful detach, so the v3.1.1 CloseNoAck exit detaches ungracefully and the Will fires ([MQTT-3.14.4-3]). WILLS KEEP THEIR LIVE SEND: durable_append/deliver_to_client carry answerable: bool — suppressing a live delivery is only justified when a publisher is being told — so an ungated refused durable copy (a Will, a settle-window back-fill) keeps the live delivery and is counted publish_dropped{reason=brownout} (a loss, nobody told), never quota_rejections_total{reason=brownout-publish} (an answered refusal; counts attempts). QOS 2 SURVIVES THE CLOSE: SessionStore::record_received returns InboundSighting {Fresh, HeldUnacked, HeldAcked}; ack_received is durable BEFORE the PUBREC reaches the wire; both close exits (v3.1.1 CloseNoAck, hub-gone Err) leave the record HELD-UNACKED so the client's mandatory resend is re-decided rather than answered from the window (only a HeldAcked flow answers a DUP); the v5 reason>=0x80 arm keeps the full release — the flow ends by spec. Persisted as a strictly-trailing unacked_received_qos2 block in SessionMeta, emitted only when non-empty (byte-identical when empty; an older binary ignores it, so rollback degrades to the pre-#238 QoS 2 behaviour, never corruption; costs one extra durable session-meta write per inbound QoS 2 exchange). THE REFUSAL TRAVELS (durable sessions are quorum-replicated by default, so the refusing node is usually a PEER): peer proto 7 — PROTO_MIN stays 6, BASELINE_REF untouched, PublishVerdict{seq,verdict} and SharedDeliverAcked appended at postcard indices 24/25 (pinned-index test), wire codes 1=brownout 2=retained-quota, exhaustive both ways; the negotiated proto is plumbed per link and answer_forward picks the frame by the LINK's proto — a proto-6 link gets today's PublishAck{ok} byte-for-byte plus a once-per-link warn; forward_answered maps Stored->complete, Refused->refuse_pending, Failed/unknown-code/undecodable->WITHHOLD (never ack, never a fabricated refusal); zero-match while routing_unsettled maps to Failed, never Refused. SHARED DELIVERIES ARE ANSWERABLE: a gated QoS>=1 cross-node shared delivery on a proto>=7 link sends SharedDeliverAcked and registers a ForwardKind::Shared obligation; a Refused verdict re-selects within the group (each candidate at most once, local or remote); the publisher is refused only when the pass exhausts (withheld if the last answer was Failed); unanswered obligations hold the ack and the sweep retransmits the SAME frame kind under the same seq; peer_dead re-selects immediately; QoS 0 and proto-6 links keep fire-and-forget SharedDeliver. FALSE REFUSALS PREVENTED: PendingPublish tracks stored-anywhere (mark_stored_since); refuse_pending WITHHOLDS with a warn for any publish already durably stored — only a publish stored nowhere is answered Refused. HARNESS MADE HONEST: publish_step dials a HEALTHY node so the publish is FORWARDED to the browned-out session owner and the refusal actually crosses the bus (a fresh clean-session publisher can never get a PUBLISH refused by a browned-out node — its CONNECT is refused first); per-seed summaries count refusals distinctly from toggles. The DETERMINISTIC guards are three real-mesh cluster tests — a_browned_out_session_owner_refuses_the_publisher_rather_than_owing_a_lost_message (v3.1.1 close + recovery replay), a_v5_publisher_on_a_healthy_node_is_told_0x97_when_the_session_owning_peer_refuses (PUBACK 0x97, connection survives), a_cross_node_shared_subscriber_is_never_bypassed_by_an_ack — the seeded sweep is sampling on top, never the guard. Every fix mutation-proven: 16 single-line production mutations each observed failing its named test, then reverted (one weak pin found and strengthened: the proto negotiation asserts now go through (PROTO_MIN, PROTO_MAX)). NOTE: the pre-#238 state is the reversion of TWO layers (plan_refusal AND durable_append's brownout arm); the arm-only mutation is absorbed by the plan pass by design — any evidence sentence claiming otherwise is false. Gates: fmt clean, clippy -D warnings clean; mqttd lib 238 passed, brownout_ack 5, cluster_stress 11, mqtt-cluster/mqtt-observability/mqtt-storage suites green." |
| 0041-T13 | ✅ done | 2026-08-14 | "conn.rs: PacketOutcome grows ClientDisconnectWithWill; the Packet::Disconnect arm branches on the reason byte (0x00 -> ClientDisconnect, graceful; non-zero -> ClientDisconnectWithWill, un-graceful detach so the Will fires per [MQTT-3.1.2-10] — 0x04 Disconnect with Will Message explicitly, any error reason implicitly); serve maps the new variant with BrokerClose to the ungraceful return. v3.1.1 untouched (its DISCONNECT has no reason byte, always decodes 0). Tests (conn::tests), one per close class, each asserting the Detach graceful flag at the hub seam, each mutation-proven: a_v5_disconnect_with_a_non_zero_reason_detaches_ungracefully_so_the_will_fires (0x04 and 0x81; the ONLY failure when the reason branch is reverted to unconditional ClientDisconnect), a_protocol_violation_close_detaches_ungracefully_so_the_will_fires (wildcard PUBLISH topic), an_eof_without_disconnect_detaches_ungracefully_so_the_will_fires (the only failure when the EOF arm is flipped to Ok(true)), a_keepalive_expiry_detaches_ungracefully_so_the_will_fires (paused virtual time; the only failure when the keepalive arm is flipped); flipping serve's ClientDisconnectWithWill|BrokerClose arm to Ok(true) fails the v5-non-zero, protocol-violation, and #238 refusal tests together. Controls already in place: a_client_disconnect_is_still_reported_as_graceful (reason 0x00 stays graceful) and the ADR 0019 graceful-drain tests (server-initiated drain stays will-withholding by design)." |
| 0041-T10 | ⬜ planned | — |  |
<!-- /status-table:0041 -->

## Changelog

- **2026-08-14** — **T12 done: the #238 refusal made honest end to end.** Three
  independent verifiers audited T11's first cut and filed 18 findings: four **new
  regressions** (the v3.1.1 refusal close was reported as a graceful disconnect,
  suppressing the Will; a cross-node shared delivery lost the message while the origin
  acked; Wills — ungated, with no publisher to refuse — were silently dropped instead of
  delivered live; the QoS 2 dedup release turned the client's mandatory resend into a
  second fan-out), the headline claim **false in the default deployment** (a peer's
  refusal collapsed to withhold+close, and quorum-replicated sessions make the peer the
  common owner of an offline subscriber's session), and the README's ack-and-drop
  enumeration missing the **default** `drop-oldest` queue truncation of already-acked
  entries. All remediated: the refusal is decided before any effect, travels the peer
  bus as a proto-7 verdict (a proto-6 link degrades to the old withhold+close for the
  rolling-upgrade skew window only), shared deliveries are answerable with in-group
  re-selection, the refusal close fires the Will, QoS 2 holds its dedup record unacked
  across the close (a trailing session-meta block, rollback-safe), and ungated publishes
  keep their live send with the lost durable copy counted as a drop. The stress
  harness's publisher now dials a healthy node so the refusal is actually reachable
  cross-node — the three deterministic cluster tests are the guard; the seeded sweep
  samples. Docs: every `0x97` claim carries the skew-window qualifier, the metric-label
  and retained-counter selectors are copy-paste correct, "the publisher retries" became
  "re-sending is the application's decision", and the README/COMPARISON durability
  cells enumerate all three ack-and-drop arms.

- **2026-08-13** — **T11 done: brownout refuses the publisher's ack (a T5 trade reversed).**
  T5 shipped "growth writes are refused while acks … continue" as *the publisher is still
  acked for a refused growth write* — so a `QoS` 1 message with only an offline persistent
  subscriber was PUBACKed and existed nowhere (issue #238). The ack now travels the same
  gate the retained quota already used, generalised to carry a **reason**: v5 gets
  `0x97`, v3.1.1 — which has no reason byte — gets no ack and a close, and the online
  persistent branch withholds the live send too rather than promising a redelivery the
  store cannot honour. The subtle half was `QoS` 2: the inbound dedup record is written
  before the hub is consulted, so every exit that does not PUBREC must release it or the
  client's mandatory resend is answered as a duplicate with a *success* PUBREC and
  nothing stored — which also closed a pre-existing hole on the hub-gone path. The
  stress oracle's brownout waiver is gone: those acks are obligations again.

- **2026-08-10** — **T8 done: the memory watermark.** `MQTTD_MEMORY_MAX_BYTES` samples RSS
  from `/proc/self/status` and drives brownout on a second axis, reusing T5's refusal
  paths exactly. The load-bearing change is that **brownout is now per-axis**: with one
  shared flag, the disk poller's routine "still fine" every 10 s would have silently
  lifted a brownout that memory pressure was still asking for, and the broker would have
  resumed accepting growth writes straight into an OOM. The effective state is the OR
  across axes, and the test named for that case is why the state is a set.

  Stated as plainly in the docs as in the code: it is a **watermark, not a ceiling**.
  Nothing here stops RSS rising — a burst that outruns the poll can still OOM, and the
  container limit remains the hard bound. What it buys is a metric, a log line and a
  degraded mode in the common case where pressure builds over minutes. On a platform
  without `/proc` it logs at WARN that it is **not enforcing** and exits, rather than
  sampling zero; a zero would sit under every watermark and never fire, which is the
  worst possible behaviour for a bound somebody is relying on.

  `SIZING.md`, `COMPARISON.md` and the README all said "there is no total-memory knob".
  All three now say what is true, including what it cannot do.
- **2026-07-10** — T5 (disk watermark + closure) landed, **ADR accepted**: per-store
  redb sizes are exported as `store_bytes{store}`, and `MQTTD_STORE_MAX_BYTES` arms a
  soft high-water brownout — above it, growth writes (new retained topics, new
  sessions, offline enqueues) are refused with the T4 behaviors while maintenance
  (acks, deletes, expiry, overwrites, resumes) continues, and recovery below the mark
  restores writes. Two honesty fixes along the way: a quota/brownout-refused stranger
  no longer leaves behind the durable session its recovery had just claimed, and a
  failed offline enqueue now withholds the publisher's ack (fail-closed, like the
  local path) so QoS ≥ 1 retries instead of silently losing the message. Every
  `MQTTD_*` cap is in the README config table.
- **2026-07-06** — T4 (global state caps) landed: retained topics and sessions are now
  capped with the growth-is-a-grant rule (overwrite/clear/resume always work at the
  cap; strangers are refused with the spec's own codes), and the 1 MiB frame-cap
  placeholder became the negotiated MQTT 5 Maximum Packet Size — one value governs the
  transport cap, the CONNACK contract, and the outbound per-subscriber drop. Queue
  limits were found already operator-wired and verified rather than re-built.
- **2026-07-06** — T3 (per-client quotas) landed: subscription caps answer with the
  spec's own 0x97 per SUBACK slot (partial packets degrade instead of failing whole),
  over-rate publishers are slowed by read-pause TCP backpressure (no drops, no
  disconnects, bystanders interleave freely), and the ADR 0012 §3 deferral is closed —
  Receive Maximum now counts QoS 1 and QoS 2 publications together, so a QoS 1 publish
  into a full window gets DISCONNECT 0x93.
- **2026-07-06** — T2 (auth-failure pushback) landed: repeated authentication failures
  now put the source address in a decaying penalty box enforced at accept — before any
  TLS or Argon2 work — converting brute-force and CPU-burn attempts into a self-limiting
  trickle. Keyed on the attacker's address, never a username; hard-bounded table under
  address spraying; audited on crossing; proven through the real binary with two
  loopback source addresses.
- **2026-07-06** — T1 (admission caps) landed: every client listener now gates accepts
  through an RAII admission permit — over-cap connections (global or per-source-IP) are
  closed before any TLS work, counted, and freed slots recycle. The per-IP table is
  structurally bounded by live connections. Proven through the real binary.
- **2026-07-05** — ADR proposed and delivery opened. Scope fixed by a bounds survey:
  per-frame/per-session costs are bounded and tested (read buffer 1 MiB, peer frame
  16 MiB, backlog 10 000 drop-oldest, offline queue 100 000 drop-oldest, alias table,
  retained mutation queue, connect/auth timeouts), but everything a client can have
  *many* of is not — connections (no global or per-IP cap; `SERVER_BUSY`/`QUOTA_EXCEEDED`
  defined but never emitted), auth attempts (no rate limit despite Argon2 per-attempt
  cost), subscriptions (unbounded per client and per SUBSCRIBE packet), publish rate,
  retained topics, total sessions, and disk (no size visibility; inconsistent disk-full
  handling). Also recorded: the offline-queue cap exists but has no operator wiring, and
  the 1 MiB frame cap is a placeholder awaiting MQTT 5 Maximum Packet Size negotiation
  (both paid off here). Ordering: T1/T2 (admission plane, independent) → T3/T4 (protocol
  planes) → T5 (disk + closure).
