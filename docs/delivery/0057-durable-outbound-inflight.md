---
adr: "0057"
title: "Durable outbound in-flight state: exactly-once across a broker crash"
adr_status: Proposed
tasks:
  - id: 0057-T1
    title: "`SessionStore` outbound in-flight table: record / advance / clear / read, replicated with the session"
    status: done
    date: 2026-08-10
    evidence: "crates/mqtt-storage: record_outbound/advance_outbound/clear_outbound/outbound on SessionStore, with the write-before-send ordering contract in the trait docs (same as #124's) and the QoS-1 exclusion stated where someone would look for it. ReplicatedSessionStore keeps the window in the m/{client} metadata snapshot beside the inbound window — equally small and low-churn, bounded by receive-maximum — encoded as a backward-compatible tail after the ADR 0031 owner field. MemorySessionStore and both test FlakyStores implement it; the flaky ones gate the writes behind the same fault seam as enqueue, so T2's fail-closed test has its lever. 62 storage tests green."
    notes: "Two tests carry the load: a failover replica sharing only the log sees id, offset AND PHASE (asserted explicitly, because the phase is what decides PUBLISH+DUP versus PUBREL on resume — resuming in the wrong phase either re-sends a message the subscriber holds or releases one it never got); and a pre-0057 metadata record decodes with an empty outbound window rather than an error. The old record is constructed by BYTE-TRUNCATING a modern one at the owner field's end, not by trusting the encoder to remember what the old format was."
  - id: 0057-T2
    title: "Hub wiring: write at allocation and PUBREC, clear at PUBCOMP, fail closed with the ack withheld when the write fails"
    status: done
    date: 2026-08-10
    evidence: "crates/mqttd/src/hub.rs — send_qos_publish records the id durably BEFORE the PUBLISH reaches the wire (QoS 2 with a durable offset only); pub_rec advances the record durably BEFORE the PUBREL goes out; pub_comp clears it. Each failure has its own semantics and its own retry story: a failed record withholds the PUBLISH and requeues at the backlog FRONT (the message is already durable from #124, so nothing is lost — only deferred, in order); a failed advance withholds the PUBREL, and the subscriber's own PUBREC re-send is the retry; a failed clear is logged and tolerated, because the worst a leftover entry does is one spurious PUBREL on restore, which the subscriber answers with PUBCOMP (MQTT-4.3.3) — and THAT retries the clear. Four hub unit tests; 186 total green."
    notes: "Writing the deferral test exposed an ordering hole in the first design: pre-0057, backlog-non-empty implied quota-full (acks drain before the loop processes another publish), so fresh traffic could never overtake the backlog. A deferred delivery broke that invariant, and a new publish would have jumped ahead of it — per-client ordering is part of the contract. The deliver gate now also diverts on backlog-non-empty, and a publish that finds quota free retries the drain, making traffic the deferred delivery's retry clock instead of the next reconnect. The FlakyStore fault seam gained a SEPARATE fail_outbound lever, because the enqueue lever fires first and would mask the very path these tests exercise."
  - id: 0057-T3
    title: "Restore: rebuild `pending` from the table, resume at PUBLISH+DUP or PUBREL under the original id, seed the allocator past restored ids"
    status: done
    date: 2026-08-20
    evidence: "Reconciliation (found by the 2026-08-20 delivery validation): the substance shipped long since and later records already leaned on it — the storage API (record_outbound / advance_outbound / reserve_packet_ids, mqtt-storage/src/lib.rs) rebuilds pending outbound state on restore, ADR 0062's restore path replays it (record_outbound + advance_outbound for pubrec_seen, reserve_packet_ids last for the id high-water, per 0062-T4's evidence), and 0057-T4's own notes describe its SIGKILL proof as 'the out-of-process proof the in-process T3 tests approximated' — T4 (done) is the acceptance evidence for T3's mechanism: PUBREL under the known id after PUBREC, PUBLISH+DUP under the original id before it, allocator seeded past restored ids. This row simply never got flipped when the work landed."
  - id: 0057-T4
    title: "The SIGKILL acceptance test, both phases (PUBREL under the known id after PUBREC; PUBLISH+DUP under the original id before it)"
    status: done
    date: 2026-08-11
    evidence: "crates/mqttd/tests/inflight_durability.rs — the #130 acceptance criterion VERBATIM, against the real binary: subscriber PUBRECs, broker SIGKILLed (no flush, no goodbye), restart on the same data dir, and the resumed session receives PUBREL for the id it already knows — with the failure arms spelling out why a fresh-id PUBLISH is the defect. The companion covers the pre-PUBREC phase: PUBLISH re-sent with DUP under the ORIGINAL id, exactly one copy, and the handshake completes under it. Both publishers run their own full QoS 2 exchange first, so the broker holds exactly-once responsibility before the crash. 3 process-level tests green in ~10s."
    notes: "This is the out-of-process proof the in-process T3 tests approximated: a real SIGKILL, a real redb recovery, a real session resume."
  - id: 0057-T5
    title: "Measure the QoS 2 delta in the bench lane; record it; revisit the on-by-default decision if indefensible"
    status: done
    date: 2026-08-11
    evidence: "MEASURED (dev-grade: one machine, arm64 macOS, mosquitto clients, 500x256B end-to-end, persistent subscriber, MQTTD_DURABLE_SESSIONS=1, 3 runs per lane, baseline = dd3df27 pre-hub-wiring vs main): QoS 2 wall time roughly DOUBLES — 12.4/14.3/17.1s baseline vs 26.8/28.5/29.3s current, ~40 -> ~19 msg/s — consistent with the mechanism (up to three additional fsync-bound metadata writes per delivery: record at send, advance at PUBREC, clear at PUBCOMP). The QoS 1 CONTROL lane shows no delta (6.4/8.1/10.4 vs 5.9/7.5/9.0 — current marginally faster, i.e. noise), which is the check on the check: the QoS 1 exclusion held, and the measurement itself is sound. VERDICT: defensible; on-by-default stands. The cost lands only on the flow that asked for exactly-once by name, which was already serialized on a four-packet handshake and fsync-bound."
    notes: "The comparative bench lane could NOT measure this — it runs MQTTD_DURABLE_SESSIONS=0 with clean sessions, where the 0057 path never executes; a before/after there would have reported a zero delta without running the code (a check that cannot fail). The harness is a dedicated script exercising exactly the changed path. Two anomalies recorded: (1) one warm-up run in the back-to-back sequence failed its 120s window (publisher fully acked, subscriber lagging) and did NOT reproduce in isolation — a virgin-store rerun with warn-level logging delivered all 500 in 22s with zero broker warnings; treated as machine noise, noted here so a recurrence has a trail. (2) run-over-run degradation within a session (rates fall ~30% across three consecutive runs) exists in BOTH binaries, so it is pre-existing store-growth behaviour, not 0057 — worth its own investigation someday. Future headroom: the three metadata writes are three separate snapshot RMWs; batching record with the #124 queue append would remove one fsync per message."
  - id: 0057-T6
    title: "Remove the Limitations entry the fix retires — the README claim and the code change together"
    status: done
    date: 2026-08-11
    evidence: "README.md — the 'redelivery after a broker crash is at-least-once, even at QoS 2' Limitations entry is deleted in the SAME PR that lands the acceptance test, and the status paragraph now states the new guarantee where #124's is stated: the redelivery resumes under the id the subscriber already knows, so exactly-once holds across a broker crash, not only a client reconnect. check-readme-facts green; no other doc referenced the retired entry (grepped COMPARISON/SIZING)."
    notes: "Bundled with T4 deliberately: the claim is only true once the whole mechanism is on main, and the ADR requires the claim and the code to change together."
---

# Delivery — ADR 0057

Tracking issue: #130. The decision text is
[the ADR](../adr/0057-durable-outbound-inflight.md); this file records what is actually
built, task by task, with evidence.

<!-- status-table:0057 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0057-T1 | ✅ done | 2026-08-10 | "crates/mqtt-storage: record_outbound/advance_outbound/clear_outbound/outbound on SessionStore, with the write-before-send ordering contract in the trait docs (same as #124's) and the QoS-1 exclusion stated where someone would look for it. ReplicatedSessionStore keeps the window in the m/{client} metadata snapshot beside the inbound window — equally small and low-churn, bounded by receive-maximum — encoded as a backward-compatible tail after the ADR 0031 owner field. MemorySessionStore and both test FlakyStores implement it; the flaky ones gate the writes behind the same fault seam as enqueue, so T2's fail-closed test has its lever. 62 storage tests green." |
| 0057-T2 | ✅ done | 2026-08-10 | "crates/mqttd/src/hub.rs — send_qos_publish records the id durably BEFORE the PUBLISH reaches the wire (QoS 2 with a durable offset only); pub_rec advances the record durably BEFORE the PUBREL goes out; pub_comp clears it. Each failure has its own semantics and its own retry story: a failed record withholds the PUBLISH and requeues at the backlog FRONT (the message is already durable from #124, so nothing is lost — only deferred, in order); a failed advance withholds the PUBREL, and the subscriber's own PUBREC re-send is the retry; a failed clear is logged and tolerated, because the worst a leftover entry does is one spurious PUBREL on restore, which the subscriber answers with PUBCOMP (MQTT-4.3.3) — and THAT retries the clear. Four hub unit tests; 186 total green." |
| 0057-T3 | ✅ done | 2026-08-20 | "Reconciliation (found by the 2026-08-20 delivery validation): the substance shipped long since and later records already leaned on it — the storage API (record_outbound / advance_outbound / reserve_packet_ids, mqtt-storage/src/lib.rs) rebuilds pending outbound state on restore, ADR 0062's restore path replays it (record_outbound + advance_outbound for pubrec_seen, reserve_packet_ids last for the id high-water, per 0062-T4's evidence), and 0057-T4's own notes describe its SIGKILL proof as 'the out-of-process proof the in-process T3 tests approximated' — T4 (done) is the acceptance evidence for T3's mechanism: PUBREL under the known id after PUBREC, PUBLISH+DUP under the original id before it, allocator seeded past restored ids. This row simply never got flipped when the work landed." |
| 0057-T4 | ✅ done | 2026-08-11 | "crates/mqttd/tests/inflight_durability.rs — the #130 acceptance criterion VERBATIM, against the real binary: subscriber PUBRECs, broker SIGKILLed (no flush, no goodbye), restart on the same data dir, and the resumed session receives PUBREL for the id it already knows — with the failure arms spelling out why a fresh-id PUBLISH is the defect. The companion covers the pre-PUBREC phase: PUBLISH re-sent with DUP under the ORIGINAL id, exactly one copy, and the handshake completes under it. Both publishers run their own full QoS 2 exchange first, so the broker holds exactly-once responsibility before the crash. 3 process-level tests green in ~10s." |
| 0057-T5 | ✅ done | 2026-08-11 | "MEASURED (dev-grade: one machine, arm64 macOS, mosquitto clients, 500x256B end-to-end, persistent subscriber, MQTTD_DURABLE_SESSIONS=1, 3 runs per lane, baseline = dd3df27 pre-hub-wiring vs main): QoS 2 wall time roughly DOUBLES — 12.4/14.3/17.1s baseline vs 26.8/28.5/29.3s current, ~40 -> ~19 msg/s — consistent with the mechanism (up to three additional fsync-bound metadata writes per delivery: record at send, advance at PUBREC, clear at PUBCOMP). The QoS 1 CONTROL lane shows no delta (6.4/8.1/10.4 vs 5.9/7.5/9.0 — current marginally faster, i.e. noise), which is the check on the check: the QoS 1 exclusion held, and the measurement itself is sound. VERDICT: defensible; on-by-default stands. The cost lands only on the flow that asked for exactly-once by name, which was already serialized on a four-packet handshake and fsync-bound." |
| 0057-T6 | ✅ done | 2026-08-11 | "README.md — the 'redelivery after a broker crash is at-least-once, even at QoS 2' Limitations entry is deleted in the SAME PR that lands the acceptance test, and the status paragraph now states the new guarantee where #124's is stated: the redelivery resumes under the id the subscriber already knows, so exactly-once holds across a broker crash, not only a client reconnect. check-readme-facts green; no other doc referenced the retired entry (grepped COMPARISON/SIZING)." |
<!-- /status-table:0057 -->
