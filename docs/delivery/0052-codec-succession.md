---
adr: "0052"
title: "Codec succession: postcard replaces bincode on every cluster surface"
adr_status: Accepted
tasks:
  - id: 0052-T1
    title: postcard succeeds bincode on all five surfaces (peer frames, SWIM payloads, raft RPCs, lease store, AppProps blob) with strict full-consumption decodes; frozen Hello/ProxyHello carved out into the hand-rolled `peer::frozen` codec, golden-bytes test unmodified; PROTO 5→6; schema stamps reset to 1; MAX_RAFT_RPC 4 MiB enforced sender- and receiver-side; deny.toml RUSTSEC-2025-0141 ignore removed
    status: done
    date: 2026-08-04
    evidence: "grep bincode over crates/ + Cargo.lock → zero build references; cargo deny check advisories/bans ok with ignore=[] ; the_frozen_frames_encode_byte_for_byte_stably green with zero edits; new adversarial tests (lying inner length, trailing bytes) green; mqtt-cluster 232 lib tests + workspace suites green (binary_smoke auth-penalty test fails on macOS only — binds 127.0.0.x aliases, pre-existing, Linux CI unaffected); clippy clean"
  - id: 0052-T2
    title: Fuzz seeds — postcard-encoded siblings for peer_decode (a Replicate frame) and swim_message (a ping); bincode-era seeds kept as adversarial corpus
    status: done
    date: 2026-08-04
    evidence: "seeds/peer_decode/replicate.bin (24 B, full frame) + seeds/swim_message/ping-postcard.bin (39 B, payload) generated by the real encoders; hello.bin (frozen layout, still current) and ping.bin (bincode-era, now adversarial) kept; 200k-run fuzz smoke green on both targets"
  - id: 0052-T3
    title: Bench re-baseline — durable_hotpaths peer-codec rows in docs/benchmarks/BASELINE.md re-measured under postcard
    status: done
    date: 2026-08-04
    evidence: "encode 439 ns / 630 MiB/s (was 284 ns), decode 357 ns / 772 MiB/s (was 418 ns) — encode pays for varint packing, decode faster; rows + re-baseline note updated in BASELINE.md"
  - id: 0052-T4
    title: BASELINE_REF bump (follow-up PR, post-merge by necessity) — cluster_upgrade.rs pinned baseline moves to this change's merge commit; nightly red window disclosed in both PR bodies
    status: done
    date: 2026-07-23
    evidence: "Delivered by 12af610 (#72): cluster_upgrade.rs BASELINE_REF moved to the codec-succession merge, so a HEAD build is only ever asked to interoperate with a postcard-speaking baseline. Recorded 2026-08-07 during a delivery-record audit — the work shipped on 2026-07-23 and the status was never flipped, the mirror image of a title claiming work that was never done. The pin has since moved twice more for later wire reshapes (c4e29d1 for the ADR 0054 cluster_id field, 833ea5a for the issue #92 generation field), which is the mechanism working as designed: it only ever moves deliberately, with the reshape that requires it.
---

# 0052 — Codec succession: delivery

**Decision:** [ADR 0052](../adr/0052-codec-succession.md). One-line story: bincode
(RUSTSEC-2025-0141, archived) is out of the tree; postcard carries every cluster
surface; the two ADR 0038 frozen bootstrap frames keep their pinned bytes via a
hand-rolled codec, so cross-version rejection stays polite.

<!-- status-table:0052 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0052-T1 | ✅ done | 2026-08-04 | "grep bincode over crates/ + Cargo.lock → zero build references; cargo deny check advisories/bans ok with ignore=[] ; the_frozen_frames_encode_byte_for_byte_stably green with zero edits; new adversarial tests (lying inner length, trailing bytes) green; mqtt-cluster 232 lib tests + workspace suites green (binary_smoke auth-penalty test fails on macOS only — binds 127.0.0.x aliases, pre-existing, Linux CI unaffected); clippy clean" |
| 0052-T2 | ✅ done | 2026-08-04 | "seeds/peer_decode/replicate.bin (24 B, full frame) + seeds/swim_message/ping-postcard.bin (39 B, payload) generated by the real encoders; hello.bin (frozen layout, still current) and ping.bin (bincode-era, now adversarial) kept; 200k-run fuzz smoke green on both targets" |
| 0052-T3 | ✅ done | 2026-08-04 | "encode 439 ns / 630 MiB/s (was 284 ns), decode 357 ns / 772 MiB/s (was 418 ns) — encode pays for varint packing, decode faster; rows + re-baseline note updated in BASELINE.md" |
| 0052-T4 | ✅ done | 2026-07-23 | "Delivered by 12af610 (#72): cluster_upgrade.rs BASELINE_REF moved to the codec-succession merge, so a HEAD build is only ever asked to interoperate with a postcard-speaking baseline. Recorded 2026-08-07 during a delivery-record audit — the work shipped on 2026-07-23 and the status was never flipped, the mirror image of a title claiming work that was never done. The pin has since moved twice more for later wire reshapes (c4e29d1 for the ADR 0054 cluster_id field, 833ea5a for the issue #92 generation field), which is the mechanism working as designed: it only ever moves deliberately, with the reshape that requires it. |
<!-- /status-table:0052 -->

## Notes

- 2026-08-04 — Clean-break decision confirmed by the maintainer: no releases owe
  compatibility, schema stamps reset to 1 (pre-release baseline; the bump history
  restarts at 1.0.0). Dev data dirs from before this change must be wiped —
  `lease.redb` in particular keeps stamp v1 while its bytes change, so it fails at
  value decode rather than at the schema gate.
- 2026-08-04 — Frozen-frame carve-out implemented; `the_frozen_frames_encode_byte_for_byte_stably`
  passes with zero edits, which is the acceptance criterion ADR 0038 sets for
  "not a cross-version wire break at bootstrap".
