---
adr: "0009"
title: MQTT 5.0 session & message expiry
adr_status: Accepted
tasks:
  - id: 0009-P1
    title: Session expiry (normalization, hub attach/detach lifecycle, sweep GC, accessor)
    status: done
    date: 2026-06-22
    evidence: hub::session_expiry_finite_retains_then_expires; hub::session_expiry_reconnect_cancels_expiry
  - id: 0009-P2
    title: Message expiry (stored absolute deadline, drop-on-expiry at replay, remaining-interval on delivery)
    status: done
    date: 2026-06-22
    evidence: logged::enqueue_with_expiry_round_trips_the_deadline; hub::replayed_message_forwards_remaining_expiry_interval
  - id: 0009-P3
    title: Durable expiry deadline (persist disconnect time so takeover preserves the clock)
    status: done
    date: 2026-06-24
    evidence: "ADR 0009 phase 3. SessionMeta persists session_expiry_at (absolute epoch); the hub's expiring map + sweep use absolute wall-clock (Clock) so deadlines are portable; detach persists the deadline, attach (persistent only) clears it; the sweep reconciles store.expiring_sessions() for OWNED, offline, untracked sessions every EXPIRY_RECONCILE_EVERY ticks so a new owner inherits orphaned deadlines after a takeover and expires them at the original time. Tests inherited_session_expiry_is_swept_after_takeover, session_expiry_finite_retains_then_expires (clock-driven), session_expiry_persists_and_enumerates, decodes_pre_expiry_meta_records; full workspace green."
  - id: 0009-P4
    title: Will Delay Interval honoured (min(delay, expiry), cancelled by a resume, persisted across node death)
    status: done
    date: 2026-08-15
    evidence: "Issue #299 — the Will Delay Interval (0x18) was decoded by the codec and read by NOTHING outside it, so the will fired the instant the connection ended and every client using the property got a spurious 'offline' on each brief blip. Found by the Eclipse paho.mqtt.testing suite, i.e. by an INDEPENDENT oracle: every test this project wrote itself was green. THIS RECORD IS THE POST-REMEDIATION ONE: the first implementation pass landed the mechanism and then died before self-review, three blocking and three lesser defects were found against the cold tree, and every claim below was re-verified by running it.\n\nMECHANICS AS DELIVERED. Properties::will_delay_interval() joins ADR 0009 §4's typed accessors. conn.rs::into_will_spec reads it into WillSpec { message, delay } so '(no will, delay 5)' is unrepresentable; Online/PendingAttach/HubCommand::Attach carry Option<WillSpec>. detach's existing `if !graceful` block calls arm_or_publish_will: effective == 0 publishes INLINE on the same dispatch exactly as before this feature (every v3.1.1 client and every v5 client without 0x18 — today's whole fleet, byte for byte), and otherwise arms. ARITHMETIC, once, at the moment the will would have fired: effective = 0 when the hub holds no session expiry for the client (expiry 0, or v3.1.1 clean_session=1 — the session ends now, so the will fires now), = delay when expiry is u32::MAX, else min(delay, expiry) per §3.1.2.11.2's 'whichever happens first'. deadline_epoch = ceil(clock.now_epoch_millis() + effective*1000) as whole epoch seconds — ROUNDED UP, see PRECISION below (absolute, for the record); due = Instant::now() + effective (MONOTONIC, saturating to a far future rather than to now, so a u32::MAX delay cannot fire immediately). WHAT CHECKS IT: a third select! branch on the hub loop, sleep_until over a cached Option<Instant> hint that may be too EARLY but never too late — not a spawned timer (ADR 0061/#242: it would need spawn_owned, could read no hub state, would need a conn_id fence and a JoinHandle per client, and a resize that rehome-closes 1 700 sessions would spawn 1 700 sleeping tasks) and not the 1 s sweep alone (its granularity is observable and would make the foreign oracle's 2-6 s bound flaky). The sweep keeps the two jobs it is better at: the expiry coupling (a pending will is published BEFORE discard_session, so a wall-clock jump cannot expire a session out from under its own will) and re-arming wills read back from the store.\n\nCANCELLATION, and the three defects that were fixed in it. (1) A RESUME IS A RESUME, whatever the resuming CONNECT asks for: the takeover branch re-derived min(delay, expiry) from the NEW connection, so a half-open old socket plus a client reconnecting without Session Expiry (the v5 default 0 — paho's default) yielded effective == 0 and PUBLISHED the replaced connection's delayed will. [MQTT-3.1.3-9] does not let the new connection's properties decide; Session Expiry bounds a will only while the session has no connection. (2) A LEASE MOVE IS NOT A SESSION END: discard_session_local's hand-off treated every caller as 'the session ended', but release_moved_sessions is a local ROUTING release whose record and deadline deliberately stay for the new owner (ADR 0043 P2) — so a lease move inside the window published the will immediately (~29 s early), labelled session-ended, and left the record's copy to fire a second time. It now takes an explicit SessionEnd reason: Ended publishes, RoutingReleased HANDS OFF when the record provably holds the will (mqttd_wills_handed_off_total; the new owner fires it at the original absolute deadline, reason=inherited) and otherwise keeps it armed here, because a memory-only will has no other holder. (3) THE ARM WAS DURABLE AND THE CANCEL WAS MEMORY-ONLY: cancel_pending_will returned early when this node's map held nothing, so a record carrying a will this node never armed (armed on a peer, or before this node restarted) was never cleared, and the inherited-session scan then re-armed the stale block with an already-past deadline and announced the client dead AFTER it had resumed and disconnected cleanly. The attach's off-loop recovery now reads the block (SessionStore::pending_will — one metadata read on a path that already does an authoritative claim plus two reads) and the cancel clears the record when there is anything to clear. CLEAN START, RE-ARGUED FROM THE SPEC TEXT IN THE CLOSING ROUND AND REVERSED: a Clean Start CONNECT for the same client id inside the window now DELETES the will rather than publishing it. §3.1.2.5's normative sentence is 'The Will Message MUST be published after the Network Connection is subsequently closed and either the Will Delay Interval has elapsed or the Session ends, unless the Will Message has been deleted by the Server on receipt of a DISCONNECT packet with Reason Code 0x00 (Normal disconnection) or a new Network Connection for the ClientID is opened before the Will Delay Interval has elapsed [MQTT-3.1.2-8]' — the 'unless' excepts the WHOLE obligation, the 'or the Session ends' trigger included, and its second clause is keyed on the CLIENTID, not on the Session. §3.1.3.2.2's [MQTT-3.1.3-9] is the narrower session-keyed rule and covers only the resume. The previous round's reading cited [MQTT-3.1.2-4] plus 'or the Session ends' and never engaged with the governing clause; it was wrong in the direction that costs users the feature, because clean_start=1 with a non-zero Session Expiry is an ordinary shape (it is literally what this project's paho oracle sends) and for every such client the delay suppressed nothing. Both halves agree again, now on 'delete': SessionEnd::ReplacedByNewConnection offline, resolve_replaced_will online, counted mqttd_wills_cancelled_total{reason=\"clean-start\"}; delay==0 still publishes on the dispatch in both, because [MQTT-3.1.2-8]'s exception needs a window to open inside and a zero interval has already elapsed at the close. Cancellation still sits beside online.insert, so a CONNECT refused for auth/quota/Unavailable/owner-mismatch does not cancel anything.\n\nNODE DEATH, PER DURABLE SHAPE — the claim that was FALSE in one of two durable shapes and is now true in both, with the third stated rather than implied. Clustered durable (MQTTD_DURABLE_SESSIONS=1, with MQTTD_DATA_DIR): survives node death, lease move, takeover, restart — replicated record, new owner's scan re-arms. NODE DEATH in that shape is now proven END TO END rather than by hub unit tests over a stub store: durable_sessions::a_delayed_will_survives_the_death_of_the_node_that_armed_it starts three real nodes over real sockets, kills the group's owner inside the window and asserts the survivor that takes the group over announces the will from the replicated record (added in the closing round, because the previous round's own lesson was that a durability claim can be true in one shape and false in another precisely where no test covered it; the other three triggers in that row ride the same record and the same scan and remain hub-level only). Clustered durable WITHOUT a data dir (announced EPHEMERAL at boot): survives node death and a lease move, not a whole-cluster restart. Single-node on-disk (MQTTD_DATA_DIR only, ADR 0018 phase 1): NOW survives crash and graceful restart over the same data dir — it did not before. The will was written to sessions.redb correctly and never read back, because ReplicatedSessionStore::all_sessions enumerates through ReplicatedLog::keys() and PersistentLog had no keys(), taking the trait's Ok(Vec::new()) default; the restarted node's scan saw NO sessions at all. The same hole silently disabled this ADR's own §3 machinery in that mode (persisted expiry deadlines never fired across a restart either). PersistentLog::keys() now range-scans the entries table skipping key-to-key (O(distinct keys x log n), so a session with a thousand queued messages costs one step). UPGRADE BEHAVIOUR CHANGE, STATED WHERE OPERATORS READ: implementing keys() switches ADR 0009 §3 session expiry ON for every ADR 0018 phase-1 deployment, so the FIRST restart onto this build discards every offline session whose persisted deadline has already passed — together with its acked offline queue. Verified on the real binary: a session with SessionExpiryInterval=5 plus one PUBACKed QoS 1 message, node down 8 s, gives session_present=0 and no replay after the restart, where the pre-fix keys() gives session_present=1 and replays it. Spec-correct and irreversible, so it is now said in four places (ADR 0009 §2 as-delivered, ADR 0018 §3, OPERATIONS' Rolling upgrades section with a pre-flight — start a node over a COPY of the data dir — and TROUBLESHOOTING as a symptom) and, more usefully, the broker says it itself BEFORE doing it: the boot scan emits one WARN 'DISCARDING offline sessions past their persisted Session Expiry deadline' carrying sessions=, oldest_overdue_s= and up to 20 client ids with how far past due each is, built by summarize_overdue_discard (pure, so the counting is asserted rather than the formatting) and pinned by hub::the_overdue_discard_summary_counts_every_session_and_names_the_worst_first plus hub::a_scan_with_nothing_overdue_says_nothing. In-memory store: nothing survives the process, which is what MemorySessionStore already says about every session it holds (residual R4).\n\nPRECISION PROMISED, AND THE CLOSING ROUND'S FIRST FIX. Never early — locally that was always true (the deadline is a monotonic tokio::time::Instant, immune to NTP steps) and ACROSS THE RECORD it was FALSE, which is the path the whole persistence exists for. The arm stored floor(now)+effective and the inherit computed deadline-floor(now), so the elapsed delay was effective - frac(arm) + frac(re-arm), uniform in (effective-1s, effective+1s). Measured on the real broker: armed 09:28:54.7408 with an 8 s delay, published 09:29:02.0524 = 7.312 s, 0.69 s EARLY; and on a real 3-node durable cluster a 12 s delay announced 11.20 s after the owner was killed. Fixed as arithmetic, not machinery: Clock gained now_epoch_millis (defaulted to now_epoch_secs()*1000 so a whole-second test clock stays exactly whole-second), Hub::will_deadline_epoch ROUNDS THE STORED DEADLINE UP to the next whole second, and inherit_sessions computes the remaining time from it in MILLISECONDS. Every rounding error in the round trip now lands LATE, by strictly less than a second. Per path as delivered: LOCALLY within one hub-loop dispatch — sub-millisecond only where the store is in memory; where it persists the fire includes an fsync'd durable clear and mqttd_hub_dispatch_seconds_sum{command=\"will\"} measured 8.9 ms mean over 486 fires (12.2 ms for a single fire on an idle node), which corrects an earlier flat 'sub-millisecond' claim that was off by ~10x in the shapes that persist. SESSION EXPIRY as the binding bound: within the 1 s sweep tick after it, never before. After a takeover, restart or node death: whole-second wall clock, late by the round-up (<1 s) plus the inherited-scan cadence (~1 s inside a takeover window, else up to EXPIRY_RECONCILE_EVERY = 30 s), plus inter-node clock skew. The foreign oracle measured 3.00 s for a 3 s delay locally.\n\nEXACTLY ONCE, AND THE FENCE THAT MAKES IT SO — the closing round's second fix, one mechanism for two defects. The inherited-session scan reads all_sessions() OFF the hub loop and its result is applied later, so anything that happens to a will in between is invisible to it. (a) DUPLICATE, in 5 of 6 restart runs: sweep_expired_sessions spawns the scan BEFORE fire_due_wills, the scan's read raced the fire's durable clear, and the stale block was re-armed with zero remaining and published a second time ~20 ms later — both copies labelled reason=\"inherited\", so an operator could not tell one duplicate from two deaths, against a README that promises 'never duplicated per node'. (b) RESURRECTION: the same staleness re-armed a will a RESUME had already cancelled and published it at once because its deadline had passed, violating [MQTT-3.1.3-9] and [MQTT-3.14.4-3] together with no connection event to correlate against. The fix is the fence this hub already uses for SessionRecovered's stale results, made per-client: Hub::note_will_decided records every will published, cancelled or handed off while a scan is in flight (wills_decided_since_scan, cleared when the next scan starts), and inherit_sessions refuses to re-arm those. Re-ordering the sweep's own steps is NOT a fix and is deliberately not relied on — the read is off-loop, so any ordering can still be raced by a slow read.\n\nRED-FIRST, MUTATION-PROVEN — 11 mutations, each reverted, run, restored, nothing left behind (grep MUTATION|PROBE|zz_|MEAS clean). [1] hub.rs takeover branch re-derives the delay from the new connection (the pre-fix code) -> a_resume_never_publishes_the_will_whatever_the_resuming_connect_asks_for RED (its clean-start companion was renamed in the closing round when that ruling was reversed — see [14]). [2] cancel_pending_will ignores the record flag (record_holds = false) -> a_pending_will_this_node_never_armed_is_cleared_by_a_resume RED. [3] discard_session_local treats every caller as SessionEnd::Ended -> a_rehome_close_is_silent_for_a_delay_using_client AND a_lease_move_hands_a_persisted_pending_will_to_the_new_owner both RED. [4] PersistentLog::keys() returns the trait default -> storage keys_enumerates_every_logical_key_and_survives_a_reopen RED, and persistence::a_delayed_will_survives_a_node_restart_over_the_same_data_dir RED. [5] detach's arm reverted to the unconditional publish_will -> a_will_delay_holds_the_will_until_the_delay_elapses, a_session_expiry_shorter_than_the_delay_bounds_the_will_at_the_expiry, a_delayed_will_is_still_delivered_live_under_brownout_and_counted_as_a_drop, a_rehome_close_is_silent_for_a_delay_using_client all RED. [6] min(delay, secs) -> delay -> a_session_expiry_shorter_than_the_delay_bounds_the_will_at_the_expiry RED. [7] the expiry-0 clamp removed -> a_zero_session_expiry_publishes_the_will_immediately_despite_a_delay RED. [8] `if !graceful` -> `if true` -> a_graceful_disconnect_arms_no_delayed_will RED (the #265/#238 anti-regression). [9] the inherit_sessions re-arm deleted -> a_delayed_will_is_fired_from_the_persisted_record_by_a_node_that_never_saw_the_disconnect RED. [10] unacked_received_qos2 restored to 'emit only when non-empty' -> logged::a_pending_will_after_an_empty_unacked_block_still_decodes RED (the decoder eats the will block's length prefix as an unacked count: the silent session-metadata corruption ADR 0058 clause 3 forbids). [11] delay hard-coded to 0 in conn.rs::into_will_spec -> ALL hub-lib tests stay GREEN and only keepalive_lwt::a_v5_will_delay_interval_is_honoured_end_to_end, a_v5_reconnect_inside_the_delay_window_cancels_the_will_end_to_end and persistence::a_delayed_will_survives_a_node_restart_over_the_same_data_dir go red — which is exactly why those exist, and it is the shape of the original defect (a property read by nothing). CLOSING ROUND, six more. [12] will_deadline_epoch reverted to floor(now)+effective and the inherited remaining back to whole seconds -> hub::a_will_inherited_across_a_restart_never_fires_before_its_deadline RED at the record-deadline assertion (persisted 1000008.000 s for a deadline of 1000008.900 s) AND persistence::a_will_inherited_across_a_restart_fires_no_earlier_than_its_deadline_exactly_once RED on run 0 with a real measurement, 'an inherited will fired 355 ms EARLY (a 6 s delay elapsed in 5644 ms)'. [13] the fence check in inherit_sessions disabled -> hub::an_inherited_will_is_published_exactly_once_across_a_scan_racing_the_fire RED ('release at 3000 ms: 2 copies arrived' — and green at the 1100/2900 ms release points, which is why the release point is SWEPT rather than fixed) and hub::a_stale_inherited_scan_never_republishes_a_will_a_resume_cancelled RED. [14] clean start restored to publishing (both halves) -> hub::a_clean_start_connect_for_the_same_client_id_deletes_the_pending_will and hub::a_clean_start_takeover_deletes_the_replaced_will_like_the_offline_half_does RED, and the FOREIGN ORACLE red too: paho case (c) reports 19 passed / 1 failed, PAHO INTEROP FAILED. [15] the clear-path guard removed from ReplicatedSessionStore::set_pending_will -> logged::clearing_a_pending_will_does_not_resurrect_a_removed_session_record RED. [16] summarize_overdue_discard reports the capped count instead of the real one -> hub::the_overdue_discard_summary_counts_every_session_and_names_the_worst_first RED. [17] the inherit_sessions re-arm deleted (mutation [9] again, but against the new cluster test) -> durable_sessions::a_delayed_will_survives_the_death_of_the_node_that_armed_it RED. Each reverted, run, restored; grep MUTATION|PROBE|zz_|MEAS clean.\n\nFOREIGN ORACLE, run locally against the real binary. scripts/interop/paho_conformance.py carries three Eclipse Paho cases (the third added in the closing round, for the ruling that changed — an interpretation this contested should have had a foreign oracle from the start): WillDelayInterval=3 + SessionExpiryInterval=30, socket killed — nothing at +1 s, arrival strictly between 2 s and 6 s; then the same kill followed by a reconnect at +1 s that sends NO Session Expiry Interval (paho's default), asserting nothing is ever published; and (c) the same kill followed by a reconnect at +1 s with clean_start=True and SessionExpiryInterval=30 — the ordinary shape, and what dying_client itself sends — asserting the will is DELETED, never published [MQTT-3.1.2-8]. 20 passed / 0 failed against the real binary, the delayed will landing at 3.00 s. Under mutation [11] the same lane reports 'landed after 0.00s' and fails 3 assertions, which matches the issue's observed 0.1 s. What the paho lane CANNOT reach: the half-open takeover of defect (1) — a foreign client cannot manufacture a half-open socket without a firewall — so that one is pinned by the hub test only.\n\nGATES (all run in this worktree, closing round): cargo fmt --all --check clean; cargo clippy --all-targets --all-features -D warnings clean; mqttd --lib 326 passed (314 originally, 320 after the first remediation, +6 here); mqtt-storage 79 (+2); mqtt-codec 59+1; mqtt-cluster 15+5+4; keepalive_lwt 9; brownout_ack 5; durable_sessions 11 (+1, the clustered node-death will); v5_protocol 16; cluster_stress 11; persistence 4 (+1, six repeated real restarts); end_to_end 5; gen-status.py regenerated and --check clean; check-readme-facts.py green (61 ADRs, 100 evidence citations, dates in sync); paho oracle 20/20 against the real binary. Every metric selector cited in the docs touched here was re-checked against its emitting call site (the only apparent misses are the Prometheus histogram suffixes _sum/_bucket on mqttd_hub_dispatch_seconds, and command=\"will\" is a real label value at hub.rs's observe_hub_dispatch), and every test name cited resolves to a real fn — one stale citation — the old clean-start takeover test's name, which the reversal renamed — was found that way and fixed. ADR 0058, RE-VERIFIED IN THE CLOSING ROUND AND STILL UNTOUCHED: the persisted block is strictly trailing, length-prefixed and EOF-defaulted, and this round changed no encoder or decoder byte either — encode_session_meta/decode_session_meta were not edited, deadline_epoch is still the same u64 epoch second in the same block (only the VALUE is now rounded up, which is arithmetic at the call site, not a format change), and the one storage edit is a guard on set_pending_will's CLEARING path that writes strictly less than before. crates/mqtt-cluster/** and crates/mqttd/tests/cluster_upgrade.rs are not in the diff. So sessions.redb SCHEMA_VERSION stays 2, the empty migration registry stays empty, and cluster_upgrade's BASELINE_REF needs no bump (nightly-tier, #[ignore] in the per-PR profile, so not run here). Pinned as before by logged::the_pending_will_block_is_strictly_trailing_and_absent_by_default and logged::a_pending_will_after_an_empty_unacked_block_still_decodes, both green. One metric-help lesson worth keeping: naming another metric's full sample selector inside a HELP string made a substring assertion in an existing test read it as a sample — the help text now spells the label out in prose instead.\n\nMINOR FIXED IN THE CLOSING ROUND: ReplicatedSessionStore::set_pending_will read-modify-wrote through load_meta(client)?.unwrap_or_default(), so set_pending_will(client, None) against a REMOVED record wrote a fresh m/{client} snapshot with owner: None — and because claim_session treats any existing meta record as present: true and adopts the first claimant, that phantom would report session_present=1 for a discarded session and, carrying no expiry deadline, would never be reaped. Reachable wherever the record is deleted between an arm and its clear. MemorySessionStore's implementation of the same seam was already guarded, which is exactly the drift a comment there claimed could not exist; the clearing path now returns early when there is no record, pinned by logged::clearing_a_pending_will_does_not_resurrect_a_removed_session_record with logged::the_memory_store_agrees_that_a_clear_creates_nothing asserting the two shapes side by side.\n\nRESIDUALS R1-R6, all named in ADR 0009 §2; the three an operator can see (R1, R2, R3) also in OPERATIONS and TROUBLESHOOTING, and R4's consequence for a roll in OPERATIONS. R1 an arm on a non-owning node cannot persist (structural after a rehome close; counted mqttd_pending_will_unpersisted_total{reason=\\"not-owner\\"}), so a node death inside the window loses that one. R2 a will still lives only in Online while its client is CONNECTED, so a node dying with live clients loses their wills — unchanged by this work, not closed by it. R3 cross-node cancellation is NOT closed: the rehome close is now SILENT at the close, but the will still fires one delay later because the client reconnects on the owner (0009-P5). R4 SIGTERM does not fire pending wills early, deliberately. R5 RETIRED by the closing round's spec reading: it named 'a Clean Start CONNECT drops a record-only pending will instead of publishing it' as a LOSS, and under [MQTT-3.1.2-8] dropping it is the correct outcome, so the residual and the durable read that would have fixed it both disappear (the number is kept as a slot so R-numbers elsewhere keep meaning what they meant). R6 NEW, measured not reasoned: the arm's durable read-modify-write (persist_pending_will -> set_pending_will -> load_meta + append + truncate, fsync'd) is AWAITED ON THE HUB LOOP inside detach, beside the pre-existing persist_detach_deadline await, so a mass ungraceful disconnect of delay-using sessions roughly DOUBLES an already-multi-second stall. Measured against the real binary, single-node on-disk, with a probe client timing hub round-trips: 500 sessions killed at once with delay 6 -> worst client round-trip 6.49 s and the loop degraded until +11.0 s, versus a delay-0 control (today's fleet) at 4.98 s / +6.84 s; at 200 sessions, 2.37 s / +4.21 s versus 1.13 s / +2.05 s. Not fixed here because moving it off-loop means arming optimistically and correcting the persisted flag from a spawned reply — a new command, a new fence, and a window in which R1's counter lies, i.e. an ADR 0017-shaped motion rather than a closing-round edit. Named with its numbers in ADR 0009 §2 and in OPERATIONS where an operator sizing a roll will meet it; follow-up drafted."
  - id: 0009-P5
    title: Cross-node cancellation of a pending delayed Will (a resume on another node)
    status: deferred
    notes: "Deferred, mechanism named (issue #299 R3). A resume that lands on a DIFFERENT node does not cancel a pending will held in the closing node's memory — and after a rehome close (ADR 0005) that is the routine case, because the reconnect is served by the owner. Two ways to close it: (a) fold an attach marker (client id + attach epoch) into the owner's existing attach-time SessionMeta write and compare it at fire time — one off-loop read, no peer-proto bump, and it reuses the record this feature already writes; or (b) add an additive PeerMessage::PendingWill hand-off, which needs a proto bump (ADR 0038) and a cluster oracle. (a) is preferred. Until then the residual is bounded, counted (mqttd_wills_published_total{reason=\"delay-elapsed\"} during a roll) and documented in OPERATIONS as 'size the device-offline suppression window to the roll PLUS your fleet's largest will delay'."
---

# Delivery — ADR 0009: MQTT 5.0 session & message expiry

Decision: [docs/adr/0009-mqtt5-expiry.md](../adr/0009-mqtt5-expiry.md).

## Plan

The decision's §5 phased implementation gives three phases: session expiry, then the
storage-format message-expiry change, then the durable-deadline follow-up that closes the
§2 carried limitation. Each task carries a stable id used by commits, tests, and the dashboard.

| Task | Acceptance criterion |
|------|----------------------|
| **0009-P1** Session expiry | Both versions normalize to `(clean_start, session_expiry)` at the connection edge (v3.1.1 falls out as the `{0, 0xFFFFFFFF}` cases); the hub records the interval on attach (cancelling any pending deadline), and on detach discards immediately (`0`), keeps forever (`0xFFFFFFFF`), or arms `now + session_expiry`; a periodic sweep tick discards every session past its deadline via a single `discard_session` helper; a `session_expiry_interval()` accessor reads the property. |
| **0009-P2** Message expiry | A queued entry carries an absolute deadline (`now + interval` on enqueue, none if absent); replay/delivery drops past-deadline entries and sets the outbound Message Expiry Interval to the remaining seconds. A storage-format change. |
| **0009-P3** Durable deadline | The disconnect time is persisted in the session's durable meta snapshot so a replica takeover preserves the session-expiry clock instead of restarting it. |
| **0009-P4** Will Delay Interval | A Will carrying `0x18` is held for `min(will delay, session expiry)` after an ungraceful end rather than published on the spot (§3.1.3.2.2 + §3.1.2.11.2); **a resume is a resume** — inside the window it cancels the will entirely [MQTT-3.1.3-9], whatever the resuming CONNECT's own Session Expiry says, and the cancel clears the DURABLE block as well as this node's memory; a session that ENDS inside the window (Clean Start online or offline, expiry) publishes it, while a lease move — which is not a session end — hands it to the new owner; a client's own DISCONNECT still suppresses it (#265/#238); the pending will and its ABSOLUTE deadline persist in the session record, so which failures it survives is stated **per store shape** rather than claimed in general; and the timing is pinned by an independent foreign-client oracle, since a self-written suite is exactly what missed this for six months. |
| **0009-P5** Cross-node will cancellation | A resume that lands on a node other than the one holding the pending will cancels it (the ADR 0005 rehome case). Deferred; mechanism named. |

## Progress

<!-- status-table:0009 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0009-P1 | ✅ done | 2026-06-22 | hub::session_expiry_finite_retains_then_expires; hub::session_expiry_reconnect_cancels_expiry |
| 0009-P2 | ✅ done | 2026-06-22 | logged::enqueue_with_expiry_round_trips_the_deadline; hub::replayed_message_forwards_remaining_expiry_interval |
| 0009-P3 | ✅ done | 2026-06-24 | "ADR 0009 phase 3. SessionMeta persists session_expiry_at (absolute epoch); the hub's expiring map + sweep use absolute wall-clock (Clock) so deadlines are portable; detach persists the deadline, attach (persistent only) clears it; the sweep reconciles store.expiring_sessions() for OWNED, offline, untracked sessions every EXPIRY_RECONCILE_EVERY ticks so a new owner inherits orphaned deadlines after a takeover and expires them at the original time. Tests inherited_session_expiry_is_swept_after_takeover, session_expiry_finite_retains_then_expires (clock-driven), session_expiry_persists_and_enumerates, decodes_pre_expiry_meta_records; full workspace green." |
| 0009-P4 | ✅ done | 2026-08-15 | "Issue #299 — the Will Delay Interval (0x18) was decoded by the codec and read by NOTHING outside it, so the will fired the instant the connection ended and every client using the property got a spurious 'offline' on each brief blip. Found by the Eclipse paho.mqtt.testing suite, i.e. by an INDEPENDENT oracle: every test this project wrote itself was green. THIS RECORD IS THE POST-REMEDIATION ONE: the first implementation pass landed the mechanism and then died before self-review, three blocking and three lesser defects were found against the cold tree, and every claim below was re-verified by running it.\n\nMECHANICS AS DELIVERED. Properties::will_delay_interval() joins ADR 0009 §4's typed accessors. conn.rs::into_will_spec reads it into WillSpec { message, delay } so '(no will, delay 5)' is unrepresentable; Online/PendingAttach/HubCommand::Attach carry Option<WillSpec>. detach's existing `if !graceful` block calls arm_or_publish_will: effective == 0 publishes INLINE on the same dispatch exactly as before this feature (every v3.1.1 client and every v5 client without 0x18 — today's whole fleet, byte for byte), and otherwise arms. ARITHMETIC, once, at the moment the will would have fired: effective = 0 when the hub holds no session expiry for the client (expiry 0, or v3.1.1 clean_session=1 — the session ends now, so the will fires now), = delay when expiry is u32::MAX, else min(delay, expiry) per §3.1.2.11.2's 'whichever happens first'. deadline_epoch = ceil(clock.now_epoch_millis() + effective*1000) as whole epoch seconds — ROUNDED UP, see PRECISION below (absolute, for the record); due = Instant::now() + effective (MONOTONIC, saturating to a far future rather than to now, so a u32::MAX delay cannot fire immediately). WHAT CHECKS IT: a third select! branch on the hub loop, sleep_until over a cached Option<Instant> hint that may be too EARLY but never too late — not a spawned timer (ADR 0061/#242: it would need spawn_owned, could read no hub state, would need a conn_id fence and a JoinHandle per client, and a resize that rehome-closes 1 700 sessions would spawn 1 700 sleeping tasks) and not the 1 s sweep alone (its granularity is observable and would make the foreign oracle's 2-6 s bound flaky). The sweep keeps the two jobs it is better at: the expiry coupling (a pending will is published BEFORE discard_session, so a wall-clock jump cannot expire a session out from under its own will) and re-arming wills read back from the store.\n\nCANCELLATION, and the three defects that were fixed in it. (1) A RESUME IS A RESUME, whatever the resuming CONNECT asks for: the takeover branch re-derived min(delay, expiry) from the NEW connection, so a half-open old socket plus a client reconnecting without Session Expiry (the v5 default 0 — paho's default) yielded effective == 0 and PUBLISHED the replaced connection's delayed will. [MQTT-3.1.3-9] does not let the new connection's properties decide; Session Expiry bounds a will only while the session has no connection. (2) A LEASE MOVE IS NOT A SESSION END: discard_session_local's hand-off treated every caller as 'the session ended', but release_moved_sessions is a local ROUTING release whose record and deadline deliberately stay for the new owner (ADR 0043 P2) — so a lease move inside the window published the will immediately (~29 s early), labelled session-ended, and left the record's copy to fire a second time. It now takes an explicit SessionEnd reason: Ended publishes, RoutingReleased HANDS OFF when the record provably holds the will (mqttd_wills_handed_off_total; the new owner fires it at the original absolute deadline, reason=inherited) and otherwise keeps it armed here, because a memory-only will has no other holder. (3) THE ARM WAS DURABLE AND THE CANCEL WAS MEMORY-ONLY: cancel_pending_will returned early when this node's map held nothing, so a record carrying a will this node never armed (armed on a peer, or before this node restarted) was never cleared, and the inherited-session scan then re-armed the stale block with an already-past deadline and announced the client dead AFTER it had resumed and disconnected cleanly. The attach's off-loop recovery now reads the block (SessionStore::pending_will — one metadata read on a path that already does an authoritative claim plus two reads) and the cancel clears the record when there is anything to clear. CLEAN START, RE-ARGUED FROM THE SPEC TEXT IN THE CLOSING ROUND AND REVERSED: a Clean Start CONNECT for the same client id inside the window now DELETES the will rather than publishing it. §3.1.2.5's normative sentence is 'The Will Message MUST be published after the Network Connection is subsequently closed and either the Will Delay Interval has elapsed or the Session ends, unless the Will Message has been deleted by the Server on receipt of a DISCONNECT packet with Reason Code 0x00 (Normal disconnection) or a new Network Connection for the ClientID is opened before the Will Delay Interval has elapsed [MQTT-3.1.2-8]' — the 'unless' excepts the WHOLE obligation, the 'or the Session ends' trigger included, and its second clause is keyed on the CLIENTID, not on the Session. §3.1.3.2.2's [MQTT-3.1.3-9] is the narrower session-keyed rule and covers only the resume. The previous round's reading cited [MQTT-3.1.2-4] plus 'or the Session ends' and never engaged with the governing clause; it was wrong in the direction that costs users the feature, because clean_start=1 with a non-zero Session Expiry is an ordinary shape (it is literally what this project's paho oracle sends) and for every such client the delay suppressed nothing. Both halves agree again, now on 'delete': SessionEnd::ReplacedByNewConnection offline, resolve_replaced_will online, counted mqttd_wills_cancelled_total{reason=\"clean-start\"}; delay==0 still publishes on the dispatch in both, because [MQTT-3.1.2-8]'s exception needs a window to open inside and a zero interval has already elapsed at the close. Cancellation still sits beside online.insert, so a CONNECT refused for auth/quota/Unavailable/owner-mismatch does not cancel anything.\n\nNODE DEATH, PER DURABLE SHAPE — the claim that was FALSE in one of two durable shapes and is now true in both, with the third stated rather than implied. Clustered durable (MQTTD_DURABLE_SESSIONS=1, with MQTTD_DATA_DIR): survives node death, lease move, takeover, restart — replicated record, new owner's scan re-arms. NODE DEATH in that shape is now proven END TO END rather than by hub unit tests over a stub store: durable_sessions::a_delayed_will_survives_the_death_of_the_node_that_armed_it starts three real nodes over real sockets, kills the group's owner inside the window and asserts the survivor that takes the group over announces the will from the replicated record (added in the closing round, because the previous round's own lesson was that a durability claim can be true in one shape and false in another precisely where no test covered it; the other three triggers in that row ride the same record and the same scan and remain hub-level only). Clustered durable WITHOUT a data dir (announced EPHEMERAL at boot): survives node death and a lease move, not a whole-cluster restart. Single-node on-disk (MQTTD_DATA_DIR only, ADR 0018 phase 1): NOW survives crash and graceful restart over the same data dir — it did not before. The will was written to sessions.redb correctly and never read back, because ReplicatedSessionStore::all_sessions enumerates through ReplicatedLog::keys() and PersistentLog had no keys(), taking the trait's Ok(Vec::new()) default; the restarted node's scan saw NO sessions at all. The same hole silently disabled this ADR's own §3 machinery in that mode (persisted expiry deadlines never fired across a restart either). PersistentLog::keys() now range-scans the entries table skipping key-to-key (O(distinct keys x log n), so a session with a thousand queued messages costs one step). UPGRADE BEHAVIOUR CHANGE, STATED WHERE OPERATORS READ: implementing keys() switches ADR 0009 §3 session expiry ON for every ADR 0018 phase-1 deployment, so the FIRST restart onto this build discards every offline session whose persisted deadline has already passed — together with its acked offline queue. Verified on the real binary: a session with SessionExpiryInterval=5 plus one PUBACKed QoS 1 message, node down 8 s, gives session_present=0 and no replay after the restart, where the pre-fix keys() gives session_present=1 and replays it. Spec-correct and irreversible, so it is now said in four places (ADR 0009 §2 as-delivered, ADR 0018 §3, OPERATIONS' Rolling upgrades section with a pre-flight — start a node over a COPY of the data dir — and TROUBLESHOOTING as a symptom) and, more usefully, the broker says it itself BEFORE doing it: the boot scan emits one WARN 'DISCARDING offline sessions past their persisted Session Expiry deadline' carrying sessions=, oldest_overdue_s= and up to 20 client ids with how far past due each is, built by summarize_overdue_discard (pure, so the counting is asserted rather than the formatting) and pinned by hub::the_overdue_discard_summary_counts_every_session_and_names_the_worst_first plus hub::a_scan_with_nothing_overdue_says_nothing. In-memory store: nothing survives the process, which is what MemorySessionStore already says about every session it holds (residual R4).\n\nPRECISION PROMISED, AND THE CLOSING ROUND'S FIRST FIX. Never early — locally that was always true (the deadline is a monotonic tokio::time::Instant, immune to NTP steps) and ACROSS THE RECORD it was FALSE, which is the path the whole persistence exists for. The arm stored floor(now)+effective and the inherit computed deadline-floor(now), so the elapsed delay was effective - frac(arm) + frac(re-arm), uniform in (effective-1s, effective+1s). Measured on the real broker: armed 09:28:54.7408 with an 8 s delay, published 09:29:02.0524 = 7.312 s, 0.69 s EARLY; and on a real 3-node durable cluster a 12 s delay announced 11.20 s after the owner was killed. Fixed as arithmetic, not machinery: Clock gained now_epoch_millis (defaulted to now_epoch_secs()*1000 so a whole-second test clock stays exactly whole-second), Hub::will_deadline_epoch ROUNDS THE STORED DEADLINE UP to the next whole second, and inherit_sessions computes the remaining time from it in MILLISECONDS. Every rounding error in the round trip now lands LATE, by strictly less than a second. Per path as delivered: LOCALLY within one hub-loop dispatch — sub-millisecond only where the store is in memory; where it persists the fire includes an fsync'd durable clear and mqttd_hub_dispatch_seconds_sum{command=\"will\"} measured 8.9 ms mean over 486 fires (12.2 ms for a single fire on an idle node), which corrects an earlier flat 'sub-millisecond' claim that was off by ~10x in the shapes that persist. SESSION EXPIRY as the binding bound: within the 1 s sweep tick after it, never before. After a takeover, restart or node death: whole-second wall clock, late by the round-up (<1 s) plus the inherited-scan cadence (~1 s inside a takeover window, else up to EXPIRY_RECONCILE_EVERY = 30 s), plus inter-node clock skew. The foreign oracle measured 3.00 s for a 3 s delay locally.\n\nEXACTLY ONCE, AND THE FENCE THAT MAKES IT SO — the closing round's second fix, one mechanism for two defects. The inherited-session scan reads all_sessions() OFF the hub loop and its result is applied later, so anything that happens to a will in between is invisible to it. (a) DUPLICATE, in 5 of 6 restart runs: sweep_expired_sessions spawns the scan BEFORE fire_due_wills, the scan's read raced the fire's durable clear, and the stale block was re-armed with zero remaining and published a second time ~20 ms later — both copies labelled reason=\"inherited\", so an operator could not tell one duplicate from two deaths, against a README that promises 'never duplicated per node'. (b) RESURRECTION: the same staleness re-armed a will a RESUME had already cancelled and published it at once because its deadline had passed, violating [MQTT-3.1.3-9] and [MQTT-3.14.4-3] together with no connection event to correlate against. The fix is the fence this hub already uses for SessionRecovered's stale results, made per-client: Hub::note_will_decided records every will published, cancelled or handed off while a scan is in flight (wills_decided_since_scan, cleared when the next scan starts), and inherit_sessions refuses to re-arm those. Re-ordering the sweep's own steps is NOT a fix and is deliberately not relied on — the read is off-loop, so any ordering can still be raced by a slow read.\n\nRED-FIRST, MUTATION-PROVEN — 11 mutations, each reverted, run, restored, nothing left behind (grep MUTATION|PROBE|zz_|MEAS clean). [1] hub.rs takeover branch re-derives the delay from the new connection (the pre-fix code) -> a_resume_never_publishes_the_will_whatever_the_resuming_connect_asks_for RED (its clean-start companion was renamed in the closing round when that ruling was reversed — see [14]). [2] cancel_pending_will ignores the record flag (record_holds = false) -> a_pending_will_this_node_never_armed_is_cleared_by_a_resume RED. [3] discard_session_local treats every caller as SessionEnd::Ended -> a_rehome_close_is_silent_for_a_delay_using_client AND a_lease_move_hands_a_persisted_pending_will_to_the_new_owner both RED. [4] PersistentLog::keys() returns the trait default -> storage keys_enumerates_every_logical_key_and_survives_a_reopen RED, and persistence::a_delayed_will_survives_a_node_restart_over_the_same_data_dir RED. [5] detach's arm reverted to the unconditional publish_will -> a_will_delay_holds_the_will_until_the_delay_elapses, a_session_expiry_shorter_than_the_delay_bounds_the_will_at_the_expiry, a_delayed_will_is_still_delivered_live_under_brownout_and_counted_as_a_drop, a_rehome_close_is_silent_for_a_delay_using_client all RED. [6] min(delay, secs) -> delay -> a_session_expiry_shorter_than_the_delay_bounds_the_will_at_the_expiry RED. [7] the expiry-0 clamp removed -> a_zero_session_expiry_publishes_the_will_immediately_despite_a_delay RED. [8] `if !graceful` -> `if true` -> a_graceful_disconnect_arms_no_delayed_will RED (the #265/#238 anti-regression). [9] the inherit_sessions re-arm deleted -> a_delayed_will_is_fired_from_the_persisted_record_by_a_node_that_never_saw_the_disconnect RED. [10] unacked_received_qos2 restored to 'emit only when non-empty' -> logged::a_pending_will_after_an_empty_unacked_block_still_decodes RED (the decoder eats the will block's length prefix as an unacked count: the silent session-metadata corruption ADR 0058 clause 3 forbids). [11] delay hard-coded to 0 in conn.rs::into_will_spec -> ALL hub-lib tests stay GREEN and only keepalive_lwt::a_v5_will_delay_interval_is_honoured_end_to_end, a_v5_reconnect_inside_the_delay_window_cancels_the_will_end_to_end and persistence::a_delayed_will_survives_a_node_restart_over_the_same_data_dir go red — which is exactly why those exist, and it is the shape of the original defect (a property read by nothing). CLOSING ROUND, six more. [12] will_deadline_epoch reverted to floor(now)+effective and the inherited remaining back to whole seconds -> hub::a_will_inherited_across_a_restart_never_fires_before_its_deadline RED at the record-deadline assertion (persisted 1000008.000 s for a deadline of 1000008.900 s) AND persistence::a_will_inherited_across_a_restart_fires_no_earlier_than_its_deadline_exactly_once RED on run 0 with a real measurement, 'an inherited will fired 355 ms EARLY (a 6 s delay elapsed in 5644 ms)'. [13] the fence check in inherit_sessions disabled -> hub::an_inherited_will_is_published_exactly_once_across_a_scan_racing_the_fire RED ('release at 3000 ms: 2 copies arrived' — and green at the 1100/2900 ms release points, which is why the release point is SWEPT rather than fixed) and hub::a_stale_inherited_scan_never_republishes_a_will_a_resume_cancelled RED. [14] clean start restored to publishing (both halves) -> hub::a_clean_start_connect_for_the_same_client_id_deletes_the_pending_will and hub::a_clean_start_takeover_deletes_the_replaced_will_like_the_offline_half_does RED, and the FOREIGN ORACLE red too: paho case (c) reports 19 passed / 1 failed, PAHO INTEROP FAILED. [15] the clear-path guard removed from ReplicatedSessionStore::set_pending_will -> logged::clearing_a_pending_will_does_not_resurrect_a_removed_session_record RED. [16] summarize_overdue_discard reports the capped count instead of the real one -> hub::the_overdue_discard_summary_counts_every_session_and_names_the_worst_first RED. [17] the inherit_sessions re-arm deleted (mutation [9] again, but against the new cluster test) -> durable_sessions::a_delayed_will_survives_the_death_of_the_node_that_armed_it RED. Each reverted, run, restored; grep MUTATION|PROBE|zz_|MEAS clean.\n\nFOREIGN ORACLE, run locally against the real binary. scripts/interop/paho_conformance.py carries three Eclipse Paho cases (the third added in the closing round, for the ruling that changed — an interpretation this contested should have had a foreign oracle from the start): WillDelayInterval=3 + SessionExpiryInterval=30, socket killed — nothing at +1 s, arrival strictly between 2 s and 6 s; then the same kill followed by a reconnect at +1 s that sends NO Session Expiry Interval (paho's default), asserting nothing is ever published; and (c) the same kill followed by a reconnect at +1 s with clean_start=True and SessionExpiryInterval=30 — the ordinary shape, and what dying_client itself sends — asserting the will is DELETED, never published [MQTT-3.1.2-8]. 20 passed / 0 failed against the real binary, the delayed will landing at 3.00 s. Under mutation [11] the same lane reports 'landed after 0.00s' and fails 3 assertions, which matches the issue's observed 0.1 s. What the paho lane CANNOT reach: the half-open takeover of defect (1) — a foreign client cannot manufacture a half-open socket without a firewall — so that one is pinned by the hub test only.\n\nGATES (all run in this worktree, closing round): cargo fmt --all --check clean; cargo clippy --all-targets --all-features -D warnings clean; mqttd --lib 326 passed (314 originally, 320 after the first remediation, +6 here); mqtt-storage 79 (+2); mqtt-codec 59+1; mqtt-cluster 15+5+4; keepalive_lwt 9; brownout_ack 5; durable_sessions 11 (+1, the clustered node-death will); v5_protocol 16; cluster_stress 11; persistence 4 (+1, six repeated real restarts); end_to_end 5; gen-status.py regenerated and --check clean; check-readme-facts.py green (61 ADRs, 100 evidence citations, dates in sync); paho oracle 20/20 against the real binary. Every metric selector cited in the docs touched here was re-checked against its emitting call site (the only apparent misses are the Prometheus histogram suffixes _sum/_bucket on mqttd_hub_dispatch_seconds, and command=\"will\" is a real label value at hub.rs's observe_hub_dispatch), and every test name cited resolves to a real fn — one stale citation — the old clean-start takeover test's name, which the reversal renamed — was found that way and fixed. ADR 0058, RE-VERIFIED IN THE CLOSING ROUND AND STILL UNTOUCHED: the persisted block is strictly trailing, length-prefixed and EOF-defaulted, and this round changed no encoder or decoder byte either — encode_session_meta/decode_session_meta were not edited, deadline_epoch is still the same u64 epoch second in the same block (only the VALUE is now rounded up, which is arithmetic at the call site, not a format change), and the one storage edit is a guard on set_pending_will's CLEARING path that writes strictly less than before. crates/mqtt-cluster/** and crates/mqttd/tests/cluster_upgrade.rs are not in the diff. So sessions.redb SCHEMA_VERSION stays 2, the empty migration registry stays empty, and cluster_upgrade's BASELINE_REF needs no bump (nightly-tier, #[ignore] in the per-PR profile, so not run here). Pinned as before by logged::the_pending_will_block_is_strictly_trailing_and_absent_by_default and logged::a_pending_will_after_an_empty_unacked_block_still_decodes, both green. One metric-help lesson worth keeping: naming another metric's full sample selector inside a HELP string made a substring assertion in an existing test read it as a sample — the help text now spells the label out in prose instead.\n\nMINOR FIXED IN THE CLOSING ROUND: ReplicatedSessionStore::set_pending_will read-modify-wrote through load_meta(client)?.unwrap_or_default(), so set_pending_will(client, None) against a REMOVED record wrote a fresh m/{client} snapshot with owner: None — and because claim_session treats any existing meta record as present: true and adopts the first claimant, that phantom would report session_present=1 for a discarded session and, carrying no expiry deadline, would never be reaped. Reachable wherever the record is deleted between an arm and its clear. MemorySessionStore's implementation of the same seam was already guarded, which is exactly the drift a comment there claimed could not exist; the clearing path now returns early when there is no record, pinned by logged::clearing_a_pending_will_does_not_resurrect_a_removed_session_record with logged::the_memory_store_agrees_that_a_clear_creates_nothing asserting the two shapes side by side.\n\nRESIDUALS R1-R6, all named in ADR 0009 §2; the three an operator can see (R1, R2, R3) also in OPERATIONS and TROUBLESHOOTING, and R4's consequence for a roll in OPERATIONS. R1 an arm on a non-owning node cannot persist (structural after a rehome close; counted mqttd_pending_will_unpersisted_total{reason=\\"not-owner\\"}), so a node death inside the window loses that one. R2 a will still lives only in Online while its client is CONNECTED, so a node dying with live clients loses their wills — unchanged by this work, not closed by it. R3 cross-node cancellation is NOT closed: the rehome close is now SILENT at the close, but the will still fires one delay later because the client reconnects on the owner (0009-P5). R4 SIGTERM does not fire pending wills early, deliberately. R5 RETIRED by the closing round's spec reading: it named 'a Clean Start CONNECT drops a record-only pending will instead of publishing it' as a LOSS, and under [MQTT-3.1.2-8] dropping it is the correct outcome, so the residual and the durable read that would have fixed it both disappear (the number is kept as a slot so R-numbers elsewhere keep meaning what they meant). R6 NEW, measured not reasoned: the arm's durable read-modify-write (persist_pending_will -> set_pending_will -> load_meta + append + truncate, fsync'd) is AWAITED ON THE HUB LOOP inside detach, beside the pre-existing persist_detach_deadline await, so a mass ungraceful disconnect of delay-using sessions roughly DOUBLES an already-multi-second stall. Measured against the real binary, single-node on-disk, with a probe client timing hub round-trips: 500 sessions killed at once with delay 6 -> worst client round-trip 6.49 s and the loop degraded until +11.0 s, versus a delay-0 control (today's fleet) at 4.98 s / +6.84 s; at 200 sessions, 2.37 s / +4.21 s versus 1.13 s / +2.05 s. Not fixed here because moving it off-loop means arming optimistically and correcting the persisted flag from a spawned reply — a new command, a new fence, and a window in which R1's counter lies, i.e. an ADR 0017-shaped motion rather than a closing-round edit. Named with its numbers in ADR 0009 §2 and in OPERATIONS where an operator sizing a roll will meet it; follow-up drafted." |
| 0009-P5 | 💤 deferred | — | "Deferred, mechanism named (issue #299 R3). A resume that lands on a DIFFERENT node does not cancel a pending will held in the closing node's memory — and after a rehome close (ADR 0005) that is the routine case, because the reconnect is served by the owner. Two ways to close it: (a) fold an attach marker (client id + attach epoch) into the owner's existing attach-time SessionMeta write and compare it at fire time — one off-loop read, no peer-proto bump, and it reuses the record this feature already writes; or (b) add an additive PeerMessage::PendingWill hand-off, which needs a proto bump (ADR 0038) and a cluster oracle. (a) is preferred. Until then the residual is bounded, counted (mqttd_wills_published_total{reason=\"delay-elapsed\"} during a roll) and documented in OPERATIONS as 'size the device-offline suppression window to the roll PLUS your fleet's largest will delay'." |
<!-- /status-table:0009 -->

**Carried limitation (from §2):** the expiry deadline lives only in the owner's in-memory
`expiring: HashMap<ClientId, Instant>`; the replicated log carries the session data but not
the deadline, so on owner death + replica takeover the session survives while its clock
restarts. P3 closes this by persisting the disconnect time. The `session_policy`
normalization and the `session_expiry_interval()` / `message_expiry_interval()` accessors
are built but have no isolated unit tests — they are exercised through the hub tests and the
`v5_protocol.rs` integration suite.

## Changelog

- **2026-08-15** — **0009-P4 done, 0009-P5 deferred.** Issue #299: the Will Delay Interval
  was decoded and never read outside the codec, so every client using it got a spurious
  "offline" on each brief blip. Now honoured as `min(will delay, session expiry)` on a
  deadline-driven hub-loop wakeup, cancelled by a resume, fired when the session ends, and
  **persisted with an absolute deadline** so a node death inside the window does not lose the
  announcement of exactly the unexpected death a Will exists to announce. Found by the
  Eclipse `paho.mqtt.testing` suite — an independent oracle — which is why the interop lane
  now carries both halves of the case. The remaining gap (a resume landing on a different
  node) is P5, and the rehome close therefore *delays* its false-offline burst rather than
  removing it: OPERATIONS and TROUBLESHOOTING say so, and `mqttd_pending_wills` shows the
  burst before it lands.

  **Remediation round, same day, before merge.** The implementation pass landed and was
  never self-reviewed, so the review read the tree cold and found six defects; the evidence
  above is the one written by someone who ran the result. Three were blocking. A **resume
  could publish the very will it must cancel**, because the takeover branch re-derived the
  delay from the *new* connection's Session Expiry — so a half-open socket plus a client
  reconnecting without that property (paho's default) announced the death of a device that
  was connected right then. A **routing release was treated as a session end**, so a lease
  move published the pending will ~29 s early, mislabelled, and left the record's copy to
  fire again — the irony being that #284's rehome close was supposed to become *silent* for
  delay-using clients, and this made it noisy instead. And the **persistence was inert in
  the single-node on-disk shape**: `PersistentLog` never implemented `ReplicatedLog::keys()`,
  so a restarted node's session scan saw nothing at all and a pending will (and, all along,
  this ADR's own §3 expiry deadlines) was silently lost across a restart. Then: the cancel
  now clears the durable block as well as memory, Clean Start answers the same way against an
  online and an offline session, and the operator docs that claimed to have been updated
  actually were — OPERATIONS' rehome bullet had still been telling operators that honouring
  the delay "is the follow-up that would remove the false event".

- **2026-06-22** — Migration audit: P1 (session expiry — normalization, attach/detach
  lifecycle, sweep GC) and P2 (message expiry — stored absolute deadline, drop-on-replay,
  remaining-interval on delivery) verified built against hub and storage tests plus
  `v5_protocol.rs` e2e. P3 (durable disconnect-time deadline) confirmed not built and split
  out as a deferred §2 follow-up.
