---
adr: "0043"
title: Elastic cluster resize (grow, shrink, replace)
adr_status: Accepted
tasks:
  - id: 0043-P1
    title: Catch-up replicas — a node entering a group's replica set back-fills the group's log (ReplicaKeys discovery + recover_key quorum reads) behind a durable caught-up watermark, and counts toward NO quorum (append or recovery) until caught up; growing 1→N back-fills laptop-mode single-replica history as the same rule
    status: done
    date: 2026-07-13
    evidence: "Recovery reads now carry a COMPLETENESS verdict with two halves: per-key gap-freedom (ReplicaState::complete — stored offsets contiguous above the truncation low-water, so a joiner holding new appends above a hole it never received reads incomplete) AND the durable caught-up watermark (new replica_caught_up redb table: per group, the replica set this node last completed catch-up against; a stamp from a different cohort — or no stamp — reads incomplete, so an EMPTY joiner cannot claim a key has no history; additive table, no schema bump, survives restart so a crash mid-catch-up resumes rather than fakes completion). recover_key requires a quorum PLUS at least one complete anchor, and the OWN copy obeys the same rule — an unstamped owner cannot fabricate an empty log (fails closed with NoQuorum until its boot sweep stamps; ADR 0017 posture). The back-fill: a catch-up sweep in the lease driver (armed at boot and on every placement membership change, CATCH_UP_SWEEP_EVERY=5 ticks, budget 24) discovers keys per-peer (ReplicaKeys, attributed — a group stamps only once every other set member was heard), heals hollow keys by asking the group owner to recommit_key them (new ReplicaCatchUp frame; owner-side CatchUpSource seam on the plane, implemented by GroupRoutedLog: route → recover → re-commit at the owner epoch, idempotent and fenced, plus a truncation-floor fan so drained prefixes read as acked-away rather than as gaps), and re-stamps pure shrinks of a known cohort immediately (custody unchanged — no data moved on 5→3). Cached group logs rebuild on REPLICA-SET changes, not only epoch changes, so appends and re-commits fan out to joiners. Wire: proto 3→4; ReplicaRead2/ReplicaReadReply2 (request version selects reply shape; pre-proto-4 replies are conservatively incomplete) and ReplicaCatchUp appended at the enum end (ADR 0038 compat). Tests — unit: completeness shapes (gap/hole/truncated-floor), stamp currency + reopen persistence, unstamped-owner-cannot-fabricate, recovery-refuses-a-quorum-of-hollow-replicas (then serves once a complete anchor joins), growing_a_one_node_group_back_fills_the_joiners (store seam), proto4 wire round-trip + catch-up routing (plane seam). Integration (cluster_stress): growing_one_node_to_three_back_fills_and_survives_the_founder — one durable node accumulates acked facts (offline durable session owed 3 acked QoS1 payloads + acked retained), grows 1→3, both joiners reach the caught-up watermark on q/, m/ and r/ keys, the FOUNDER IS KILLED (the only pre-grow copy), and the session resumes present=true on the survivors with every acked payload replayed and the retained value served — zero loss, zero waivers (~17s). Workspace green (all suites), clippy zero warnings."
  - id: 0043-P2
    title: Eager migration on ring change — membership growth triggers the takeover-scan materialization (sessions, retained, interest) for groups whose owner moved, instead of first-touch; the settle/re-route + mesh-whole ack machinery holds acks honest during the window
    status: done
    date: 2026-07-13
    evidence: "The hub now watches the placement member set on its sweep tick (new known_members snapshot): ANY change — growth above all; shrink arrives via SWIM eviction too, overlapping the PeerDead arm — re-arms the takeover window (takeover_reconcile_ticks=8), so every node runs the eager inherited-session scan for the next ~8s and publish acks registered in the window gate on awaiting_settle exactly as after a death (settle releases only when the window is over AND the mesh is whole — the T9/T4 honesty rules, inherited unchanged). Two sides of the move: the NEW owner's scan materializes the moved groups' sessions (all_sessions unions local + remote keys; per-key recovery rides P1's complete-anchor gate, with the old owner — alive and stamped — as the anchor) and gossips their filters, re-targeting held forwards; and the OLD owner now has the missing mirror — release_moved_sessions, run after every scan: offline, not-attaching sessions whose group this node no longer owns drop their LOCAL routing state (durable data stays; the node usually remains a replica) and interest re-gossips without them, so forwards stop targeting a node that can only answer NotOwner (every such ack was being withheld — the pre-P2 state poisoned post-grow publishes indefinitely). Durable-cluster-gated: with local-only session storage the data cannot follow ownership, so routing is kept. Tests — unit: growth_releases_moved_sessions_on_the_old_owner (paused-clock hub with a real idle plane: boot scan gossips the materialized filter, placement growth re-gossips without it). Integration (cluster_stress): growth_migrates_moved_sessions_eagerly_and_acks_stay_honest — offline durable session whose owner MOVES on a 1→3 grow (no deaths): 2 payloads acked pre-grow, 3 acked post-grow against the old owner with the session touched by no client; resume on the NEW owner is present=true and replays all 5 (~12s). Validated non-vacuous: the same test against the pre-P2 hub FAILS (post-grow acks withheld behind the old owner's stale NotOwner routing). Workspace green, clippy zero warnings."
  - id: 0043-P3
    title: Decommission — an explicit drain (stop new sessions → successors caught up + replica counts restored among remaining members → voter demotion → ADR 0019 leave), observable via the health endpoint, interruptible (crash mid-drain = crash); operator trigger chosen here (signal vs admin endpoint)
    status: done
    date: 2026-07-13
    evidence: "Operator trigger: SIGUSR1 (chosen over an admin endpoint — the health listener is deliberately read-only and unauthenticated; signals match the existing SIGHUP-reload / SIGTERM-shutdown operational grammar). On SIGUSR1, readiness fails immediately (orchestrators steer new traffic away — the ADR 0019 §4 posture standing in for 'stop accepting new sessions' pre-proxy) and the DECOMMISSION DRAIN runs (new mqtt_cluster::decommission::Drain, assembled from the plane's shared handles via DurablePlane::decommission_drain). The drain cannot push data itself — a non-owner writing old-epoch entries would be fenced, and minting epochs is the owner's privilege — so each round it VERIFIES every key it stores against every member of the key's post-departure replica set (Placement::group_replica_set_without — HRW monotonicity keeps every surviving current member in the set) using plain recovery reads, with CONTENT SUFFICIENCY as the verdict: the successor's truncation watermark has reached ours (an acked-away prefix can never be resurrected from a merge that no longer includes us) and every entry we hold is either below that watermark or present in its log. Each shortfall asks the key's group OWNER for a targeted re-commit — new proto-5 ReplicaCatchUpTo frame (appended at the enum end, PROTO_MAX 4→5), served through the CatchUpSource seam by ClusterLog::recommit_key_to: every committed entry re-tagged at the owner's epoch (the P1 recommit convention, idempotent) plus the truncation floor, to ONE node, no quorum gate — the drain re-verifies and re-asks until nothing falls short. Rounds repeat 1s apart; the drain never gives up on its own (an unreachable successor keeps it honestly pending; the operator can always escalate to plain SIGTERM = crash semantics — the drain mutates nothing locally, so interruption at ANY point is just a crash). On completion the ordinary ADR 0019 graceful shutdown runs: SWIM leave → placement eviction → voter demotion via the ADR 0021 reconciler → P2's eager migration materializes the moved sessions on the survivors. Observability: /readyz gains decommission{pending,rounds,complete} while a drain is active (DrainStatus published into the health state). Tests — unit: a_solo_drain_completes_immediately (nowhere to hand off = shutdown); the_drain_hands_off_and_verifies_a_held_key (two-node ring, truncated key, empty successor: round 1 finds the shortfall, targeted re-commit lands entries AND watermark, rounds converge to 0); plane wire test extended (ReplicaCatchUpTo round-trips to the owner's CatchUpSource with its target). Integration (cluster_stress): a_decommissioned_nodes_departure_loses_nothing — FOUR-node cluster (4 members, R=3: removing one adds exactly one NEWCOMER to every set it was in, so the targeted hand-off is load-bearing), acked payloads + acked retained on sets including the leaver, the durable subscriber OWNED by the leaver; drain converges (~120s bound, actual seconds), reports complete, the node dies (kill — harsher than the graceful leave), and everything acked survives: resume present=true on the new owner, all payloads replay, retained serves (~14s). Workspace green, clippy zero warnings."
  - id: 0043-P4
    title: Resize test vocabulary — join/decommission steps in the ADR 0042 stress schedules, plus dedicated upgrade-path tests (1→3 laptop-to-server, 3→5 zone-spread, 5→3 cost reduction, rolling host replacement, rolling binary upgrade across the proto window) under the unchanged acked-obligations oracle
    status: done
    date: 2026-07-13
    evidence: "SCHEDULE VOCABULARY: the seeded stress schedules gain join_step (grow by a fresh node mid-schedule, ≤2 per schedule; new dirs, joins via alive seeds) and decommission_step (drain → leave only if the drain CONVERGES within its bound; aborted honestly under faults — the operator semantics; requires ≥4 alive so the schedule's kill still lands afterwards), mixed into the seeded composition and counted on the per-seed composition line. DEDICATED PATH TESTS (with 1→3 already landed in P1 and the 4-node decommission in P3): growing_three_to_five_zone_spread_survives_losing_two_originals — 3 zones advertised over the LIVE gossip-label plumbing (start_stress_node_in_zone → Placement::with_local_domain + SWIM domain, the ADR 0016 T5 path), grow 3→5, wait for every member of each fact key's new replica set to reach the P1 caught-up watermark, then kill TWO originals at once: all acked payloads + retained survive on {original, joiner, joiner} (~30s incl. bring-up); cost_reduction_five_to_three_via_two_decommissions — two sequential drain→leave cycles with membership settling between; rolling_replacement_swaps_a_node_without_loss — join one, decommission the subscriber's owner, same size before/after (the ADR 0039 rolling-upgrade motion). All under the unchanged acked-obligations oracle, zero waivers. PROTO WINDOW: catch_up_requests_are_proto_gated_per_link pins the mixed-proto mesh honesty (proto-3 peer: neither catch-up frame; proto-4: P1's but not P3's targeted one; current: both) — a whole-cluster TWO-BINARY rolling-upgrade test needs an out-of-process harness and is recorded as the remaining gap (the in-process harness shares one binary). THE HEADLINE — the re-mixed schedules found a REAL pre-existing defect on their first sweep (exhibit ② below, the fast-restart void ack: seeds 4 and 1, kill+restart faster than SWIM death confirmation): fixed with four coordinated honesty changes — SessionScan.complete surfaces transiently-skipped keys from all_sessions; interest gossip is suppressed until the first complete whole-mesh scan (fresh-boot empty snapshots no longer erase peers' knowledge; 30-tick loud liveness backstop); a gated forward that matched nobody while routing_unsettled() answers a retriable refusal instead of OK; clustered() honors a startup-time cluster-CONFIGURED flag so a restarted node is never mistaken for a standalone broker; and the settle window closes on the observable routing_unsettled() predicate, never on tick count alone. Verified: seed 1 (previously ~75% failing) 5/5 green, seed 4 deterministic-green, 8-seed sweep green, all resize path tests green, workspace green, clippy zero warnings."
  - id: 0043-P5
    title: Operator docs — the "grow your broker" guide (one paragraph per direction), the two-node-waypoint honesty note (quorum 2-of-2; recommend 1→3), README interim warning that durable resize is unsupported until P1 lands
    status: done
    date: 2026-07-14
    evidence: "README gains a 'Resizing the cluster' operator section (before Upgrades & versioning, whose rolling-upgrade motion it feeds): GROW is one paragraph — start the node with MQTTD_SWIM_SEEDS at any member; the joiner back-fills behind the durable caught-up watermark and counts toward no recovery until caught up; watch /readyz lease_group_ready — with the laptop→server case called out (growing 1 node re-replicates its whole history). THE TWO-NODE TRUTH is stated plainly: two members = replica sets of two = write quorum 2-of-2, strictly worse write availability than one node; supported as a waypoint, recommended upgrade 1→3 in one motion. SHRINK documents the SIGUSR1 decommission (readiness fails at once, drain hands every held key to the post-departure sets and verifies, progress on /readyz decommission{pending,rounds,complete}, then the graceful leave; a non-converging drain waits rather than lies; SIGTERM escalates; mid-drain crash = crash), one node at a time for 5→3. REPLACE = grow-then-decommission, the motion rolling binary upgrades ride. The interim 'durable resize unsupported' warning (placed at P1's start, softened as P1–P3 landed) is REMOVED — the durable-storage feature bullet now states resize is data-safe and points at this guide. Signal documented in main.rs's operator header since P3. ADR 0043 flipped to ACCEPTED (P1–P5 delivered); the historical warning note moved to the ADR's consequences as a record. gen-status green."
  - id: 0043-P6
    title: Rehome on settle — an ONLINE persistent session hosted on a node that does not own its placement group is closed so the client relocates, instead of sitting undeliverable until its keepalive fires (issue #284); bounded against a flapping placement, and against an owner this node cannot route to
    status: done
    date: 2026-08-15
    evidence: "Issue #284's wedge is closed by REHOME ON SETTLE, and the issue's own stated mechanism is CORRECTED in the process. CORRECTION: there is no ring/lease split here. Placement::owner_route (what conn.rs relocates against) and the enqueue gate Placement::owns_group BOTH resolve through the same Placement::group_owner, and both were self-consistent at every measured point — at the relocation node c saw committed_owner=Some(a) hrw_owner=a, and at every refusal node a saw committed_owner=b hrw_owner=b with a full voter set. The lease-epoch gate the 2026-07-20 post-mortem comment describes is never reached; issue #284's exit 2 as phrased ('make owner_route and the enqueue-side check read the same source') is a no-op. THE REAL MECHANISM: relocation (ADR 0005) is a one-shot CONNECT-time snapshot of an ownership that is guaranteed to move again moments later. hrw_owner is voter-restricted (ADR 0049) and the lease assigner drives every group's lease toward it, but a rolled node rejoins SWIM membership — and turns /readyz green — BEFORE it re-enters the lease voter set, so its groups' leases are still parked on the interim holder at that instant and are legitimately pulled back 2-4s later. Measured for group 212 (= group_of(rc-4848-2-0)): lease b->a at the drain, client resumes via c 8.8s later while c's voters were still [a,c] -> relocated to a (CORRECT on the committed view), lease a->b again 3.5s after that. Nothing re-evaluated a LIVE session: release_moved_sessions (0043-P2) is the one place that detects exactly this condition and its filter excludes online sessions — instrumentation showed node a logging the condition on 8 consecutive scans over 94s and never acting. THE FIX: new Hub::rehome_misplaced_sessions() on the 1s sweep tick (one decision site, re-derived from placement each tick — deliberately NOT the traffic-driven NotOwner refusal, which cannot see an idle misplaced session, and NOT a placement-change event, which would need new cross-crate plumbing for a signal a self-correcting poll already gives). It closes an ONLINE persistent session whose group's COMMITTED lease names another node: v5 gets DISCONNECT 0x9C (Use another server; no Server Reference property — placement holds PEER-BUS addresses), v3.1.1 sees the relay's half-close, then detach(graceful=false) and the UNCHANGED release_moved_sessions drops the local routing and re-gossips the shrunken interest in the same tick. FOUR BOUNDS, each its own test: MISPLACED_GRACE_TICKS=2 (a sub-tick lease blip must not cost a live client its connection); a COMMITTED lease only — new Placement::committed_group_owner / committed_session_owner, the eligible-filtered lease map with NO HRW fallback, so an unsettled group and a corpse's lease are structurally non-triggers (the transient split the post-mortem describes); a per-session REHOME_COOLDOWN=30s (a flapping placement or an LB that keeps returning the client here cannot become a close loop, or a will-publish loop); and a relocatability precondition (owner_route must agree) because with the owner's address unknown the next CONNECT is served locally anyway (ADR 0005 §5) and closing would loop — that case is counted and gauged instead. INVARIANTS: nothing about acks changed. The NotOwner refusal and the 'withholding the publisher's ack (ADR 0041 T5)' withhold are untouched; only how LONG the honest refusal persists changes (measured unbounded -> ~1-2s). No durable repair is attempted or needed: the only durable ownership record is the replicated lease, which read the CORRECT owner (b) in all 392 refusal lines — the stale placement is purely in-memory. No new task of any kind, so #242/ADR 0061's spawn_owned rule is not engaged (nothing here captures the store); the fn-not-async send chain is unmodified; the only awaits are detach's pre-existing ones. Exactly-one-owner (ADR 0005/0007) is strengthened, not weakened: today TWO nodes disagree about who hosts the session for seconds-to-forever; the close collapses that to one, structurally (a drops its routing before the client can learn to reconnect, and the only way back online anywhere is a fresh CONNECT through the existing takeover fence). A TRANSPARENT re-relocation was rejected: the proxy is structurally one hop (run_framed refuses to re-proxy), the CONNECT and its session_present CONNACK are already on the wire with no vocabulary for replaying them elsewhere, and a hand-off would make exactly-one-owner a timing argument. TESTS — integration: roll_cost's post-roll section rewritten from modelling the defect (it counted 'clients needing a second (keepalive-style) reconnect: 1') to asserting the fix — it watches every socket for the server-initiated close over a 20s bound, reconnects the rehomed client ONCE (session_present asserted true), then requires ack+delivery for all 9, and asserts blind_retries == 0 (nobody heals by waiting out dead air). Measured before/after at seed 4848: worst post-roll QoS1 ack stall 26.4s -> 0.1s, rehomes 1, blind retries 1 -> 0, suite 91s -> 43s. Unit (hub): an_online_session_whose_group_moved_is_closed_so_it_relocates (DISCONNECT 0x9C, leaves online, filter released from gossiped interest, session_rehomes{stale-owner}=1); a_transient_ownership_blip_does_not_close_a_live_session; a_group_with_no_committed_lease_is_never_rehomed (the post-mortem split, pinned as a non-trigger); an_unrelocatable_owner_is_counted_not_closed; a_rehomed_session_is_not_closed_again_within_the_cooldown. Unit (placement): the_committed_group_owner_never_falls_back_to_the_hrw_ring. MUTATION-PROVEN, each run and restored: drop the sweep call -> 'no rehome DISCONNECT; got None' AND roll_cost fails '1 client(s) were left to discover a stale placement by dead air'; GRACE_TICKS=1 -> 'a sub-tick ownership blip must not disturb a live session'; committed_session_owner -> owner (HRW view) -> 'an unsettled (lease-less) group must not close a live session'; drop the owner_route precondition -> 'a session whose owner has no known address must be kept, not closed'; drop the cooldown -> 'a second close inside the cooldown would be a close loop'; give committed_group_owner an HRW fallback -> 'an unsettled group has no committed owner, however loudly HRW answers'. METRICS: mqttd_session_rehomes_total{reason=stale-owner|unrelocatable|cooldown} and the mqttd_misplaced_sessions gauge, both with rustdocs and OPERATIONS alert rows. DOCS: ADR 0005 (owns the relocation claim) and ADR 0043 §2 amended in place with As-delivered notes; OPERATIONS.md's 'straggler wart' bullet replaced by the measured new cost and the client-backoff mitigation withdrawn; ADR 0047's amendment corrected in place (it recorded the wrong mechanism and 'until it lands'); TROUBLESHOOTING gains the expected-close symptom and the unrelocatable failure form. RESIDUAL, stated not hidden: the unrelocatable branch deliberately leaves such a session undeliverable (ADR 0005 §5 beats availability when the alternative is a reconnect loop) — now counted and gauged; and one immediate reconnect is still a reconnect, so a roll is not transparent to a straggler. The readiness half — /readyz turning green before the readmitted node is back in the lease voter set, which is what opens client traffic inside the window — is NOT fixed here and is the remaining follow-up. ROUND 2 — the adversarial review found FIVE defects in the round-1 shape. Findings 2-5 are remediated and their remediations SHIP (see below). FINDING 1 (BLOCKING) — the round-1 close released the session's routing in the same tick (via the unchanged release_moved_sessions), which is an ACKED-BUT-DROPPED publish: reconfirmed red-first ('got Ok(Ok(Accepted))' for a QoS 1 publish toward a rehomed session, with zero peer forwards and nothing stored anywhere). Round 2's remediation was a HAND-OFF-OBSERVED RELEASE: hold the routing after the close and release it only on positive evidence (Hub::handed_off, handoff_claimed, release_handed_off_sessions, routing_released_ticks, the sessions_awaiting_handoff gauge, the handed-off / handoff-stalled reasons). ROUND 3 DELETED ALL OF IT, because its own review found three more blocking defects, each in machinery added for OPTIMISATION rather than in the behaviour change: (i) routing_released_ticks armed routing_unsettled() for 2 ticks but awaiting_settle is cleared ONLY in settle_pending_publishes, reachable only from an inherited-session scan, so every gated publish on the node was held up to EXPIRY_RECONCILE_EVERY = 30s rather than 2 ticks (measured 330us before a release vs 19.998s after — a node-wide availability regression on traffic unrelated to the rehomed session, which also falsified the delivered 0.1s post-roll stall claim); (ii) the evidence-gated release was IRREVERSIBLE and its evidence never re-checked, so when the owner's advertised interest disappeared before the owner materialised the session a publish was answered Accepted with nothing stored — the same lie, proven in the fixture, and unavoidable because PeerMessage::Interest carries FILTERS, not client ids, so a co-subscriber of the same filter satisfies the predicate; (iii) the ownership_epoch skip made detection edge-triggered on ownership, so a session that became misplaced by ATTACHING never entered misplaced, the pass never ran again, and the #284 wedge returned silently with mqttd_misplaced_sessions reading 0. ROUND 3 — SHRINK THE MECHANISM. WHAT SHIPS is the CLOSE ONLY: rehome_misplaced_sessions queues DISCONNECT 0x9C for v5, calls detach(graceful=false), and does NOTHING to routing, interest or the settle machinery — not even at the close. The session becomes an ordinary offline persistent session on a non-owning node, which is exactly what the PRE-EXISTING release_moved_sessions has always handled, on its own scan cadence, at a call site (inherit_sessions) whose very next line is settle_pending_publishes — the only thing that clears a held ack. The count of new code paths that can answer a publish is ZERO, which is why round 2's blocking findings (i) and (ii) are not guarded but STRUCTURALLY UNEXPRESSIBLE: there is no new term in routing_unsettled() and no evidence predicate. WHY IT IS HONEST, proven at the entry points that killed both earlier rounds: while the old node holds the routing it still advertises the session's filters, so every publish toward it is answered by a node that KNOWS the message is owed — locally the append fails NotOwner and the ack is WITHHELD; as a peer forward it is answered Failed; and at a THIRD node, where the fan-out reaches both the old node and the new owner, forward_answered is first-terminal-verdict-wins and try_complete_pending additionally requires every obligation to have resolved, so the old node's Failed withholds the ack even when the owner's Stored arrives FIRST. Round 2 asserted that composition in prose and nothing tested it; it is now enumerated mechanically and pinned in both verdict orders. THE COST, STATED: for the length of the hold BOTH nodes advertise the session's filters, so publishers to them are withheld and retry even once the session is healthy on the owner — about a tick inside a roll (the readmission's membership change arms 8 eager ticks on every node; roll_cost measures the worst post-roll ack stall at 0.1s, i.e. after the window closed) and up to the 30-tick reconcile cadence for a lease move with no membership change. EXPLICITLY CONSIDERED AND REJECTED, recorded so a fourth round does not re-litigate it: arming the pre-existing takeover_reconcile_ticks (max 2) at the close. It is a one-line reuse of a reviewed armer that IS paired with a scan, so it could not reproduce blocking (i), and it would shorten the double-advertise window from <=30s to ~2 ticks. Rejected because it works by making the old node RELEASE SOONER, and that release is justified only by evidence that this node is not the owner, never by evidence that the new owner routes the session — so it buys availability with ack honesty, and an unbounded honest refusal beats a bounded lie. TWO REPAIRS SHIP WITH IT. (1) The candidate pass must fire for a session that becomes misplaced by ARRIVING, not only by an ownership move (round-2 blocking (iii)). Triggered with an O(1) attach-time seed in finish_attach — for a persistent session, one Placement::committed_session_owner read, and if it names another node, misplaced.entry(client).or_default(); the pass's existing escape hatch (misplaced not empty) then keeps it running until the episode ends, and the pass's own retain drops the entry if the reading was stale. Zero per-tick cost, so the epoch skip survives correctness intact; measured, the skip is worth only ~1.5-2 ms/tick at 5000 sessions, an order of magnitude under the ~100 ms the hub_dispatch_seconds registration text calls a blocked loop, so it would have been paid if it had cost anything. (2) session_rehomes{reason=deferred} counts each SESSION once per deferral episode (new Misplaced.deferred, set on the false->true transition, cleared with ticks/noted on the close and when candidacy ends) rather than each per-tick deferral EVENT: the pass re-derives its candidates every tick, so an n-session move counted ~n^2/(2*cap) — the documented 1700-session resize would have reported ~45,000 — and the operator sizing a drain from increase(...[5m]) overestimated the backlog by more than an order of magnitude. Measured red-first at n = 3*cap + 8: 120 events vs 72 sessions. WHAT ROUND 2 CONTRIBUTED THAT SURVIVES VERBATIM. FINDING 2 (Will) — DECIDED: the Will FIRES on a rehome close; suppression rejected. graceful controls exactly one thing (will publication), so graceful=true would be a pure silent suppression. Consistency: evict -> detach(false) and session takeover both fire it, and issue #265 (closed) existed precisely because broker-initiated closes were silently NOT firing it — its exit 1 was 'every broker-initiated close fires the Will' and its exit 2 (documenting suppression) was rejected in the issue text as a spec violation. Spec: [MQTT-3.1.2-8]/3.14.4 delete the will only on a CLIENT DISCONNECT with reason 0x00; a server 0x9C does not. The spec's own answer to 'this close is not a death' is the Will Delay Interval (0x18), which mqtt-codec DECODES (properties.rs) and the broker never honours — a real conformance gap, and the only spec-clean way to make a rehome (or a takeover) will-free; honouring it in a CLUSTER needs the delay and its cancellation to survive the client reconnecting on a DIFFERENT node, which no peer frame or durable record expresses, so it is the named follow-up. The cost is documented loudly instead (OPERATIONS roll bullet + alert row, TROUBLESHOOTING symptom, ADR 0005 note, metric rustdoc: one LWT per rehomed session; treat session_rehomes{reason=stale-owner} as the device-offline alert suppressor) and locked by a test in BOTH directions. No config knob — a switch for spec conformance is the wrong surface. FINDING 3 (loop cost) — the O(online sessions) pass is EVENT-GATED on a new monotone Placement::ownership_epoch, bumped only when the committed lease map actually changes or when eligibility/addrs change (deliberately NOT on set_voters: voters steer the assigner's hrw_owner, never the committed map), and it no longer holds the placement lock — it runs over a new Placement::committed_ownership() snapshot (CommittedOwnership, bounded by NUM_GROUPS=256 + members), so the cluster driver's per-tick lease pushes no longer queue behind an O(sessions) scan. MEASURED (release build, 5000 online persistent sessions, mean of mqttd_hub_dispatch_seconds{command=sweep}): no rehome pass at all 3.85-4.2 ms/tick; unconditional 5.8-7.0 ms/tick; gated 2.5-4.7 ms/tick. The relocatability check also lost its redundant owner_route comparison: wherever a committed owner exists it IS the routed owner (group_owner falls back to HRW only where the lease map is empty), so only the peer address remains to check. FINDING 4 (unbounded closes) — REHOME_CLOSES_PER_TICK = 32; the remainder is deferred to later ticks (the pass re-derives its candidates every tick, so deferral costs only time) and counted once per session per episode, plus one info log per tick. Because the will rate IS the close rate, this is also the will-storm cap. FINDING 5 (lost expiry deadline) — detach's `let _ = set_session_expiry(...)` is replaced by Hub::persist_detach_deadline: on a clustered durable node that does not own the session's group the write is SKIPPED deliberately (it is refused NotOwner by construction), warned, and counted as session_expiry_unpersisted{reason=not-owner}; any other failure is warned and counted as reason=error instead of being discarded. RESIDUAL, NAMED in ADR 0009's as-delivered note: the new owner then holds a record with no deadline, so a client that never returns leaves a persistent session behind; it self-heals on the client's next CONNECT anywhere, and it cannot be closed in this seam because only the absolute deadline is persisted (never the INTERVAL) and no peer frame carries a session deadline — the same hole is PRE-EXISTING for every takeover of an online session. TESTS as shipped, all mutation-proven: no_publish_toward_a_rehomed_session_is_ever_acked_while_this_node_routes_it (a table-driven MECHANICAL enumeration of {local entry, peer-forward entry} x {owner interest absent, present, present-then-withdrawn} x {before the close, after the close} — 12 cells, every one a withhold, with a zero-match-forward CONTROL before and after proving the routing view was settled throughout, so no cell can pass because of a pre-existing gate and no rehome close arms a node-wide window); a_third_node_composes_a_refusal_and_a_store_into_a_withhold (two peers, both verdict orders, the publisher's outcome sender must be DROPPED — the load-bearing composition rule nothing else in the suite pinned); a_session_that_becomes_misplaced_by_attaching_is_rehomed (lease committed elsewhere BEFORE the attach, with the epoch bump consumed by candidate-free ticks); a_mass_move_counts_each_deferred_session_once; an_online_session_whose_group_moved_is_closed_so_it_relocates (amended: after the close the filter is STILL advertised, no shrunken Interest follows, and a publish for the session is withheld — the whole honesty story in one assertion); a_rehome_close_publishes_the_clients_will (with the clean-DISCONNECT control in the same test); a_transient_ownership_blip_does_not_close_a_live_session; a_group_with_no_committed_lease_is_never_rehomed; an_unrelocatable_owner_is_counted_not_closed; a_rehomed_session_is_not_closed_again_within_the_cooldown; the_committed_group_owner_never_falls_back_to_the_hrw_ring; the_rehome_scan_is_skipped_while_ownership_has_not_moved; one_tick_closes_at_most_the_per_tick_cap_and_defers_the_rest; a_rehomed_finite_expiry_session_reports_its_unpersisted_deadline; and roll_cost end to end (asserts blind_retries == 0 AND, added in this pass after review found the guard one-sided, !rehomed.is_empty() — without the second assertion the test passes identically on a run where nothing was ever misplaced, so it would have gone green while exercising nothing). MUTATIONS RUN, OBSERVED, RESTORED (round 3): re-add release_moved_sessions() at the close -> '[Absent/Local/after-the-close] ... got Ok(Ok(Accepted))' and 'after a rehome close this node still routes the session, so the publisher's ack must be WITHHELD, got Ok(Ok(Accepted))'; make DurableOutcome::Failed fall through to try_complete_pending in forward_answered -> '[stored-then-failed] ... got Ok(Ok(Accepted))'; delete the attach-time seed -> 'no rehome DISCONNECT; got None'; revert the deferral counter to per-tick events -> 'at most 72 sessions could be deferred, the counter says 120'; detach(..., false) -> true -> 'a rehome close must publish the client's Last Will'; delete rehome_misplaced_sessions() from the sweep -> 'no rehome DISCONNECT; got None' on both the ownership-move and the attach-arrival tests. Round 1's and round 2's mutation tables for the surviving bounds (grace, committed-lease-only, cooldown, relocatability, the HRW-fallback guard, the per-tick cap, the ownership_epoch bump, persist_detach_deadline) stand; those tests and their production lines are unchanged. RE-MEASURED against the shipped shape (roll_cost, three runs, 2026-08-15): 1 of 9 clients rehomed per roll, 0 blind retries, worst mid-roll ack stall 0.1s, worst post-roll ack stall 0.1s, drain-to-exit 0.3s, restart-to-readmission 0.5-1.5s. The 0.1s figure in OPERATIONS therefore stands, but it is now measured with round 2's routing_released_ticks GONE, which is what had falsified it. FOLLOW-UPS: (a) THE DEFERRAL OF THIS ROUND — release_moved_sessions releases a moved session's routing with NO successor claim, so between that release and the new owner materialising the session a publish for it is acked with nothing stored (Ok(Ok(Accepted)), proven in a fixture with no rehome anywhere in the story: a client detaches itself, then its group's lease moves, and at the ~30-tick scan the gossiped Interest loses the filter and the next gated publish is Accepted). It is not introduced here — it is the behaviour of any misplaced session whose client disconnects for any reason — and #284's close adds a trigger to that path without changing its timing, which is exactly why arming the eager window at the close was rejected. Honest exits: a per-session hand-off frame on the peer bus (a PROTO_MAX bump, which also removes the filter-level-evidence asymmetry that shared subscriptions already avoid via SharedInterest's member list), or making a lease/voter move arm the cluster-wide settle window the membership events already arm — with the existing release/settle CO-LOCATION preserved, since arming without a scan is round-2 blocking (i). (b) The double-advertise retry window (bounded, honest, visible) closes WITH (a), not before it. (c) Honour the MQTT 5 Will Delay Interval cluster-wide — the only spec-clean way to make a rehome or a takeover will-free. (d) Persist the session-expiry INTERVAL alongside the absolute deadline (finding 5's residual and the pre-existing takeover hole). (e) Still open from round 1 and now the load-bearing half: /readyz turns green before the readmitted node is back in the lease VOTER set, which is what opens client traffic inside the window this whole task exists to survive (ADR 0047's corrected amendment). NOT COVERED BY A TEST, named: REHOME_COOLDOWN's expiry (only its suppression is pinned — it needs a virtual clock the durable fixture does not take), and the CLEARING of Misplaced.deferred, which only matters for a second deferral episode of the same session and would need a wait past the 30 s cooldown to observe."
---

# Delivery: ADR 0043 — Elastic cluster resize

[ADR 0043](../adr/0043-elastic-cluster-resize.md) · tasks and status in the frontmatter
above · this file is the plan, progress log, and changelog.

<!-- status-table:0043 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0043-P1 | ✅ done | 2026-07-13 | "Recovery reads now carry a COMPLETENESS verdict with two halves: per-key gap-freedom (ReplicaState::complete — stored offsets contiguous above the truncation low-water, so a joiner holding new appends above a hole it never received reads incomplete) AND the durable caught-up watermark (new replica_caught_up redb table: per group, the replica set this node last completed catch-up against; a stamp from a different cohort — or no stamp — reads incomplete, so an EMPTY joiner cannot claim a key has no history; additive table, no schema bump, survives restart so a crash mid-catch-up resumes rather than fakes completion). recover_key requires a quorum PLUS at least one complete anchor, and the OWN copy obeys the same rule — an unstamped owner cannot fabricate an empty log (fails closed with NoQuorum until its boot sweep stamps; ADR 0017 posture). The back-fill: a catch-up sweep in the lease driver (armed at boot and on every placement membership change, CATCH_UP_SWEEP_EVERY=5 ticks, budget 24) discovers keys per-peer (ReplicaKeys, attributed — a group stamps only once every other set member was heard), heals hollow keys by asking the group owner to recommit_key them (new ReplicaCatchUp frame; owner-side CatchUpSource seam on the plane, implemented by GroupRoutedLog: route → recover → re-commit at the owner epoch, idempotent and fenced, plus a truncation-floor fan so drained prefixes read as acked-away rather than as gaps), and re-stamps pure shrinks of a known cohort immediately (custody unchanged — no data moved on 5→3). Cached group logs rebuild on REPLICA-SET changes, not only epoch changes, so appends and re-commits fan out to joiners. Wire: proto 3→4; ReplicaRead2/ReplicaReadReply2 (request version selects reply shape; pre-proto-4 replies are conservatively incomplete) and ReplicaCatchUp appended at the enum end (ADR 0038 compat). Tests — unit: completeness shapes (gap/hole/truncated-floor), stamp currency + reopen persistence, unstamped-owner-cannot-fabricate, recovery-refuses-a-quorum-of-hollow-replicas (then serves once a complete anchor joins), growing_a_one_node_group_back_fills_the_joiners (store seam), proto4 wire round-trip + catch-up routing (plane seam). Integration (cluster_stress): growing_one_node_to_three_back_fills_and_survives_the_founder — one durable node accumulates acked facts (offline durable session owed 3 acked QoS1 payloads + acked retained), grows 1→3, both joiners reach the caught-up watermark on q/, m/ and r/ keys, the FOUNDER IS KILLED (the only pre-grow copy), and the session resumes present=true on the survivors with every acked payload replayed and the retained value served — zero loss, zero waivers (~17s). Workspace green (all suites), clippy zero warnings." |
| 0043-P2 | ✅ done | 2026-07-13 | "The hub now watches the placement member set on its sweep tick (new known_members snapshot): ANY change — growth above all; shrink arrives via SWIM eviction too, overlapping the PeerDead arm — re-arms the takeover window (takeover_reconcile_ticks=8), so every node runs the eager inherited-session scan for the next ~8s and publish acks registered in the window gate on awaiting_settle exactly as after a death (settle releases only when the window is over AND the mesh is whole — the T9/T4 honesty rules, inherited unchanged). Two sides of the move: the NEW owner's scan materializes the moved groups' sessions (all_sessions unions local + remote keys; per-key recovery rides P1's complete-anchor gate, with the old owner — alive and stamped — as the anchor) and gossips their filters, re-targeting held forwards; and the OLD owner now has the missing mirror — release_moved_sessions, run after every scan: offline, not-attaching sessions whose group this node no longer owns drop their LOCAL routing state (durable data stays; the node usually remains a replica) and interest re-gossips without them, so forwards stop targeting a node that can only answer NotOwner (every such ack was being withheld — the pre-P2 state poisoned post-grow publishes indefinitely). Durable-cluster-gated: with local-only session storage the data cannot follow ownership, so routing is kept. Tests — unit: growth_releases_moved_sessions_on_the_old_owner (paused-clock hub with a real idle plane: boot scan gossips the materialized filter, placement growth re-gossips without it). Integration (cluster_stress): growth_migrates_moved_sessions_eagerly_and_acks_stay_honest — offline durable session whose owner MOVES on a 1→3 grow (no deaths): 2 payloads acked pre-grow, 3 acked post-grow against the old owner with the session touched by no client; resume on the NEW owner is present=true and replays all 5 (~12s). Validated non-vacuous: the same test against the pre-P2 hub FAILS (post-grow acks withheld behind the old owner's stale NotOwner routing). Workspace green, clippy zero warnings." |
| 0043-P3 | ✅ done | 2026-07-13 | "Operator trigger: SIGUSR1 (chosen over an admin endpoint — the health listener is deliberately read-only and unauthenticated; signals match the existing SIGHUP-reload / SIGTERM-shutdown operational grammar). On SIGUSR1, readiness fails immediately (orchestrators steer new traffic away — the ADR 0019 §4 posture standing in for 'stop accepting new sessions' pre-proxy) and the DECOMMISSION DRAIN runs (new mqtt_cluster::decommission::Drain, assembled from the plane's shared handles via DurablePlane::decommission_drain). The drain cannot push data itself — a non-owner writing old-epoch entries would be fenced, and minting epochs is the owner's privilege — so each round it VERIFIES every key it stores against every member of the key's post-departure replica set (Placement::group_replica_set_without — HRW monotonicity keeps every surviving current member in the set) using plain recovery reads, with CONTENT SUFFICIENCY as the verdict: the successor's truncation watermark has reached ours (an acked-away prefix can never be resurrected from a merge that no longer includes us) and every entry we hold is either below that watermark or present in its log. Each shortfall asks the key's group OWNER for a targeted re-commit — new proto-5 ReplicaCatchUpTo frame (appended at the enum end, PROTO_MAX 4→5), served through the CatchUpSource seam by ClusterLog::recommit_key_to: every committed entry re-tagged at the owner's epoch (the P1 recommit convention, idempotent) plus the truncation floor, to ONE node, no quorum gate — the drain re-verifies and re-asks until nothing falls short. Rounds repeat 1s apart; the drain never gives up on its own (an unreachable successor keeps it honestly pending; the operator can always escalate to plain SIGTERM = crash semantics — the drain mutates nothing locally, so interruption at ANY point is just a crash). On completion the ordinary ADR 0019 graceful shutdown runs: SWIM leave → placement eviction → voter demotion via the ADR 0021 reconciler → P2's eager migration materializes the moved sessions on the survivors. Observability: /readyz gains decommission{pending,rounds,complete} while a drain is active (DrainStatus published into the health state). Tests — unit: a_solo_drain_completes_immediately (nowhere to hand off = shutdown); the_drain_hands_off_and_verifies_a_held_key (two-node ring, truncated key, empty successor: round 1 finds the shortfall, targeted re-commit lands entries AND watermark, rounds converge to 0); plane wire test extended (ReplicaCatchUpTo round-trips to the owner's CatchUpSource with its target). Integration (cluster_stress): a_decommissioned_nodes_departure_loses_nothing — FOUR-node cluster (4 members, R=3: removing one adds exactly one NEWCOMER to every set it was in, so the targeted hand-off is load-bearing), acked payloads + acked retained on sets including the leaver, the durable subscriber OWNED by the leaver; drain converges (~120s bound, actual seconds), reports complete, the node dies (kill — harsher than the graceful leave), and everything acked survives: resume present=true on the new owner, all payloads replay, retained serves (~14s). Workspace green, clippy zero warnings." |
| 0043-P4 | ✅ done | 2026-07-13 | "SCHEDULE VOCABULARY: the seeded stress schedules gain join_step (grow by a fresh node mid-schedule, ≤2 per schedule; new dirs, joins via alive seeds) and decommission_step (drain → leave only if the drain CONVERGES within its bound; aborted honestly under faults — the operator semantics; requires ≥4 alive so the schedule's kill still lands afterwards), mixed into the seeded composition and counted on the per-seed composition line. DEDICATED PATH TESTS (with 1→3 already landed in P1 and the 4-node decommission in P3): growing_three_to_five_zone_spread_survives_losing_two_originals — 3 zones advertised over the LIVE gossip-label plumbing (start_stress_node_in_zone → Placement::with_local_domain + SWIM domain, the ADR 0016 T5 path), grow 3→5, wait for every member of each fact key's new replica set to reach the P1 caught-up watermark, then kill TWO originals at once: all acked payloads + retained survive on {original, joiner, joiner} (~30s incl. bring-up); cost_reduction_five_to_three_via_two_decommissions — two sequential drain→leave cycles with membership settling between; rolling_replacement_swaps_a_node_without_loss — join one, decommission the subscriber's owner, same size before/after (the ADR 0039 rolling-upgrade motion). All under the unchanged acked-obligations oracle, zero waivers. PROTO WINDOW: catch_up_requests_are_proto_gated_per_link pins the mixed-proto mesh honesty (proto-3 peer: neither catch-up frame; proto-4: P1's but not P3's targeted one; current: both) — a whole-cluster TWO-BINARY rolling-upgrade test needs an out-of-process harness and is recorded as the remaining gap (the in-process harness shares one binary). THE HEADLINE — the re-mixed schedules found a REAL pre-existing defect on their first sweep (exhibit ② below, the fast-restart void ack: seeds 4 and 1, kill+restart faster than SWIM death confirmation): fixed with four coordinated honesty changes — SessionScan.complete surfaces transiently-skipped keys from all_sessions; interest gossip is suppressed until the first complete whole-mesh scan (fresh-boot empty snapshots no longer erase peers' knowledge; 30-tick loud liveness backstop); a gated forward that matched nobody while routing_unsettled() answers a retriable refusal instead of OK; clustered() honors a startup-time cluster-CONFIGURED flag so a restarted node is never mistaken for a standalone broker; and the settle window closes on the observable routing_unsettled() predicate, never on tick count alone. Verified: seed 1 (previously ~75% failing) 5/5 green, seed 4 deterministic-green, 8-seed sweep green, all resize path tests green, workspace green, clippy zero warnings." |
| 0043-P5 | ✅ done | 2026-07-14 | "README gains a 'Resizing the cluster' operator section (before Upgrades & versioning, whose rolling-upgrade motion it feeds): GROW is one paragraph — start the node with MQTTD_SWIM_SEEDS at any member; the joiner back-fills behind the durable caught-up watermark and counts toward no recovery until caught up; watch /readyz lease_group_ready — with the laptop→server case called out (growing 1 node re-replicates its whole history). THE TWO-NODE TRUTH is stated plainly: two members = replica sets of two = write quorum 2-of-2, strictly worse write availability than one node; supported as a waypoint, recommended upgrade 1→3 in one motion. SHRINK documents the SIGUSR1 decommission (readiness fails at once, drain hands every held key to the post-departure sets and verifies, progress on /readyz decommission{pending,rounds,complete}, then the graceful leave; a non-converging drain waits rather than lies; SIGTERM escalates; mid-drain crash = crash), one node at a time for 5→3. REPLACE = grow-then-decommission, the motion rolling binary upgrades ride. The interim 'durable resize unsupported' warning (placed at P1's start, softened as P1–P3 landed) is REMOVED — the durable-storage feature bullet now states resize is data-safe and points at this guide. Signal documented in main.rs's operator header since P3. ADR 0043 flipped to ACCEPTED (P1–P5 delivered); the historical warning note moved to the ADR's consequences as a record. gen-status green." |
| 0043-P6 | ✅ done | 2026-08-15 | "Issue #284's wedge is closed by REHOME ON SETTLE, and the issue's own stated mechanism is CORRECTED in the process. CORRECTION: there is no ring/lease split here. Placement::owner_route (what conn.rs relocates against) and the enqueue gate Placement::owns_group BOTH resolve through the same Placement::group_owner, and both were self-consistent at every measured point — at the relocation node c saw committed_owner=Some(a) hrw_owner=a, and at every refusal node a saw committed_owner=b hrw_owner=b with a full voter set. The lease-epoch gate the 2026-07-20 post-mortem comment describes is never reached; issue #284's exit 2 as phrased ('make owner_route and the enqueue-side check read the same source') is a no-op. THE REAL MECHANISM: relocation (ADR 0005) is a one-shot CONNECT-time snapshot of an ownership that is guaranteed to move again moments later. hrw_owner is voter-restricted (ADR 0049) and the lease assigner drives every group's lease toward it, but a rolled node rejoins SWIM membership — and turns /readyz green — BEFORE it re-enters the lease voter set, so its groups' leases are still parked on the interim holder at that instant and are legitimately pulled back 2-4s later. Measured for group 212 (= group_of(rc-4848-2-0)): lease b->a at the drain, client resumes via c 8.8s later while c's voters were still [a,c] -> relocated to a (CORRECT on the committed view), lease a->b again 3.5s after that. Nothing re-evaluated a LIVE session: release_moved_sessions (0043-P2) is the one place that detects exactly this condition and its filter excludes online sessions — instrumentation showed node a logging the condition on 8 consecutive scans over 94s and never acting. THE FIX: new Hub::rehome_misplaced_sessions() on the 1s sweep tick (one decision site, re-derived from placement each tick — deliberately NOT the traffic-driven NotOwner refusal, which cannot see an idle misplaced session, and NOT a placement-change event, which would need new cross-crate plumbing for a signal a self-correcting poll already gives). It closes an ONLINE persistent session whose group's COMMITTED lease names another node: v5 gets DISCONNECT 0x9C (Use another server; no Server Reference property — placement holds PEER-BUS addresses), v3.1.1 sees the relay's half-close, then detach(graceful=false) and the UNCHANGED release_moved_sessions drops the local routing and re-gossips the shrunken interest in the same tick. FOUR BOUNDS, each its own test: MISPLACED_GRACE_TICKS=2 (a sub-tick lease blip must not cost a live client its connection); a COMMITTED lease only — new Placement::committed_group_owner / committed_session_owner, the eligible-filtered lease map with NO HRW fallback, so an unsettled group and a corpse's lease are structurally non-triggers (the transient split the post-mortem describes); a per-session REHOME_COOLDOWN=30s (a flapping placement or an LB that keeps returning the client here cannot become a close loop, or a will-publish loop); and a relocatability precondition (owner_route must agree) because with the owner's address unknown the next CONNECT is served locally anyway (ADR 0005 §5) and closing would loop — that case is counted and gauged instead. INVARIANTS: nothing about acks changed. The NotOwner refusal and the 'withholding the publisher's ack (ADR 0041 T5)' withhold are untouched; only how LONG the honest refusal persists changes (measured unbounded -> ~1-2s). No durable repair is attempted or needed: the only durable ownership record is the replicated lease, which read the CORRECT owner (b) in all 392 refusal lines — the stale placement is purely in-memory. No new task of any kind, so #242/ADR 0061's spawn_owned rule is not engaged (nothing here captures the store); the fn-not-async send chain is unmodified; the only awaits are detach's pre-existing ones. Exactly-one-owner (ADR 0005/0007) is strengthened, not weakened: today TWO nodes disagree about who hosts the session for seconds-to-forever; the close collapses that to one, structurally (a drops its routing before the client can learn to reconnect, and the only way back online anywhere is a fresh CONNECT through the existing takeover fence). A TRANSPARENT re-relocation was rejected: the proxy is structurally one hop (run_framed refuses to re-proxy), the CONNECT and its session_present CONNACK are already on the wire with no vocabulary for replaying them elsewhere, and a hand-off would make exactly-one-owner a timing argument. TESTS — integration: roll_cost's post-roll section rewritten from modelling the defect (it counted 'clients needing a second (keepalive-style) reconnect: 1') to asserting the fix — it watches every socket for the server-initiated close over a 20s bound, reconnects the rehomed client ONCE (session_present asserted true), then requires ack+delivery for all 9, and asserts blind_retries == 0 (nobody heals by waiting out dead air). Measured before/after at seed 4848: worst post-roll QoS1 ack stall 26.4s -> 0.1s, rehomes 1, blind retries 1 -> 0, suite 91s -> 43s. Unit (hub): an_online_session_whose_group_moved_is_closed_so_it_relocates (DISCONNECT 0x9C, leaves online, filter released from gossiped interest, session_rehomes{stale-owner}=1); a_transient_ownership_blip_does_not_close_a_live_session; a_group_with_no_committed_lease_is_never_rehomed (the post-mortem split, pinned as a non-trigger); an_unrelocatable_owner_is_counted_not_closed; a_rehomed_session_is_not_closed_again_within_the_cooldown. Unit (placement): the_committed_group_owner_never_falls_back_to_the_hrw_ring. MUTATION-PROVEN, each run and restored: drop the sweep call -> 'no rehome DISCONNECT; got None' AND roll_cost fails '1 client(s) were left to discover a stale placement by dead air'; GRACE_TICKS=1 -> 'a sub-tick ownership blip must not disturb a live session'; committed_session_owner -> owner (HRW view) -> 'an unsettled (lease-less) group must not close a live session'; drop the owner_route precondition -> 'a session whose owner has no known address must be kept, not closed'; drop the cooldown -> 'a second close inside the cooldown would be a close loop'; give committed_group_owner an HRW fallback -> 'an unsettled group has no committed owner, however loudly HRW answers'. METRICS: mqttd_session_rehomes_total{reason=stale-owner|unrelocatable|cooldown} and the mqttd_misplaced_sessions gauge, both with rustdocs and OPERATIONS alert rows. DOCS: ADR 0005 (owns the relocation claim) and ADR 0043 §2 amended in place with As-delivered notes; OPERATIONS.md's 'straggler wart' bullet replaced by the measured new cost and the client-backoff mitigation withdrawn; ADR 0047's amendment corrected in place (it recorded the wrong mechanism and 'until it lands'); TROUBLESHOOTING gains the expected-close symptom and the unrelocatable failure form. RESIDUAL, stated not hidden: the unrelocatable branch deliberately leaves such a session undeliverable (ADR 0005 §5 beats availability when the alternative is a reconnect loop) — now counted and gauged; and one immediate reconnect is still a reconnect, so a roll is not transparent to a straggler. The readiness half — /readyz turning green before the readmitted node is back in the lease voter set, which is what opens client traffic inside the window — is NOT fixed here and is the remaining follow-up. ROUND 2 — the adversarial review found FIVE defects in the round-1 shape. Findings 2-5 are remediated and their remediations SHIP (see below). FINDING 1 (BLOCKING) — the round-1 close released the session's routing in the same tick (via the unchanged release_moved_sessions), which is an ACKED-BUT-DROPPED publish: reconfirmed red-first ('got Ok(Ok(Accepted))' for a QoS 1 publish toward a rehomed session, with zero peer forwards and nothing stored anywhere). Round 2's remediation was a HAND-OFF-OBSERVED RELEASE: hold the routing after the close and release it only on positive evidence (Hub::handed_off, handoff_claimed, release_handed_off_sessions, routing_released_ticks, the sessions_awaiting_handoff gauge, the handed-off / handoff-stalled reasons). ROUND 3 DELETED ALL OF IT, because its own review found three more blocking defects, each in machinery added for OPTIMISATION rather than in the behaviour change: (i) routing_released_ticks armed routing_unsettled() for 2 ticks but awaiting_settle is cleared ONLY in settle_pending_publishes, reachable only from an inherited-session scan, so every gated publish on the node was held up to EXPIRY_RECONCILE_EVERY = 30s rather than 2 ticks (measured 330us before a release vs 19.998s after — a node-wide availability regression on traffic unrelated to the rehomed session, which also falsified the delivered 0.1s post-roll stall claim); (ii) the evidence-gated release was IRREVERSIBLE and its evidence never re-checked, so when the owner's advertised interest disappeared before the owner materialised the session a publish was answered Accepted with nothing stored — the same lie, proven in the fixture, and unavoidable because PeerMessage::Interest carries FILTERS, not client ids, so a co-subscriber of the same filter satisfies the predicate; (iii) the ownership_epoch skip made detection edge-triggered on ownership, so a session that became misplaced by ATTACHING never entered misplaced, the pass never ran again, and the #284 wedge returned silently with mqttd_misplaced_sessions reading 0. ROUND 3 — SHRINK THE MECHANISM. WHAT SHIPS is the CLOSE ONLY: rehome_misplaced_sessions queues DISCONNECT 0x9C for v5, calls detach(graceful=false), and does NOTHING to routing, interest or the settle machinery — not even at the close. The session becomes an ordinary offline persistent session on a non-owning node, which is exactly what the PRE-EXISTING release_moved_sessions has always handled, on its own scan cadence, at a call site (inherit_sessions) whose very next line is settle_pending_publishes — the only thing that clears a held ack. The count of new code paths that can answer a publish is ZERO, which is why round 2's blocking findings (i) and (ii) are not guarded but STRUCTURALLY UNEXPRESSIBLE: there is no new term in routing_unsettled() and no evidence predicate. WHY IT IS HONEST, proven at the entry points that killed both earlier rounds: while the old node holds the routing it still advertises the session's filters, so every publish toward it is answered by a node that KNOWS the message is owed — locally the append fails NotOwner and the ack is WITHHELD; as a peer forward it is answered Failed; and at a THIRD node, where the fan-out reaches both the old node and the new owner, forward_answered is first-terminal-verdict-wins and try_complete_pending additionally requires every obligation to have resolved, so the old node's Failed withholds the ack even when the owner's Stored arrives FIRST. Round 2 asserted that composition in prose and nothing tested it; it is now enumerated mechanically and pinned in both verdict orders. THE COST, STATED: for the length of the hold BOTH nodes advertise the session's filters, so publishers to them are withheld and retry even once the session is healthy on the owner — about a tick inside a roll (the readmission's membership change arms 8 eager ticks on every node; roll_cost measures the worst post-roll ack stall at 0.1s, i.e. after the window closed) and up to the 30-tick reconcile cadence for a lease move with no membership change. EXPLICITLY CONSIDERED AND REJECTED, recorded so a fourth round does not re-litigate it: arming the pre-existing takeover_reconcile_ticks (max 2) at the close. It is a one-line reuse of a reviewed armer that IS paired with a scan, so it could not reproduce blocking (i), and it would shorten the double-advertise window from <=30s to ~2 ticks. Rejected because it works by making the old node RELEASE SOONER, and that release is justified only by evidence that this node is not the owner, never by evidence that the new owner routes the session — so it buys availability with ack honesty, and an unbounded honest refusal beats a bounded lie. TWO REPAIRS SHIP WITH IT. (1) The candidate pass must fire for a session that becomes misplaced by ARRIVING, not only by an ownership move (round-2 blocking (iii)). Triggered with an O(1) attach-time seed in finish_attach — for a persistent session, one Placement::committed_session_owner read, and if it names another node, misplaced.entry(client).or_default(); the pass's existing escape hatch (misplaced not empty) then keeps it running until the episode ends, and the pass's own retain drops the entry if the reading was stale. Zero per-tick cost, so the epoch skip survives correctness intact; measured, the skip is worth only ~1.5-2 ms/tick at 5000 sessions, an order of magnitude under the ~100 ms the hub_dispatch_seconds registration text calls a blocked loop, so it would have been paid if it had cost anything. (2) session_rehomes{reason=deferred} counts each SESSION once per deferral episode (new Misplaced.deferred, set on the false->true transition, cleared with ticks/noted on the close and when candidacy ends) rather than each per-tick deferral EVENT: the pass re-derives its candidates every tick, so an n-session move counted ~n^2/(2*cap) — the documented 1700-session resize would have reported ~45,000 — and the operator sizing a drain from increase(...[5m]) overestimated the backlog by more than an order of magnitude. Measured red-first at n = 3*cap + 8: 120 events vs 72 sessions. WHAT ROUND 2 CONTRIBUTED THAT SURVIVES VERBATIM. FINDING 2 (Will) — DECIDED: the Will FIRES on a rehome close; suppression rejected. graceful controls exactly one thing (will publication), so graceful=true would be a pure silent suppression. Consistency: evict -> detach(false) and session takeover both fire it, and issue #265 (closed) existed precisely because broker-initiated closes were silently NOT firing it — its exit 1 was 'every broker-initiated close fires the Will' and its exit 2 (documenting suppression) was rejected in the issue text as a spec violation. Spec: [MQTT-3.1.2-8]/3.14.4 delete the will only on a CLIENT DISCONNECT with reason 0x00; a server 0x9C does not. The spec's own answer to 'this close is not a death' is the Will Delay Interval (0x18), which mqtt-codec DECODES (properties.rs) and the broker never honours — a real conformance gap, and the only spec-clean way to make a rehome (or a takeover) will-free; honouring it in a CLUSTER needs the delay and its cancellation to survive the client reconnecting on a DIFFERENT node, which no peer frame or durable record expresses, so it is the named follow-up. The cost is documented loudly instead (OPERATIONS roll bullet + alert row, TROUBLESHOOTING symptom, ADR 0005 note, metric rustdoc: one LWT per rehomed session; treat session_rehomes{reason=stale-owner} as the device-offline alert suppressor) and locked by a test in BOTH directions. No config knob — a switch for spec conformance is the wrong surface. FINDING 3 (loop cost) — the O(online sessions) pass is EVENT-GATED on a new monotone Placement::ownership_epoch, bumped only when the committed lease map actually changes or when eligibility/addrs change (deliberately NOT on set_voters: voters steer the assigner's hrw_owner, never the committed map), and it no longer holds the placement lock — it runs over a new Placement::committed_ownership() snapshot (CommittedOwnership, bounded by NUM_GROUPS=256 + members), so the cluster driver's per-tick lease pushes no longer queue behind an O(sessions) scan. MEASURED (release build, 5000 online persistent sessions, mean of mqttd_hub_dispatch_seconds{command=sweep}): no rehome pass at all 3.85-4.2 ms/tick; unconditional 5.8-7.0 ms/tick; gated 2.5-4.7 ms/tick. The relocatability check also lost its redundant owner_route comparison: wherever a committed owner exists it IS the routed owner (group_owner falls back to HRW only where the lease map is empty), so only the peer address remains to check. FINDING 4 (unbounded closes) — REHOME_CLOSES_PER_TICK = 32; the remainder is deferred to later ticks (the pass re-derives its candidates every tick, so deferral costs only time) and counted once per session per episode, plus one info log per tick. Because the will rate IS the close rate, this is also the will-storm cap. FINDING 5 (lost expiry deadline) — detach's `let _ = set_session_expiry(...)` is replaced by Hub::persist_detach_deadline: on a clustered durable node that does not own the session's group the write is SKIPPED deliberately (it is refused NotOwner by construction), warned, and counted as session_expiry_unpersisted{reason=not-owner}; any other failure is warned and counted as reason=error instead of being discarded. RESIDUAL, NAMED in ADR 0009's as-delivered note: the new owner then holds a record with no deadline, so a client that never returns leaves a persistent session behind; it self-heals on the client's next CONNECT anywhere, and it cannot be closed in this seam because only the absolute deadline is persisted (never the INTERVAL) and no peer frame carries a session deadline — the same hole is PRE-EXISTING for every takeover of an online session. TESTS as shipped, all mutation-proven: no_publish_toward_a_rehomed_session_is_ever_acked_while_this_node_routes_it (a table-driven MECHANICAL enumeration of {local entry, peer-forward entry} x {owner interest absent, present, present-then-withdrawn} x {before the close, after the close} — 12 cells, every one a withhold, with a zero-match-forward CONTROL before and after proving the routing view was settled throughout, so no cell can pass because of a pre-existing gate and no rehome close arms a node-wide window); a_third_node_composes_a_refusal_and_a_store_into_a_withhold (two peers, both verdict orders, the publisher's outcome sender must be DROPPED — the load-bearing composition rule nothing else in the suite pinned); a_session_that_becomes_misplaced_by_attaching_is_rehomed (lease committed elsewhere BEFORE the attach, with the epoch bump consumed by candidate-free ticks); a_mass_move_counts_each_deferred_session_once; an_online_session_whose_group_moved_is_closed_so_it_relocates (amended: after the close the filter is STILL advertised, no shrunken Interest follows, and a publish for the session is withheld — the whole honesty story in one assertion); a_rehome_close_publishes_the_clients_will (with the clean-DISCONNECT control in the same test); a_transient_ownership_blip_does_not_close_a_live_session; a_group_with_no_committed_lease_is_never_rehomed; an_unrelocatable_owner_is_counted_not_closed; a_rehomed_session_is_not_closed_again_within_the_cooldown; the_committed_group_owner_never_falls_back_to_the_hrw_ring; the_rehome_scan_is_skipped_while_ownership_has_not_moved; one_tick_closes_at_most_the_per_tick_cap_and_defers_the_rest; a_rehomed_finite_expiry_session_reports_its_unpersisted_deadline; and roll_cost end to end (asserts blind_retries == 0 AND, added in this pass after review found the guard one-sided, !rehomed.is_empty() — without the second assertion the test passes identically on a run where nothing was ever misplaced, so it would have gone green while exercising nothing). MUTATIONS RUN, OBSERVED, RESTORED (round 3): re-add release_moved_sessions() at the close -> '[Absent/Local/after-the-close] ... got Ok(Ok(Accepted))' and 'after a rehome close this node still routes the session, so the publisher's ack must be WITHHELD, got Ok(Ok(Accepted))'; make DurableOutcome::Failed fall through to try_complete_pending in forward_answered -> '[stored-then-failed] ... got Ok(Ok(Accepted))'; delete the attach-time seed -> 'no rehome DISCONNECT; got None'; revert the deferral counter to per-tick events -> 'at most 72 sessions could be deferred, the counter says 120'; detach(..., false) -> true -> 'a rehome close must publish the client's Last Will'; delete rehome_misplaced_sessions() from the sweep -> 'no rehome DISCONNECT; got None' on both the ownership-move and the attach-arrival tests. Round 1's and round 2's mutation tables for the surviving bounds (grace, committed-lease-only, cooldown, relocatability, the HRW-fallback guard, the per-tick cap, the ownership_epoch bump, persist_detach_deadline) stand; those tests and their production lines are unchanged. RE-MEASURED against the shipped shape (roll_cost, three runs, 2026-08-15): 1 of 9 clients rehomed per roll, 0 blind retries, worst mid-roll ack stall 0.1s, worst post-roll ack stall 0.1s, drain-to-exit 0.3s, restart-to-readmission 0.5-1.5s. The 0.1s figure in OPERATIONS therefore stands, but it is now measured with round 2's routing_released_ticks GONE, which is what had falsified it. FOLLOW-UPS: (a) THE DEFERRAL OF THIS ROUND — release_moved_sessions releases a moved session's routing with NO successor claim, so between that release and the new owner materialising the session a publish for it is acked with nothing stored (Ok(Ok(Accepted)), proven in a fixture with no rehome anywhere in the story: a client detaches itself, then its group's lease moves, and at the ~30-tick scan the gossiped Interest loses the filter and the next gated publish is Accepted). It is not introduced here — it is the behaviour of any misplaced session whose client disconnects for any reason — and #284's close adds a trigger to that path without changing its timing, which is exactly why arming the eager window at the close was rejected. Honest exits: a per-session hand-off frame on the peer bus (a PROTO_MAX bump, which also removes the filter-level-evidence asymmetry that shared subscriptions already avoid via SharedInterest's member list), or making a lease/voter move arm the cluster-wide settle window the membership events already arm — with the existing release/settle CO-LOCATION preserved, since arming without a scan is round-2 blocking (i). (b) The double-advertise retry window (bounded, honest, visible) closes WITH (a), not before it. (c) Honour the MQTT 5 Will Delay Interval cluster-wide — the only spec-clean way to make a rehome or a takeover will-free. (d) Persist the session-expiry INTERVAL alongside the absolute deadline (finding 5's residual and the pre-existing takeover hole). (e) Still open from round 1 and now the load-bearing half: /readyz turns green before the readmitted node is back in the lease VOTER set, which is what opens client traffic inside the window this whole task exists to survive (ADR 0047's corrected amendment). NOT COVERED BY A TEST, named: REHOME_COOLDOWN's expiry (only its suppression is pinned — it needs a virtual clock the durable fixture does not take), and the CLEARING of Misplaced.deferred, which only matters for a second deferral episode of the same session and would need a wait past the 30 s cooldown to observe." |
<!-- /status-table:0043 -->

## Plan

| Task | Done means |
|---|---|
| **0043-P1** Catch-up replicas | A replica-set joiner back-fills before counting toward any quorum, behind a durable watermark that survives restart; the hollow-replica recovery hazard (a "quorum" of {empty joiner, lagging survivor} silently truncating) is closed by construction; growing a 1-node broker re-replicates its history. Unit tests at the cluster_log/cluster_store seams + a stress-harness join under load. |
| **0043-P2** Eager migration | A ring change materializes moved groups (sessions, retained, interest) without waiting for first touch — the ADR 0042 T9 takeover scan generalized to membership growth; acks stay honest through the window via the existing settle/re-route + mesh-whole rules. |
| **0043-P3** Decommission | An operator-triggered drain completes with every owned group handed to a caught-up successor and every replicated group at full replica count among the survivors, then demotes and leaves; progress observable, crash-safe at every point. |
| **0043-P4** Resize vocabulary | The stress harness composes join/decommission into seeded schedules; dedicated tests drive 1→3, 3→5 (zone-spread), 5→3, rolling replacement, and a rolling binary upgrade; all hold the acked-obligations oracle with zero waivers. |
| **0043-P5** Operator docs | The grow/shrink/replace guide ships with the two-node honesty note; the README carries an interim "durable resize unsupported" warning until P1 lands, removed by this task. |

Order: P1 → P2 → P3 (each unblocks the next), P4 grows alongside each, P5 last.

## Exhibits / findings ledger

| # | Finding | Where | Status |
|---|---|---|---|
| — | 2026-07-13 inventory: consensus-plane resize built and unit-tested (ADR 0016/0021/0026/0028); data-plane resize missing — hollow replicas count toward quorum, ownership moves are first-touch, graceful leave hands off nothing, laptop-mode history never re-replicates; no integration test resizes a running durable cluster | code/test survey (see ADR context) | open — this ADR is the plan |
| ② | **The fast-restart void ack** (pre-existing; found by the P4 re-mixed seeded schedules, seeds 4 and 1): a node killed and restarted faster than SWIM confirms its death re-enters the mesh with a FRESH hub — no membership change is ever observed anywhere, so no takeover window arms — and three honesty holes compound: (a) its empty boot interest snapshot ERASES peers' correct knowledge of its offline sessions' filters, so their publishes conclude "no interest anywhere" and ack into the void; (b) its own gated-forward replies say OK for fan-outs that matched nobody while its sessions were still unmaterialized; (c) its own `clustered()` gate read a momentary single-member ring as "standalone" and switched every honesty gate off; and the settle window closed on TIME (8 ticks) even while scans were still skipping unreadable sessions (a lease not yet reassigned back). | `cluster_stress` seeded schedules (kill+restart+flap compositions) | **fixed in P4**: interest gossip is suppressed until the first COMPLETE whole-mesh session scan (`SessionScan.complete` — transient skips counted, `NotOwner` clean; 30-tick loud liveness backstop); a zero-match gated forward answered while `routing_unsettled()` refuses (retriable) instead of OK; `clustered()` = cluster-CONFIGURED (told by main/harness at startup) or members>1; the settle window closes on the observable `routing_unsettled()` predicate (scan complete + authoritative + mesh whole + no window ticks), never on time alone |

## Changelog

- **2026-08-15 (review round 3, before commit)** — the shape SHRANK, and that is the finding.
  Rounds 1 and 2 each closed the hole they were shown and opened another in the machinery
  they added for it: round 1's release acked-and-dropped; round 2's hand-off-observed release
  did too (its evidence was never re-checked, so an owner whose advertised interest vanished
  before it materialised the session let a publish be answered Accepted with nothing stored),
  its 2-tick honesty gate actually held gated acks until the next inherited-session scan
  (measured 330us before a release vs 19.998s after — a node-wide availability regression
  that also falsified the "0.1s worst post-roll stall" it shipped), and its epoch skip meant a
  session misplaced by ATTACHING was never rehomed at all, silently, with no counter moving.
  Every one of those lived in an OPTIMISATION, not in the behaviour change. So round 3 deleted
  the whole layer — `handed_off`, `routing_released_ticks`, `handoff_claimed`,
  `release_handed_off_sessions`, the stall gauge and its two metric reasons — and ships CLOSE
  ONLY: the misplaced session's connection is closed (v5 `DISCONNECT 0x9C`) and routing,
  interest and the settle machinery are untouched, so the session becomes an ordinary offline
  persistent session and the PRE-EXISTING inherited-session scan releases it on its own
  cadence. Honesty during the window this change owns is then proven rather than argued: while
  the old node still routes and advertises the session, every publish toward it — at the old
  node, or forwarded in, or fanned out from a third node where the owner's copy stores and the
  old node's append refuses — composes to a WITHHELD ack, pinned by a mechanical entry-point
  enumeration (entry point x owner-interest state x before/after close) rather than by
  sampling, because rounds 1 and 2 each missed a cell that enumeration catches. Two repairs
  kept: an O(1) attach-time seed so a session misplaced by attaching is caught without paying
  the unconditional pass, and the deferral counter now counts each session once per episode
  instead of once per tick (it grew ~n^2/2cap, so the documented 1700-session resize would
  have reported ~45,000). Verification then found ZERO blocking defects — the first round of
  this issue to do so — and its majors were doc overclaims, fixed here: OPERATIONS asserted
  categorically that "there is no window in which a publish is acked and then found to be
  stored nowhere" during an ownership move, which the shipped shape deliberately does NOT
  deliver (a node releases a moved session's routing on evidence that IT is not the owner,
  never on evidence that the new owner routes it — a PRE-EXISTING hole, named now instead of
  promised away, with the witnessed-release protocol left to its own issue). Also corrected
  from that review: the post-roll stall is 0.2s worst across four runs, not 0.1s (round 3 had
  narrowed round 1's honest 0.1-0.2s to the optimistic end); the double-advertise window is
  ~2s inside a roll, measured, not "about a second"; two code comments still cited round 2's
  superseded sweep figures (0.65 -> 5.2-7.2ms) against the ADR's re-measured ~4.0 -> ~6.6ms;
  `note_unactionable_misplacement` was being handed `relocatable` for a parameter named
  `cooling` (correct only by coincidence of the guard, while two operator alert rows key off
  the label it picks) and now takes both facts with stated precedence; and
  `note_session_ownership`'s doc claimed self-healing for the lease-less case the pass
  deliberately excludes.

- **2026-07-17** — **The recorded gap is closed.** The whole-cluster two-binary
  rolling-upgrade test that P4/P5 recorded as awaiting an out-of-process harness
  now exists: ADR 0044 P3's `cluster_upgrade::a_rolling_upgrade_and_rollback_lose_no_acked_fact`
  rolls a live 3-node cluster from a pinned baseline binary to HEAD one node at
  a time and back, under acked load, dirs reopened across versions — the resize
  replace motion ridden with two real binaries.
- **2026-07-14** — **Pre-release proto collapse.** The project is pre-release:
  breaking changes are free and no protocol/schema version coexistence is
  supported until after 1.0.0 (rolling upgrades are a post-release capability).
  The version ceremony P1/P3 had added is folded away accordingly: the
  `ReplicaRead2`/`ReplicaReadReply2` side-by-side variants are gone —
  `ReplicaReadReply` itself now carries the completeness verdict — and the
  per-link proto gating (request-version-selects-reply-shape, the ≥4/≥5 guards
  on catch-up frames, the hub's pre-proto-3 fire-and-forget fallbacks, the
  negotiated version threaded through `PeerConnected`) is deleted, with
  `PROTO_MIN` raised to the ceiling (5) — the same floor-with-ceiling motion as
  the ADR 0042 T7 reshape, legal exactly because no release exists. The ADR 0038
  fail-closed `Hello` range negotiation itself stays (a mismatched build still
  gets a clean refusal at handshake), as does the schema fail-closed gate; what
  is deferred until post-1.0.0 is *serving* multiple versions concurrently. The
  earlier changelog/evidence entries below describe the versioned shapes as they
  were built and are left as history.
  The README gains the "Resizing the cluster" operator guide (grow = one paragraph:
  seeds + watch `/readyz`; the two-node 2-of-2 truth with the 1→3 recommendation;
  shrink = the SIGUSR1 decommission with its observable drain; replace = grow then
  decommission — the motion rolling upgrades ride), and the interim "durable resize
  unsupported" warning is gone. The capability now stands end to end: catch-up
  replicas behind a durable watermark (P1), eager migration on every ring change
  (P2), the verified decommission drain (P3), and a resize-literate stress harness
  (P4) that already paid for itself by finding and fixing the fast-restart void ack
  (exhibit ②). Remaining known gap, recorded in P4's evidence: a whole-cluster
  two-binary rolling-upgrade test awaits an out-of-process harness.
- **2026-07-13** — **0043-P4 done: resize test vocabulary — and it caught a real one.**
  The seeded stress schedules gain `join` and `decommission` steps (a decommission that
  cannot converge under the schedule's faults ABORTS and the node stays — the operator
  semantics), and the dedicated path tests land: 3→5 with live zone labels then losing
  two originals at once, 5→3 via two sequential drains, and the rolling host
  replacement the ADR 0039 binary upgrade rides. Mixed-proto catch-up honesty is pinned
  at the wire (per-link version gates); the whole-cluster two-binary upgrade test needs
  an out-of-process harness and is recorded as the remaining gap. On their FIRST sweep
  the re-mixed schedules found **exhibit ②** — the fast-restart void ack (kill+restart
  quicker than SWIM death confirmation: the fresh hub's empty interest snapshot erased
  peers' knowledge, its momentary single-member ring switched the `clustered()` gates
  off, and the settle window expired on time while its scans still skipped unreadable
  sessions) — fixed with observable-state honesty end to end: scan completeness
  (`SessionScan.complete`) gates interest gossip, zero-match forwards refuse while
  `routing_unsettled()`, cluster-configuredness is a startup fact, and the settle
  window closes on state, never on tick count.
- **2026-07-13** — **0043-P3 done: decommission.** `SIGUSR1` begins a drain (signal
  chosen over an admin endpoint: the health listener stays read-only, and signals match
  the SIGHUP-reload / SIGTERM-shutdown grammar). Readiness fails at once; the new
  `decommission::Drain` then verifies every key this node stores against every member
  of the key's **post-departure** replica set (`group_replica_set_without` — HRW
  monotonicity keeps surviving members in place) with a content-sufficiency verdict
  (successor watermark ≥ ours; every held entry acked-away-or-present), and asks each
  group's owner for a **targeted re-commit** of whatever falls short — new proto-5
  `ReplicaCatchUpTo` frame served by `ClusterLog::recommit_key_to` (owner-epoch
  re-tagged entries + truncation floor to one node, idempotent, no quorum gate). The
  drain mutates nothing locally, so a crash (or SIGTERM escalation) mid-drain is just a
  crash. On completion the ordinary ADR 0019 leave runs: SWIM departure → placement
  eviction → ADR 0021 voter demotion → P2's eager migration on the survivors. `/readyz`
  reports `decommission{pending,rounds,complete}` throughout. Proven on a four-node
  cluster where removing the node adds a **newcomer** to every replica set it was in
  (the hand-off is load-bearing) and the leaver owned the durable subscriber: drain
  converges, node dies, everything acked survives.
- **2026-07-13** — **0043-P2 done: eager migration on ring change.** The hub watches the
  placement member set on its 1s sweep tick; any change re-arms the takeover window, so
  the ADR 0042 T9 machinery — eager inherited-session scans, settle-held gated acks,
  mesh-whole release rules — now covers **growth**, not only death. The new owner
  materializes moved sessions (recovery anchored by P1's caught-up old owner) and
  advertises their filters without waiting for first touch; the old owner gets the
  missing mirror, `release_moved_sessions`: after every scan it drops local routing for
  offline sessions whose group moved away and re-gossips the shrunken interest —
  before this, its stale routing answered every forward with `NotOwner`, which
  **withheld post-grow publish acks indefinitely** (found by the new integration test:
  it fails against the pre-P2 hub and passes in ~12s with it). Durable-cluster-gated;
  local-only storage keeps routing since its data cannot follow ownership.

- **2026-07-13** — **0043-P1 done: catch-up replicas.** A recovery read now carries a
  **completeness verdict** — gap-free stored offsets above the truncation low-water
  *and* a durable per-group **caught-up stamp** (`replica_caught_up` table: the replica
  set this node last completed catch-up against; survives restart, so a crash
  mid-catch-up resumes rather than fakes). `recover_key` requires a quorum **plus at
  least one complete anchor**, which closes both faces of the hollow-replica hazard: a
  joiner holding new appends above a hole it never received (gap ⇒ incomplete), and an
  **empty** joiner claiming a key has no history (no stamp ⇒ incomplete — an unstamped
  owner cannot fabricate an empty log either). The back-fill itself: a catch-up sweep in
  the lease driver (armed at boot and on every membership change) discovers keys
  per-peer (`ReplicaKeys`), asks each hollow key's owner to `recommit_key` it
  (`ReplicaCatchUp`, proto 4) — the existing fenced idempotent re-commit — plus a
  truncation-floor fan so drained prefixes read as acked-away, not as gaps; groups
  re-stamp when every discovered key is gap-free and every other set member was heard
  (pure shrinks of a known cohort re-stamp immediately — custody unchanged). Cached
  group logs now rebuild on **replica-set changes** too, so appends and re-commits fan
  out to joiners. Wire: proto 3→4, `ReplicaRead2`/`ReplicaReadReply2`/`ReplicaCatchUp`
  appended (pre-proto-4 peers keep the legacy read; their replies are conservatively
  incomplete). Proven end to end by the harness: grow 1→3 under acked facts, wait for
  the joiners' watermarks, kill the founder — session resumes present, every acked
  payload replays, the acked retained value serves (zero waivers).
- **2026-07-13** — ADR 0043 drafted with delivery plan P1–P5, from the cluster-resize
  inventory (consensus half ready, data half missing). Motivated by the capability
  plan's "adding a node adds throughput" claim and the laptop→server upgrade sell.
