---
adr: "0051"
title: "Evaluation readiness: an assessable, comparable, migratable first release"
adr_status: Proposed
tasks:
  - id: 0051-T1
    title: README truthing & restructure — differentiators and a TOC up top, an architecture sketch, a Bridge section + the missing mqtt-bridge crate-table row, stale facts fixed (44→55 ADRs) with a CI guard on derivable facts, and the MSRV / supported-platforms / pre-1.0-stability statements
    status: done
    date: 2026-08-07
    evidence: "Delivered across PRs #110, #111 and this one. TRUTHING (#110): 'linear horizontal scalability' softened to a design claim (ADR 0048 T3 is planned and forbids measuring the curve on one host); 'the main thing not yet done is cutting a tagged release' replaced by a pointer to a new Limitations section; stale bincode peer-frame numbers corrected (~280/~420 ns -> ~439/~357 ns, the postcard re-baseline — the old figures under-reported encode cost by ~35% IN OUR FAVOUR); 44 ADRs -> 55; mqtt-bridge and mqttd-operator added to the crate table. MSRV/PLATFORMS/STABILITY (#111): stated only after being VERIFIED — 1.88 builds the workspace minus mqttd-operator, 1.89 builds all of it — with a nightly msrv job that asserts the rustc version actually in use, because rust-toolchain.toml pins 1.97.0 and overrides an installed default (a job that merely installed 1.88 would have compiled with 1.97 and verified nothing). Released platforms and the pre-1.0 stability rule (ADR 0039 applies FROM 1.0.0) stated too. RESTRUCTURE (this PR): a jump list at the top; a 'What's different about it' block arguing four differentiators, each traceable to a row of the dated COMPARISON matrix; an ASCII 'How it fits together' sketch showing listeners -> hub actor -> gossip/peer mesh -> durable plane, with the bridge called out as the separate-trust-domain tool; and a Bridging section for mqtt-bridge — a crate with 11/11 delivered tasks and its OWN BINARY that the README had never once mentioned (deny-by-default direction-enforced rules, two-level loop prevention, bounded store-and-forward spool, shared-subscription HA without duplicates). CI GUARD (scripts/check-readme-facts.py, wired into the delivery-dashboard job): checks the ADR count, the crate table, and — added here — every intra-document #anchor, since a link to a renamed heading fails silently and the file now leans on them. Verified falsifiable three ways: reintroducing '44 ADRs' and deleting the mqtt-bridge row both fail with named errors, and renaming '## Limitations' fails with 'link to #limitations matches no heading'. 36 anchors checked."
  - id: 0051-T2
    title: Secured quickstart — generate certs → TLS + mTLS + ACL → foreign client connects, as a copy-paste block beside the plaintext one, wired into the quickstart-as-test CI job so the secure path cannot rot
    status: done
    date: 2026-08-07
    evidence: "Every getting-started command in the README ran with a plaintext listener and anonymous clients — for a security-first broker, the only copy-pasteable path was the insecure one, and the secure path existed solely as a config table the reader had to assemble. New README section 'Single node, secured (TLS 1.3 + mTLS + ACL)': a local CA, a server leaf, and TWO device certs, with one deny-by-default rule topics=[\"sensors/%i/#\"] — %i substitutes the authenticated identity (the cert CN), so a single rule confines each device to its own subtree. scripts/quickstart-smoke.sh (already wired into ci.yml) runs the block, adding six assertions: the config logs NO INSECURE warning (every opt-out of a secure default is loudly logged, so their absence is a checkable signal and a plaintext bind creeping in would otherwise pass unnoticed); an mTLS round-trip inside the grant; a client with no certificate refused at the TLS handshake; sensor-2 denied sensor-1's subtree and receiving NOTHING; and the same cert working normally in its own subtree, so the isolation assertion proves isolation rather than a broken client. Isolation check verified falsifiable by weakening the rule to sensors/# — fails with 'sensor-2 received sensor-1's traffic: [private]'. TWO TRAPS FOUND AND DOCUMENTED: (1) mosquitto_sub exits 0 when every filter is DENIED and 27 on a clean timeout — the first version of this test asserted on exit status and passed a broker that was refusing correctly; it now judges by delivery, and the README warns the reader off the same trap. (2) A denied PUBLISH is dropped but still acknowledged (3.1.1 has no negative PUBACK; withholding it would leave a conforming publisher retrying forever), so a publisher cannot tell it was refused — the denial is recorded in the audit log as acl.deny.publish, and the README now says so, because someone relying on the ACL as a security boundary would otherwise look in the wrong place. Denied SUBSCRIPTIONS are refused visibly with a per-filter reason code. Verified in CI on Linux, not only locally: run 31212672552 shows all six ok lines and QUICKSTART OK. openssl added to the script's tool preflight. PR #112."
  - id: 0051-T3
    title: Community surface — CONTRIBUTING.md (human-facing), CODE_OF_CONDUCT.md, issue/PR templates, changelog policy (GitHub Releases canonical, CHANGELOG.md pointer)
    status: done
    date: 2026-08-07
    evidence: "None of these existed. CONTRIBUTING.md is written for a person rather than as a formality: the build/test gates stated as gates (CI runs with RUSTFLAGS=-D warnings, so a warning is a failed build — the most common reason a first PR goes red), the local check commands worth running, an explicit note that the heavy assurance tiers run NIGHTLY so a contributor is not expected to run them, the MSRV (1.88; 1.89 for mqttd-operator) with rust-toolchain.toml explained as a reproducibility anchor rather than a requirement on the reader, and the two conventions that actually trip people here: ADR-records-why vs delivery-doc-records-progress, and 'a task title is a claim' — when scope changes you deliver the missing clause or narrow the title and open a task for the remainder, never delete the clause quietly. CODE_OF_CONDUCT.md is Contributor Covenant 2.1 with enforcement routed through GitHub private vulnerability reporting (the one private channel this repo already has). CHANGELOG.md is deliberately a POINTER, not a hand-maintained list: GitHub Releases is canonical, and this repo already retired one hand-maintained catalogue that drifted three ADRs behind — a second source of truth is always the one that goes stale. It carries a where-to-look table (releases / delivery dashboard / ADRs / Limitations / upgrade rules) and the pre-1.0 versioning statement. .github/ISSUE_TEMPLATE/: bug_report.yml (repro, logs with RUST_LOG, version, and a durable-sessions dropdown because several behaviours differ between durable and not; asks for the quote if a DOC created the wrong expectation — a misleading doc is treated as a real defect here), feature_request.yml (problem before solution, alternatives — which tend to become the ADR record — and a security-boundary dropdown), config.yml routing vulnerabilities to private reporting and 'is it built yet' to the dashboard. .github/pull_request_template.md asks why-not-what, how it was verified (a test that FAILS against the old behaviour is worth more than one that passes against the new), and includes the no-claim-broader-than-what-was-built check. README gained a Contributing section linking all of it. All template YAML parses; every relative link in the four new/edited docs resolves."
  - id: 0051-T4
    title: Cut v0.9.0 — flip ADR 0045 to Accepted, maintainer pushes the signed tag per RELEASING.md, verify the pipeline's artifacts end to end (first real signatures + SBOM complete 0045-T3/T5)
    status: done
    date: 2026-08-07
    evidence: "v0.9.0 is a real PUBLISHED release — not draft, not prerelease, 15 signed assets (both musl binaries + .sig/.pem, checksums, sbom-0.9.0.cdx.json + signatures), published 2026-08-07 (gh release view v0.9.0). ADR 0045 is now Accepted (its pipeline is proven, not aspirational), flipped in this same change — the half of this task that had been missed. Cargo.toml reads 0.9.0."
    notes: "The record showed this planned while the release was live for days — exactly the drift the 0051 gate exists to catch, found by a human reading the doc 2026-08-11."
  - id: 0051-T5
    title: docs/COMPARISON.md + condensed README matrix — Mosquitto / EMQX / NanoMQ / VerneMQ; every cell matched / exceeded / missing-by-design (with the deciding ADR) / missing-for-now (with the tracking task); versions pinned, claims dated, losses as prominent as wins
    status: done
    date: 2026-08-03
    evidence: "docs/COMPARISON.md (dated 2026-08-03; pinned: mosquitto 2.0.22/2.1.2, EMQX 6.2.2, NanoMQ 0.25.5, VerneMQ 2.1.1, mqttd 0.9.0-rc) + README 'How it compares' condensed table. Competitor cells from docs/changelogs/source researched 2026-07-29→08-03; mqttd cells verified against source (conn.rs: ResponseTopic/CorrelationData forwarded, Receive Maximum 0x93 enforced; codec-only: subscription identifiers NOT delivered, no ServerKeepAlive, no AssignedClientIdentifier — all printed as losses). Unverifiable competitor cells marked n/v, never guessed; by-design absences cite their ADRs (dashboard/HTTP-API per signal-driven-ops posture). Losses printed: footprint, maturity (no released version, no users — stated verbatim), subscription ids, assigned client id, voter-set-bounded durable capacity."
  - id: 0051-T6
    title: Migration from Mosquitto — scripts/migrate/from-mosquitto.py (mosquitto.conf → ADR 0046 TOML, acl_file → ACL TOML, bridge blocks → mqtt-bridge rules) + guide + fixture tests; loud unmapped report, secrets never transformed, output must pass --check-config
    status: done
    date: 2026-08-07
    evidence: "scripts/migrate/from-mosquitto.py converts config + ACL, emitting TODO(migrate) for anything without an exact equivalent (never a silent drop). Reimplemented byte-identically in Rust as `mqttui migrate mosquitto` (ADR 0056 T10) with a differential test. test-from-mosquitto.sh boots the REAL broker on the converted config (CI-gated). Two real bugs — invalid multi-listener TOML and a silent mTLS mandate — were found by the 2026-08-11 review panel RUNNING it and fixed (#162), with the differential test hardened to parse-check the agreed output."
  - id: 0051-T7
    title: Migration from EMQX — scripts/migrate/from-emqx.py (listeners, TLS, authn/authz sources, bridges → common-subset TOML) + guide + fixture tests; same three converter rules
    status: done
    date: 2026-08-14
    evidence: "scripts/migrate/from-emqx.py: a tolerant hand-rolled HOCON reader (nested blocks, dotted keys, arrays, 1MB/2h/15s/1000-per-second unit normalisation, ${VAR} passed through UNRESOLVED with a NOTE, stdlib only — no new dependency) plus a tolerant Erlang-term tokeniser for acl.conf. MAPS: node.name->[node] id (local part) and data_dir; listeners.{tcp,ssl,ws,wss,quic}.*.bind -> the one bind mqttd has per protocol (extras become inline TODOs inside [listeners], the #162 collapse precedent); ssl_options certfile/keyfile -> [tls]; mqtt.max_packet_size/max_topic_alias/max_mqueue_len/max_subscriptions -> [limits] (max_inflight is DELIBERATELY NOT mapped — see the 2026-08-14 remediation in T19); peer_cert_as_username=cn -> mtls_identity_source; per-listener max_connections and messages_rate -> [limits] max_connections/max_publish_rate; retainer max_retained_messages; authorization.no_match -> the ACL default; authn [password_based/http] url -> [security.http_auth]; jwt issuer/audience; and bridges.mqtt/connectors.mqtt ingress+egress -> mqtt-bridge in/out rules with prefix remaps (--out-bridge), which is the 'bridges -> common-subset TOML' half of this task's title, delivered rather than narrowed. DELIBERATE TODOs, each naming what the operator must decide: rule_engine, connectors/actions/sources, gateways, exhook, plugins, dashboard, api_key, prometheus, zones, license, psk, sys_topics, sysmon, alarm, flapping_detect, force_gc/force_shutdown, delayed, log, limiter, banned, durable_sessions, crl_cache + ~40 named per-key reasons. POSTURE GATE (ADR 0051 §3's new fourth rule): cacertfile becomes client_ca ONLY with verify=verify_peer AND fail_if_no_peer_cert=true, otherwise it is emitted COMMENTED OUT with the mTLS-mandate TODO. ACL: ${username}/${clientid}/${cert_common_name} -> %i/%c; the first-match-wins vs deny-wins difference gets a capitalised TODO at the TOP of the file whenever both effects are present (the one class where a conversion can be MORE permissive); {ipaddr}/{ipaddrs}/{re,...}/{client_attr}/{zone}/{listener}/{'and'}/{'or'}/{eq,...}/{security_profile} and clientid-conditions each become a TODO and the rule is NOT emitted; qos/retain action qualifiers are reported as broadening. Exit codes match from-mosquitto.py (0 with TODOs, 1 unreadable input); --help carries the version provenance on the docstring's first line plus an epilog. test-from-emqx.sh: 12 assertion groups, CI-gated, including the two the Mosquitto harness lacked — `mqttd --check-config` on every converted config (ADR 0051 §3 rule 3) and expected-TODO-marker greps — plus mqtt-bridge's own parse+validate on the generated bridge config (the real binary's verdict), a secrets-never-copied scan (the fixture's inline JWT secret and bridge password must appear in NO output), the adversarial fixture, an empty file, a missing file, and a real broker BOOT on the converted ACL. MUTATION-PROVEN (superseded and widened by T19's 21-row mutation table; the original two were deleting DIRECT['mqtt.max_inflight'] and removing the verify_peer+fail_if_no_peer_cert gate, the first of which is void because that mapping was WITHDRAWN as a direction flip). FIXTURES cite repository, TAG, path and fetch date. NOTE: emqx-6.2.2.conf's header originally claimed VERBATIM and was WRONG — nine values differ from the vendor's; it is relabelled COMPOSED with a per-value deviation table, and a genuinely verbatim emqx-6.2.2-vendor-verbatim.conf was added beside it (T19). emqx-acl-6.2.2.conf is apps/emqx_auth/etc/acl.conf @ 6.2.2 VERBATIM (a perfect adversarial case — all four shipped rules land on a different gap); emqx-acl-documented-examples.conf is that file's own documentation examples; emqx-adversarial.conf is hostile input written here (unknown keys, invented sections, an unterminated block, three tcp listeners, cert-optional TLS, no_match=allow). HONESTY LIMIT stated in the fixtures, --help and docs/MIGRATION.md: no live EMQX was run, dashboard-managed deployments keep authn/authz in data/configs/cluster.hocon (the converter accepts either file and TODOs the silence), built-in-database users and ACL rules live in the data directory where the converter CANNOT see them, and 5.x parses because the parser is tolerant, not because it was tested."
  - id: 0051-T8
    title: Migration from NanoMQ — scripts/migrate/from-nanomq.py (listeners, TLS, auth, bridge config → common-subset TOML) + guide + fixture tests; same three converter rules
    status: planned
  - id: 0051-T13
    title: "Issue #259 — the panel method learns from its own false finding: metadata-verification rules, the 2026-08-13 run recorded, the durability trace becomes the standing baseline, and the report is committed"
    status: done
    date: 2026-08-14
    evidence: "REVIEW-PANEL.md amended in four places. SETUP RULE (Running it): fetch tags in the review clone (or hand reviewers the GitHub release list) before a run, plus the standing reviewer instruction that release claims verify against the GitHub release list and never local git tag — three of five reviewers in the 2026-08-13 run reported 'the release does not verify; the only tag is v0.9.0-rc' because the review container's clone fetched branch heads only, and reviewer 2 built verdict language on the artifact. METADATA RULE (Reading the results): the existing verify-correctness-claims-yourself paragraph extended to repository metadata — any finding resting on tags, remotes, release artifacts, or CI history describes the reviewer's SANDBOX until the session running the panel re-verifies it against the hosting platform. RUNS SECTION (new): both runs recorded — 2026-08-09 with its outputs and reviewer 5's starting verdict, 2026-08-13 (main @ bf2084f) with the verdict table's shape (migrators not-yet on 6-18 month windows over scale/1.0/integration rather than correctness; reviewer 4 at 7.5/10; reviewer 5 moved 'would not choose' -> 'cautious yes, single secured node'), the false-finding note, and the observation that independent agreement measures shared environment as well as shared truth. STANDING BASELINE: reviewer 4's first end-to-end durability trace (conn.rs ack-withholding -> hub.rs append-before-wire -> redb Durability::Immediate -> self-ack-counts-only-when-durable in cluster_log.rs) recorded so the next run is asked to FALSIFY the traced chain rather than re-derive it. REPORT COMMITTED: docs/review-panels/2026-08-13.md — the distilled record (verdicts, agreement ranking, filed/not-filed, the false finding), citing tracking issue #261 as the per-finding citation record. Done criterion updated with the verdict movement."
    notes: "The report is a distillation, and says so: the per-finding citations live in the filed issues (#238-#260 as grouped in #261), which is where an auditor can check each attack against the tree it was made on. Nothing in the committed report goes beyond what #259/#261 record — the reviewer reports themselves were not retained as artifacts, and inventing detail for them would be exactly the class of unverifiable claim the panel exists to catch."
  - id: 0051-T9
    title: NanoMQ and VerneMQ join the bench harness (amends ADR 0048's competitor set; VerneMQ under the disclosed-posture fairness terms in 0048's 2026-08-03 amendment) and the first comparative results are published to docs/benchmarks/ under 0048-T4's honesty rules
    status: done
    date: 2026-08-10
    evidence: "bench/run.sh and bench/docker-compose.yml carry NanoMQ and VerneMQ lanes; the four-broker comparison matrix (mqttd/Mosquitto/EMQX/NanoMQ/VerneMQ) is in docs/COMPARISON.md, verified against each vendor's docs with unverified cells printing n/v. bench/results/results.md records a dev-grade smoke across all lanes. [2026-08-14, issue #244: that path is UNTRACKED — bench/results/ is gitignored — so this citation was never checkable by a reader. Left in place rather than rewritten, per this file's convention that evidence prose records what was true when the task closed; the published, tracked durable-path measurement is docs/benchmarks/DURABLE-PATH.md (0048-T5). check-readme-facts.py's untracked-citation guard deliberately excludes docs/delivery/ for the same reason, so this class is corrected by dated forward pointers like this one, not by the guard.]"
  - id: 0051-T10
    title: The bridge made assessable — a demo/ second security zone (Mosquitto upstream + mqtt-bridge with directional rules), a walkthrough doc, and the Grafana screenshot into the README
    status: planned
  - id: 0051-T12
    title: "Issue #253 drift sweep: the five factually-wrong doc claims from the 2026-08-13 panel corrected, and two new mechanical guards so the classes cannot return"
    status: done
    date: 2026-08-14
    evidence: "All five corrected, dates cross-checked against gh release list (v0.9.0 = 2026-08-07T23:44Z, v0.9.0-rc = 2026-07-22T08:24Z): (1) COMPARISON's header citation of an untracked bench/results file now states plainly that the benchmark results are unpublished and points at issue #244; (2) COMPARISON's 2026-08-11 changelog bullet claimed 'v0.9.0 shipped 2026-07-22' — the RC date — corrected in place to 2026-08-07 with the RC noted; (3) README's 'dated 2026-08-11' for COMPARISON now matches the file's own 2026-08-13 header; (4) the compose resources comment and deploy/compose/README's memory bullet said 'no total-memory knob yet' — MQTTD_MEMORY_MAX_BYTES exists; both now say what SIZING.md says: a brownout watermark, not a ceiling, so the container limit is still the hard bound (the comment's conclusion was right; its premise contradicted the README); (5) mqtt-auth's crate doc listed LDAP in a way readable as a shipped backend — it now names the shipped backends, states the HTTP hook as the supported path to LDAP/DB/OAuth2 stores, and says explicitly that a custom trait impl is operator code, not a shipped feature. GUARDS (check-readme-facts.py, already in the delivery-dashboard CI job): every COMPARISON citation — backticked file paths and relative markdown link targets, resolved against docs/ then the repo root via git ls-files — must be a TRACKED file (or a directory with tracked contents), and README's stated COMPARISON date must equal the file's Dated header. Both mutation-proven: re-citing the original dangling bench path fails naming it; regressing the README date to 2026-08-11 fails quoting both dates. 8 citations checked at present. COMPARISON got its convention's dated changelog line."
    notes: "Scoped to COMPARISON for the tracked-citation guard on purpose: docs legitimately cite generated artifacts elsewhere (secrets/ca.pem, dist/mqttd, target paths), so a repo-wide guard would drown in allowlist; COMPARISON is the document whose stated honesty rules make an uncheckable citation a defect. Item 1's underlying benchmark gap stays tracked by #244 — this task fixes the claim, not the evidence."
  - id: 0051-T11
    title: The 1.0.0 freeze — after the bake window, run the ADR 0038 wire/schema review consciously, run the 0039-T3 skew smoke against two real tags, then the maintainer cuts the freeze tag
    status: blocked
    notes: "Needs v0.9.0 shipped (T4), a bake window survived, and a second tag (0.9.x/0.10) to make the 0039-T3 adjacent-skew smoke real — impossible before two releases exist by definition."
  - id: 0051-T14
    title: README reordered for adopters (issue #255) — the two-minute demo and the MQTT primer moved above the status/consensus paragraph, mqttui demoted out of "Start here", and every acronym in the first 400 lines defined or linked at first use (SWIM expanded; HRW, CP/CAP, a Security-and-PKI section, redb and distroless added to the glossary)
    status: done
    date: 2026-08-14
    evidence: "Reviewer 5's finding was that the README's own ordering defeated its best feature: the two genuinely good onboarding assets (the two-minute demo, the five-concept primer) sat BELOW the consensus jargon that filters a newcomer out, and 'Start here' routed to cargo-installing a TUI rather than to the broker. RESTRUCTURE: '## Start here' now IS the demo — the two-minute docker run (verbatim, unchanged commands) plus the primer as subsections, inserted directly after the tagline; the primer gained a one-sentence expansion of MQTT itself (Message Queuing Telemetry Transport, topics, broker). The status paragraph is kept word-for-word (it earned the reviewer's respect) under a new '## Where it stands' heading AFTER the demo and the jump list; the mqttui block kept its position but is retitled 'The runnable map: mqttui' and no longer claims to be the front door. A top-to-bottom reader now reaches `docker run` at line ~18 and first meets 'quorum' at line ~95 — acceptance criterion checked mechanically with grep, not eyeballed. JARGON AT FIRST USE: SWIM expanded below the architecture sketch (its first appearance); HRW expanded inline at Session placement AND given a glossary entry (it was defined NOWHERE before, glossary included); the CP trade annotated in CAP terms with a glossary entry for CP/CAP; CRL expanded at its first (Security) use, OCSP and PSK expanded in Limitations; clientAuth EKU expanded in the Before-production checklist with a deep link to TROUBLESHOOTING's mTLS-rejection section (the only place it was explained); redb and distroless one-lined where they appear and glossaried; BSL, ECDHE+AEAD, HMAC glossed; lease/epoch/quorum and voter linked to the glossary at first use; SBOM linked to the supply-chain glossary section. GLOSSARY: SWIM acronym expanded; new HRW and CP/CAP entries; a new 'Security and PKI' section (TLS/mTLS, CA/CN, ACL, JWT/OIDC, CRL/OCSP, EKU/clientAuth, PSK, HMAC); distroless + redb under supply chain. Nothing tested-in-CI changed: the quickstart commands, the two-node cluster block and the secured quickstart are byte-identical (scripts/quickstart-smoke.sh untouched). check-readme-facts.py green — 60 ADRs, 12 crates, 27 scripts, 44 anchors (the jump list gained #where-it-stands; #start-here survives as the relocated section; the one inbound #start-here reference was repointed at #the-runnable-map-mqttui)."
  - id: 0051-T15
    title: A secured container invocation exists and cannot rot (issue #257) — the README's secured quickstart gains a hardened one-command `docker run` (mounted certs + ACL, TLS 1.3 + mTLS, durable volume), exercised end to end by scripts/image-smoke.sh in the nightly image lane
    status: done
    date: 2026-08-14
    evidence: "Issue #257 named two first-experience contradictions. (1) The two-minute demo had no volume — ALREADY FIXED by #240's change (c25686e): the block carries -v mqttd-data:/var/lib/mqttd + MQTTD_DATA_DIR and the paragraph beside it explains why the volume is what makes the demo honest; verified, not re-delivered. (2) No secured docker invocation existed anywhere — the Install docker run was plaintext+anonymous and the secured path was cargo-run only, leaving the TLS-env-to-volume-mount translation to the reader. NEW README SECTION 'Single node, secured, in a container': one hardened docker run (--read-only --cap-drop ALL --security-opt no-new-privileges) reusing the secured quickstart's own pki/ and acl.toml, mounted read-only, with MQTTD_DATA_DIR on a named volume — no plaintext bind, no anonymous, no ephemeral opt-in. The one non-obvious step is stated with its limits: chmod 0644 on the mounted key because the image runs as uid 65532, called out as acceptable for the THROWAWAY quickstart PKI only, with the real answer (compose/systemd/Helm mount secrets with owned permissions) named. SMOKE: scripts/image-smoke.sh (nightly image lane, ADR 0045 T4) gains step 5 running that invocation's exact shape — container stays up with mounted certs+ACL, an mTLS client round-trips inside its ACL grant (judged by delivery, retained so no race), a client with NO certificate is refused at the TLS handshake, and the configuration logs no INSECURE line (absence-as-signal, same rule as quickstart-smoke). VERIFIED LOCALLY against the PUBLISHED ghcr.io/mbilling/fss-mqtt-broker:latest image — all seven ok lines and IMAGE SMOKE OK, so the documented command works against the artifact a reader actually pulls, not only a tree build. Install's plaintext parenthetical now points at the container variant; the secured quickstart intro links it; mqttui's image-smoke task about/requires updated (docker+mosquitto+openssl). The two-minute path is untouched and still CI-tested; shellcheck clean; check-readme-facts green (two new intra-README anchors resolve)."
  - id: 0051-T17
    title: Migration from HiveMQ — scripts/migrate/from-hivemq.py (config.xml listeners/TLS/limits, File RBAC credentials.xml → ACL policy) + fixture tests; same converter rules, and every HiveMQ Enterprise construct reported rather than dropped
    status: done
    date: 2026-08-14
    evidence: "HiveMQ was NOT in ADR 0051 §3's converter list; the ADR is amended in this same change to add it (issue #250, two review-panel reviewers blocked on EMQX and HiveMQ respectively). scripts/migrate/from-hivemq.py parses config.xml with xml.etree.ElementTree (stdlib) and walks it depth-first, so an unknown ELEMENT is reported by its full element PATH — which is how HiveMQ Enterprise-only sections are handled honestly WITHOUT this converter ever having read their (closed-source) schema. MAPS: the four CE listener kinds -> plaintext_bind/tls_bind/ws_bind/wss_bind (one per protocol, extras inline TODOs); mqtt/packets/max-packet-size, receive-maximum/server-receive-maximum, topic-alias/max-per-client, queued-messages/max-queue-size -> [limits]; restrictions/max-connections with -1=unlimited -> unset + NOTE; queued-messages/strategy discard->reject-newest and discard-oldest->drop-oldest (getting that pair backwards would silently change WHICH message is lost, so the test asserts the direction); tls/client-authentication-mode REQUIRED -> client_ca. TWO FINDINGS THAT SHAPED THE OUTPUT, both verified in-tree rather than assumed: (1) HiveMQ TLS material is a JKS keystore WITH PASSWORDS and mqttd wants PEM paths — no conversion exists, so a keytool+openssl extraction recipe is emitted and no key material is ever touched; the recipe's openssl half was RUN (a locally minted PKCS#12 -> PEM pair, and the REAL broker booted a TLS listener on it, bag-attribute preamble included) while its two keytool steps were NOT (no Java runtime on this machine, no HiveMQ-generated keystore), which the output, the docs and this record all state. (2) file-RBAC ROLES CANNOT become mqttd `groups`: groups are populated only by an OIDC token's groups_claim or the HTTP hook's response body, and the password-file authenticator hard-codes an empty group list (crates/mqtt-auth/src/password.rs) — so roles are FLATTENED onto per-user `identities` (11 rules from 2 users x 2 roles in the fixture), correct but larger, with the OIDC/HTTP alternative named in a TODO. POSTURE GATE: client-authentication-mode OPTIONAL emits client_ca COMMENTED OUT plus a TODO, because mqttd has no cert-optional mode and mandating mTLS silently would lock out every client without a certificate. CORRECTED 2026-08-14 (T19): the gate read only tls_listeners[0], so a REQUIRED mandate on any later TLS listener was dropped silently; it is now decided across ALL TLS listeners, with a mixed posture emitting the candidate commented plus a TODO naming which listeners required certificates. DELIBERATE TODOs: <cluster>, <control-center>, <license>, <extensions> (no SDK, no ABI), <ese>, <bridge> (-> mqtt-bridge, pointed at the dual-run playbook), <overload-protection>, mqtt-sn, plus every permission qualifier file-RBAC has and mqttd does not (<qos>, <retain>, <shared-subscription>, <shared-group>) reported as making the emitted rule BROADER than the original, and the CE truth that there is no built-in auth at all so a --credentials-less config describes an ANONYMOUS broker. Passwords never read, copied or transformed — a per-username `mqttd --hash-password` re-enrolment list instead. VERIFIED IN PASSING and used in the output: mqttd accepts a WebSocket upgrade on ANY path (crates/mqtt-net/src/ws.rs checks the `mqtt` subprotocol, not the URI), so HiveMQ's <path>/mqtt</path> needs no translation while a <subprotocol>mqttv3.1</subprotocol> DOES get a TODO. test-from-hivemq.sh: 10 assertion groups, CI-gated, including --check-config on every converted config, a real broker BOOT on the translated ACL, a secrets scan over the keystore passwords AND both PBKDF2 hashes, the adversarial fixture, an EMPTY <hivemq/> document, MALFORMED XML (reported, never raised), and a missing file exiting 1. MUTATION-PROVEN three times: deleting the queued-messages strategy mapping fails with 'strategy discard did not become queue_overflow=reject-newest'; widening the client-auth gate to treat OPTIONAL as REQUIRED fails with 'OPTIONAL was mapped to client_ca — that silently mandates mTLS'; leaking a user's password hash into the ACL output fails with \"the secret 'FY12nwpUEbBK9EKQ' was copied into the converter's output\". All restored byte-identically. FIXTURES cite repository, TAG, path and fetch date. AMENDED 2026-08-14 (T19): hivemq-2026.5-config.xml is a MERGE of four vendor sample files plus <security> and <persistence> written from config.xsd, so its header no longer says 'every ELEMENT and VALUE as the vendor ships it' — and its <security> children were reshaped to the XSD's real <enabled> form, which the flat form they had is not. Two genuinely verbatim vendor files were added beside it (hivemq-2026.5-default-config.xml, hivemq-2026.5-tls-client-auth.xml). hivemq-credentials-4.6.16.xml from hivemq/hivemq-file-rbac-extension @ 4.6.16 README.adoc VERBATIM plus two clearly-marked added permissions that exercise the qos and shared-subscription qualifier gaps; hivemq-adversarial.xml is hostile input written here. HONESTY LIMITS stated everywhere they matter: no live HiveMQ was run, CE or Enterprise; Enterprise's config schema is not open source so Enterprise elements are recognised BY NAME and reported, never translated from a schema this tool read; and HiveMQ CE is calendar-versioned (2023.x..2026.5) while 'HiveMQ 4.x' now names the Enterprise line — a versioning correction carried into the docs."
    notes: "No Rust twin. ADR 0056 Decision D scoped `mqttui migrate` to Mosquitto only, and this task does not widen it — so a user with no python3 cannot run the EMQX or HiveMQ converters. docs/MIGRATION.md states that plainly rather than implying parity."
  - id: 0051-T18
    title: The dual-run cutover playbook (docs/MIGRATION.md) — written against what mqtt-bridge actually supports, with the bridge step RUN against a real third-party broker and every untested step marked
    status: done
    date: 2026-08-14
    evidence: "Issue #250's other half: a config converter leaves the operator with the harder problem, because mqttd cannot import another broker's SESSION state. docs/MIGRATION.md is the new home (docs/ had no migration document at all; ADR 0051 §3's promised per-target 'guide' had never been written, and from-mosquitto.py's docstring was the only one) and it owns both halves — the per-broker mapping/non-mapping tables for all three converters, then an dual-run playbook of seven numbered steps (0 through 6) plus a rollback. WRITTEN AGAINST THE BRIDGE AS IT IS, from crates/mqtt-bridge/src/{config,client,forward,engine}.rs, not a generic recipe: the cutover shape is a `both` rule with NO remap over the shared namespace, because remap on `both` is REFUSED (#192, it double-prefixes the reverse leg); a `$`-prefixed filter is REFUSED (#193, so a legacy $SYS tree cannot be mirrored); qos = 1 must be EXPLICIT because the default is 0 and would silently downgrade every migrated message (and QoS 2 is downgraded to 1 by the engine anyway, ADR 0025 §7); a QoS>=1 rule is REFUSED without [spool] dir (ADR 0060 T4) and that volume holds production payloads in the clear so it must be encrypted at rest; an mTLS half-identity is refused. Loop prevention is documented at its real strength: no_local = true on EVERY bridge subscription (client.rs) is a property of the SUBSCRIPTION that no publisher can forge, with the fss-bridge-hop-count user property (default 8) as the multi-broker-cycle backstop and its attacker-settability (#191) stated. THE FINDING THAT RESHAPED THE PLAYBOOK, contradicting the issue's own framing of 'no session-state or retained-state migration of any kind': RETAINED STATE DOES MIGRATE. Subscriptions set retain_as_published = true with retain_handling = 0 and the engine preserves the source retain bit, so on each connect the bridge receives the far side's retained SET and republishes it with RETAIN intact — an idempotent re-sync, not a fake-live storm. Only session state (offline queue, subscriptions, in-flight QoS 2, will message) is genuinely unmigratable, which is why the playbook makes a clean-start cohort the safe first wave. RUN, NOT ASSERTED: scripts/migrate/dual-run-smoke.sh executes the document's own bridge config shape against mqttd 0.9.0 (tree build) + mqtt-bridge + REAL Mosquitto 2.1.2 as the incumbent stand-in, and asserts SIX claims (widened from four by the 2026-08-14 verification pass, T19) — the inbound leg crosses at QoS 1, the outbound leg crosses, a retained value that pre-existed on the incumbent BEFORE the bridge started arrives retained on mqttd for a fresh subscriber, ONE publish inbound yields EXACTLY ONE delivery, ONE publish OUTBOUND yields exactly one delivery on the incumbent, and a retained value pruned with the bridge UP stays pruned on both sides. ATTRIBUTION CORRECTED 2026-08-14, because the first version of this record and of the doc had it backwards: the inbound no-amplification assertion is NOT a claim about the foreign broker. Measured with the bridge's own counters, fss_bridge_forwarded_total{direction="in"} moves by one while {direction="out"} does not — the message never travelled back toward Mosquitto, so MQTTD's own No Local cut the loop before Mosquitto's behaviour was ever consulted. The added OUTBOUND assertion (publish on mqttd, COUNT on the incumbent, inbound counter unmoved) is the one that puts Mosquitto 2.1.2's No Local on the hook, and it is now the only thing here supporting the 'the protocol property was checked against a third-party broker' claim. Both assertions check their counter pair, so the attribution is measured rather than asserted. Executed 2026-08-14 on Darwin 23.5.0, eleven ok lines, DUAL RUN SMOKE OK, re-run twice back to back by the 2026-08-14 docs verification pass with the same result (the doc quotes that run's output verbatim, ephemeral ports and all); exits 2 (not 1) when a binary or the mosquitto broker is missing so 'environment not ready' is never read as 'assertion failed'. Two first-draft failures were real and are recorded as design lessons in the script: a pre-existing retained message satisfied a `-C 1` subscription on `fleet/#` before the live publish arrived (the leg assertions are now scoped to `fleet/+/telemetry`), and `| head -1` under pipefail produced a spurious exit 141. EVERY DOCUMENTED COMMAND WAS RUN: the converters, `mqttd --check-config`, `mqttd --probe /readyz` (verified 0 against a live broker and 2 with none), `scripts/migrate/cert-audit.sh` (run on a throwaway cert dir), and the openssl half of the JKS recipe. ONE CLAIM WAS CORRECTED BY VERIFYING IT: the draft cited a Prometheus counter `mqttd_acl_denials_total` for step 5's ACL check; provoking a real denial showed the signal is the AUDIT LOG event kind `acl.deny.subscribe` and that no such counter exists. CORRECTED AGAIN 2026-08-14 (T19), because the replacement over-generalised: 'there is NO Prometheus counter for ACL denials' is false for one of the four denial kinds. Measured on a booted broker — a CONNECT refused for an unauthorized WILL topic raises `mqttd_connection_errors_total{reason="acl"}` (conn.rs `count_connection_error`), while `acl.deny.publish`, `acl.deny.subscribe` and `acl.deny.connect` raise nothing. Also measured: the audit log is `tracing` target `audit` at INFO, so `RUST_LOG=warn` silences the ACL events while still printing the INSECURE warnings (3 of them on a plaintext + anonymous node, 0 on a secured one) — the doc now says both. UNTESTED, STATED IN THE DOC: the bridge step was verified against Mosquitto 2.1.2 and NOT against EMQX or HiveMQ (both document the same No Local behaviour — EMQX's own docs call bridge_mode 'equivalent to No Local = 1', quoted in client.rs — and both speak v5 so the hop-count user property survives, but neither was run); no converter has ever been fed a live-broker config; EMQX 5.x parses because the parser is tolerant; the keytool half of the JKS recipe was not run; and steps 0/1/2/4/6 are procedure whose commands were each run in isolation but which no harness walks end to end as a cohort migration. Inbound links from README's migration paragraph and Limitations bullet, docs/OPERATIONS.md and docs/BRIDGE.md's 'Choosing' section."
  - id: 0051-T19
    title: Independent verification of the EMQX/HiveMQ converters and the cutover playbook (round 2 of issue #250) — sixteen findings remediated: the first-TLS-listener-only posture defect in BOTH converters, TOML escaping, fixture-provenance honesty, and every doc claim re-measured
    status: done
    date: 2026-08-14
    evidence: "A verification pass on 2026-08-14 read the two new converters and the playbook against the real code and the real vendor files, and found SIXTEEN defects — two blocking, both the SAME bug in both converters and both fail-OPEN. ALL SIXTEEN ARE FIXED. THE BLOCKING PAIR: build_tls() (HiveMQ) and convert_tls() (EMQX) read TLS settings from tls_listeners[0] only, so an mTLS MANDATE, a truststore, `versions`, `enable_crl_check`, `depth` and `ssl_options.password` on any TLS listener that was not first in document order vanished with no TODO(migrate) and no commented candidate — an operator migrating a broker that REQUIRED client certificates would have deployed one that did not, `mqttd --check-config` passing all the while. Both now decide the posture across EVERY TLS listener (mqttd has one [tls] table serving tls_bind, wss_bind AND quic_bind, verified at crates/mqttd/src/main.rs): unanimous REQUIRED maps client_ca; MIXED emits client_ca COMMENTED OUT plus a TODO naming which listeners demanded certificates and which did not; every non-first listener's ssl_options come out as TODOs prefixed with the listener they came from; every truststore gets a keytool line, not just the first. ADR 0051 §3 rule 4 gained the clause that makes this a rule rather than a bug fix, and a new rule 5 ('an equivalence must be same-direction') was written down for the max_inflight flip below. THE OTHER CODE FIXES: no string was TOML-ESCAPED anywhere, so an AD-style username (CORP\jdoe) or a Windows cert path produced a file tomllib REJECTS — one such user poisoned the whole ACL; fixed centrally (toml_escape/toml_str/toml_list/comment_safe in all three converters AND in the Rust port, no f'"{...}"' left) and tested on a hostile fixture the real broker BOOTS. `enable = false` was never read on an EMQX authenticator or authz source, so a deliberately-disabled authenticator was silently switched back ON; now reported and not translated, with a new TODO when an authorization block yields no policy file at all. convert_bridges() dropped every bridge key except server/url/credentials/ssl/topic/qos — including `remote.payload` templates and `retain` flags — under a count-only TODO that REASSURED the operator; every unconsumed key is now named, payload and retain get their own TODOs, and the section TODO no longer claims translation. An unreadable ACL source (EMQX ships the RELATIVE path etc/acl.conf, so this is the DEFAULT outcome outside the install root) was reported on stderr only while acl_file still pointed at a file that was never written; the gap is now in the config AND in an ACL file that IS written (default = deny, zero rules, 'NOTHING WAS TRANSLATED INTO THIS FILE'). mqtt.max_inflight was mapped to [limits] receive_maximum although they run in OPPOSITE directions (EMQX bounds broker->client; mqttd's is the inbound window granted to clients, crates/mqtt-config/src/lib.rs, and mqttd honours each v5 client's own Receive Maximum, conn.rs) — EMQX ships 32 and mqttd defaults 256, so every stock conversion silently cut the inbound window 8x; the mapping is WITHDRAWN in favour of a TODO stating the flip, in from-emqx.py, from-mosquitto.py and the Rust port. Smaller: the glob TODO claimed `?` was supported (mqtt-auth's glob_match implements `*` only); credentials.xml had weaker unknown-element handling than the config walk (an <enabled>false</enabled> user and a whole unknown policy section passed silently); per-listener `max_connections = infinity` left no trace while the identically-shaped DIRECT-key case emitted a NOTE. NON-VACUITY, 21 mutations, each: rule deleted -> test run -> named failure observed -> restored byte-identically (md5 checked). E1 delete DIRECT['mqtt.max_packet_size'] -> 'not normalised to bytes'. E2 map client_ca whenever a cacertfile exists -> 'a MIXED mTLS posture was mapped to client_ca'. E3 report ssl_options for the FIRST TLS listener only (the blocking regression) -> 'no TODO(migrate) for listeners.wss.browsers.ssl_options.enable_crl_check'. E4 stop escaping -> 'hostile.toml is not valid TOML'. E5 ignore enable = false -> 'a DISABLED http authenticator's URL was carried into [security.http_auth]'. E6 re-add max_inflight -> 'mapped to [limits] receive_maximum — opposite directions'. E7 stop reporting unconsumed bridge keys -> 'no TODO for egress.remote.payload'. E8 report an unreadable ACL on stderr only -> 'no TODO for THE AUTHORIZATION POLICY WAS NOT TRANSLATED'. H1 delete the queued-messages/strategy mapping -> "strategy 'discard' did not become queue_overflow=reject-newest". H2 treat a mixed REQUIRED/NONE posture as REQUIRED -> 'no TODO for TLS listeners DISAGREE about client certificates'. H3 read TLS from the first listener only (the blocking regression) -> same failure. H4 stop escaping -> 'hostile-acl.toml is not valid TOML'. H5 ignore unknown credentials elements -> 'no TODO for UNKNOWN top-level element <extra-policy-section>'. M1 stop escaping -> 'hostile.toml is not valid TOML'. M2 delete the %u -> %i substitution -> "mosquitto's %u was not translated to %i". M3 drop the data_dir fallback -> 'the broker REJECTED the config built from hostile strings'. R1/R2 the same two on the Rust port -> the differential test reports 'the two converters produced DIFFERENT configs/ACL policies'. A 19th was added by the docs pass, which found a fixture whose header did not match it: emqx-6.2.2.conf claimed 'two max_connections -> finite and UNEQUAL ... what exercises the smallest-wins collapse', but both values were 1000000, so the collapse path and its TODO were never exercised and MIGRATION.md's 'smallest, if they differ' row rested on nothing. The ssl listener is now 500000 against the tcp listener's 1000000, test-from-emqx.sh asserts both the collapsed value and the TODO naming the pair, and the mutation (min -> max in convert_listener_keys, i.e. silently RAISING an operator's cap) fails with 'two listener max_connections (1000000, 500000) did not collapse onto the SMALLEST'; converter restored byte-identically (md5 checked). Two more (20, 21) came out of the same pass on the HiveMQ side: the queued-messages pair had only ONE arm asserted (`discard`), so swapping the mapping table to {discard: drop-oldest, discard-oldest: reject-newest} — which silently changes WHICH message is lost — passed; the adversarial fixture's `discard-oldest` is now asserted and the swap fails with "strategy 'discard' did not become queue_overflow=reject-newest". And that NOTE cited the metric `publish_dropped{reason="queue-overflow"}`, which the broker does not export — the same unprefixed-series defect as the bridge metrics, inside CONVERTER OUTPUT this time; it now reads mqttd_publish_dropped_total{...}, the test asserts the real name, and reverting to the bare one fails with 'the drop-oldest NOTE cites a metric name the broker does not export'. Both restored byte-identically. A REAL DEFECT THE NEW GATE FOUND: test-from-mosquitto.sh documented its own weakness (it proved the output PARSED, never that it passed `mqttd --check-config`). Adding that check — now in all three converter tests, on every converted config — immediately failed: a mosquitto.conf with no persistence_location (the common case) produced a config the broker REJECTS ('EPHEMERAL durability REFUSED'). Fixed with the data_dir fallback the other two converters already had; mutation M3 pins it. Gates after remediation: test-from-emqx.sh 19 ok, test-from-hivemq.sh 15 ok, test-from-mosquitto.sh 6 ok, dual-run-smoke.sh 11 ok (run twice), py_compile x3, shellcheck clean on all four scripts, mqttui cargo test/clippy/fmt clean, vendor-mqttui-examples.sh --check current, check-readme-facts.py all match. THE DOC AND PROVENANCE HALF, which is this lane's whole value. (1) THE FIXTURE PROVENANCE CLAIM WAS FALSE: emqx-6.2.2.conf's header said the files were 'fetched verbatim' and 'every KEY and VALUE below is as the vendor ships it', while NINE values differed — and they were precisely the values that switch the converter's positive mappings ON (verify verify_none->verify_peer, fail_if_no_peer_cert false->true, peer_cert_as_username disabled->cn, max_retained_messages 0->100000, two max_connections infinity->finite, enable_ocsp_stapling false->true, two cipher strings shortened) plus the vendor's `my_quick_listener_name` typo renamed. Fixtures are now explicitly VERBATIM or COMPOSED, and both kinds exist because a vendor's stock defaults take the REFUSING branch of nearly every security mapping: new emqx-6.2.2-vendor-verbatim.conf (the ten cited rel/config/examples files concatenated, 21196 bytes, sha256:5f1542e1…, per-part hashes in its header), hivemq-2026.5-default-config.xml and hivemq-2026.5-tls-client-auth.xml (single vendor files, sha256:9a8a93f3… and 72f561d6…), beside the relabelled composed ones whose headers list EVERY deviation and why. emqx-acl-documented-examples.conf's header claimed all six rules were copied from the vendor's shipped examples; three were composed here, and it now says which, citing the vendor acl.conf's lines 97/129/135 for the copied ones and 123 for the fragment behind a composed one. RE-DERIVED, NOT ASSERTED (2026-08-14 docs pass): all ten EMQX parts re-fetched at tag 6.2.2 and byte-compared (clean, hashes and byte counts match the header); the whole verbatim body re-hashed by the two commands its header prints, both 5f1542e1…; the two HiveMQ verbatim files re-derived from the fixtures by the head+sed recipe now in their headers and matched against fresh curls; the composed EMQX fixture diffed leaf-by-leaf with the converter's own HOCON reader (exactly the nine documented changes, the one rename, 53 dropped keys, nothing undocumented); the composed HiveMQ fixture compared element-by-element against each cited vendor file (equal path-for-path and value-for-value, only comments dropped) and its <security>/<persistence> defaults checked against the vendor config.xsd. Three header numbers were wrong and are corrected: 'thirteen' <mqtt> child blocks (twelve), 'four vendor sample files' (five — four samples plus the default config.xml), and the 53-dropped-key breakdown. The HiveMQ tls-client-auth sample's path was cited as conf/examples/... in docs/MIGRATION.md; the real path is src/distribution/conf/examples/... and the short form 404s. Also recorded because it is not what a reader assumes: tag 6.2.2 in emqx/emqx is titled 'EMQX Enterprise 6.2.2' (both editions ship from that repo since 6.x) and these example files are not edition-specific — listeners.ssl.conf.example is byte-identical at v5.8.9, mqtt.conf.example is not. (2) METRIC NAMES THAT DO NOT EXIST: the playbook's verification table told operators to alert on bridge_connected, bridge_forwarded_total, bridge_dropped_total, bridge_spool_depth and bridge_reconnects_total. The real series carry the `fss` registry prefix, so every one of those alerts would have returned no data forever — and the smoke could not catch it because it asserted the SUBSTRING 'bridge_'. The doc now names the fss_ series with their real label sets, and dual-run-smoke.sh matches each one ANCHORED and fails if an unprefixed bridge_* series ever appears. Scraped from a running bridge to confirm. README.md and docs/BRIDGE.md already used the fss_ names for the BRIDGE series, but grepping every doc for the same defect class found four more citations of BROKER series that the registry does not export, each of them in a sentence telling the reader to check or alert on it: README.md's publish_dropped{reason="outbound-full"}, docs/OPERATIONS.md's security_reloads_total{trigger="watch"}, and docs/TROUBLESHOOTING.md's publish_dropped{reason="queue-overflow"} and quota_rejections_total{reason="retained"}. Every broker series carries the mqttd_ prefix — a scrape of a fresh node has 160 sample lines and not one without it — and a counter's Prometheus text form carries _total; all four are corrected to the scraped names. (3) THE FOREIGN-BROKER HEDGE WAS BACKWARDS — see T18, corrected there and in the doc, with a sixth assertion added. (4) BIDIRECTIONAL RETAINED RE-SYNC WAS PRESENTED AS PURE UPSIDE: it runs both ways on every reconnect, so a retained value DELETED on one side while the bridge is down is RESURRECTED from the other. Reproduced twice: retained fleet/truck7/config crossed to mqttd, the bridge was stopped, the value was cleared on the incumbent with `mosquitto_pub -r -n` (confirmed gone), the bridge was restarted — and the value was retained on the incumbent again, republished from mqttd. Documented as a hazard in MIGRATION.md and docs/BRIDGE.md with the mitigation (prune with the bridge RUNNING so the zero-length retained publish crosses as a tombstone; then check BOTH sides), and the mitigation is now smoke assertion 6 rather than advice. (5) THE ACL-DENIAL COUNTER CLAIM: 'there is no Prometheus counter for ACL denials' is false for one of the four kinds. Measured on a booted broker — a CONNECT refused for an unauthorized WILL topic raises mqttd_connection_errors_total{reason="acl"} 1 (conn.rs count_connection_error), while a denied publish and a denied CONNECT-ACL client id raise nothing and appear only as audit events acl.deny.publish / acl.deny.connect. Note the round-1 finding itself was half wrong here: it claimed the connect path also counts, and the code and the measurement both say it does not. EVERY OTHER LOAD-BEARING CLAIM ON THE PAGE WAS RE-MEASURED IN THE SAME PASS, on the assumption that more were wrong: the four bridge refusals (remap on `both`, a $-prefixed filter, QoS>=1 without [spool] dir, an mTLS half-identity) each provoked from the real binary and quoted by their real error text; hop_count_limit default 8 and no_local = true in client.rs; `mqttd --probe /readyz` measured 0 against a live broker, 1 against a dead one, 2 with no health endpoint; cert-audit.sh exits 1 on a fleet containing one EKU-less client certificate and names the missing clientAuth EKU; three INSECURE lines on a plaintext + anonymous + no-ACL node at RUST_LOG=warn, two when an ACL file is set; the JKS recipe's openssl half re-run in the exact form the converter prints, after which the broker logged 'accepting MQTT 3.1.1 clients over TLS 1.3' on the extracted pair; the vendor default ACL's 6 TODOs / 1 inert rule counted in the output (and pinned by an assertion); the verbatim EMQX config's three TLS listeners all named and its cacertfile refused as a mandate; the HiveMQ CE default config reported ANONYMOUS and its plaintext listener flagged; REQUIRED mapping to client_ca on vendor bytes; 11 flattened ACL rules from 2 users x 2 roles; EMQX's shipped max_inflight = 32 and HiveMQ's XSD-documented server-receive-maximum default of 10 against mqttd's 256; mqttd's HTTP auth reading the status code (200 allow, 401/403 deny, anything else deny, http_auth.rs); the file-RBAC extension's own README denying connections whose username or client id contains # or +; and all four scripts exiting 2 — not 1 — when a binary or the mosquitto broker is missing. The version ranges were spot-checked too, and the doc now says exactly what that means: EMQX v5.8.9's ten shipped examples and HiveMQ CE 2023.10/2024.9's config.xml + mqtt sample each convert with exit 0 and pass --check-config, which is a parser claim and not a fidelity claim. Still true and still stated: no live EMQX or HiveMQ broker was ever run, and the bridge step was exercised against Mosquitto 2.1.2 only."
    notes: "Scope note: tools/mqttui/src/migrate.rs and its differential test had to change too — `mqttui migrate mosquitto` is a byte-identical Rust port of from-mosquitto.py and is what a `cargo install mqttui` user actually runs, so it carried the same unescaped-string and max_inflight defects. xmllint's schema check of the HiveMQ fixtures against the vendor config.xsd is a one-off, not CI: the schema is not vendored. No src/ change was needed anywhere — every finding was a converter, fixture or documentation defect."
  - id: 0051-T20
    title: "Round 3 of issue #250 — the defect CLASSES swept across all three converters and the Rust port, and a CI-gated property sweep that makes A/B/C/D re-detectable instead of re-findable"
    status: done
    date: 2026-08-15
    evidence: 'Round 3 of issue #250, and the brief was the PATTERN rather than the findings: two adversarial rounds had each fixed the NAMED SITES of a defect and left the CLASS alive somewhere else — round 2\'s blocking finding was literally round 1\'s blocking finding, surviving in the third converter because nobody was told to look at it. So this pass swept each class across all three Python converters AND the shipped Rust port, and then INSTALLED A CHECK so the classes are detectable rather than re-findable. THE CHECK: scripts/migrate/property_sweep.py, run by all three test-from-*.sh steps (so it is CI-gated) — 26 mosquitto + 50 emqx + 37 hivemq generated cases from a cross product of listener ORDER permutations, enable flags, unanimous/mixed mTLS postures, both no_match postures, and truststore present/absent. Five invariants per case: (A) every security-relevant input VALUE appears in the output, translated or named in a TODO/NOTE — each generated value is a unique string, so \'appears\' is exact and holds under every permutation rather than the one ordering a fixture happens to use; (B) nothing the source DISABLED is a live bind, URL or [[rules]] entry, checked against the output with comment lines stripped so a commented candidate (the contract\'s prescribed handling) is not mistaken for an activation; (C) no deny/allow claim in a document contradicts the `default` that same document writes; (D) every `step N` the output tells the operator to run is a step the output printed; (E) `mqttd --check-config` on every generated config. NON-VACUITY, 10 mutations, each reverted -> sweep run -> named failure -> restored byte-identically (diff clean on all three converters): M1 decide the mosquitto mTLS gate from tls_listeners[0] -> \'the mTLS MANDATE on listener 0.0.0.0:8883 is NOWHERE in the output\'. M2 emit `crl` unconditionally -> \'the broker REJECTED the generated config: tls.crl requires tls.client_ca\'. M3 stop emitting [security] acl_file -> \'acl_file, or the policy is not enforced, is NOWHERE in the output\'. M4 stop reporting the per-listener security collapse -> \'the node-wide collapse of a per-listener allow_anonymous is NOWHERE\'. M5 drop the include_dir not-read report -> \'/etc/mosquitto/conf.d is NOWHERE\'. M6 ignore an EMQX listener\'s enable = false -> \'a DISABLED listener bound anyway: plaintext_bind = "0.0.0.0:1883" as a LIVE setting\'. M7 remove BOTH reporters of a dropped TLS path -> \'/certs/ssl.crt is NOWHERE\'. M8 hard-code the fail-closed claim in the zero-rule ACL TODO -> \'acl.toml writes `default = "allow"` but a comment asserts fail-closed\'. M9 emit a disabled file-RBAC user\'s rules as live policy -> \'a DISABLED user\'s live rule as a LIVE setting\'. M10 put step 2 back inside the truststore loop (the exact round-2 defect) -> \'out.toml tells the operator to run step(s) [2] but only printed [1, 3, 4]\'. WHAT THE SWEEP FOUND THAT NEITHER FINDINGS FILE NAMED. (1) A BROKEN CONFIG, in the most-reached converter: from-mosquitto.py and the Rust port emitted `crl` whenever any TLS listener named a crlfile, and the broker REFUSES that pair outright — `invalid configuration: tls.crl requires tls.client_ca`. The differential test\'s OWN fixture has cafile + crlfile with no require_certificate, so both converters agreed on a config `mqttd --check-config` rejects; the shell fixture had no crlfile and the differential test only parsed TOML, so neither could see it. Rule 3, \'the output must validate\', broken in the converter the docs call the most mature. (2) THE TRANSLATED ACL WAS NEVER ENFORCED: from-mosquitto.py translated a whole policy and never wrote [security] acl_file, so mqttd enforced NO authorization at all (mqtt-config: acl_file is Option<String>, None by default, \'without it authorization is not enforced and loudly logged\') while the generated ACL\'s own header said it denied by default. Both halves fixed, and a config with no acl_file now says so. (3) PER-LISTENER SECURITY COLLAPSE, unreported: mosquitto.conf(5) makes password_file, acl_file, psk_file, allow_anonymous, allow_zero_length_clientid and auto_id_prefix PER-LISTENER under per_listener_settings, and mqttd\'s [security] is node-wide. per_listener_settings fell into the generic \'no direct equivalent\' bucket, and allow_anonymous kept the FIRST true it saw — so `true` on a retired listener followed by `false` on the live one carried anonymous access forward AND emitted a NOTE saying so beside a config that did not. Now last-wins (what Mosquitto does with the flag off), with a TODO naming every site and the value that won. (4) EMQX\'s actions/sources BRIDGE SHAPE WAS NEVER READ: `bridges` is not a root in 6.2.2\'s schema at all (emqx_conf_schema:roots/0, emqx_bridge_v2_schema:roots/0 give connectors/actions/sources; bridges.* survives only through emqx_bridge_compatible_config\'s v1 upgrade path), so a current EMQX bridge produced an upstream with ZERO rules — forwarding nothing — while the section TODO said MQTT-type actions map to mqtt-bridge rules. convert_bridge_actions() now reads actions.mqtt.<n>.parameters and sources.mqtt.<n>.parameters per emqx_bridge_mqtt_pubsub_schema, and MIGRATION.md\'s row says which shape is which. (5) A DROPPED TLS PATH WAS REPORTED WITHOUT BEING NAMED: from-emqx\'s inventory gave each listener\'s verify posture but not its material, and the \'material differs\' TODO said the other listeners\' PEM files were \'referenced NOWHERE\' without naming them — so the dropped certfile/keyfile paths appeared nowhere in the output. Same for a disabled authenticator: \'an http authenticator was disabled\' without its URL cannot be checked against the input. (6) TWO AUTHENTICATORS OF ONE KIND SILENTLY OVERWROTE EACH OTHER (EMQX\'s authn is an ordered CHAIN; mqttd has one [security.http_auth] and one [security.jwt]), and several `file` authz sources let the LAST one win. Both now report the collision and name what was not translated. (7) A DISABLED file-RBAC USER GOT LIVE ALLOW RULES — round 1\'s fix reported <enabled>false</enabled> and said in the same breath that \'their rules WERE still emitted below\'. The grant and the re-enrolment command are now COMMENTED OUT with a TODO. (8) THE HIVEMQ RE-ENROLMENT BLOCK was emitted only when users existed, while [security] password_file pointed at the file it creates — the same dangling-cross-reference class as the missing step 2; it is unconditional now, with a \'no user was found\' arm. (9) enable_authn read the same whether it was the default `true` or the fail-open `false`; the value is now named and the bypass gets its own sentence. (10) tasks.toml said dual-run-smoke asserts \'four claims\' while it asserts six (round 2 widened the script and 0051-T18 but not the manifest), and README.md was a FOURTH un-hedged echo site for the retained-resurrection hazard beside OPERATIONS.md, which round 1 had named and round 2 had missed. CLASS E, RE-DERIVED FROM THE VENDORS\' OWN SCHEMAS AT THE PINNED TAGS RATHER THAN TRUSTED: EMQX\'s JWT authenticator has NO issuer/audience field (emqx_authn_jwt_schema.erl @ 6.2.2 re-fetched: mechanism, acl_claim_name, on_missing_jwt, verify_claims, disconnect_after_expire, from, plus per-type keys; grep -cE \'issuer|audience\' = 0), so the mapping row and the converter\'s read fired only on input EMQX cannot produce while the real construct — verify_claims as a MAP of claim name to expected value — was dropped and called unimplementable; iss/aud now map onto [security.jwt] issuer/audience (which crates/mqtt-auth/src/token.rs verifies only when set, and disables aud validation when unset), every other claim is a TODO saying a correctly-signed token now passes, and a top-level issuer/audience is read with a TODO saying the vendor schema has no such field. `enable` (alias `enabled`, default true) confirmed as a real base_listener field (emqx_schema.erl base_listener/1 @ 6.2.2). Every other EMQX row checked against the vendor source at 6.2.2 (node.name, node.data_dir, mqtt.max_packet_size/max_topic_alias/max_mqueue_len/max_subscriptions/peer_cert_as_username, listener max_connections and messages_rate via emqx_limiter_schema\'s mqtt_shared_limiters, retainer.backend.max_retained_messages default 0, authorization.no_match enum allow|deny default deny, authz file `path`, authn http `url`/`request_timeout`, every bridge leaf including retain and payload). Every HiveMQ row checked against config.xsd @ 2026.5 (max-packet-size, server-receive-maximum default 10, max-per-client, max-queue-size, strategy enum discard|discard-oldest default discard, max-connections -1 = unlimited, clientAuthenticationMode enum OPTIONAL|REQUIRED|NONE default NONE) and every file-RBAC element against README.adoc @ 4.6.16 (topic, activity, qos with its seven-value enum, retain, shared-subscription, shared-group default #, ${{clientid}}/${{username}}). The Mosquitto converter had NO vendor provenance at all, so its directive vocabulary was checked against man/mosquitto.conf.5.xml @ v2.0.22 — which is where per_listener_settings\' exact affected-key list, tls_version being a MINIMUM since 1.6 (so tlsv1.3 is a NOTE, tlsv1.2 is the allow_tls12 TODO and older is \'those clients cannot connect\'), use_identity_as_username\'s password_file interaction, use_subject_as_username (previously unhandled), include_dir\'s documented load order and max_inflight_messages\' \'outgoing QoS 1 or 2\' definition all came from. CLASS F: --help now carries the same VERSION SCOPE sentence on all three converters and in `mqttui --help`, which MIGRATION.md line 180 had claimed while from-mosquitto.py printed one line; docs/MIGRATION.md gained a Mosquitto mapping section (it had none, and the converter the docs call the most mature was the only one with no table); the retained-resurrection hazard is now on OPERATIONS.md, README.md and in dual-run-smoke.sh\'s own comment beside the four sites that already had it. CLASS G: an EMQX HOCON `include` and a Mosquitto `include_dir` now say their CONTENTS WERE NOT READ in those words, and so does capath (a directory of anchors the converter never opened) — the previous generic line read as \'mqttd has no includes, fine\' rather than \'your whole authn/authz may be in there and was never seen\'. GATES: test-from-mosquitto.sh 9 ok, test-from-emqx.sh 22 ok, test-from-hivemq.sh 18 ok — all PASS, each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL, and each now running the property sweep; py_compile clean on the three converters, property_sweep.py, gen-status.py and check-readme-facts.py; shellcheck clean on all four migrate scripts; tools/mqttui cargo test 42+2+1 pass (the differential test still byte-identical), cargo clippy --all-targets -D warnings clean, cargo fmt --check clean; vendor-mqttui-examples.sh --check \'current (76 files)\' with diff -r on the bundle clean; check-readme-facts.py all match (34 scripts, README bumped from 33 for the new sweep, which is declared hidden in tasks.toml).'
    notes: 'Scope note: tools/mqttui/src/migrate.rs, tools/mqttui/tests/differential.rs and tools/mqttui/src/main.rs had to change — `mqttui migrate mosquitto` is a byte-identical Rust port of from-mosquitto.py and is what a `cargo install mqttui` user runs, so it carried the first-listener-only TLS gate, the invalid `crl` beside a commented `client_ca`, the missing [security] acl_file and the undisclosed provenance. The byte-identity is still enforced by the differential test. DELIBERATELY LEFT: the property sweep generates INPUTS, not vendor files, so it proves invariants and not fidelity — the fixture tests remain the only thing pinning provenance and exact wording, and both must run. The sweep also does not boot the broker on generated ACLs (the fixture tests do, on two ACLs each); xmllint schema validation of the HiveMQ fixtures is still a one-off because the vendor XSD is not vendored. No src/ change was needed: every defect was in a converter, a fixture, a manifest or a document.'
  - id: 0051-T21
    title: "Round 4 of issue #250 — the fail-open CLASS made structurally impossible: provenance-or-nothing emission, a posture change is always commented, the ACL default derived, two new invariants plus a fuzz pass, and the honest DRAFT scope in all four places"
    status: done
    date: 2026-08-15
    evidence: 'Round 4 of issue #250, and the mandate was the DIAGNOSIS rather than the findings: three adversarial rounds had each fixed what they were shown, honestly and well — round 3''s class sweep even found 15 defects the two previous rounds never named — and the count went UP, not down. The reason is structural: "every input construct is either translated or marked TODO(migrate)" is a TOTAL-COVERAGE claim over three large foreign vendor schemas, and total coverage cannot be reached by inspection. But every serious finding across all three rounds pointed the same way — THE TOOL EMITTED A LIVE SETTING IT HAD NOT ACTUALLY DERIVED FROM THE INPUT: a bind fabricated as 0.0.0.0:1883 when the input said `bind_address 127.0.0.1`; a listener EMQX had switched OFF converted to a live bind; `allow_anonymous true` from a retired listener carried onto the live one; an mTLS mandate, a CRL check and a TLS-version floor dropped from any listener that was not first; an EMQX bridge that used TLS converted to a LIVE PLAINTEXT upstream; an ACL that permits everything beside a comment saying it denies everything; a WebSocket listener emitted as a raw-MQTT bind. So this pass makes that SHAPE IMPOSSIBLE rather than absent. (1) PROVENANCE OR NOTHING. Each converter (and the Rust port) gained one small emission gate — `Provenance.line(field, rendered, source)` plus `Conversion.set(..., source=)` — and SECURITY_FIELDS naming the 15-21 fields whose value decides who may connect and what they may do (every *_bind, cert/key/client_ca/crl/allow_tls12, acl_file/password_file/allow_anonymous/mtls_identity_source, [security.*] url/issuer/audience/*_file, the ACL `default`, a bridge upstream url and its tls ca/cert/key). The gate REFUSES to write a live line without the INPUT KEY the value came from: a field with no provenance is emitted COMMENTED OUT beside a TODO naming the decision, so a construct a converter failed to read can leave the config INCOMPLETE and can no longer leave a live security setting nobody derived. Every live security-relevant line now carries `# from: <input key>` (and `; defaulted: <what and the vendor default it came from>` where part of the value is a documented default of a directive that WAS present, or a path the converter itself owns). `--provenance-json` writes the same ledger as JSON. CALL SITES CONVERTED: 4 mosquitto binds + cert/key/client_ca/crl/mtls_identity_source/acl_file/allow_anonymous/ACL default; 5 EMQX binds + cert/key/client_ca + password_file/http_auth url/jwt issuer+audience/mtls_identity_source/acl_file/allow_anonymous/ACL default + the bridge [local] url, every [[upstreams]] url and the whole [upstreams.tls] block; 4 HiveMQ binds + cert/key/client_ca + password_file/acl_file/allow_anonymous/ACL default. LITERALS AND FALLBACKS REMOVED: from-emqx.py''s `lst.bind or ("0.0.0.0:1883" if lst.proto == "tcp" else "0.0.0.0:8883")`; normalise_bind''s `default_host="0.0.0.0"` and its `f"{v}:1883"` for a host with no port; from-hivemq.py''s `self.port or "1883"` and `self.bind or "0.0.0.0"` in Listener.address; from-mosquitto.py''s (and the Rust port''s) `lst.port if lst.port is not None else 1883` / `lst.bind or "0.0.0.0"` in FIVE places (the listener comment block, the plaintext bind, the TLS bind and both `additional listener` TODOs) plus `Listener.where`, which named a fabricated address in EVERY message about a listener; the bridge config''s hard-coded `url = "127.0.0.1:1883"` for [local]; and HiveMQ''s three bare `# client_ca = "/etc/mqttd/tls/client-ca.crt"` candidate lines and its live `cert`/`key` placeholders, which are now provenance-carrying (`# from:` the keystore element, `; defaulted:` the PEM path step 1 must write). (2) A POSTURE CHANGE IS ALWAYS COMMENTED, without exception: `allow_anonymous true` in mosquitto.conf is no longer carried over (it is node-wide in mqttd and one listener''s posture cannot decide it) — the candidate is commented with the input key it came from; the EMQX bridge''s TLS block is commented with every dropped ssl.* path named, because mqtt-bridge''s [upstreams.tls] is Optional and ABSENT MEANS PLAINTEXT; a Mosquitto listener whose `protocol` is neither mqtt nor websockets gets NO bind at all. (3) THE ACL DEFAULT IS DERIVED: one `policy_effect(default)` / `empty_policy_effect(default)` per converter now generates every sentence about what the policy will do, including the ACL headers (which asserted "anything not allowed below is refused" as fixed prose in two of the three) and both zero-rule TODOs; the `default` line itself carries its source (`authorization.no_match` for EMQX, the vendor''s own allow-list semantics for the other two). (4) THE CHECK. property_sweep.py gained invariant F (PROVENANCE: every live security-relevant value is one the INPUT held — for a *_bind the PORT always, for a path/URL the string; a missing part is allowed only where the line says `defaulted:`, and every one of those is counted and PRINTED) and invariant G (NO LIVE SETTING WITHOUT A SOURCE: every uncommented security-relevant line carries `# from:`), plus generators for the axes round 3''s own verifier named as blind spots — the Mosquitto default-listener form, `protocol websockets`, an unknown transport, per-listener max_connections and the `-1` sentinel, the dynsec plugin, a listener-scoped EMQX authentication chain, the other keys on a LIVE authenticator, acl_claim_name, a TLS bridge via --out-bridge (invariant E had never even looked at bridge output), an EMQX `bind` with no port and a HiveMQ listener with no <port>. 124 generated cases (34/52/38). WHAT F AND G CAUGHT ON THEIR FIRST RUN: G fired on EVERY [listeners]/[tls]/[security] line of the EMQX and HiveMQ output (none carried a source); F caught `acl_file = "/etc/mqttd/acl.toml"` — a path that appears in no input — and, once the same rule ran inside the fuzz pass, a LIVE `plaintext_bind` on port 1883 derived from a `bind_address` with no port, in a from-mosquitto.py fix written three hours earlier in this same pass. That last catch is the argument for the invariant: the mistake was mine, it was hours old, and only a machine reading the output against the input would have found it. Both are mutation-proved (revert the gate -> named failure). (5) THE FUZZ PASS (`--fuzz N`, run by all three test-from-*.sh at N=40 so it is CI-gated): each fixture mutated mechanically — delete random lines, truncate at a random byte, permute blocks, flip a boolean/enum, swap a transport, duplicate or chop a line — asserting only what must hold for ANY byte sequence: the converter EXITS 0 or 1 WITH A MESSAGE, whatever it writes is valid TOML, and no live security-relevant line lacks provenance. It found the HANG round 3 reported: from-emqx.py''s HOCON `_array` fell through to `_bare(",]\n}")`, which cannot advance when the current character is already a stop character, so a `}` reached inside an unterminated array appended "" and re-entered the loop unchanged — 100% CPU, unbounded memory, no error, no exit, on `authentication = [` followed by `}`. Fixed structurally: `Hocon._guard(before, where)` refuses to iterate without consuming a character (called from BOTH loops, so no future edit to a stop set can reintroduce it), an unterminated `[`/`{` is reported as a TODO saying everything after the missing bracket was read in the WRONG SCOPE, and a HoconError exits 1 with the byte offset. 1,800+ mutated inputs across five seeds: no hangs, no crashes, no invalid TOML, no unprovenanced live line. ROUND 3''s 22 ITEMS, all dispositioned — R3.1 default-listener form (fixed, both languages), R3.2 + R3.15 `protocol websockets` -> ws_bind/wss_bind (fixed, both), R3.3 listener-scoped authentication chain (fixed: read, named, saw_auth set, the contradicting TODO gone), R3.4 + R3.16 every other key on a LIVE authenticator (fixed: AUTHN_NO_EQUIVALENT, 20 entries, headers/method/body/pool_size/ssl.* each with the reason its loss breaks login), R3.5 acl_claim_name (fixed: an entire authorization source, now named), R3.6 TLS bridge -> plaintext (fixed: commented [upstreams.tls] naming every dropped path), R3.7 the HOCON hang (fixed structurally + seeded in the fuzz pass), R3.8 non-file authz sources named with their store and query (fixed), R3.9 SECTION_NO_EQUIVALENT count-only swallowing gateway/dashboard binds and CA material (fixed: security-relevant leaves named), R3.10 elided values (fixed: HiveMQ cipher-suites and listener <name>, EMQX''s generic listener line now names the value — mountpoint and access_rules included), R3.11 the sweep''s own weakness (fixed: F, G and the missing generators), R3.12 the dynsec plugin and the false "your broker also authorized everything" claim (fixed: NOT_READ entries for plugin/auth_plugin/plugin_opt_*, and that sentence is now derived from what was read), R3.13 per-listener max_connections (fixed: smallest-wins + TODO), R3.14 `max_connections -1` producing a config the broker rejects (fixed: unset + NOTE), R3.17 messages_rate last-wins (fixed: smallest-wins + TODO), R3.18 max_topic_alias and message_size_limit (fixed: both now map), R3.19 the `tls_version` "since 1.6" claim (fixed in docs/MIGRATION.md and in both converters'' comments — the man page says the MINIMUM reading begins AFTER 1.6.x, and the 1.2 TODO now states the older ONLY-version reading too), R3.20 the per_listener_settings list stated four ways (fixed: ONE SCOPED_SECURITY_LIST constant of EIGHT keys, quoted by the emitted TODO, --help, `mqttui --help` and MIGRATION.md), R3.21 the JWT dead-field overwrite (fixed: a top-level issuer/audience can no longer displace the verify_claims value; it is a commented candidate), R3.22 TROUBLESHOOTING.md''s un-hedged "prune retained" (fixed). HONEST SCOPE, in the four places the mandate names: docs/MIGRATION.md has a new section "What a converter produces: a DRAFT, where anything undecidable is INERT and named" with the diagnosis, the gate, worked output, and an explicit VERIFIED / NOT VERIFIED split; the same DRAFT paragraph is in all three converters'' --help and in `mqttui --help`; the condensed six-line form is in the header of every config, ACL and bridge file they write; and README''s migration bullet now leads with "what it produces is a reviewed DRAFT, not a translated configuration", says every security-relevant value carries its input key, and states that no live broker of ANY of the three was run and that no total-coverage claim is made. GATES: test-from-mosquitto.sh, test-from-emqx.sh and test-from-hivemq.sh all PASS (each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL, and each now running both the property sweep and the fuzz pass); property sweep 34+52+38 cases hold every property; fuzz 43+47+45 in-CI runs plus 1,800+ across five seeds locally; py_compile clean on the three converters, property_sweep.py, gen-status.py and check-readme-facts.py; shellcheck clean on the migrate scripts; tools/mqttui cargo test PASS including the byte-for-byte differential test (13 hand-built inputs compared byte-identical besides it); cargo clippy --all-targets -D warnings clean; cargo fmt --check clean; vendor-mqttui-examples.sh --check current with diff -r clean; check-readme-facts.py all match; gen-status.py --check clean.'
    notes: 'Scope note: tools/mqttui/src/migrate.rs and tools/mqttui/src/main.rs changed again — `mqttui migrate mosquitto` is a byte-identical port of from-mosquitto.py and the differential test enforces that, so the provenance gate, the default-listener form, `protocol websockets`, the per-listener connection cap and the commented `allow_anonymous` candidate all had to be ported line for line (verified byte-identical on 13 hand-built inputs plus the shared fixture). Two behaviour changes an operator will notice, both deliberate: `allow_anonymous true` is no longer carried over (it is a posture change, so it is a commented candidate), and the generated mqtt-bridge config no longer starts as-is — `[local] url` is commented out because nothing in an EMQX configuration names YOUR broker''s address, so mqtt-bridge refuses it until the operator sets it. test-from-emqx.sh asserts BOTH halves of that: the draft is inert, and completing the two paths makes the real binary accept it. What is NOT done: the property sweep still generates from axes a human chose (F and G are what make the CLASS detectable, not the generators), no live EMQX/HiveMQ/Mosquitto was run, and no total-coverage claim over any vendor schema is made or implied anywhere in the output.'
  - id: 0051-T23
    title: "Issue #297 — the five open semantic misreads fixed with tests, and a DIFFERENTIAL lane against the real Mosquitto broker: observable behaviour compared, not config text"
    status: done
    date: 2026-08-17
    evidence: 'Issue #297 is the residue T22 enumerated: the provenance gate closes FABRICATION and cannot see a MISREADING, five named misreads were still open, and the issue''s own argument is that the class does not converge under inspection — it needs a semantic oracle. Both halves delivered. THE FIVE FIXES, each cited to the vendor''s documented semantics and each with a test that fails without it: (1) TLS KNOBS ON A CERTIFICATE-LESS LISTENER (tls_version/ciphers/ciphers_tls1.3/dhparamfile) — mosquitto.conf(5) @ v2.0.22 terminates TLS only with a certificate (`certfile`, with `keyfile`) or a PSK, so on a listener with neither every knob was INERT in Mosquitto too; the converter nevertheless reported ''so it accepted TLS 1.2 AND 1.3'' beside the LIVE PLAINTEXT bind it correctly wrote — the dangerous direction, read as ''encrypted before cutover, not after''. ciphers/ciphers_tls1.3/dhparamfile were not in TLS_KEYS at all (they fell through to ''check the mqttd configuration table'', which has nothing for them). Now: the knob translations run only where the listener terminates TLS; a certificate-less listener''s TODO inventories every inert setting BY NAME and says plainly it did NOT terminate TLS and the bind below is plaintext because the source was; a real TLS listener''s ciphers/dhparamfile get rustls-specific reports (fixed suite set, no cipher config, no non-EC DHE). (2) MALFORMED VENDOR VALUES REFUSED, NEVER EMITTED: the DIRECT table''s int arm wrote `message_size_limit 10OO` (letter O) straight into the TOML — a document `mqttd --check-config` cannot even PARSE, so the prescribed validation could not run — while exiting 0, which a migration script reads as success; the retain arm read any unrecognised spelling (`flase`) as the PERMISSIVE direction silently; the str arm accepted an empty path. A value outside the vendor''s own schema (count/bool/path) now refuses the WHOLE run: exit 1, NOTHING written, every key+value+file named on stderr, and the documented exit contract updated (0 translated / 1 input unusable). Well-formed neighbours still translate — asserted. (3) THE ANONYMOUS-SCOPE TODO''s remediation named a line the converter does not write: it said allow_anonymous ''is emitted COMMENTED OUT'' unconditionally, but the commented candidate exists only when mosquitto.conf set it TRUE — with false/unset the operator greps for a line nobody wrote. The advice is now DERIVED from what the run actually emitted (candidate/false/unset postures, three texts), and the false/unset texts say outright there is nothing to uncomment. Tested in both postures. (1)-(3) are the stranded 786-line half of this task, verified and shipped as found; both languages (from-mosquitto.py + tools/mqttui/src/migrate.rs), plus three new Rust tests. (4) A LITERAL %c/%i IN AN EMQX acl.conf topic_match() OR A HIVEMQ FILE-RBAC <topic> was still emitted as a rule mqttd SUBSTITUTES — a per-client grant the source never gave. EMQX 5/6 substitutes only ${...} placeholders (the pinned schema fixture''s own ''Supported placeholders are:'' list; %c/%i absent) and file-RBAC only ${{clientid}}/${{username}} (4.6.16 reference), so both refusals mirror the Mosquitto one from T22, checked BEFORE placeholder rewriting so a %c the converter itself produces is never confused with one the source carried; the EMQX message also names the EMQX 4.x escape hatch (%c/%u WERE its placeholders — outside the parser''s version scope, rewrite and re-run). Example tests in both suites plus 2 new property-sweep cases (emqx/hivemq literal-%c, forbidden = the live rule, witness = the topic named). (5) THE JWT/JWKS BRANCH now names the ENDPOINT URL it read (or says ''no URL in the input — only the switch'' when use_jwks is on without one) — the same operator-can-verify rule every other authenticator''s server/database/url already followed. THE ORACLE: scripts/migrate/differential-mosquitto.sh boots the REAL mosquitto broker on the source config and mqttd on the CONVERTED config (finished exactly as the draft''s own TODOs instruct — re-hashed credentials, acl path, data dir; every finishing step is an env overlay naming the draft line that demands it), probes both with the same client binaries, and diffs seven verdicts: anonymous-connect, wrong-password, valid-credential, permitted/forbidden publish, permitted/forbidden subscribe — ACCEPTED/REFUSED and DELIVERED/NOT_DELIVERED deliberately, because reason codes legitimately differ while whether a client gets in and a message crosses is exactly the meaning the mappings claim to preserve. Run 2026-08-17 against mosquitto 2.1.2: all 7 identical; transcript in docs/MIGRATION.md. NON-VACUOUS twice over: harness anchors fail the run if the ORACLE side misbehaves (vendor accepting anonymous under allow_anonymous false = broken probe, not agreement), and mutation-proved — mqttd booted with anonymous ON records ACCEPTED and the diff fails; a hand-widened ACL records publish-outside-acl DELIVERED and the diff fails. Exit-2-on-missing-environment verified both ways (MQTTD_BIN at nothing; mosquitto off PATH), same convention as dual-run-smoke.sh. GATES: test-from-mosquitto.sh PASS (incl. the new refusal/knob/anon-posture sections), test-from-emqx.sh PASS, test-from-hivemq.sh PASS; property sweep now 40+57+41 = 138 cases (was 136), MIGRATION.md counts updated; fuzz 44+48+46; tools/mqttui cargo test PASS incl. the byte-for-byte differential test and 3 new unit tests; clippy -D warnings clean; fmt clean; py_compile clean; vendored mqttui bundle re-vendored; gen-status clean. docs/MIGRATION.md: KNOWN GAPS %c/%i row widened to all three converters, EMQX placeholder prose + HiveMQ mapping row updated, the Verified list gains the differential lane and the ''no running broker'' bullet is re-scoped to what the seven probes now measure, and the lane''s transcript + mutation proofs recorded beside the dual-run smoke''s.'
    notes: 'NOT DONE, stated rather than implied: (a) the differential lane is NOT yet a per-PR CI step — .github/workflows was outside this change''s scope; wiring is a 3-line step after the dual-run smoke''s mosquitto install, and MIGRATION.md says ''not yet a CI step'' in exactly those words until it lands. (b) Seven probes over one config is a PROBE of the auth/ACL/bind mappings, not coverage of mosquitto.conf(5), and no EMQX or HiveMQ broker has ever been run — the issue''s own acceptance text starts with Mosquitto for a reason. (c) The semantic-misreading CLASS stays open by nature (KNOWN GAPS'' last row); what changed is that five named instances are closed with tests and the first vendor-as-oracle measurement exists to build on. (d) README.md needed two mechanical count updates (37 runnable scripts -> 38, 136 sweep inputs -> 138) because check-readme-facts.py is CI-gated on the first and the repository wins; those two numerals are the only README change.'
  - id: 0051-T22
    title: "Round 5 (final) of issue #250 — the SEMANTIC-MISREADING class named and bounded: TLS-PSK, anonymous-scoped ACLs, a TLS bridge upstream, unbindable binds and vendor sentinels fixed; every overclaim withdrawn; KNOWN GAPS enumerated"
    status: done
    date: 2026-08-15
    evidence: 'Round 4''s verification pass confirmed the round-3/4 restructuring WORKED — the provenance gate makes it structurally impossible to emit a live security-relevant value without naming the input key it came from, the new invariants caught a fabricated bind the same round had introduced hours earlier, and the fuzz pass found and structurally fixed a HOCON parser hang — and then produced 14 findings of a DIFFERENT class the gate cannot catch by design: SEMANTIC MISREADING, where the tool derives a live value from real input but misreads what that input MEANT. Six fixes, every overclaim withdrawn, and the class enumerated rather than implied to be closed. (1) BLOCKING — A TLS-PSK LISTENER BECAME A LIVE PLAINTEXT BIND. psk_file/psk_hint were in neither TLS_KEYS nor the half-material safety net, so Listener.is_tls was false and BIND_KEYS[(transport, False)] chose plaintext_bind: a listener mosquitto.conf(5) describes as encrypted (''The psk_hint option enables pre-shared-key support for this listener'') was published in CLEARTEXT on its own port, while another TODO in the same file reported that listener''s tls_version — and check-config said config OK. The gate could not see it: the bind carried a genuine `# from: listener 8883`, because the gate checks where the VALUE came from and the FIELD is what encodes the transport. Now PSK_KEYS are collected PER LISTENER, convert_psk() reports the listener (and psk_file''s identities, each of which needs a new credential), the bind is emitted INERT ON THE TLS KEY of its transport with the downgrade named, and the half-material report no longer claims a PSK listener was served as plaintext. Both languages, byte-identical. (2) BLOCKING — MOSQUITTO''S ANONYMOUS-SCOPED ACL BLOCK BECAME A GRANT TO EVERY AUTHENTICATED CLIENT. mosquitto.conf(5) @ v2.0.22, verbatim: ''The first set of topics are applied to anonymous clients, assuming allow_anonymous is true'' — those pre-`user` topic lines were emitted with NO identities, which crates/mqtt-auth/src/acl.rs applies to EVERY authenticated identity (''Both lists empty means everyone''). Strictly broader under both postures: with allow_anonymous false those topics were reachable by NOBODY. They are now scoped to identities = ["anonymous"] — the subject mqttd gives an unauthenticated client (crates/mqtt-auth/src/basic.rs) — each rule carrying an inline comment, plus a TODO quoting the man page and naming both consequences (they grant nothing until allow_anonymous is uncommented; a real user named `anonymous` collides). Both languages. (3) MAJOR — THE EMQX TLS BRIDGE STILL PRODUCED A LIVE PLAINTEXT UPSTREAM. Round 3 commented [upstreams.tls] out and left the upstream url live; but mqtt-bridge''s tls block is Optional and ABSENT MEANS PLAINTEXT, so commenting it IS the posture change and the url is the line whose liveness decides the posture. Completing the draft exactly as the file instructs ([local] url + a spool dir) connected to a TLS peer in cleartext. BridgeUpstream gained tls_enabled and the url is now emitted through the gate with source=None when the EMQX side had ssl enabled. (4) MAJOR — report_unread_authn_keys() was wired on the http and jwt branches ONLY, so a live authenticator on any other backend lost every key unnamed — on THIS REPOSITORY''S OWN pinned fixture, whose `backend = mysql` entry''s server/database/query appeared nowhere. Wired on every branch including the fall-through, plus AUTHN_NO_EQUIVALENT entries for the credential-store keys (server/servers/database/query/cmd/collection/selector/filter/base_dn/bind_dn) carrying convert_authz''s own reason: the store is named so the operator can check that the source they believe was in force is the source the converter could not translate. The reason lookup now also tests ANCESTOR paths, so password_hash_algorithm.name gets its block''s reason instead of the generic sentence. (5) MAJOR — THE SAME DOCUMENT EMITTED A JWT CLAIM AS A LIVE MAPPING AND REPORTED IT AS HAVING NO EQUIVALENT: verify_claims was absent from AUTHN_JWT_READ, so the leaf reporter enumerated its claims; the guard meant to suppress that (''verify_claims'': ''handled — …'') could never fire because the lookup tested the full path and the leaf and never the parent. verify_claims is now in AUTHN_JWT_READ (the branch reads it claim by claim) and the dead entry is gone. (6) MAJOR — NO GENERATED BIND WAS EVER CHECKED FOR BINDABILITY, and `mqttd --check-config` — the verification the docs, every --help and every generated header point the operator at — accepts ANY string there, so the prescribed gate said config OK on addresses the broker refuses at STARTUP: ws_bind = ":8085" (a form EMQX''s own ip_port accepts), plaintext_bind = "10.0.0.1:abc" from a HiveMQ <port>abc</port>, and plaintext_bind = "/tmp/mosq.sock:0" from `listener 0 /tmp/mosq.sock` — a UNIX-socket listener that declares no TCP endpoint at all, which is a transport fabrication the provenance gate cannot see. bind_gap() in all three converters and the Rust port parses host and port before a line goes out live; anything unparseable is INERT with the reason. A non-scalar EMQX `bind` is rejected outright rather than str()-ed into the Python repr "[''0.0.0.0:1883'']" (a live value in no input, which invariant F missed because for a *_bind it compares only the PORT). ALSO FIXED, same class: a literal `*` in a username became an mqttd identity GLOB in all three converters (identities are globs with NO escape, crates/mqtt-auth/src/acl.rs glob_match, while every source matches the username literally) — the rule is now refused with the user named, the same refusal from-emqx.py already made for a {re, …} condition; a literal %c/%i in a Mosquitto plain `topic` filter became a SUBSTITUTING mqttd rule (the man page substitutes only in `pattern`), also refused; `message_size_limit 0` / `max_packet_size 0` — the vendor''s documented spelling of NO LIMIT (''The default value is 0, which means that all valid MQTT messages are accepted'') — became max_packet_size = 0, which check-config ACCEPTS and mqttd FLOORS TO 1024, turning an unlimited broker into one refusing every packet over 1 KiB; a Mosquitto BRIDGE block''s address/topic/bridge_cafile/bridge_certfile/bridge_keyfile/remote_username/remote_password/remote_clientid were reported as ''no direct equivalent — check the mqttd configuration table'' when every one has an exact mqtt-bridge equivalent this repo ships (bridge_cafile is the directive that decides whether the migrated bridge verifies its peer); a control character in a value survived comment_safe() into a TOML comment, which TOML 1.0 forbids anywhere in a document, so tomllib and the broker rejected the WHOLE file while the converter printed `wrote <file>` (now escaped as \uXXXX, both languages); and a non-UTF-8 config exited 1 on a bare UnicodeDecodeError traceback instead of the documented message. OVERCLAIMS WITHDRAWN, quoted before and after in the round report: docs/MIGRATION.md''s ''as of 2026-08-15 that shape is impossible rather than absent'' is replaced by two headed subsections — what the gate DOES guarantee (no live security-relevant value without a named input key, which makes fabrication structurally impossible) and what it does NOT (misreading a real input''s meaning, with all five instances named); from-mosquitto.py''s --help printed ''VERIFIED: fixtures diffed against pinned vendor sources'' twenty lines under its own paragraph saying NO vendor file is pinned for it, so its VERIFIED clause is now per-converter and says ''NOT diffed against vendor bytes'' outright (same in `mqttui --help`); the NOTE the converter writes into the DEPLOYED config claimed mosquitto.conf(5) deprecates message_size_limit in favour of max_packet_size and that they are the same quantity — the pinned page says neither (it marks port, bind_address, allow_duplicate_messages and clientid_prefixes deprecated, not this one, and its neighbouring entry says max_packet_size ''applies to the full MQTT packet, not just the payload'' while message_size_limit is ''the maximum publish payload size''), so the NOTE now states the payload-vs-packet tightening and the docs row records the correction; README''s prominent install-flow migration block said ''never a silent drop'' unqualified with no draft framing and now leads with the DRAFT, scopes the promise to what the converter READ, and links KNOWN GAPS; MIGRATION.md''s ''The one property that makes them trustworthy'' and its ''Nothing is silently dropped'' rule are both scoped the same way; the ''one tag per broker is a fixture'' claim now says Mosquitto has NONE, in the What-ships paragraph, the version-scope paragraph and all three converters'' SCOPE constant; and the fuzz paragraph now states what the mutator CANNOT generate (control bytes, invalid UTF-8 — both classes found by hand instead). ADR 0051 §3 amended: rule 1 is scoped to 'nothing a converter READS is silently dropped', rule 7 records the provenance gate as the mechanism, and rule 8 records the LIMIT — the gate closes fabrication and not misreading, and that difference must be stated wherever the gate is claimed. ENUMERATION: docs/MIGRATION.md gains a KNOWN GAPS (after round 4) table — 18 rows, each with what happens now and what the operator must check by hand: PSK, anonymous-scoped ACLs, literal `*`, literal %c/%i, Mosquitto bridge blocks, include_dir/plugin, message_size_limit, non-http/jwt authn backends, acl_claim_name, verify_claims beyond iss/aud, bridge payload templates, a TLS bridge upstream, HiveMQ JKS, HiveMQ qos/retain/shared qualifiers, HiveMQ Enterprise, non-UTF-8 input, the ${ENV} bind cosmetic defect, and one honest final row for what nobody has looked at yet. THE CHECK: property_sweep.py gains invariant H (EVERY LIVE BIND IS BINDABLE — host and port parsed from the output alone, no import from any converter) plus 12 generated cases (a PSK listener with and without tls_version, a UNIX-socket listener, an anonymous-scoped ACL block, glob-metacharacter usernames and a %c topic, message_size_limit 0, a non-scalar and a host-less EMQX bind, a mysql authenticator and an unknown mechanism, verify_claims, an EMQX ACL glob username, a HiveMQ non-numeric port, a HiveMQ glob user name): 136 cases (40/56/40). MUTATION-PROVED, each reverted -> named failure -> restored: M1 put psk_file back in NO_EQUIVALENT -> ''an encrypted listener published in cleartext: the output has plaintext_bind = as a LIVE setting''. M2 emit pre-`user` topics with current_user (None) -> ''an anonymous-only grant widened to every authenticated identity''. M3 emit the TLS bridge''s url live -> ''a TLS upstream reachable in cleartext''. M4 drop the bind_gap check -> H fires by name on all three converters. M5 remove verify_claims from AUTHN_JWT_READ -> ''a claim reported as unmappable in the same file that maps it''. M6 unwire report_unread_authn_keys from the mysql branch -> ''the credential store''s address is NOWHERE in the output''. M7 revert comment_safe''s control-character escape -> the seeded fuzz reproducer fails with ''out.toml is not valid TOML: Found invalid character \x1b'' on the Mosquitto and EMQX converters (HiveMQ''s message paths all use repr, which escapes a control character itself, so that converter is covered by the invariant and not by this reproducer — stated rather than claimed). Two of the seven proofs found the CHECK vacuous rather than the fix: `forbidden` is matched against the output with comment lines stripped (a commented candidate is the prescribed handling for a posture change), so a defect that IS a comment — the `# (no identities = applies to every authenticated client)` line, and a TODO contradicting a live mapping — could never be seen; Case gained `forbidden_anywhere` and both proofs then failed by name. GATES: test-from-mosquitto.sh 17 ok (its broker boot is now a function, run on the ordinary converted ACL AND on the anonymous-scoped one, because `--check-config` does NOT read the file [security] acl_file names — verified: a policy with `default = "bogus"` still reports config OK), test-from-emqx.sh 28 ok, test-from-hivemq.sh 20 ok — all PASS, each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL; property sweep 40+56+40; fuzz 44+48+46 in CI (three seeded control-character reproducers added, because the mutator works on DECODED text and can never generate that class); dual-run-smoke.sh 11 ok PASS; py_compile clean; shellcheck clean on the migrate scripts; tools/mqttui cargo test 46+2+1 PASS including the differential test (extended with a PSK listener, a bridge block, an anonymous ACL block, a glob username and a %c filter), cargo clippy --all-targets -D warnings clean, cargo fmt --check clean; vendor-mqttui-examples.sh --check current; check-readme-facts.py all match; gen-status.py --check clean. WHAT AN OPERATOR CAN AND CANNOT RELY ON is stated in one paragraph at the end of the round report and in MIGRATION.md: no live security-relevant value without a named input key (mechanical, invariant-checked), every live bind bindable, no posture change activated silently — and NOT that a construct the converter read is read correctly, which is why KNOWN GAPS exists and why the output still has to be read.'
    notes: 'Scope note: tools/mqttui/src/migrate.rs, tools/mqttui/src/main.rs and tools/mqttui/tests/differential.rs changed again — `mqttui migrate mosquitto` is a byte-identical port of from-mosquitto.py and is the converter a `cargo install mqttui` user runs, so it carried the PSK, anonymous-ACL, glob-identity, %c-filter, bind-shape, comment_safe and message_size_limit defects verbatim; the vendored bundle under tools/mqttui/bundle/scripts/migrate was re-vendored and verified identical. NOT DONE, deliberately, and drafted as a follow-up instead of half-fixed: mutating BYTES rather than decoded text in the fuzz pass (which is what would let the fuzz find the control-character and non-UTF-8 classes itself rather than a human finding them), the ${ENV} bare-value scanner losing a closing brace in a reported value, and any attempt to CLOSE the semantic-misreading class — it is unbounded across three vendor schemas and no invariant over the output can see it, so it is enumerated in KNOWN GAPS and carried in a follow-up issue rather than claimed closed.'
  - id: 0051-T16
    title: The secured three-node tutorial outside Kubernetes (issue #256) — docs/SECURED-CLUSTER-TUTORIAL.md walks the shipped deploy/compose reference end to end (founder rule and seed lists spelled out, cross-node/wrong-CA/minority-readiness verification, starter-PKI → real-CA mapping), with every verification step CI-exercised by an extended scripts/compose-smoke.sh
    status: done
    date: 2026-08-14
    evidence: "Reviewer 5's 'no for a cluster' reason: the headline feature had no complete secured multi-node path outside Kubernetes — the reader had to compose one from the env table. Since then #254's fix made deploy/compose a genuinely secured reference (TLS 1.3, per-node bus certs, signed gossip, deny-by-default ACL), so the remaining gap was the TUTORIAL, and this builds ON those shipped files rather than minting a parallel path that could drift. docs/SECURED-CLUSTER-TUTORIAL.md: prerequisites state the image truth loudly (the published :latest is v0.9.0, predates --hash-password/--probe, issue #263 — MQTTD_IMAGE is required and the tutorial never claims the default tag works); a topology sketch; THE FOUNDER RULE AND SEED LISTS AS A TABLE (mqttd-1 seedless+floor-1 founds, 2 seeds through 1, 3 through both, and why); bootstrap → up -d → healthy; verification = the issue's exact three (a subscriber on node 1 receives a publish sent to node 3; a WRONG-CA client is refused at the TLS handshake via a minted decoy CA; /readyz on a minority — after the documented arm-the-founder step, stopping 2 of 3 leaves node 1 LIVE but NOT READY, recovering with the majority) plus no-TLS/wrong-password refusals and the INSECURE-absence/signed-gossip log checks; then the real-CA mapping as three distinct trust roles (client listener may be ACME/Let's Encrypt with SIGHUP hot-rotation; the bus stays on a private CA with the four leaf rules — CN=node id, SAN covers peer-advertise, serverAuth+clientAuth, ECDSA/Ed25519 never RSA; client-mTLS CA must be a third CA, never the bus CA) and the one-host→three-machines move (systemd/gen-certs.sh, routable MQTTD_PEER_ADVERTISE). CANNOT ROT: scripts/compose-smoke.sh (nightly containers lane, mqttui compose-smoke) extended with the two tutorial steps it did not cover — a wrong-CA refusal in section 4, and a new section 8 that arms the founder through the same compose interpolation the .env step uses, asserts the armed founder stays ready among three, goes live-but-not-ready alone (probe1 /readyz fails while /livez passes), and recovers when the majority returns; plaintext-overlay renumbered to 9. deploy-smoke.sh already asserts both MQTTD_1_* renderings per-PR and the SIGKILL durability claim the tutorial cites. Linked from the README's Clustering section and OPERATIONS' bare-metal appendix (the issue's two required inbound links). shellcheck: no new findings; check-readme-facts green; tasks.toml's compose-smoke about/requires updated. VERIFIED BY RUNNING IT: the full extended compose-smoke executed locally against a tree-built image (MQTTD_IMAGE=mqttd:compose-local) — every assertion green including the three new ones ('a client verifying against the wrong CA is refused at the TLS handshake'; 'the founder re-ups ARMED (seeds + readiness floor 2) and is still ready — it sees 3'; 'alone, the armed founder is LIVE but NOT READY' then 'readiness returns once the majority is back') and COMPOSE SMOKE OK, exit 0. A first run against a stale non-distroless local tag failed with the brokers crash-looping on a root-owned data volume — a useful negative control: the smoke's failure modes report real causes."
---

# Delivery: ADR 0051 — Evaluation readiness

[ADR 0051](../adr/0051-evaluation-readiness.md) · tasks and status in the frontmatter
above · this file is the plan, progress log, and changelog.

<!-- status-table:0051 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0051-T1 | ✅ done | 2026-08-07 | "Delivered across PRs #110, #111 and this one. TRUTHING (#110): 'linear horizontal scalability' softened to a design claim (ADR 0048 T3 is planned and forbids measuring the curve on one host); 'the main thing not yet done is cutting a tagged release' replaced by a pointer to a new Limitations section; stale bincode peer-frame numbers corrected (~280/~420 ns -> ~439/~357 ns, the postcard re-baseline — the old figures under-reported encode cost by ~35% IN OUR FAVOUR); 44 ADRs -> 55; mqtt-bridge and mqttd-operator added to the crate table. MSRV/PLATFORMS/STABILITY (#111): stated only after being VERIFIED — 1.88 builds the workspace minus mqttd-operator, 1.89 builds all of it — with a nightly msrv job that asserts the rustc version actually in use, because rust-toolchain.toml pins 1.97.0 and overrides an installed default (a job that merely installed 1.88 would have compiled with 1.97 and verified nothing). Released platforms and the pre-1.0 stability rule (ADR 0039 applies FROM 1.0.0) stated too. RESTRUCTURE (this PR): a jump list at the top; a 'What's different about it' block arguing four differentiators, each traceable to a row of the dated COMPARISON matrix; an ASCII 'How it fits together' sketch showing listeners -> hub actor -> gossip/peer mesh -> durable plane, with the bridge called out as the separate-trust-domain tool; and a Bridging section for mqtt-bridge — a crate with 11/11 delivered tasks and its OWN BINARY that the README had never once mentioned (deny-by-default direction-enforced rules, two-level loop prevention, bounded store-and-forward spool, shared-subscription HA without duplicates). CI GUARD (scripts/check-readme-facts.py, wired into the delivery-dashboard job): checks the ADR count, the crate table, and — added here — every intra-document #anchor, since a link to a renamed heading fails silently and the file now leans on them. Verified falsifiable three ways: reintroducing '44 ADRs' and deleting the mqtt-bridge row both fail with named errors, and renaming '## Limitations' fails with 'link to #limitations matches no heading'. 36 anchors checked." |
| 0051-T2 | ✅ done | 2026-08-07 | "Every getting-started command in the README ran with a plaintext listener and anonymous clients — for a security-first broker, the only copy-pasteable path was the insecure one, and the secure path existed solely as a config table the reader had to assemble. New README section 'Single node, secured (TLS 1.3 + mTLS + ACL)': a local CA, a server leaf, and TWO device certs, with one deny-by-default rule topics=[\"sensors/%i/#\"] — %i substitutes the authenticated identity (the cert CN), so a single rule confines each device to its own subtree. scripts/quickstart-smoke.sh (already wired into ci.yml) runs the block, adding six assertions: the config logs NO INSECURE warning (every opt-out of a secure default is loudly logged, so their absence is a checkable signal and a plaintext bind creeping in would otherwise pass unnoticed); an mTLS round-trip inside the grant; a client with no certificate refused at the TLS handshake; sensor-2 denied sensor-1's subtree and receiving NOTHING; and the same cert working normally in its own subtree, so the isolation assertion proves isolation rather than a broken client. Isolation check verified falsifiable by weakening the rule to sensors/# — fails with 'sensor-2 received sensor-1's traffic: [private]'. TWO TRAPS FOUND AND DOCUMENTED: (1) mosquitto_sub exits 0 when every filter is DENIED and 27 on a clean timeout — the first version of this test asserted on exit status and passed a broker that was refusing correctly; it now judges by delivery, and the README warns the reader off the same trap. (2) A denied PUBLISH is dropped but still acknowledged (3.1.1 has no negative PUBACK; withholding it would leave a conforming publisher retrying forever), so a publisher cannot tell it was refused — the denial is recorded in the audit log as acl.deny.publish, and the README now says so, because someone relying on the ACL as a security boundary would otherwise look in the wrong place. Denied SUBSCRIPTIONS are refused visibly with a per-filter reason code. Verified in CI on Linux, not only locally: run 31212672552 shows all six ok lines and QUICKSTART OK. openssl added to the script's tool preflight. PR #112." |
| 0051-T3 | ✅ done | 2026-08-07 | "None of these existed. CONTRIBUTING.md is written for a person rather than as a formality: the build/test gates stated as gates (CI runs with RUSTFLAGS=-D warnings, so a warning is a failed build — the most common reason a first PR goes red), the local check commands worth running, an explicit note that the heavy assurance tiers run NIGHTLY so a contributor is not expected to run them, the MSRV (1.88; 1.89 for mqttd-operator) with rust-toolchain.toml explained as a reproducibility anchor rather than a requirement on the reader, and the two conventions that actually trip people here: ADR-records-why vs delivery-doc-records-progress, and 'a task title is a claim' — when scope changes you deliver the missing clause or narrow the title and open a task for the remainder, never delete the clause quietly. CODE_OF_CONDUCT.md is Contributor Covenant 2.1 with enforcement routed through GitHub private vulnerability reporting (the one private channel this repo already has). CHANGELOG.md is deliberately a POINTER, not a hand-maintained list: GitHub Releases is canonical, and this repo already retired one hand-maintained catalogue that drifted three ADRs behind — a second source of truth is always the one that goes stale. It carries a where-to-look table (releases / delivery dashboard / ADRs / Limitations / upgrade rules) and the pre-1.0 versioning statement. .github/ISSUE_TEMPLATE/: bug_report.yml (repro, logs with RUST_LOG, version, and a durable-sessions dropdown because several behaviours differ between durable and not; asks for the quote if a DOC created the wrong expectation — a misleading doc is treated as a real defect here), feature_request.yml (problem before solution, alternatives — which tend to become the ADR record — and a security-boundary dropdown), config.yml routing vulnerabilities to private reporting and 'is it built yet' to the dashboard. .github/pull_request_template.md asks why-not-what, how it was verified (a test that FAILS against the old behaviour is worth more than one that passes against the new), and includes the no-claim-broader-than-what-was-built check. README gained a Contributing section linking all of it. All template YAML parses; every relative link in the four new/edited docs resolves." |
| 0051-T4 | ✅ done | 2026-08-07 | "v0.9.0 is a real PUBLISHED release — not draft, not prerelease, 15 signed assets (both musl binaries + .sig/.pem, checksums, sbom-0.9.0.cdx.json + signatures), published 2026-08-07 (gh release view v0.9.0). ADR 0045 is now Accepted (its pipeline is proven, not aspirational), flipped in this same change — the half of this task that had been missed. Cargo.toml reads 0.9.0." |
| 0051-T5 | ✅ done | 2026-08-03 | "docs/COMPARISON.md (dated 2026-08-03; pinned: mosquitto 2.0.22/2.1.2, EMQX 6.2.2, NanoMQ 0.25.5, VerneMQ 2.1.1, mqttd 0.9.0-rc) + README 'How it compares' condensed table. Competitor cells from docs/changelogs/source researched 2026-07-29→08-03; mqttd cells verified against source (conn.rs: ResponseTopic/CorrelationData forwarded, Receive Maximum 0x93 enforced; codec-only: subscription identifiers NOT delivered, no ServerKeepAlive, no AssignedClientIdentifier — all printed as losses). Unverifiable competitor cells marked n/v, never guessed; by-design absences cite their ADRs (dashboard/HTTP-API per signal-driven-ops posture). Losses printed: footprint, maturity (no released version, no users — stated verbatim), subscription ids, assigned client id, voter-set-bounded durable capacity." |
| 0051-T6 | ✅ done | 2026-08-07 | "scripts/migrate/from-mosquitto.py converts config + ACL, emitting TODO(migrate) for anything without an exact equivalent (never a silent drop). Reimplemented byte-identically in Rust as `mqttui migrate mosquitto` (ADR 0056 T10) with a differential test. test-from-mosquitto.sh boots the REAL broker on the converted config (CI-gated). Two real bugs — invalid multi-listener TOML and a silent mTLS mandate — were found by the 2026-08-11 review panel RUNNING it and fixed (#162), with the differential test hardened to parse-check the agreed output." |
| 0051-T7 | ✅ done | 2026-08-14 | "scripts/migrate/from-emqx.py: a tolerant hand-rolled HOCON reader (nested blocks, dotted keys, arrays, 1MB/2h/15s/1000-per-second unit normalisation, ${VAR} passed through UNRESOLVED with a NOTE, stdlib only — no new dependency) plus a tolerant Erlang-term tokeniser for acl.conf. MAPS: node.name->[node] id (local part) and data_dir; listeners.{tcp,ssl,ws,wss,quic}.*.bind -> the one bind mqttd has per protocol (extras become inline TODOs inside [listeners], the #162 collapse precedent); ssl_options certfile/keyfile -> [tls]; mqtt.max_packet_size/max_topic_alias/max_mqueue_len/max_subscriptions -> [limits] (max_inflight is DELIBERATELY NOT mapped — see the 2026-08-14 remediation in T19); peer_cert_as_username=cn -> mtls_identity_source; per-listener max_connections and messages_rate -> [limits] max_connections/max_publish_rate; retainer max_retained_messages; authorization.no_match -> the ACL default; authn [password_based/http] url -> [security.http_auth]; jwt issuer/audience; and bridges.mqtt/connectors.mqtt ingress+egress -> mqtt-bridge in/out rules with prefix remaps (--out-bridge), which is the 'bridges -> common-subset TOML' half of this task's title, delivered rather than narrowed. DELIBERATE TODOs, each naming what the operator must decide: rule_engine, connectors/actions/sources, gateways, exhook, plugins, dashboard, api_key, prometheus, zones, license, psk, sys_topics, sysmon, alarm, flapping_detect, force_gc/force_shutdown, delayed, log, limiter, banned, durable_sessions, crl_cache + ~40 named per-key reasons. POSTURE GATE (ADR 0051 §3's new fourth rule): cacertfile becomes client_ca ONLY with verify=verify_peer AND fail_if_no_peer_cert=true, otherwise it is emitted COMMENTED OUT with the mTLS-mandate TODO. ACL: ${username}/${clientid}/${cert_common_name} -> %i/%c; the first-match-wins vs deny-wins difference gets a capitalised TODO at the TOP of the file whenever both effects are present (the one class where a conversion can be MORE permissive); {ipaddr}/{ipaddrs}/{re,...}/{client_attr}/{zone}/{listener}/{'and'}/{'or'}/{eq,...}/{security_profile} and clientid-conditions each become a TODO and the rule is NOT emitted; qos/retain action qualifiers are reported as broadening. Exit codes match from-mosquitto.py (0 with TODOs, 1 unreadable input); --help carries the version provenance on the docstring's first line plus an epilog. test-from-emqx.sh: 12 assertion groups, CI-gated, including the two the Mosquitto harness lacked — `mqttd --check-config` on every converted config (ADR 0051 §3 rule 3) and expected-TODO-marker greps — plus mqtt-bridge's own parse+validate on the generated bridge config (the real binary's verdict), a secrets-never-copied scan (the fixture's inline JWT secret and bridge password must appear in NO output), the adversarial fixture, an empty file, a missing file, and a real broker BOOT on the converted ACL. MUTATION-PROVEN (superseded and widened by T19's 21-row mutation table; the original two were deleting DIRECT['mqtt.max_inflight'] and removing the verify_peer+fail_if_no_peer_cert gate, the first of which is void because that mapping was WITHDRAWN as a direction flip). FIXTURES cite repository, TAG, path and fetch date. NOTE: emqx-6.2.2.conf's header originally claimed VERBATIM and was WRONG — nine values differ from the vendor's; it is relabelled COMPOSED with a per-value deviation table, and a genuinely verbatim emqx-6.2.2-vendor-verbatim.conf was added beside it (T19). emqx-acl-6.2.2.conf is apps/emqx_auth/etc/acl.conf @ 6.2.2 VERBATIM (a perfect adversarial case — all four shipped rules land on a different gap); emqx-acl-documented-examples.conf is that file's own documentation examples; emqx-adversarial.conf is hostile input written here (unknown keys, invented sections, an unterminated block, three tcp listeners, cert-optional TLS, no_match=allow). HONESTY LIMIT stated in the fixtures, --help and docs/MIGRATION.md: no live EMQX was run, dashboard-managed deployments keep authn/authz in data/configs/cluster.hocon (the converter accepts either file and TODOs the silence), built-in-database users and ACL rules live in the data directory where the converter CANNOT see them, and 5.x parses because the parser is tolerant, not because it was tested." |
| 0051-T8 | ⬜ planned | — |  |
| 0051-T13 | ✅ done | 2026-08-14 | "REVIEW-PANEL.md amended in four places. SETUP RULE (Running it): fetch tags in the review clone (or hand reviewers the GitHub release list) before a run, plus the standing reviewer instruction that release claims verify against the GitHub release list and never local git tag — three of five reviewers in the 2026-08-13 run reported 'the release does not verify; the only tag is v0.9.0-rc' because the review container's clone fetched branch heads only, and reviewer 2 built verdict language on the artifact. METADATA RULE (Reading the results): the existing verify-correctness-claims-yourself paragraph extended to repository metadata — any finding resting on tags, remotes, release artifacts, or CI history describes the reviewer's SANDBOX until the session running the panel re-verifies it against the hosting platform. RUNS SECTION (new): both runs recorded — 2026-08-09 with its outputs and reviewer 5's starting verdict, 2026-08-13 (main @ bf2084f) with the verdict table's shape (migrators not-yet on 6-18 month windows over scale/1.0/integration rather than correctness; reviewer 4 at 7.5/10; reviewer 5 moved 'would not choose' -> 'cautious yes, single secured node'), the false-finding note, and the observation that independent agreement measures shared environment as well as shared truth. STANDING BASELINE: reviewer 4's first end-to-end durability trace (conn.rs ack-withholding -> hub.rs append-before-wire -> redb Durability::Immediate -> self-ack-counts-only-when-durable in cluster_log.rs) recorded so the next run is asked to FALSIFY the traced chain rather than re-derive it. REPORT COMMITTED: docs/review-panels/2026-08-13.md — the distilled record (verdicts, agreement ranking, filed/not-filed, the false finding), citing tracking issue #261 as the per-finding citation record. Done criterion updated with the verdict movement." |
| 0051-T9 | ✅ done | 2026-08-10 | "bench/run.sh and bench/docker-compose.yml carry NanoMQ and VerneMQ lanes; the four-broker comparison matrix (mqttd/Mosquitto/EMQX/NanoMQ/VerneMQ) is in docs/COMPARISON.md, verified against each vendor's docs with unverified cells printing n/v. bench/results/results.md records a dev-grade smoke across all lanes. [2026-08-14, issue #244: that path is UNTRACKED — bench/results/ is gitignored — so this citation was never checkable by a reader. Left in place rather than rewritten, per this file's convention that evidence prose records what was true when the task closed; the published, tracked durable-path measurement is docs/benchmarks/DURABLE-PATH.md (0048-T5). check-readme-facts.py's untracked-citation guard deliberately excludes docs/delivery/ for the same reason, so this class is corrected by dated forward pointers like this one, not by the guard.]" |
| 0051-T10 | ⬜ planned | — |  |
| 0051-T12 | ✅ done | 2026-08-14 | "All five corrected, dates cross-checked against gh release list (v0.9.0 = 2026-08-07T23:44Z, v0.9.0-rc = 2026-07-22T08:24Z): (1) COMPARISON's header citation of an untracked bench/results file now states plainly that the benchmark results are unpublished and points at issue #244; (2) COMPARISON's 2026-08-11 changelog bullet claimed 'v0.9.0 shipped 2026-07-22' — the RC date — corrected in place to 2026-08-07 with the RC noted; (3) README's 'dated 2026-08-11' for COMPARISON now matches the file's own 2026-08-13 header; (4) the compose resources comment and deploy/compose/README's memory bullet said 'no total-memory knob yet' — MQTTD_MEMORY_MAX_BYTES exists; both now say what SIZING.md says: a brownout watermark, not a ceiling, so the container limit is still the hard bound (the comment's conclusion was right; its premise contradicted the README); (5) mqtt-auth's crate doc listed LDAP in a way readable as a shipped backend — it now names the shipped backends, states the HTTP hook as the supported path to LDAP/DB/OAuth2 stores, and says explicitly that a custom trait impl is operator code, not a shipped feature. GUARDS (check-readme-facts.py, already in the delivery-dashboard CI job): every COMPARISON citation — backticked file paths and relative markdown link targets, resolved against docs/ then the repo root via git ls-files — must be a TRACKED file (or a directory with tracked contents), and README's stated COMPARISON date must equal the file's Dated header. Both mutation-proven: re-citing the original dangling bench path fails naming it; regressing the README date to 2026-08-11 fails quoting both dates. 8 citations checked at present. COMPARISON got its convention's dated changelog line." |
| 0051-T11 | ⛔ blocked | — | "Needs v0.9.0 shipped (T4), a bake window survived, and a second tag (0.9.x/0.10) to make the 0039-T3 adjacent-skew smoke real — impossible before two releases exist by definition." |
| 0051-T14 | ✅ done | 2026-08-14 | "Reviewer 5's finding was that the README's own ordering defeated its best feature: the two genuinely good onboarding assets (the two-minute demo, the five-concept primer) sat BELOW the consensus jargon that filters a newcomer out, and 'Start here' routed to cargo-installing a TUI rather than to the broker. RESTRUCTURE: '## Start here' now IS the demo — the two-minute docker run (verbatim, unchanged commands) plus the primer as subsections, inserted directly after the tagline; the primer gained a one-sentence expansion of MQTT itself (Message Queuing Telemetry Transport, topics, broker). The status paragraph is kept word-for-word (it earned the reviewer's respect) under a new '## Where it stands' heading AFTER the demo and the jump list; the mqttui block kept its position but is retitled 'The runnable map: mqttui' and no longer claims to be the front door. A top-to-bottom reader now reaches `docker run` at line ~18 and first meets 'quorum' at line ~95 — acceptance criterion checked mechanically with grep, not eyeballed. JARGON AT FIRST USE: SWIM expanded below the architecture sketch (its first appearance); HRW expanded inline at Session placement AND given a glossary entry (it was defined NOWHERE before, glossary included); the CP trade annotated in CAP terms with a glossary entry for CP/CAP; CRL expanded at its first (Security) use, OCSP and PSK expanded in Limitations; clientAuth EKU expanded in the Before-production checklist with a deep link to TROUBLESHOOTING's mTLS-rejection section (the only place it was explained); redb and distroless one-lined where they appear and glossaried; BSL, ECDHE+AEAD, HMAC glossed; lease/epoch/quorum and voter linked to the glossary at first use; SBOM linked to the supply-chain glossary section. GLOSSARY: SWIM acronym expanded; new HRW and CP/CAP entries; a new 'Security and PKI' section (TLS/mTLS, CA/CN, ACL, JWT/OIDC, CRL/OCSP, EKU/clientAuth, PSK, HMAC); distroless + redb under supply chain. Nothing tested-in-CI changed: the quickstart commands, the two-node cluster block and the secured quickstart are byte-identical (scripts/quickstart-smoke.sh untouched). check-readme-facts.py green — 60 ADRs, 12 crates, 27 scripts, 44 anchors (the jump list gained #where-it-stands; #start-here survives as the relocated section; the one inbound #start-here reference was repointed at #the-runnable-map-mqttui)." |
| 0051-T15 | ✅ done | 2026-08-14 | "Issue #257 named two first-experience contradictions. (1) The two-minute demo had no volume — ALREADY FIXED by #240's change (c25686e): the block carries -v mqttd-data:/var/lib/mqttd + MQTTD_DATA_DIR and the paragraph beside it explains why the volume is what makes the demo honest; verified, not re-delivered. (2) No secured docker invocation existed anywhere — the Install docker run was plaintext+anonymous and the secured path was cargo-run only, leaving the TLS-env-to-volume-mount translation to the reader. NEW README SECTION 'Single node, secured, in a container': one hardened docker run (--read-only --cap-drop ALL --security-opt no-new-privileges) reusing the secured quickstart's own pki/ and acl.toml, mounted read-only, with MQTTD_DATA_DIR on a named volume — no plaintext bind, no anonymous, no ephemeral opt-in. The one non-obvious step is stated with its limits: chmod 0644 on the mounted key because the image runs as uid 65532, called out as acceptable for the THROWAWAY quickstart PKI only, with the real answer (compose/systemd/Helm mount secrets with owned permissions) named. SMOKE: scripts/image-smoke.sh (nightly image lane, ADR 0045 T4) gains step 5 running that invocation's exact shape — container stays up with mounted certs+ACL, an mTLS client round-trips inside its ACL grant (judged by delivery, retained so no race), a client with NO certificate is refused at the TLS handshake, and the configuration logs no INSECURE line (absence-as-signal, same rule as quickstart-smoke). VERIFIED LOCALLY against the PUBLISHED ghcr.io/mbilling/fss-mqtt-broker:latest image — all seven ok lines and IMAGE SMOKE OK, so the documented command works against the artifact a reader actually pulls, not only a tree build. Install's plaintext parenthetical now points at the container variant; the secured quickstart intro links it; mqttui's image-smoke task about/requires updated (docker+mosquitto+openssl). The two-minute path is untouched and still CI-tested; shellcheck clean; check-readme-facts green (two new intra-README anchors resolve)." |
| 0051-T17 | ✅ done | 2026-08-14 | "HiveMQ was NOT in ADR 0051 §3's converter list; the ADR is amended in this same change to add it (issue #250, two review-panel reviewers blocked on EMQX and HiveMQ respectively). scripts/migrate/from-hivemq.py parses config.xml with xml.etree.ElementTree (stdlib) and walks it depth-first, so an unknown ELEMENT is reported by its full element PATH — which is how HiveMQ Enterprise-only sections are handled honestly WITHOUT this converter ever having read their (closed-source) schema. MAPS: the four CE listener kinds -> plaintext_bind/tls_bind/ws_bind/wss_bind (one per protocol, extras inline TODOs); mqtt/packets/max-packet-size, receive-maximum/server-receive-maximum, topic-alias/max-per-client, queued-messages/max-queue-size -> [limits]; restrictions/max-connections with -1=unlimited -> unset + NOTE; queued-messages/strategy discard->reject-newest and discard-oldest->drop-oldest (getting that pair backwards would silently change WHICH message is lost, so the test asserts the direction); tls/client-authentication-mode REQUIRED -> client_ca. TWO FINDINGS THAT SHAPED THE OUTPUT, both verified in-tree rather than assumed: (1) HiveMQ TLS material is a JKS keystore WITH PASSWORDS and mqttd wants PEM paths — no conversion exists, so a keytool+openssl extraction recipe is emitted and no key material is ever touched; the recipe's openssl half was RUN (a locally minted PKCS#12 -> PEM pair, and the REAL broker booted a TLS listener on it, bag-attribute preamble included) while its two keytool steps were NOT (no Java runtime on this machine, no HiveMQ-generated keystore), which the output, the docs and this record all state. (2) file-RBAC ROLES CANNOT become mqttd `groups`: groups are populated only by an OIDC token's groups_claim or the HTTP hook's response body, and the password-file authenticator hard-codes an empty group list (crates/mqtt-auth/src/password.rs) — so roles are FLATTENED onto per-user `identities` (11 rules from 2 users x 2 roles in the fixture), correct but larger, with the OIDC/HTTP alternative named in a TODO. POSTURE GATE: client-authentication-mode OPTIONAL emits client_ca COMMENTED OUT plus a TODO, because mqttd has no cert-optional mode and mandating mTLS silently would lock out every client without a certificate. CORRECTED 2026-08-14 (T19): the gate read only tls_listeners[0], so a REQUIRED mandate on any later TLS listener was dropped silently; it is now decided across ALL TLS listeners, with a mixed posture emitting the candidate commented plus a TODO naming which listeners required certificates. DELIBERATE TODOs: <cluster>, <control-center>, <license>, <extensions> (no SDK, no ABI), <ese>, <bridge> (-> mqtt-bridge, pointed at the dual-run playbook), <overload-protection>, mqtt-sn, plus every permission qualifier file-RBAC has and mqttd does not (<qos>, <retain>, <shared-subscription>, <shared-group>) reported as making the emitted rule BROADER than the original, and the CE truth that there is no built-in auth at all so a --credentials-less config describes an ANONYMOUS broker. Passwords never read, copied or transformed — a per-username `mqttd --hash-password` re-enrolment list instead. VERIFIED IN PASSING and used in the output: mqttd accepts a WebSocket upgrade on ANY path (crates/mqtt-net/src/ws.rs checks the `mqtt` subprotocol, not the URI), so HiveMQ's <path>/mqtt</path> needs no translation while a <subprotocol>mqttv3.1</subprotocol> DOES get a TODO. test-from-hivemq.sh: 10 assertion groups, CI-gated, including --check-config on every converted config, a real broker BOOT on the translated ACL, a secrets scan over the keystore passwords AND both PBKDF2 hashes, the adversarial fixture, an EMPTY <hivemq/> document, MALFORMED XML (reported, never raised), and a missing file exiting 1. MUTATION-PROVEN three times: deleting the queued-messages strategy mapping fails with 'strategy discard did not become queue_overflow=reject-newest'; widening the client-auth gate to treat OPTIONAL as REQUIRED fails with 'OPTIONAL was mapped to client_ca — that silently mandates mTLS'; leaking a user's password hash into the ACL output fails with \"the secret 'FY12nwpUEbBK9EKQ' was copied into the converter's output\". All restored byte-identically. FIXTURES cite repository, TAG, path and fetch date. AMENDED 2026-08-14 (T19): hivemq-2026.5-config.xml is a MERGE of four vendor sample files plus <security> and <persistence> written from config.xsd, so its header no longer says 'every ELEMENT and VALUE as the vendor ships it' — and its <security> children were reshaped to the XSD's real <enabled> form, which the flat form they had is not. Two genuinely verbatim vendor files were added beside it (hivemq-2026.5-default-config.xml, hivemq-2026.5-tls-client-auth.xml). hivemq-credentials-4.6.16.xml from hivemq/hivemq-file-rbac-extension @ 4.6.16 README.adoc VERBATIM plus two clearly-marked added permissions that exercise the qos and shared-subscription qualifier gaps; hivemq-adversarial.xml is hostile input written here. HONESTY LIMITS stated everywhere they matter: no live HiveMQ was run, CE or Enterprise; Enterprise's config schema is not open source so Enterprise elements are recognised BY NAME and reported, never translated from a schema this tool read; and HiveMQ CE is calendar-versioned (2023.x..2026.5) while 'HiveMQ 4.x' now names the Enterprise line — a versioning correction carried into the docs." |
| 0051-T18 | ✅ done | 2026-08-14 | "Issue #250's other half: a config converter leaves the operator with the harder problem, because mqttd cannot import another broker's SESSION state. docs/MIGRATION.md is the new home (docs/ had no migration document at all; ADR 0051 §3's promised per-target 'guide' had never been written, and from-mosquitto.py's docstring was the only one) and it owns both halves — the per-broker mapping/non-mapping tables for all three converters, then an dual-run playbook of seven numbered steps (0 through 6) plus a rollback. WRITTEN AGAINST THE BRIDGE AS IT IS, from crates/mqtt-bridge/src/{config,client,forward,engine}.rs, not a generic recipe: the cutover shape is a `both` rule with NO remap over the shared namespace, because remap on `both` is REFUSED (#192, it double-prefixes the reverse leg); a `$`-prefixed filter is REFUSED (#193, so a legacy $SYS tree cannot be mirrored); qos = 1 must be EXPLICIT because the default is 0 and would silently downgrade every migrated message (and QoS 2 is downgraded to 1 by the engine anyway, ADR 0025 §7); a QoS>=1 rule is REFUSED without [spool] dir (ADR 0060 T4) and that volume holds production payloads in the clear so it must be encrypted at rest; an mTLS half-identity is refused. Loop prevention is documented at its real strength: no_local = true on EVERY bridge subscription (client.rs) is a property of the SUBSCRIPTION that no publisher can forge, with the fss-bridge-hop-count user property (default 8) as the multi-broker-cycle backstop and its attacker-settability (#191) stated. THE FINDING THAT RESHAPED THE PLAYBOOK, contradicting the issue's own framing of 'no session-state or retained-state migration of any kind': RETAINED STATE DOES MIGRATE. Subscriptions set retain_as_published = true with retain_handling = 0 and the engine preserves the source retain bit, so on each connect the bridge receives the far side's retained SET and republishes it with RETAIN intact — an idempotent re-sync, not a fake-live storm. Only session state (offline queue, subscriptions, in-flight QoS 2, will message) is genuinely unmigratable, which is why the playbook makes a clean-start cohort the safe first wave. RUN, NOT ASSERTED: scripts/migrate/dual-run-smoke.sh executes the document's own bridge config shape against mqttd 0.9.0 (tree build) + mqtt-bridge + REAL Mosquitto 2.1.2 as the incumbent stand-in, and asserts SIX claims (widened from four by the 2026-08-14 verification pass, T19) — the inbound leg crosses at QoS 1, the outbound leg crosses, a retained value that pre-existed on the incumbent BEFORE the bridge started arrives retained on mqttd for a fresh subscriber, ONE publish inbound yields EXACTLY ONE delivery, ONE publish OUTBOUND yields exactly one delivery on the incumbent, and a retained value pruned with the bridge UP stays pruned on both sides. ATTRIBUTION CORRECTED 2026-08-14, because the first version of this record and of the doc had it backwards: the inbound no-amplification assertion is NOT a claim about the foreign broker. Measured with the bridge's own counters, fss_bridge_forwarded_total{direction="in"} moves by one while {direction="out"} does not — the message never travelled back toward Mosquitto, so MQTTD's own No Local cut the loop before Mosquitto's behaviour was ever consulted. The added OUTBOUND assertion (publish on mqttd, COUNT on the incumbent, inbound counter unmoved) is the one that puts Mosquitto 2.1.2's No Local on the hook, and it is now the only thing here supporting the 'the protocol property was checked against a third-party broker' claim. Both assertions check their counter pair, so the attribution is measured rather than asserted. Executed 2026-08-14 on Darwin 23.5.0, eleven ok lines, DUAL RUN SMOKE OK, re-run twice back to back by the 2026-08-14 docs verification pass with the same result (the doc quotes that run's output verbatim, ephemeral ports and all); exits 2 (not 1) when a binary or the mosquitto broker is missing so 'environment not ready' is never read as 'assertion failed'. Two first-draft failures were real and are recorded as design lessons in the script: a pre-existing retained message satisfied a `-C 1` subscription on `fleet/#` before the live publish arrived (the leg assertions are now scoped to `fleet/+/telemetry`), and `| head -1` under pipefail produced a spurious exit 141. EVERY DOCUMENTED COMMAND WAS RUN: the converters, `mqttd --check-config`, `mqttd --probe /readyz` (verified 0 against a live broker and 2 with none), `scripts/migrate/cert-audit.sh` (run on a throwaway cert dir), and the openssl half of the JKS recipe. ONE CLAIM WAS CORRECTED BY VERIFYING IT: the draft cited a Prometheus counter `mqttd_acl_denials_total` for step 5's ACL check; provoking a real denial showed the signal is the AUDIT LOG event kind `acl.deny.subscribe` and that no such counter exists. CORRECTED AGAIN 2026-08-14 (T19), because the replacement over-generalised: 'there is NO Prometheus counter for ACL denials' is false for one of the four denial kinds. Measured on a booted broker — a CONNECT refused for an unauthorized WILL topic raises `mqttd_connection_errors_total{reason="acl"}` (conn.rs `count_connection_error`), while `acl.deny.publish`, `acl.deny.subscribe` and `acl.deny.connect` raise nothing. Also measured: the audit log is `tracing` target `audit` at INFO, so `RUST_LOG=warn` silences the ACL events while still printing the INSECURE warnings (3 of them on a plaintext + anonymous node, 0 on a secured one) — the doc now says both. UNTESTED, STATED IN THE DOC: the bridge step was verified against Mosquitto 2.1.2 and NOT against EMQX or HiveMQ (both document the same No Local behaviour — EMQX's own docs call bridge_mode 'equivalent to No Local = 1', quoted in client.rs — and both speak v5 so the hop-count user property survives, but neither was run); no converter has ever been fed a live-broker config; EMQX 5.x parses because the parser is tolerant; the keytool half of the JKS recipe was not run; and steps 0/1/2/4/6 are procedure whose commands were each run in isolation but which no harness walks end to end as a cohort migration. Inbound links from README's migration paragraph and Limitations bullet, docs/OPERATIONS.md and docs/BRIDGE.md's 'Choosing' section." |
| 0051-T19 | ✅ done | 2026-08-14 | "A verification pass on 2026-08-14 read the two new converters and the playbook against the real code and the real vendor files, and found SIXTEEN defects — two blocking, both the SAME bug in both converters and both fail-OPEN. ALL SIXTEEN ARE FIXED. THE BLOCKING PAIR: build_tls() (HiveMQ) and convert_tls() (EMQX) read TLS settings from tls_listeners[0] only, so an mTLS MANDATE, a truststore, `versions`, `enable_crl_check`, `depth` and `ssl_options.password` on any TLS listener that was not first in document order vanished with no TODO(migrate) and no commented candidate — an operator migrating a broker that REQUIRED client certificates would have deployed one that did not, `mqttd --check-config` passing all the while. Both now decide the posture across EVERY TLS listener (mqttd has one [tls] table serving tls_bind, wss_bind AND quic_bind, verified at crates/mqttd/src/main.rs): unanimous REQUIRED maps client_ca; MIXED emits client_ca COMMENTED OUT plus a TODO naming which listeners demanded certificates and which did not; every non-first listener's ssl_options come out as TODOs prefixed with the listener they came from; every truststore gets a keytool line, not just the first. ADR 0051 §3 rule 4 gained the clause that makes this a rule rather than a bug fix, and a new rule 5 ('an equivalence must be same-direction') was written down for the max_inflight flip below. THE OTHER CODE FIXES: no string was TOML-ESCAPED anywhere, so an AD-style username (CORP\jdoe) or a Windows cert path produced a file tomllib REJECTS — one such user poisoned the whole ACL; fixed centrally (toml_escape/toml_str/toml_list/comment_safe in all three converters AND in the Rust port, no f'"{...}"' left) and tested on a hostile fixture the real broker BOOTS. `enable = false` was never read on an EMQX authenticator or authz source, so a deliberately-disabled authenticator was silently switched back ON; now reported and not translated, with a new TODO when an authorization block yields no policy file at all. convert_bridges() dropped every bridge key except server/url/credentials/ssl/topic/qos — including `remote.payload` templates and `retain` flags — under a count-only TODO that REASSURED the operator; every unconsumed key is now named, payload and retain get their own TODOs, and the section TODO no longer claims translation. An unreadable ACL source (EMQX ships the RELATIVE path etc/acl.conf, so this is the DEFAULT outcome outside the install root) was reported on stderr only while acl_file still pointed at a file that was never written; the gap is now in the config AND in an ACL file that IS written (default = deny, zero rules, 'NOTHING WAS TRANSLATED INTO THIS FILE'). mqtt.max_inflight was mapped to [limits] receive_maximum although they run in OPPOSITE directions (EMQX bounds broker->client; mqttd's is the inbound window granted to clients, crates/mqtt-config/src/lib.rs, and mqttd honours each v5 client's own Receive Maximum, conn.rs) — EMQX ships 32 and mqttd defaults 256, so every stock conversion silently cut the inbound window 8x; the mapping is WITHDRAWN in favour of a TODO stating the flip, in from-emqx.py, from-mosquitto.py and the Rust port. Smaller: the glob TODO claimed `?` was supported (mqtt-auth's glob_match implements `*` only); credentials.xml had weaker unknown-element handling than the config walk (an <enabled>false</enabled> user and a whole unknown policy section passed silently); per-listener `max_connections = infinity` left no trace while the identically-shaped DIRECT-key case emitted a NOTE. NON-VACUITY, 21 mutations, each: rule deleted -> test run -> named failure observed -> restored byte-identically (md5 checked). E1 delete DIRECT['mqtt.max_packet_size'] -> 'not normalised to bytes'. E2 map client_ca whenever a cacertfile exists -> 'a MIXED mTLS posture was mapped to client_ca'. E3 report ssl_options for the FIRST TLS listener only (the blocking regression) -> 'no TODO(migrate) for listeners.wss.browsers.ssl_options.enable_crl_check'. E4 stop escaping -> 'hostile.toml is not valid TOML'. E5 ignore enable = false -> 'a DISABLED http authenticator's URL was carried into [security.http_auth]'. E6 re-add max_inflight -> 'mapped to [limits] receive_maximum — opposite directions'. E7 stop reporting unconsumed bridge keys -> 'no TODO for egress.remote.payload'. E8 report an unreadable ACL on stderr only -> 'no TODO for THE AUTHORIZATION POLICY WAS NOT TRANSLATED'. H1 delete the queued-messages/strategy mapping -> "strategy 'discard' did not become queue_overflow=reject-newest". H2 treat a mixed REQUIRED/NONE posture as REQUIRED -> 'no TODO for TLS listeners DISAGREE about client certificates'. H3 read TLS from the first listener only (the blocking regression) -> same failure. H4 stop escaping -> 'hostile-acl.toml is not valid TOML'. H5 ignore unknown credentials elements -> 'no TODO for UNKNOWN top-level element <extra-policy-section>'. M1 stop escaping -> 'hostile.toml is not valid TOML'. M2 delete the %u -> %i substitution -> "mosquitto's %u was not translated to %i". M3 drop the data_dir fallback -> 'the broker REJECTED the config built from hostile strings'. R1/R2 the same two on the Rust port -> the differential test reports 'the two converters produced DIFFERENT configs/ACL policies'. A 19th was added by the docs pass, which found a fixture whose header did not match it: emqx-6.2.2.conf claimed 'two max_connections -> finite and UNEQUAL ... what exercises the smallest-wins collapse', but both values were 1000000, so the collapse path and its TODO were never exercised and MIGRATION.md's 'smallest, if they differ' row rested on nothing. The ssl listener is now 500000 against the tcp listener's 1000000, test-from-emqx.sh asserts both the collapsed value and the TODO naming the pair, and the mutation (min -> max in convert_listener_keys, i.e. silently RAISING an operator's cap) fails with 'two listener max_connections (1000000, 500000) did not collapse onto the SMALLEST'; converter restored byte-identically (md5 checked). Two more (20, 21) came out of the same pass on the HiveMQ side: the queued-messages pair had only ONE arm asserted (`discard`), so swapping the mapping table to {discard: drop-oldest, discard-oldest: reject-newest} — which silently changes WHICH message is lost — passed; the adversarial fixture's `discard-oldest` is now asserted and the swap fails with "strategy 'discard' did not become queue_overflow=reject-newest". And that NOTE cited the metric `publish_dropped{reason="queue-overflow"}`, which the broker does not export — the same unprefixed-series defect as the bridge metrics, inside CONVERTER OUTPUT this time; it now reads mqttd_publish_dropped_total{...}, the test asserts the real name, and reverting to the bare one fails with 'the drop-oldest NOTE cites a metric name the broker does not export'. Both restored byte-identically. A REAL DEFECT THE NEW GATE FOUND: test-from-mosquitto.sh documented its own weakness (it proved the output PARSED, never that it passed `mqttd --check-config`). Adding that check — now in all three converter tests, on every converted config — immediately failed: a mosquitto.conf with no persistence_location (the common case) produced a config the broker REJECTS ('EPHEMERAL durability REFUSED'). Fixed with the data_dir fallback the other two converters already had; mutation M3 pins it. Gates after remediation: test-from-emqx.sh 19 ok, test-from-hivemq.sh 15 ok, test-from-mosquitto.sh 6 ok, dual-run-smoke.sh 11 ok (run twice), py_compile x3, shellcheck clean on all four scripts, mqttui cargo test/clippy/fmt clean, vendor-mqttui-examples.sh --check current, check-readme-facts.py all match. THE DOC AND PROVENANCE HALF, which is this lane's whole value. (1) THE FIXTURE PROVENANCE CLAIM WAS FALSE: emqx-6.2.2.conf's header said the files were 'fetched verbatim' and 'every KEY and VALUE below is as the vendor ships it', while NINE values differed — and they were precisely the values that switch the converter's positive mappings ON (verify verify_none->verify_peer, fail_if_no_peer_cert false->true, peer_cert_as_username disabled->cn, max_retained_messages 0->100000, two max_connections infinity->finite, enable_ocsp_stapling false->true, two cipher strings shortened) plus the vendor's `my_quick_listener_name` typo renamed. Fixtures are now explicitly VERBATIM or COMPOSED, and both kinds exist because a vendor's stock defaults take the REFUSING branch of nearly every security mapping: new emqx-6.2.2-vendor-verbatim.conf (the ten cited rel/config/examples files concatenated, 21196 bytes, sha256:5f1542e1…, per-part hashes in its header), hivemq-2026.5-default-config.xml and hivemq-2026.5-tls-client-auth.xml (single vendor files, sha256:9a8a93f3… and 72f561d6…), beside the relabelled composed ones whose headers list EVERY deviation and why. emqx-acl-documented-examples.conf's header claimed all six rules were copied from the vendor's shipped examples; three were composed here, and it now says which, citing the vendor acl.conf's lines 97/129/135 for the copied ones and 123 for the fragment behind a composed one. RE-DERIVED, NOT ASSERTED (2026-08-14 docs pass): all ten EMQX parts re-fetched at tag 6.2.2 and byte-compared (clean, hashes and byte counts match the header); the whole verbatim body re-hashed by the two commands its header prints, both 5f1542e1…; the two HiveMQ verbatim files re-derived from the fixtures by the head+sed recipe now in their headers and matched against fresh curls; the composed EMQX fixture diffed leaf-by-leaf with the converter's own HOCON reader (exactly the nine documented changes, the one rename, 53 dropped keys, nothing undocumented); the composed HiveMQ fixture compared element-by-element against each cited vendor file (equal path-for-path and value-for-value, only comments dropped) and its <security>/<persistence> defaults checked against the vendor config.xsd. Three header numbers were wrong and are corrected: 'thirteen' <mqtt> child blocks (twelve), 'four vendor sample files' (five — four samples plus the default config.xml), and the 53-dropped-key breakdown. The HiveMQ tls-client-auth sample's path was cited as conf/examples/... in docs/MIGRATION.md; the real path is src/distribution/conf/examples/... and the short form 404s. Also recorded because it is not what a reader assumes: tag 6.2.2 in emqx/emqx is titled 'EMQX Enterprise 6.2.2' (both editions ship from that repo since 6.x) and these example files are not edition-specific — listeners.ssl.conf.example is byte-identical at v5.8.9, mqtt.conf.example is not. (2) METRIC NAMES THAT DO NOT EXIST: the playbook's verification table told operators to alert on bridge_connected, bridge_forwarded_total, bridge_dropped_total, bridge_spool_depth and bridge_reconnects_total. The real series carry the `fss` registry prefix, so every one of those alerts would have returned no data forever — and the smoke could not catch it because it asserted the SUBSTRING 'bridge_'. The doc now names the fss_ series with their real label sets, and dual-run-smoke.sh matches each one ANCHORED and fails if an unprefixed bridge_* series ever appears. Scraped from a running bridge to confirm. README.md and docs/BRIDGE.md already used the fss_ names for the BRIDGE series, but grepping every doc for the same defect class found four more citations of BROKER series that the registry does not export, each of them in a sentence telling the reader to check or alert on it: README.md's publish_dropped{reason="outbound-full"}, docs/OPERATIONS.md's security_reloads_total{trigger="watch"}, and docs/TROUBLESHOOTING.md's publish_dropped{reason="queue-overflow"} and quota_rejections_total{reason="retained"}. Every broker series carries the mqttd_ prefix — a scrape of a fresh node has 160 sample lines and not one without it — and a counter's Prometheus text form carries _total; all four are corrected to the scraped names. (3) THE FOREIGN-BROKER HEDGE WAS BACKWARDS — see T18, corrected there and in the doc, with a sixth assertion added. (4) BIDIRECTIONAL RETAINED RE-SYNC WAS PRESENTED AS PURE UPSIDE: it runs both ways on every reconnect, so a retained value DELETED on one side while the bridge is down is RESURRECTED from the other. Reproduced twice: retained fleet/truck7/config crossed to mqttd, the bridge was stopped, the value was cleared on the incumbent with `mosquitto_pub -r -n` (confirmed gone), the bridge was restarted — and the value was retained on the incumbent again, republished from mqttd. Documented as a hazard in MIGRATION.md and docs/BRIDGE.md with the mitigation (prune with the bridge RUNNING so the zero-length retained publish crosses as a tombstone; then check BOTH sides), and the mitigation is now smoke assertion 6 rather than advice. (5) THE ACL-DENIAL COUNTER CLAIM: 'there is no Prometheus counter for ACL denials' is false for one of the four kinds. Measured on a booted broker — a CONNECT refused for an unauthorized WILL topic raises mqttd_connection_errors_total{reason="acl"} 1 (conn.rs count_connection_error), while a denied publish and a denied CONNECT-ACL client id raise nothing and appear only as audit events acl.deny.publish / acl.deny.connect. Note the round-1 finding itself was half wrong here: it claimed the connect path also counts, and the code and the measurement both say it does not. EVERY OTHER LOAD-BEARING CLAIM ON THE PAGE WAS RE-MEASURED IN THE SAME PASS, on the assumption that more were wrong: the four bridge refusals (remap on `both`, a $-prefixed filter, QoS>=1 without [spool] dir, an mTLS half-identity) each provoked from the real binary and quoted by their real error text; hop_count_limit default 8 and no_local = true in client.rs; `mqttd --probe /readyz` measured 0 against a live broker, 1 against a dead one, 2 with no health endpoint; cert-audit.sh exits 1 on a fleet containing one EKU-less client certificate and names the missing clientAuth EKU; three INSECURE lines on a plaintext + anonymous + no-ACL node at RUST_LOG=warn, two when an ACL file is set; the JKS recipe's openssl half re-run in the exact form the converter prints, after which the broker logged 'accepting MQTT 3.1.1 clients over TLS 1.3' on the extracted pair; the vendor default ACL's 6 TODOs / 1 inert rule counted in the output (and pinned by an assertion); the verbatim EMQX config's three TLS listeners all named and its cacertfile refused as a mandate; the HiveMQ CE default config reported ANONYMOUS and its plaintext listener flagged; REQUIRED mapping to client_ca on vendor bytes; 11 flattened ACL rules from 2 users x 2 roles; EMQX's shipped max_inflight = 32 and HiveMQ's XSD-documented server-receive-maximum default of 10 against mqttd's 256; mqttd's HTTP auth reading the status code (200 allow, 401/403 deny, anything else deny, http_auth.rs); the file-RBAC extension's own README denying connections whose username or client id contains # or +; and all four scripts exiting 2 — not 1 — when a binary or the mosquitto broker is missing. The version ranges were spot-checked too, and the doc now says exactly what that means: EMQX v5.8.9's ten shipped examples and HiveMQ CE 2023.10/2024.9's config.xml + mqtt sample each convert with exit 0 and pass --check-config, which is a parser claim and not a fidelity claim. Still true and still stated: no live EMQX or HiveMQ broker was ever run, and the bridge step was exercised against Mosquitto 2.1.2 only." |
| 0051-T20 | ✅ done | 2026-08-15 | 'Round 3 of issue #250, and the brief was the PATTERN rather than the findings: two adversarial rounds had each fixed the NAMED SITES of a defect and left the CLASS alive somewhere else — round 2\'s blocking finding was literally round 1\'s blocking finding, surviving in the third converter because nobody was told to look at it. So this pass swept each class across all three Python converters AND the shipped Rust port, and then INSTALLED A CHECK so the classes are detectable rather than re-findable. THE CHECK: scripts/migrate/property_sweep.py, run by all three test-from-*.sh steps (so it is CI-gated) — 26 mosquitto + 50 emqx + 37 hivemq generated cases from a cross product of listener ORDER permutations, enable flags, unanimous/mixed mTLS postures, both no_match postures, and truststore present/absent. Five invariants per case: (A) every security-relevant input VALUE appears in the output, translated or named in a TODO/NOTE — each generated value is a unique string, so \'appears\' is exact and holds under every permutation rather than the one ordering a fixture happens to use; (B) nothing the source DISABLED is a live bind, URL or [[rules]] entry, checked against the output with comment lines stripped so a commented candidate (the contract\'s prescribed handling) is not mistaken for an activation; (C) no deny/allow claim in a document contradicts the `default` that same document writes; (D) every `step N` the output tells the operator to run is a step the output printed; (E) `mqttd --check-config` on every generated config. NON-VACUITY, 10 mutations, each reverted -> sweep run -> named failure -> restored byte-identically (diff clean on all three converters): M1 decide the mosquitto mTLS gate from tls_listeners[0] -> \'the mTLS MANDATE on listener 0.0.0.0:8883 is NOWHERE in the output\'. M2 emit `crl` unconditionally -> \'the broker REJECTED the generated config: tls.crl requires tls.client_ca\'. M3 stop emitting [security] acl_file -> \'acl_file, or the policy is not enforced, is NOWHERE in the output\'. M4 stop reporting the per-listener security collapse -> \'the node-wide collapse of a per-listener allow_anonymous is NOWHERE\'. M5 drop the include_dir not-read report -> \'/etc/mosquitto/conf.d is NOWHERE\'. M6 ignore an EMQX listener\'s enable = false -> \'a DISABLED listener bound anyway: plaintext_bind = "0.0.0.0:1883" as a LIVE setting\'. M7 remove BOTH reporters of a dropped TLS path -> \'/certs/ssl.crt is NOWHERE\'. M8 hard-code the fail-closed claim in the zero-rule ACL TODO -> \'acl.toml writes `default = "allow"` but a comment asserts fail-closed\'. M9 emit a disabled file-RBAC user\'s rules as live policy -> \'a DISABLED user\'s live rule as a LIVE setting\'. M10 put step 2 back inside the truststore loop (the exact round-2 defect) -> \'out.toml tells the operator to run step(s) [2] but only printed [1, 3, 4]\'. WHAT THE SWEEP FOUND THAT NEITHER FINDINGS FILE NAMED. (1) A BROKEN CONFIG, in the most-reached converter: from-mosquitto.py and the Rust port emitted `crl` whenever any TLS listener named a crlfile, and the broker REFUSES that pair outright — `invalid configuration: tls.crl requires tls.client_ca`. The differential test\'s OWN fixture has cafile + crlfile with no require_certificate, so both converters agreed on a config `mqttd --check-config` rejects; the shell fixture had no crlfile and the differential test only parsed TOML, so neither could see it. Rule 3, \'the output must validate\', broken in the converter the docs call the most mature. (2) THE TRANSLATED ACL WAS NEVER ENFORCED: from-mosquitto.py translated a whole policy and never wrote [security] acl_file, so mqttd enforced NO authorization at all (mqtt-config: acl_file is Option<String>, None by default, \'without it authorization is not enforced and loudly logged\') while the generated ACL\'s own header said it denied by default. Both halves fixed, and a config with no acl_file now says so. (3) PER-LISTENER SECURITY COLLAPSE, unreported: mosquitto.conf(5) makes password_file, acl_file, psk_file, allow_anonymous, allow_zero_length_clientid and auto_id_prefix PER-LISTENER under per_listener_settings, and mqttd\'s [security] is node-wide. per_listener_settings fell into the generic \'no direct equivalent\' bucket, and allow_anonymous kept the FIRST true it saw — so `true` on a retired listener followed by `false` on the live one carried anonymous access forward AND emitted a NOTE saying so beside a config that did not. Now last-wins (what Mosquitto does with the flag off), with a TODO naming every site and the value that won. (4) EMQX\'s actions/sources BRIDGE SHAPE WAS NEVER READ: `bridges` is not a root in 6.2.2\'s schema at all (emqx_conf_schema:roots/0, emqx_bridge_v2_schema:roots/0 give connectors/actions/sources; bridges.* survives only through emqx_bridge_compatible_config\'s v1 upgrade path), so a current EMQX bridge produced an upstream with ZERO rules — forwarding nothing — while the section TODO said MQTT-type actions map to mqtt-bridge rules. convert_bridge_actions() now reads actions.mqtt.<n>.parameters and sources.mqtt.<n>.parameters per emqx_bridge_mqtt_pubsub_schema, and MIGRATION.md\'s row says which shape is which. (5) A DROPPED TLS PATH WAS REPORTED WITHOUT BEING NAMED: from-emqx\'s inventory gave each listener\'s verify posture but not its material, and the \'material differs\' TODO said the other listeners\' PEM files were \'referenced NOWHERE\' without naming them — so the dropped certfile/keyfile paths appeared nowhere in the output. Same for a disabled authenticator: \'an http authenticator was disabled\' without its URL cannot be checked against the input. (6) TWO AUTHENTICATORS OF ONE KIND SILENTLY OVERWROTE EACH OTHER (EMQX\'s authn is an ordered CHAIN; mqttd has one [security.http_auth] and one [security.jwt]), and several `file` authz sources let the LAST one win. Both now report the collision and name what was not translated. (7) A DISABLED file-RBAC USER GOT LIVE ALLOW RULES — round 1\'s fix reported <enabled>false</enabled> and said in the same breath that \'their rules WERE still emitted below\'. The grant and the re-enrolment command are now COMMENTED OUT with a TODO. (8) THE HIVEMQ RE-ENROLMENT BLOCK was emitted only when users existed, while [security] password_file pointed at the file it creates — the same dangling-cross-reference class as the missing step 2; it is unconditional now, with a \'no user was found\' arm. (9) enable_authn read the same whether it was the default `true` or the fail-open `false`; the value is now named and the bypass gets its own sentence. (10) tasks.toml said dual-run-smoke asserts \'four claims\' while it asserts six (round 2 widened the script and 0051-T18 but not the manifest), and README.md was a FOURTH un-hedged echo site for the retained-resurrection hazard beside OPERATIONS.md, which round 1 had named and round 2 had missed. CLASS E, RE-DERIVED FROM THE VENDORS\' OWN SCHEMAS AT THE PINNED TAGS RATHER THAN TRUSTED: EMQX\'s JWT authenticator has NO issuer/audience field (emqx_authn_jwt_schema.erl @ 6.2.2 re-fetched: mechanism, acl_claim_name, on_missing_jwt, verify_claims, disconnect_after_expire, from, plus per-type keys; grep -cE \'issuer|audience\' = 0), so the mapping row and the converter\'s read fired only on input EMQX cannot produce while the real construct — verify_claims as a MAP of claim name to expected value — was dropped and called unimplementable; iss/aud now map onto [security.jwt] issuer/audience (which crates/mqtt-auth/src/token.rs verifies only when set, and disables aud validation when unset), every other claim is a TODO saying a correctly-signed token now passes, and a top-level issuer/audience is read with a TODO saying the vendor schema has no such field. `enable` (alias `enabled`, default true) confirmed as a real base_listener field (emqx_schema.erl base_listener/1 @ 6.2.2). Every other EMQX row checked against the vendor source at 6.2.2 (node.name, node.data_dir, mqtt.max_packet_size/max_topic_alias/max_mqueue_len/max_subscriptions/peer_cert_as_username, listener max_connections and messages_rate via emqx_limiter_schema\'s mqtt_shared_limiters, retainer.backend.max_retained_messages default 0, authorization.no_match enum allow|deny default deny, authz file `path`, authn http `url`/`request_timeout`, every bridge leaf including retain and payload). Every HiveMQ row checked against config.xsd @ 2026.5 (max-packet-size, server-receive-maximum default 10, max-per-client, max-queue-size, strategy enum discard|discard-oldest default discard, max-connections -1 = unlimited, clientAuthenticationMode enum OPTIONAL|REQUIRED|NONE default NONE) and every file-RBAC element against README.adoc @ 4.6.16 (topic, activity, qos with its seven-value enum, retain, shared-subscription, shared-group default #, ${{clientid}}/${{username}}). The Mosquitto converter had NO vendor provenance at all, so its directive vocabulary was checked against man/mosquitto.conf.5.xml @ v2.0.22 — which is where per_listener_settings\' exact affected-key list, tls_version being a MINIMUM since 1.6 (so tlsv1.3 is a NOTE, tlsv1.2 is the allow_tls12 TODO and older is \'those clients cannot connect\'), use_identity_as_username\'s password_file interaction, use_subject_as_username (previously unhandled), include_dir\'s documented load order and max_inflight_messages\' \'outgoing QoS 1 or 2\' definition all came from. CLASS F: --help now carries the same VERSION SCOPE sentence on all three converters and in `mqttui --help`, which MIGRATION.md line 180 had claimed while from-mosquitto.py printed one line; docs/MIGRATION.md gained a Mosquitto mapping section (it had none, and the converter the docs call the most mature was the only one with no table); the retained-resurrection hazard is now on OPERATIONS.md, README.md and in dual-run-smoke.sh\'s own comment beside the four sites that already had it. CLASS G: an EMQX HOCON `include` and a Mosquitto `include_dir` now say their CONTENTS WERE NOT READ in those words, and so does capath (a directory of anchors the converter never opened) — the previous generic line read as \'mqttd has no includes, fine\' rather than \'your whole authn/authz may be in there and was never seen\'. GATES: test-from-mosquitto.sh 9 ok, test-from-emqx.sh 22 ok, test-from-hivemq.sh 18 ok — all PASS, each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL, and each now running the property sweep; py_compile clean on the three converters, property_sweep.py, gen-status.py and check-readme-facts.py; shellcheck clean on all four migrate scripts; tools/mqttui cargo test 42+2+1 pass (the differential test still byte-identical), cargo clippy --all-targets -D warnings clean, cargo fmt --check clean; vendor-mqttui-examples.sh --check \'current (76 files)\' with diff -r on the bundle clean; check-readme-facts.py all match (34 scripts, README bumped from 33 for the new sweep, which is declared hidden in tasks.toml).' |
| 0051-T21 | ✅ done | 2026-08-15 | 'Round 4 of issue #250, and the mandate was the DIAGNOSIS rather than the findings: three adversarial rounds had each fixed what they were shown, honestly and well — round 3''s class sweep even found 15 defects the two previous rounds never named — and the count went UP, not down. The reason is structural: "every input construct is either translated or marked TODO(migrate)" is a TOTAL-COVERAGE claim over three large foreign vendor schemas, and total coverage cannot be reached by inspection. But every serious finding across all three rounds pointed the same way — THE TOOL EMITTED A LIVE SETTING IT HAD NOT ACTUALLY DERIVED FROM THE INPUT: a bind fabricated as 0.0.0.0:1883 when the input said `bind_address 127.0.0.1`; a listener EMQX had switched OFF converted to a live bind; `allow_anonymous true` from a retired listener carried onto the live one; an mTLS mandate, a CRL check and a TLS-version floor dropped from any listener that was not first; an EMQX bridge that used TLS converted to a LIVE PLAINTEXT upstream; an ACL that permits everything beside a comment saying it denies everything; a WebSocket listener emitted as a raw-MQTT bind. So this pass makes that SHAPE IMPOSSIBLE rather than absent. (1) PROVENANCE OR NOTHING. Each converter (and the Rust port) gained one small emission gate — `Provenance.line(field, rendered, source)` plus `Conversion.set(..., source=)` — and SECURITY_FIELDS naming the 15-21 fields whose value decides who may connect and what they may do (every *_bind, cert/key/client_ca/crl/allow_tls12, acl_file/password_file/allow_anonymous/mtls_identity_source, [security.*] url/issuer/audience/*_file, the ACL `default`, a bridge upstream url and its tls ca/cert/key). The gate REFUSES to write a live line without the INPUT KEY the value came from: a field with no provenance is emitted COMMENTED OUT beside a TODO naming the decision, so a construct a converter failed to read can leave the config INCOMPLETE and can no longer leave a live security setting nobody derived. Every live security-relevant line now carries `# from: <input key>` (and `; defaulted: <what and the vendor default it came from>` where part of the value is a documented default of a directive that WAS present, or a path the converter itself owns). `--provenance-json` writes the same ledger as JSON. CALL SITES CONVERTED: 4 mosquitto binds + cert/key/client_ca/crl/mtls_identity_source/acl_file/allow_anonymous/ACL default; 5 EMQX binds + cert/key/client_ca + password_file/http_auth url/jwt issuer+audience/mtls_identity_source/acl_file/allow_anonymous/ACL default + the bridge [local] url, every [[upstreams]] url and the whole [upstreams.tls] block; 4 HiveMQ binds + cert/key/client_ca + password_file/acl_file/allow_anonymous/ACL default. LITERALS AND FALLBACKS REMOVED: from-emqx.py''s `lst.bind or ("0.0.0.0:1883" if lst.proto == "tcp" else "0.0.0.0:8883")`; normalise_bind''s `default_host="0.0.0.0"` and its `f"{v}:1883"` for a host with no port; from-hivemq.py''s `self.port or "1883"` and `self.bind or "0.0.0.0"` in Listener.address; from-mosquitto.py''s (and the Rust port''s) `lst.port if lst.port is not None else 1883` / `lst.bind or "0.0.0.0"` in FIVE places (the listener comment block, the plaintext bind, the TLS bind and both `additional listener` TODOs) plus `Listener.where`, which named a fabricated address in EVERY message about a listener; the bridge config''s hard-coded `url = "127.0.0.1:1883"` for [local]; and HiveMQ''s three bare `# client_ca = "/etc/mqttd/tls/client-ca.crt"` candidate lines and its live `cert`/`key` placeholders, which are now provenance-carrying (`# from:` the keystore element, `; defaulted:` the PEM path step 1 must write). (2) A POSTURE CHANGE IS ALWAYS COMMENTED, without exception: `allow_anonymous true` in mosquitto.conf is no longer carried over (it is node-wide in mqttd and one listener''s posture cannot decide it) — the candidate is commented with the input key it came from; the EMQX bridge''s TLS block is commented with every dropped ssl.* path named, because mqtt-bridge''s [upstreams.tls] is Optional and ABSENT MEANS PLAINTEXT; a Mosquitto listener whose `protocol` is neither mqtt nor websockets gets NO bind at all. (3) THE ACL DEFAULT IS DERIVED: one `policy_effect(default)` / `empty_policy_effect(default)` per converter now generates every sentence about what the policy will do, including the ACL headers (which asserted "anything not allowed below is refused" as fixed prose in two of the three) and both zero-rule TODOs; the `default` line itself carries its source (`authorization.no_match` for EMQX, the vendor''s own allow-list semantics for the other two). (4) THE CHECK. property_sweep.py gained invariant F (PROVENANCE: every live security-relevant value is one the INPUT held — for a *_bind the PORT always, for a path/URL the string; a missing part is allowed only where the line says `defaulted:`, and every one of those is counted and PRINTED) and invariant G (NO LIVE SETTING WITHOUT A SOURCE: every uncommented security-relevant line carries `# from:`), plus generators for the axes round 3''s own verifier named as blind spots — the Mosquitto default-listener form, `protocol websockets`, an unknown transport, per-listener max_connections and the `-1` sentinel, the dynsec plugin, a listener-scoped EMQX authentication chain, the other keys on a LIVE authenticator, acl_claim_name, a TLS bridge via --out-bridge (invariant E had never even looked at bridge output), an EMQX `bind` with no port and a HiveMQ listener with no <port>. 124 generated cases (34/52/38). WHAT F AND G CAUGHT ON THEIR FIRST RUN: G fired on EVERY [listeners]/[tls]/[security] line of the EMQX and HiveMQ output (none carried a source); F caught `acl_file = "/etc/mqttd/acl.toml"` — a path that appears in no input — and, once the same rule ran inside the fuzz pass, a LIVE `plaintext_bind` on port 1883 derived from a `bind_address` with no port, in a from-mosquitto.py fix written three hours earlier in this same pass. That last catch is the argument for the invariant: the mistake was mine, it was hours old, and only a machine reading the output against the input would have found it. Both are mutation-proved (revert the gate -> named failure). (5) THE FUZZ PASS (`--fuzz N`, run by all three test-from-*.sh at N=40 so it is CI-gated): each fixture mutated mechanically — delete random lines, truncate at a random byte, permute blocks, flip a boolean/enum, swap a transport, duplicate or chop a line — asserting only what must hold for ANY byte sequence: the converter EXITS 0 or 1 WITH A MESSAGE, whatever it writes is valid TOML, and no live security-relevant line lacks provenance. It found the HANG round 3 reported: from-emqx.py''s HOCON `_array` fell through to `_bare(",]\n}")`, which cannot advance when the current character is already a stop character, so a `}` reached inside an unterminated array appended "" and re-entered the loop unchanged — 100% CPU, unbounded memory, no error, no exit, on `authentication = [` followed by `}`. Fixed structurally: `Hocon._guard(before, where)` refuses to iterate without consuming a character (called from BOTH loops, so no future edit to a stop set can reintroduce it), an unterminated `[`/`{` is reported as a TODO saying everything after the missing bracket was read in the WRONG SCOPE, and a HoconError exits 1 with the byte offset. 1,800+ mutated inputs across five seeds: no hangs, no crashes, no invalid TOML, no unprovenanced live line. ROUND 3''s 22 ITEMS, all dispositioned — R3.1 default-listener form (fixed, both languages), R3.2 + R3.15 `protocol websockets` -> ws_bind/wss_bind (fixed, both), R3.3 listener-scoped authentication chain (fixed: read, named, saw_auth set, the contradicting TODO gone), R3.4 + R3.16 every other key on a LIVE authenticator (fixed: AUTHN_NO_EQUIVALENT, 20 entries, headers/method/body/pool_size/ssl.* each with the reason its loss breaks login), R3.5 acl_claim_name (fixed: an entire authorization source, now named), R3.6 TLS bridge -> plaintext (fixed: commented [upstreams.tls] naming every dropped path), R3.7 the HOCON hang (fixed structurally + seeded in the fuzz pass), R3.8 non-file authz sources named with their store and query (fixed), R3.9 SECTION_NO_EQUIVALENT count-only swallowing gateway/dashboard binds and CA material (fixed: security-relevant leaves named), R3.10 elided values (fixed: HiveMQ cipher-suites and listener <name>, EMQX''s generic listener line now names the value — mountpoint and access_rules included), R3.11 the sweep''s own weakness (fixed: F, G and the missing generators), R3.12 the dynsec plugin and the false "your broker also authorized everything" claim (fixed: NOT_READ entries for plugin/auth_plugin/plugin_opt_*, and that sentence is now derived from what was read), R3.13 per-listener max_connections (fixed: smallest-wins + TODO), R3.14 `max_connections -1` producing a config the broker rejects (fixed: unset + NOTE), R3.17 messages_rate last-wins (fixed: smallest-wins + TODO), R3.18 max_topic_alias and message_size_limit (fixed: both now map), R3.19 the `tls_version` "since 1.6" claim (fixed in docs/MIGRATION.md and in both converters'' comments — the man page says the MINIMUM reading begins AFTER 1.6.x, and the 1.2 TODO now states the older ONLY-version reading too), R3.20 the per_listener_settings list stated four ways (fixed: ONE SCOPED_SECURITY_LIST constant of EIGHT keys, quoted by the emitted TODO, --help, `mqttui --help` and MIGRATION.md), R3.21 the JWT dead-field overwrite (fixed: a top-level issuer/audience can no longer displace the verify_claims value; it is a commented candidate), R3.22 TROUBLESHOOTING.md''s un-hedged "prune retained" (fixed). HONEST SCOPE, in the four places the mandate names: docs/MIGRATION.md has a new section "What a converter produces: a DRAFT, where anything undecidable is INERT and named" with the diagnosis, the gate, worked output, and an explicit VERIFIED / NOT VERIFIED split; the same DRAFT paragraph is in all three converters'' --help and in `mqttui --help`; the condensed six-line form is in the header of every config, ACL and bridge file they write; and README''s migration bullet now leads with "what it produces is a reviewed DRAFT, not a translated configuration", says every security-relevant value carries its input key, and states that no live broker of ANY of the three was run and that no total-coverage claim is made. GATES: test-from-mosquitto.sh, test-from-emqx.sh and test-from-hivemq.sh all PASS (each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL, and each now running both the property sweep and the fuzz pass); property sweep 34+52+38 cases hold every property; fuzz 43+47+45 in-CI runs plus 1,800+ across five seeds locally; py_compile clean on the three converters, property_sweep.py, gen-status.py and check-readme-facts.py; shellcheck clean on the migrate scripts; tools/mqttui cargo test PASS including the byte-for-byte differential test (13 hand-built inputs compared byte-identical besides it); cargo clippy --all-targets -D warnings clean; cargo fmt --check clean; vendor-mqttui-examples.sh --check current with diff -r clean; check-readme-facts.py all match; gen-status.py --check clean.' |
| 0051-T23 | ✅ done | 2026-08-17 | 'Issue #297 is the residue T22 enumerated: the provenance gate closes FABRICATION and cannot see a MISREADING, five named misreads were still open, and the issue''s own argument is that the class does not converge under inspection — it needs a semantic oracle. Both halves delivered. THE FIVE FIXES, each cited to the vendor''s documented semantics and each with a test that fails without it: (1) TLS KNOBS ON A CERTIFICATE-LESS LISTENER (tls_version/ciphers/ciphers_tls1.3/dhparamfile) — mosquitto.conf(5) @ v2.0.22 terminates TLS only with a certificate (`certfile`, with `keyfile`) or a PSK, so on a listener with neither every knob was INERT in Mosquitto too; the converter nevertheless reported ''so it accepted TLS 1.2 AND 1.3'' beside the LIVE PLAINTEXT bind it correctly wrote — the dangerous direction, read as ''encrypted before cutover, not after''. ciphers/ciphers_tls1.3/dhparamfile were not in TLS_KEYS at all (they fell through to ''check the mqttd configuration table'', which has nothing for them). Now: the knob translations run only where the listener terminates TLS; a certificate-less listener''s TODO inventories every inert setting BY NAME and says plainly it did NOT terminate TLS and the bind below is plaintext because the source was; a real TLS listener''s ciphers/dhparamfile get rustls-specific reports (fixed suite set, no cipher config, no non-EC DHE). (2) MALFORMED VENDOR VALUES REFUSED, NEVER EMITTED: the DIRECT table''s int arm wrote `message_size_limit 10OO` (letter O) straight into the TOML — a document `mqttd --check-config` cannot even PARSE, so the prescribed validation could not run — while exiting 0, which a migration script reads as success; the retain arm read any unrecognised spelling (`flase`) as the PERMISSIVE direction silently; the str arm accepted an empty path. A value outside the vendor''s own schema (count/bool/path) now refuses the WHOLE run: exit 1, NOTHING written, every key+value+file named on stderr, and the documented exit contract updated (0 translated / 1 input unusable). Well-formed neighbours still translate — asserted. (3) THE ANONYMOUS-SCOPE TODO''s remediation named a line the converter does not write: it said allow_anonymous ''is emitted COMMENTED OUT'' unconditionally, but the commented candidate exists only when mosquitto.conf set it TRUE — with false/unset the operator greps for a line nobody wrote. The advice is now DERIVED from what the run actually emitted (candidate/false/unset postures, three texts), and the false/unset texts say outright there is nothing to uncomment. Tested in both postures. (1)-(3) are the stranded 786-line half of this task, verified and shipped as found; both languages (from-mosquitto.py + tools/mqttui/src/migrate.rs), plus three new Rust tests. (4) A LITERAL %c/%i IN AN EMQX acl.conf topic_match() OR A HIVEMQ FILE-RBAC <topic> was still emitted as a rule mqttd SUBSTITUTES — a per-client grant the source never gave. EMQX 5/6 substitutes only ${...} placeholders (the pinned schema fixture''s own ''Supported placeholders are:'' list; %c/%i absent) and file-RBAC only ${{clientid}}/${{username}} (4.6.16 reference), so both refusals mirror the Mosquitto one from T22, checked BEFORE placeholder rewriting so a %c the converter itself produces is never confused with one the source carried; the EMQX message also names the EMQX 4.x escape hatch (%c/%u WERE its placeholders — outside the parser''s version scope, rewrite and re-run). Example tests in both suites plus 2 new property-sweep cases (emqx/hivemq literal-%c, forbidden = the live rule, witness = the topic named). (5) THE JWT/JWKS BRANCH now names the ENDPOINT URL it read (or says ''no URL in the input — only the switch'' when use_jwks is on without one) — the same operator-can-verify rule every other authenticator''s server/database/url already followed. THE ORACLE: scripts/migrate/differential-mosquitto.sh boots the REAL mosquitto broker on the source config and mqttd on the CONVERTED config (finished exactly as the draft''s own TODOs instruct — re-hashed credentials, acl path, data dir; every finishing step is an env overlay naming the draft line that demands it), probes both with the same client binaries, and diffs seven verdicts: anonymous-connect, wrong-password, valid-credential, permitted/forbidden publish, permitted/forbidden subscribe — ACCEPTED/REFUSED and DELIVERED/NOT_DELIVERED deliberately, because reason codes legitimately differ while whether a client gets in and a message crosses is exactly the meaning the mappings claim to preserve. Run 2026-08-17 against mosquitto 2.1.2: all 7 identical; transcript in docs/MIGRATION.md. NON-VACUOUS twice over: harness anchors fail the run if the ORACLE side misbehaves (vendor accepting anonymous under allow_anonymous false = broken probe, not agreement), and mutation-proved — mqttd booted with anonymous ON records ACCEPTED and the diff fails; a hand-widened ACL records publish-outside-acl DELIVERED and the diff fails. Exit-2-on-missing-environment verified both ways (MQTTD_BIN at nothing; mosquitto off PATH), same convention as dual-run-smoke.sh. GATES: test-from-mosquitto.sh PASS (incl. the new refusal/knob/anon-posture sections), test-from-emqx.sh PASS, test-from-hivemq.sh PASS; property sweep now 40+57+41 = 138 cases (was 136), MIGRATION.md counts updated; fuzz 44+48+46; tools/mqttui cargo test PASS incl. the byte-for-byte differential test and 3 new unit tests; clippy -D warnings clean; fmt clean; py_compile clean; vendored mqttui bundle re-vendored; gen-status clean. docs/MIGRATION.md: KNOWN GAPS %c/%i row widened to all three converters, EMQX placeholder prose + HiveMQ mapping row updated, the Verified list gains the differential lane and the ''no running broker'' bullet is re-scoped to what the seven probes now measure, and the lane''s transcript + mutation proofs recorded beside the dual-run smoke''s.' |
| 0051-T22 | ✅ done | 2026-08-15 | 'Round 4''s verification pass confirmed the round-3/4 restructuring WORKED — the provenance gate makes it structurally impossible to emit a live security-relevant value without naming the input key it came from, the new invariants caught a fabricated bind the same round had introduced hours earlier, and the fuzz pass found and structurally fixed a HOCON parser hang — and then produced 14 findings of a DIFFERENT class the gate cannot catch by design: SEMANTIC MISREADING, where the tool derives a live value from real input but misreads what that input MEANT. Six fixes, every overclaim withdrawn, and the class enumerated rather than implied to be closed. (1) BLOCKING — A TLS-PSK LISTENER BECAME A LIVE PLAINTEXT BIND. psk_file/psk_hint were in neither TLS_KEYS nor the half-material safety net, so Listener.is_tls was false and BIND_KEYS[(transport, False)] chose plaintext_bind: a listener mosquitto.conf(5) describes as encrypted (''The psk_hint option enables pre-shared-key support for this listener'') was published in CLEARTEXT on its own port, while another TODO in the same file reported that listener''s tls_version — and check-config said config OK. The gate could not see it: the bind carried a genuine `# from: listener 8883`, because the gate checks where the VALUE came from and the FIELD is what encodes the transport. Now PSK_KEYS are collected PER LISTENER, convert_psk() reports the listener (and psk_file''s identities, each of which needs a new credential), the bind is emitted INERT ON THE TLS KEY of its transport with the downgrade named, and the half-material report no longer claims a PSK listener was served as plaintext. Both languages, byte-identical. (2) BLOCKING — MOSQUITTO''S ANONYMOUS-SCOPED ACL BLOCK BECAME A GRANT TO EVERY AUTHENTICATED CLIENT. mosquitto.conf(5) @ v2.0.22, verbatim: ''The first set of topics are applied to anonymous clients, assuming allow_anonymous is true'' — those pre-`user` topic lines were emitted with NO identities, which crates/mqtt-auth/src/acl.rs applies to EVERY authenticated identity (''Both lists empty means everyone''). Strictly broader under both postures: with allow_anonymous false those topics were reachable by NOBODY. They are now scoped to identities = ["anonymous"] — the subject mqttd gives an unauthenticated client (crates/mqtt-auth/src/basic.rs) — each rule carrying an inline comment, plus a TODO quoting the man page and naming both consequences (they grant nothing until allow_anonymous is uncommented; a real user named `anonymous` collides). Both languages. (3) MAJOR — THE EMQX TLS BRIDGE STILL PRODUCED A LIVE PLAINTEXT UPSTREAM. Round 3 commented [upstreams.tls] out and left the upstream url live; but mqtt-bridge''s tls block is Optional and ABSENT MEANS PLAINTEXT, so commenting it IS the posture change and the url is the line whose liveness decides the posture. Completing the draft exactly as the file instructs ([local] url + a spool dir) connected to a TLS peer in cleartext. BridgeUpstream gained tls_enabled and the url is now emitted through the gate with source=None when the EMQX side had ssl enabled. (4) MAJOR — report_unread_authn_keys() was wired on the http and jwt branches ONLY, so a live authenticator on any other backend lost every key unnamed — on THIS REPOSITORY''S OWN pinned fixture, whose `backend = mysql` entry''s server/database/query appeared nowhere. Wired on every branch including the fall-through, plus AUTHN_NO_EQUIVALENT entries for the credential-store keys (server/servers/database/query/cmd/collection/selector/filter/base_dn/bind_dn) carrying convert_authz''s own reason: the store is named so the operator can check that the source they believe was in force is the source the converter could not translate. The reason lookup now also tests ANCESTOR paths, so password_hash_algorithm.name gets its block''s reason instead of the generic sentence. (5) MAJOR — THE SAME DOCUMENT EMITTED A JWT CLAIM AS A LIVE MAPPING AND REPORTED IT AS HAVING NO EQUIVALENT: verify_claims was absent from AUTHN_JWT_READ, so the leaf reporter enumerated its claims; the guard meant to suppress that (''verify_claims'': ''handled — …'') could never fire because the lookup tested the full path and the leaf and never the parent. verify_claims is now in AUTHN_JWT_READ (the branch reads it claim by claim) and the dead entry is gone. (6) MAJOR — NO GENERATED BIND WAS EVER CHECKED FOR BINDABILITY, and `mqttd --check-config` — the verification the docs, every --help and every generated header point the operator at — accepts ANY string there, so the prescribed gate said config OK on addresses the broker refuses at STARTUP: ws_bind = ":8085" (a form EMQX''s own ip_port accepts), plaintext_bind = "10.0.0.1:abc" from a HiveMQ <port>abc</port>, and plaintext_bind = "/tmp/mosq.sock:0" from `listener 0 /tmp/mosq.sock` — a UNIX-socket listener that declares no TCP endpoint at all, which is a transport fabrication the provenance gate cannot see. bind_gap() in all three converters and the Rust port parses host and port before a line goes out live; anything unparseable is INERT with the reason. A non-scalar EMQX `bind` is rejected outright rather than str()-ed into the Python repr "[''0.0.0.0:1883'']" (a live value in no input, which invariant F missed because for a *_bind it compares only the PORT). ALSO FIXED, same class: a literal `*` in a username became an mqttd identity GLOB in all three converters (identities are globs with NO escape, crates/mqtt-auth/src/acl.rs glob_match, while every source matches the username literally) — the rule is now refused with the user named, the same refusal from-emqx.py already made for a {re, …} condition; a literal %c/%i in a Mosquitto plain `topic` filter became a SUBSTITUTING mqttd rule (the man page substitutes only in `pattern`), also refused; `message_size_limit 0` / `max_packet_size 0` — the vendor''s documented spelling of NO LIMIT (''The default value is 0, which means that all valid MQTT messages are accepted'') — became max_packet_size = 0, which check-config ACCEPTS and mqttd FLOORS TO 1024, turning an unlimited broker into one refusing every packet over 1 KiB; a Mosquitto BRIDGE block''s address/topic/bridge_cafile/bridge_certfile/bridge_keyfile/remote_username/remote_password/remote_clientid were reported as ''no direct equivalent — check the mqttd configuration table'' when every one has an exact mqtt-bridge equivalent this repo ships (bridge_cafile is the directive that decides whether the migrated bridge verifies its peer); a control character in a value survived comment_safe() into a TOML comment, which TOML 1.0 forbids anywhere in a document, so tomllib and the broker rejected the WHOLE file while the converter printed `wrote <file>` (now escaped as \uXXXX, both languages); and a non-UTF-8 config exited 1 on a bare UnicodeDecodeError traceback instead of the documented message. OVERCLAIMS WITHDRAWN, quoted before and after in the round report: docs/MIGRATION.md''s ''as of 2026-08-15 that shape is impossible rather than absent'' is replaced by two headed subsections — what the gate DOES guarantee (no live security-relevant value without a named input key, which makes fabrication structurally impossible) and what it does NOT (misreading a real input''s meaning, with all five instances named); from-mosquitto.py''s --help printed ''VERIFIED: fixtures diffed against pinned vendor sources'' twenty lines under its own paragraph saying NO vendor file is pinned for it, so its VERIFIED clause is now per-converter and says ''NOT diffed against vendor bytes'' outright (same in `mqttui --help`); the NOTE the converter writes into the DEPLOYED config claimed mosquitto.conf(5) deprecates message_size_limit in favour of max_packet_size and that they are the same quantity — the pinned page says neither (it marks port, bind_address, allow_duplicate_messages and clientid_prefixes deprecated, not this one, and its neighbouring entry says max_packet_size ''applies to the full MQTT packet, not just the payload'' while message_size_limit is ''the maximum publish payload size''), so the NOTE now states the payload-vs-packet tightening and the docs row records the correction; README''s prominent install-flow migration block said ''never a silent drop'' unqualified with no draft framing and now leads with the DRAFT, scopes the promise to what the converter READ, and links KNOWN GAPS; MIGRATION.md''s ''The one property that makes them trustworthy'' and its ''Nothing is silently dropped'' rule are both scoped the same way; the ''one tag per broker is a fixture'' claim now says Mosquitto has NONE, in the What-ships paragraph, the version-scope paragraph and all three converters'' SCOPE constant; and the fuzz paragraph now states what the mutator CANNOT generate (control bytes, invalid UTF-8 — both classes found by hand instead). ADR 0051 §3 amended: rule 1 is scoped to 'nothing a converter READS is silently dropped', rule 7 records the provenance gate as the mechanism, and rule 8 records the LIMIT — the gate closes fabrication and not misreading, and that difference must be stated wherever the gate is claimed. ENUMERATION: docs/MIGRATION.md gains a KNOWN GAPS (after round 4) table — 18 rows, each with what happens now and what the operator must check by hand: PSK, anonymous-scoped ACLs, literal `*`, literal %c/%i, Mosquitto bridge blocks, include_dir/plugin, message_size_limit, non-http/jwt authn backends, acl_claim_name, verify_claims beyond iss/aud, bridge payload templates, a TLS bridge upstream, HiveMQ JKS, HiveMQ qos/retain/shared qualifiers, HiveMQ Enterprise, non-UTF-8 input, the ${ENV} bind cosmetic defect, and one honest final row for what nobody has looked at yet. THE CHECK: property_sweep.py gains invariant H (EVERY LIVE BIND IS BINDABLE — host and port parsed from the output alone, no import from any converter) plus 12 generated cases (a PSK listener with and without tls_version, a UNIX-socket listener, an anonymous-scoped ACL block, glob-metacharacter usernames and a %c topic, message_size_limit 0, a non-scalar and a host-less EMQX bind, a mysql authenticator and an unknown mechanism, verify_claims, an EMQX ACL glob username, a HiveMQ non-numeric port, a HiveMQ glob user name): 136 cases (40/56/40). MUTATION-PROVED, each reverted -> named failure -> restored: M1 put psk_file back in NO_EQUIVALENT -> ''an encrypted listener published in cleartext: the output has plaintext_bind = as a LIVE setting''. M2 emit pre-`user` topics with current_user (None) -> ''an anonymous-only grant widened to every authenticated identity''. M3 emit the TLS bridge''s url live -> ''a TLS upstream reachable in cleartext''. M4 drop the bind_gap check -> H fires by name on all three converters. M5 remove verify_claims from AUTHN_JWT_READ -> ''a claim reported as unmappable in the same file that maps it''. M6 unwire report_unread_authn_keys from the mysql branch -> ''the credential store''s address is NOWHERE in the output''. M7 revert comment_safe''s control-character escape -> the seeded fuzz reproducer fails with ''out.toml is not valid TOML: Found invalid character \x1b'' on the Mosquitto and EMQX converters (HiveMQ''s message paths all use repr, which escapes a control character itself, so that converter is covered by the invariant and not by this reproducer — stated rather than claimed). Two of the seven proofs found the CHECK vacuous rather than the fix: `forbidden` is matched against the output with comment lines stripped (a commented candidate is the prescribed handling for a posture change), so a defect that IS a comment — the `# (no identities = applies to every authenticated client)` line, and a TODO contradicting a live mapping — could never be seen; Case gained `forbidden_anywhere` and both proofs then failed by name. GATES: test-from-mosquitto.sh 17 ok (its broker boot is now a function, run on the ordinary converted ACL AND on the anonymous-scoped one, because `--check-config` does NOT read the file [security] acl_file names — verified: a policy with `default = "bogus"` still reports config OK), test-from-emqx.sh 28 ok, test-from-hivemq.sh 20 ok — all PASS, each ending in `mqttd --check-config` on every generated config and a real broker BOOT on the converted ACL; property sweep 40+56+40; fuzz 44+48+46 in CI (three seeded control-character reproducers added, because the mutator works on DECODED text and can never generate that class); dual-run-smoke.sh 11 ok PASS; py_compile clean; shellcheck clean on the migrate scripts; tools/mqttui cargo test 46+2+1 PASS including the differential test (extended with a PSK listener, a bridge block, an anonymous ACL block, a glob username and a %c filter), cargo clippy --all-targets -D warnings clean, cargo fmt --check clean; vendor-mqttui-examples.sh --check current; check-readme-facts.py all match; gen-status.py --check clean. WHAT AN OPERATOR CAN AND CANNOT RELY ON is stated in one paragraph at the end of the round report and in MIGRATION.md: no live security-relevant value without a named input key (mechanical, invariant-checked), every live bind bindable, no posture change activated silently — and NOT that a construct the converter read is read correctly, which is why KNOWN GAPS exists and why the output still has to be read.' |
| 0051-T16 | ✅ done | 2026-08-14 | "Reviewer 5's 'no for a cluster' reason: the headline feature had no complete secured multi-node path outside Kubernetes — the reader had to compose one from the env table. Since then #254's fix made deploy/compose a genuinely secured reference (TLS 1.3, per-node bus certs, signed gossip, deny-by-default ACL), so the remaining gap was the TUTORIAL, and this builds ON those shipped files rather than minting a parallel path that could drift. docs/SECURED-CLUSTER-TUTORIAL.md: prerequisites state the image truth loudly (the published :latest is v0.9.0, predates --hash-password/--probe, issue #263 — MQTTD_IMAGE is required and the tutorial never claims the default tag works); a topology sketch; THE FOUNDER RULE AND SEED LISTS AS A TABLE (mqttd-1 seedless+floor-1 founds, 2 seeds through 1, 3 through both, and why); bootstrap → up -d → healthy; verification = the issue's exact three (a subscriber on node 1 receives a publish sent to node 3; a WRONG-CA client is refused at the TLS handshake via a minted decoy CA; /readyz on a minority — after the documented arm-the-founder step, stopping 2 of 3 leaves node 1 LIVE but NOT READY, recovering with the majority) plus no-TLS/wrong-password refusals and the INSECURE-absence/signed-gossip log checks; then the real-CA mapping as three distinct trust roles (client listener may be ACME/Let's Encrypt with SIGHUP hot-rotation; the bus stays on a private CA with the four leaf rules — CN=node id, SAN covers peer-advertise, serverAuth+clientAuth, ECDSA/Ed25519 never RSA; client-mTLS CA must be a third CA, never the bus CA) and the one-host→three-machines move (systemd/gen-certs.sh, routable MQTTD_PEER_ADVERTISE). CANNOT ROT: scripts/compose-smoke.sh (nightly containers lane, mqttui compose-smoke) extended with the two tutorial steps it did not cover — a wrong-CA refusal in section 4, and a new section 8 that arms the founder through the same compose interpolation the .env step uses, asserts the armed founder stays ready among three, goes live-but-not-ready alone (probe1 /readyz fails while /livez passes), and recovers when the majority returns; plaintext-overlay renumbered to 9. deploy-smoke.sh already asserts both MQTTD_1_* renderings per-PR and the SIGKILL durability claim the tutorial cites. Linked from the README's Clustering section and OPERATIONS' bare-metal appendix (the issue's two required inbound links). shellcheck: no new findings; check-readme-facts green; tasks.toml's compose-smoke about/requires updated. VERIFIED BY RUNNING IT: the full extended compose-smoke executed locally against a tree-built image (MQTTD_IMAGE=mqttd:compose-local) — every assertion green including the three new ones ('a client verifying against the wrong CA is refused at the TLS handshake'; 'the founder re-ups ARMED (seeds + readiness floor 2) and is still ready — it sees 3'; 'alone, the armed founder is LIVE but NOT READY' then 'readiness returns once the majority is back') and COMPOSE SMOKE OK, exit 0. A first run against a stale non-distroless local tag failed with the brokers crash-looping on a root-owned data volume — a useful negative control: the smoke's failure modes report real causes." |
<!-- /status-table:0051 -->

## Plan

| Task | Done means |
|---|---|
| **0051-T1** README truthing | A reader discovers every shipped capability (bridge included), the top of the file argues the differentiators, no stated fact is stale, and MSRV/platforms/stability are explicit. The CI guard fails on a drifted derivable fact. |
| **0051-T2** Secured quickstart | Copy-paste from a clean checkout to a TLS+mTLS+ACL node with a foreign client connected — and CI runs those exact commands. |
| **0051-T3** Community surface | A first-time contributor and a first-time issue reporter each have a path; the changelog policy is written down. |
| **0051-T4** v0.9.0 | ADR 0045 Accepted; the signed tag exists; every published artifact verifies per RELEASING.md (cosign, provenance, SBOM, reproduce). |
| **0051-T5** Comparison | An expert from any of the four brokers can see in one read what they gain, what they lose, and which losses are on purpose. |
| **0051-T6..T8, T17** Converters | A working competitor config converts to a validating mqttd TOML, with every unconverted directive explained — proven by fixture tests per product, each asserting `--check-config` and a real broker boot. |
| **0051-T18** Cutover | An operator can move a live fleet onto mqttd without importing session state: one document, written against the bridge's real refusals, with its bridge step executed against a third-party broker and every untested step marked as such. |
| **0051-T9** New brokers benched | NanoMQ and VerneMQ run in the same harness under disclosed postures (VerneMQ per the 0048 fairness terms); the first honest numbers are in `docs/benchmarks/` linked from the README. |
| **0051-T10** Bridge assessable | Ten minutes with `demo/` shows a message crossing a security boundary under a deny-by-default rule — and the README shows the dashboard. |
| **0051-T11** 1.0.0 | The freeze is a reviewed act on evidence: bake survived, skew smoke green, ADR 0038 checklist walked. |

## Phased execution plan

| Phase | Tasks | Gate | Output |
|---|---|---|---|
| **A — truth the surface, ship** | T1, T2, T3 → T4 | fixes what actively misleads | `v0.9.0` exists; the repo stops under-selling and mis-stating |
| **B — the evaluation package** | T5–T10 (parallelizable; T9 rides ADR 0048's harness) | during the 0.9.x bake | an evaluator can assess, compare, and migrate without doing our work for us |
| **C — the conscious freeze** | T11 | bake + second tag + ADR 0038 review | `v1.0.0` |

Order inside A: T1/T2/T3 in any order, each its own PR; T4 last (the tag push is the
maintainer's act, gated on the ADR 0044 checklist which is already green).

## Changelog

- **2026-08-15 (close-out review, before commit)** — the final adversarial pass found ONE
  blocking regression introduced by the round that was supposed to close this out, and it is
  worth recording because it is the same shape as everything else this issue taught. Fix 6
  stopped the converter fabricating a bind it could not derive; in doing so
  `build_listener_binds` began `continue`-ing on a listener whose protocol bind key was
  already taken (mqttd binds one listener per protocol) and on a transport mqttd does not
  serve — producing NO live line, NO commented candidate and NO TODO. A live-wrong value had
  been traded for a SILENT DROP of an entire listener: the precise contract violation the
  provenance gate exists to prevent, reintroduced by the fix for a fabrication. Both arms now
  emit a TODO naming the listener AND its address (verified: an EMQX config with two `tcp`
  listeners plus a `quic` one puts `10.9.9.9:11883` in the output twice, in the TODO, instead
  of nowhere). The lesson generalises past this issue: **when you remove a fabrication, check
  what now happens on the path that used to fabricate** — "no output" is not the safe default
  it looks like, because for this contract silence IS the defect.
  Shipped with the honest scope this issue's exit 2 allows: the provenance gate makes a
  FABRICATED live security-relevant value structurally impossible (mutation-proven, and the
  invariant caught a fabrication the same round had introduced hours earlier), while
  MISREADING a real input's meaning is a separate class the gate cannot catch — the known
  instances are enumerated in MIGRATION.md's KNOWN GAPS rather than implied absent, and the
  residue is filed. Remaining known misreads at ship time, from the final pass and left in
  KNOWN GAPS: a literal `%c`/`%i` in an EMQX/HiveMQ topic still substitutes (the Mosquitto
  converter now refuses it); a Mosquitto listener carrying only TLS *knobs* and no certificate
  still gets a plaintext bind with a NOTE claiming it terminated TLS; from-mosquitto's DIRECT
  int branch writes a non-numeric value unvalidated; the anonymous-scope TODO's remediation
  advice points at a line the converter does not write; and the JWT branch names the construct
  but not a JWKS `endpoint` URL.

- **2026-07-27** — ADR 0051 drafted from the release-readiness review: engineering ready
  (ADR 0044 green, ADR 0045 pipeline waiting on a tag), evaluation experience not — the
  finished bridge invisible in the README, no comparison or migration story, plaintext-only
  quickstarts, unpublished benchmarks, stale hand-written facts, missing community files.
  Decision: ship `v0.9.0` now (gated only on what misleads), deliver the evaluation package
  during the bake, keep `1.0.0` as the conscious ADR 0038 freeze. Maintainer widened the
  comparison set to include **NanoMQ** (benchmark + matrix + migration), triggering ADR
  0048's "widen if there is demand" clause — recorded there as an amendment.
- **2026-08-03** — Second widening: **VerneMQ** joins the benchmark and comparison set
  (maintainer decision, informed by the 2026-07-29 VerneMQ architecture + MQTT 5 analysis:
  masterless clustering makes it the closest structural neighbor and the most informative
  head-to-head for the clustering/durability claims). Fairness terms recorded in ADR
  0048's 2026-08-03 amendment: durability postures disclosed (their queues are node-local
  and unreplicated — like-for-like pairs our in-memory opt-out against their default),
  partition regime stated per scenario (their default fails closed on netsplit), pinned
  EULA'd image (free for testing). T5/T9 widened accordingly. Comparison-matrix rows
  already drafted from the analysis: durable sessions across node loss, cross-node
  backpressure vs bounded-buffer drop, partition behavior, built-in vs plugin enhanced
  auth, binary licensing (their packages are paywalled since 1.10; ours signed and free).
  Migration tooling (a from-vernemq converter) deliberately **not** added yet — pending a
  maintainer call on whether VerneMQ operators are a migration audience or a comparison
  audience only.
- **2026-08-03** — T5 delivered and T9's harness half wired (maintainer: "implement the
  amendment and add the comparison data"). `docs/COMPARISON.md` + README condensed matrix
  land with pinned versions and printed losses; competitor research 2026-07-29→08-03
  (VerneMQ arch + MQTT 5; NanoMQ; Mosquitto/EMQX/pinning verification), mqttd cells
  verified in source — which surfaced three of our own gaps now printed as ✖ and worth
  backlog consideration: **subscription-identifier delivery** (codec-only today),
  **assigned client ids** (empty v5 id refused, not assigned), **Server Keep Alive**
  (never sent). Licensing landscape shifted under the matrix: **EMQX is BSL 1.1 since
  5.9** (single node free, clustering commercial, last Apache line 5.8 EOL'd 2026-02-28)
  — bench's EMQX 5.8.6 pin flagged for re-review at publication. VerneMQ + NanoMQ compose
  profiles, nanomq.conf, run.sh wiring, and bench/README fairness notes committed;
  smoke pending a docker host (none on the wiring machine — honestly recorded in T9).
