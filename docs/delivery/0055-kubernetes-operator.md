---
adr: "0055"
title: "The mqttd Kubernetes operator (MqttdCluster CRD, kube-rs controller)"
adr_status: Accepted
tasks:
  - id: 0055-T1
    title: Scaffold — crates/mqttd-operator (kube-rs, leader election, reconcile loop skeleton), MqttdCluster v1alpha1 spec/status schema (CRD YAML generated from Rust types), CI wiring (build/test/clippy/deny + CRD schema validated in the helm job)
    status: done
    date: 2026-08-05
    evidence: "crates/mqttd-operator: kube 4 / k8s-openapi 0.28 (jiff time), rustls-tls (single stack, ring ban still holds — cargo tree -i ring empty, deny all four gates ok with the kube tree); MqttdCluster v1alpha1 spec/status with alert-only remediation defaults (unit-tested), printcolumns, shortname mqc; CRD manifest generated as JSON (deploy/crds/, kubectl-appliable, avoids the unmaintained serde_yaml) and golden-tested against the Rust types (regenerate: cargo run -p mqttd-operator --bin gen_crd); reconcile skeleton stamps observedGeneration + Reconciled condition via patch_status; Lease-based leader gate (coordination.k8s.io, no third-party election crate). CI = workspace membership (fmt/clippy/deny/test) + the golden test; kubeconform lacks a CRD meta-schema, so live validation is the T7 kind e2e's kubectl apply. NOTE deviation from the T1 title: CRD committed as JSON not YAML, and the helm-job kubeconform idea replaced by the golden test — both recorded here"

  - id: 0055-T2
    title: Resource rendering with render-parity — operator renders StatefulSet/Services/ConfigMap/PDB from the CR; CI diffs against helm template for equivalent inputs so chart and operator cannot drift
    status: done
    date: 2026-08-05
    evidence: "crates/mqttd-operator/src/render.rs renders the chart's five objects from a CR as serde_json::Value (server-side apply takes JSON directly, so this is the production shape, not a test fixture); scripts/k8s/render-parity.sh renders both sides for equivalent inputs (shared deploy/helm/mqttd/parity-config.toml so the config block cannot diverge), normalizes ONLY by-construction differences (provenance labels helm.sh/chart + managed-by + version, metadata.namespace, chart-optional ServiceAccount/ServiceMonitor) and diffs the rest — RENDER PARITY OK on 5/5 objects. Gate proven to work: its first run caught three real drifts (missing checksum/config roll-trigger annotation — an actual operator feature gap, a dropped comment block in the init script, and a trailing-newline difference in the config), and an injected maxUnavailable change is caught with a precise unified diff. Wired into the CI helm job (+ rust-cache); render unit tests cover the object set, the operational contracts (OrderedReady, preStop decommission, PVC retention, per-pod render env), and by-path secret wiring" 
  - id: 0055-T3
    title: Observed state — /statusz + metrics polling into CR status (phase, members, clusterId, brownout, decommission) and conditions (SplitBrain, Converged, RotationInProgress) + Events; alert-only detection for split-brain and brownout
    status: done
    date: 2026-08-05
    evidence: "probe.rs reads each pod's /statusz over plain HTTP (the ADR 0054 contract — no new control surface, no admin API; body parsed via serde_json since the workspace pins reqwest with default-features off); observe.rs is PURE (gather → conditions_for/events_for, no I/O or clock) so every judgement is unit-tested. Status carries phase/readyReplicas/members/clusterId/brownout/observedGeneration; conditions Reconciled/SplitBrain/Converged/RotationInProgress/Brownout/Draining; Warning Events for SplitBrain, Brownout, ConfigDivergence. The load-bearing invariant — an unreachable pod is ABSENT EVIDENCE, never bad news — has its own regression test (a pod mid-restart must not read as a split brain or lost quorum), as does a joiner that has not adopted an identity yet (silent, not dissenting). 13 operator tests green, clippy clean, render parity still 5/5. Alert-only by design: T3 decides what is TRUE, T4's opt-in remediations decide what to do.
  - id: 0055-T4
    title: Opt-in remediations — splitBrain Fence (delete new-founder pod, quarantine PVC by label), brownout ExpandPVC (PVC sizes only, bounded by expansion.maxSize)
    status: done
    date: 2026-08-05
    evidence: "remediate.rs: PURE plan() + thin execute(). Four safety properties, each unit-tested: (1) nothing acts unless the CR opts in — defaults_never_act; (2) NO ACTION DELETES DATA — a fenced node's PVC is labelled mqttd.io/quarantined, never deleted, and the destructive wipe stays manual; (3) ambiguous evidence never acts — a_tie_is_never_fenced and unreachable_pods_that_could_flip_the_verdict_block_the_fence (the rolling-restart safety property, resting on T3's absent-evidence rule); (4) one destructive act per reconcile, everything idempotent — a_fence_preempts_an_expansion_in_the_same_pass. Fence targets the minority FOUNDER (the re-founder, not a symptom node). Expansion steps 50% bounded by expansionMaxSize, refuses entirely when no ceiling is declared (no blank cheque), clamps an overshoot; quantity parse/format refuses unknown units rather than guessing. 21 operator tests, clippy clean. ALSO caught here: kube 4.2 needs rustc 1.89 while the workspace declares 1.88 — the operator now carries its own rust-version so the BROKER's MSRV promise stays 1.88 (verified: cargo +1.88.0 check --workspace --exclude mqttd-operator). NOTE deviation from the original T4 title, recorded 2026-08-06: it also named \"seeds-override recovery\" and \"founder-PVC-loss guard before pod-0 recreation\", and NEITHER was delivered — execute() only labels the PVC and deletes the pod, and no pod-0 guard exists. The title above is corrected to what shipped; the missing work is owned by 0055-T9, and ADR 0055 §3.5 turned out to be unimplementable as written (the operator does not create pods, so there is no race-free hook before pod-0 starts — the guarantee has to be render-time). SECOND deviation note, recorded 2026-08-07 in a delivery-record audit: the original title ALSO claimed the brownout remediation raises the watermark via config roll, matching ADR 0055 section 3.2 (patch PVC sizes AND raise store_max_bytes accordingly through the config contract). That half was never built. Action::ExpandPvcs patches only spec.resources.requests.storage; store_max_bytes lives in cr.spec.config and the operator never edits config. This is a FUNCTIONAL consequence, not just wording: a user who opts into ExpandPvc gets a LARGER VOLUME AND A BROKER STILL REFUSING WRITES, because the brownout the remediation exists to clear does not clear. Exposure is bounded — remediation defaults to Alert, so this is opt-in. Title narrowed to what shipped; the config-roll half is owned by 0055-T10.
  - id: 0055-T9
    title: Founder guard — ordinal 0 renders seeds once a cluster exists (chart clusterEstablished + operator status.bootstrapped latch), bootstrapPolicy AllowRebootstrap break-glass, render-parity in both guard states
    status: done
    date: 2026-08-06
    evidence: "Founding stops being an emergent property of an empty seed list and becomes a decision. The init script (byte-identical in chart and operator) gains CLUSTER_ESTABLISHED: when set, ordinal 0 renders seeds pointing at its PEERS, so a pod-0 whose volume is lost REJOINS and back-fills (ADR 0043) instead of minting a second identity beside the survivors. Chart: clusterEstablished (default false — a first install must still bootstrap). Operator: latched from status.bootstrapped the first time exactly one identity is observed from a REACHABLE pod, monotonic after (zero identities is absent evidence; a split brain is no evidence either; neither re-arms founding — latch_bootstrapped is pure and unit-tested for all of it). spec.bootstrapPolicy: AllowRebootstrap is the break-glass for total volume loss: it CLEARS the latch rather than leaving it stale, and warns every reconcile. Carve-outs, both load-bearing: replicaCount < 2 still founds (no survivor to split from), and READY_MIN. CORRECTED 2026-08-07 (0055-T11): this originally read that the floor stays 1 for ordinal 0 because 'with seeds the node mints nothing and bootstraps no lease group', so readiness gates on real admission. That holds ONLY with durable sessions enabled — without a durable plane lease_group_ready is None and cannot gate, and member_count() counts self, so an isolated pod-0 reported members=1 against a floor of 1 and served an empty store. An armed ordinal 0 now takes the joiner's MAJORITY floor; an unarmed one keeps 1, so a founder and a single-node deployment still come Ready alone. The unstated precondition is the same defect shape this audit keeps finding. NO new SplitBrainAction variant: the guard must hold under the DEFAULT Alert-only posture, and a 'rejoin' remediation would imply the operator wiping a data dir, which the missing PVC-delete verb forbids. ADR 0055 §3.5 recorded as unimplementable as written — the operator does not create pods, so there is no race-free hook before pod-0 starts; the guarantee has to be render-time. TESTED WHERE IT WAS NOT: the render script decides which pods may found a cluster and nothing exercised it outside kind — a shell matrix now runs the real script over (ordinal, guard, replicas) and asserts the rendered seeds line, including that an armed pod-0 seeds to its peers and NEVER to itself (which would still found); its paths are overridable (TMPL_DIR/OUT_DIR) so it runs in a temp dir. Render parity runs in BOTH guard states, so the armed branch is a per-PR gate rather than nightly-only — verified it fails, naming CLUSTER_ESTABLISHED, when only one side emits it. e2e GREEN 5/5 on kind: 'founder guard held: pod-0 rejoined the SAME cluster, no split brain, no fence', and with the break-glass set 'fenced once, self-quarantined (Ready=False), data intact'."
  - id: 0055-T5
    title: Unattended gossip key rotation — three key_accept phases as Secret/config rolls, each gated on swim_keys_accepted returning to 1 and config checksum convergence
    status: planned
  - id: 0055-T6
    title: Drain-aware rolls — operator-set annotation via Downward API consulted by preStop (hook shipped identically in chart and operator paths); shrink = full drain, roll = rejoin-and-catch-up
    status: planned
  - id: 0055-T7
    title: Nightly kind e2e — install operator, apply CR, cluster forms, scale up/down with drain assertion, roll without drain, induced split-brain (founder PVC wipe) detected and fenced under Fence
    status: done
    date: 2026-08-05
    evidence: "controller.rs now OWNS what it renders: server-side apply of every object with an ownerReference to the CR (deleting the MqttdCluster garbage-collects the cluster), applied before observation so the thing being observed exists. render.rs gained the ServiceAccount the StatefulSet references — the operator rendered serviceAccountName for an SA nobody created, so the API server refused every pod; render-parity is correspondingly STRICTER (ServiceAccount out of IGNORED_KINDS: 6 objects compared, not 5). scripts/k8s/operator-e2e.sh is the runtime proof, wired as a nightly job (45 min), GREEN locally on arm64: (1) CR -> StatefulSet -> 3/3 Ready -> phase=Ready with one clusterId and objects owned; (2) a routine ROLLING RESTART provokes NO fence — the safety property, asserted BEFORE the fence is shown to work at all; (3) an induced split brain (founder PVC wiped so pod-0 re-founds) is DETECTED and FENCED with the PVC quarantined by label and still present, and the verdict HOLDS (20/20 samples over 100s). Four runtime defects only a live API server could surface, each fixed with a regression guard where one is possible: missing aws-lc-rs process CryptoProvider; Lease renewTime rejected (MicroTime needs microsecond precision — unit-tested now); reqwest::Client::new() panicking in a CA-less image (probe client built with an EMPTY root store — plaintext by ADR 0020 design, fails closed on https); and a 403 storm from a cluster-scoped watch, fixed by narrowing the watch to one namespace to match the ADR's namespaced-RBAC promise rather than widening RBAC to match the code. deploy/operator/operator.yaml is the RBAC actually proven sufficient (no cluster-admin, no Secret access, and NO delete on PVCs — the absence is the never-destroy-data guarantee). BEHAVIOUR CHANGE the e2e forced (ADR §3.1 amended): the fence now fires AT MOST ONCE per node. The first build re-fenced every 30s forever — the pod returns with the same volume, re-founds, and each delete removed it from the observation, so SplitBrain flickered True for ~25ms per cycle and no alert or human would ever have seen the incident. plan() takes the already-quarantined PVCs; test an_already_quarantined_node_is_not_fenced_again. 23 operator tests, clippy clean. TWO findings outside the operator: a full ephemeral-storage volume makes the render init container write a 0-BYTE config and exit 0, after which the broker boots on pure defaults (no listeners, node id "node-local") and only fails its probes — the render script (chart and operator, byte-identical) now fails the init container on an empty render; and issue #92, a broker-side membership flap after ANY roll that leaves a pod NotReady forever. DEVIATION from the T7 title: the scale up/down step was written and briefly WITHHELD because it reproduced #92; it is asserted again now that #92 is fixed (ADR 0016 generation amendment). The drain assertion on a scale belongs to T6 regardless."
  - id: 0055-T8
    title: Packaging + docs — deploy/helm/mqttd-operator chart (Deployment + CRD + namespaced RBAC), operator image in the ADR 0045 release pipeline (signed/reproducible/SBOM), OPERATIONS.md operator mode, COMPARISON.md Kubernetes cell update
    status: done
    date: 2026-08-14
    evidence: "Issue #252 (review panel: 'built but you cannot use it'). RELEASE PIPELINE: mqttd-operator joins scripts/release/build-repro.sh's package set and release.yml end to end, mirroring the bridge exactly — reproducible static musl binary both arches (in the byte-identical rebuild verification), staged + checksummed + SLSA-attested release asset, Dockerfile.operator (distroless/static:nonroot, binary copied in, never rebuilt in-image), per-arch push by digest, multi-arch manifest, cosign keyless image signature, provenance + SBOM attestations pushed to the registry, sbom-operator-X.Y.Z.cdx.json in the per-crate SBOM loop, and the release notes name the image + chart. CHART: deploy/helm/mqttd-operator — CRD in crds/ as a BYTE-IDENTICAL copy of the generated schema (JSON is valid YAML; a new golden test, the_chart_crd_is_the_same_bytes_as_the_generated_schema, makes drift impossible), the namespaced RBAC proven by the nightly e2e (copied from deploy/operator/operator.yaml with lockstep comments both ways; no cluster-admin, no Secret access, no PVC delete), leader-gated single-replica Deployment (downward-API POD_NAME/POD_NAMESPACE, nonroot, read-only rootfs, caps dropped), NOTES.txt with the CRD-upgrade caveat (helm never upgrades crds/), and an annotated SECURE example MqttdCluster mirroring the mqttd chart's default config (TLS + ACL + durable + majority floor — not the e2e's allow_anonymous shape, per the #254 lesson). Verified locally: helm lint clean, helm template --include-crds renders the object set, kubeconform valid. CI: the helm job lints/templates/kubeconform-validates the operator chart per PR. FORWARD PIN, gate-proven (the #263 discipline): appVersion 0.9.1 — no operator image predates that tag — and check-deploy-image-pin.sh gains property 4: the operator chart and the compose default must pin the SAME forward tag (mutation-proven: 0.9.2 in the chart fails naming both pins). FIXING THAT GATE SURFACED A LATENT #263 RESIDUE, closed here: the compose pin shipped in GIT-tag form (:v0.9.1) but the release pipeline publishes REGISTRY-form tags (VERSION strips the v; verified against GHCR — 0.9.0 is 200, v0.9.0 is 404), so on release day the reader's default would have 404'd and compose-smoke's default-image lane (which keys its skip on docker manifest inspect of that ref) would have skipped FOREVER instead of engaging. compose.yaml + bootstrap.sh + the compose README now pin :0.9.1, and the gate enforces the registry form and maps it to git tag v<tag> for the released-branch check (mutation-proven: re-pinning the v-form fails). DOCS: README limitations bullet flipped + workspace table row; COMPARISON Kubernetes row + changelog; deploy/README.md operator row now points at the chart; OPERATIONS.md gains 'Running the operator' (install, upgrade story, alert-only posture, CRD stability); RELEASING.md lists the third image + operator binary + SBOM and states the no-v image-tag form; operator.yaml's header now names itself dev/e2e and points at the chart. ADR 0055 amended (2026-08-14) including the CRD stability posture the issue's acceptance asked for: v1alpha1, schema-pinned within a release, changeable between pre-1.0 releases, chart-only path stability-conservative. RESIDUAL, stated: the acceptance closes fully at the next release — pushing v0.9.1 publishes the image; until then the chart is complete but the image pull will 404 (same posture as 0047-T12's compose pin). cargo test -p mqttd-operator green (34 tests incl. both CRD pins); render-parity untouched (the operator chart is a NEW chart, not a second render of the broker's objects)."
    notes: "Exit 1 of the issue (publish) rather than exit 2 (scope-decision): T8 was always this record's plan, every prerequisite existed (proven RBAC, golden-tested CRD, a release pipeline with an established second-image pattern), and the repo has once already refused the cheaper align-the-docs exit when nothing blocked the artifact fix (#254). The one deliberate scope line: no operator-chart entry in the kind e2e — the nightly e2e keeps installing the dev manifest (same RBAC by lockstep comment + the byte-comparison discipline), and wiring the e2e to install via the chart is a natural follow-up once the first image is published to pull."
  - id: 0055-T10
    title: ExpandPVC must also raise store_max_bytes through the config contract — expanding the volume without moving the watermark leaves the brownout it exists to clear still raised (ADR 0055 section 3.2, second half)
    status: done
    date: 2026-08-13
    evidence: "Delivered through the config contract exactly as titled (issue #228). The shared per-pod render script (operator render.rs + chart statefulset.yaml, byte-identical under the parity gate) gains a __STORE_MAX_BYTES__ placeholder, substituted from a STORE_MAX_BYTES env (absent -> 0, so templates that do not use the placeholder are unaffected). The OPERATOR computes the env per reconcile: persistence.storeMaxBytesPercent (new CRD field, opt-in) of the LARGEST observed data-PVC storage request — the same observation the #203 expansion baseline uses, fetched by data-<sts>- prefix BEFORE apply_owned — floored by persistence.size so a fresh cluster starts from the spec and the value never regresses (k8s never shrinks a PVC). An env change rolls the StatefulSet; the init container re-renders; the broker starts with the raised watermark: brownout -> ExpandPvc grows the volume -> next reconcile observes it -> watermark rises -> roll -> brownout clears, no manual config roll. The CHART takes a declared values.storeMaxBytes instead (no controller to observe with). CRD schema regenerated (gen_crd); the stale crd.rs/BrownoutAction docstrings that promised the manual roll are rewritten to describe the delivered loop. Tests: the_watermark_env_follows_the_observed_pvc_size (spec-floor fresh, rises with observed 20Gi, never lowered by a smaller observation, absent knob = absent env); the CRD schema pin; render parity green in both founder-guard states (CI). Residual: the e2e brownout-clears assertion rides the existing T11 harness's brownout case on an expandable StorageClass."

  - id: 0055-T12
    title: "ExpandPvc steps from the OBSERVED PVC request, not the static spec — the declared expansionMaxSize was unreachable after the first step (audit #203)"
    status: done
    date: 2026-08-12
    evidence: "plan_expansion computed the step baseline from cr.spec.persistence.size, which nothing updates: every reconcile recomputed the same spec*1.5 target, so a PVC took exactly one step (and was then re-patched to that same size forever) and the declared expansionMaxSize above it could never be reached — the audit's 'unreachable ceiling', moot only while T10 keeps the watermark unmoved but a correctness gap regardless. The controller now reads the data PVCs' current storage REQUESTS (one namespaced list per reconcile, parsed by the same bounded quantity parser the planner emits with) and plan() takes the observed max as the baseline, floored by the spec size so a fresh or foreign-unit PVC can never plan a shrink. Steps now ladder observed*1.5 up to the ceiling, stop exactly there (no perpetual no-op re-patching at the cap), and an operator's manual grow becomes the new baseline instead of being fought. Tests: expansion_steps_from_the_observed_size_up_to_the_ceiling (15Gi observed under a 20Gi cap plans 20Gi; 20Gi observed plans nothing; 5Gi observed is floored by the 10Gi spec), plus the pre-existing step/clamp/no-ceiling tests unchanged with observed=None (spec fallback). The crd.rs ExpandPvc docstring's 'grow PVC sizes up to persistence.expansionMaxSize' is now true as written."
  - id: 0055-T11
    title: Operator assurance — safety claims turned into gates (RBAC minimality, CR-deletion GC, controller.rs tests), the readiness floor follows the founder guard, and resilience coverage (restart mid-incident, leader failover, brownout runtime)
    status: done
    date: 2026-08-07
    evidence: "Shipped in 21d30bd (#101), c9c6ff9 (#102) and 96b6fbc (#105); recorded 2026-08-07 in a delivery-record audit. An audit found several properties CLAIMED but never verified — the same class as this ADR's own T4 title. The most load-bearing: the absence of a PVC delete verb, the never-destroy-data guarantee, lived only in a COMMENT. Now gates: auth can-i asserts the operator cannot delete PVCs and cannot read or delete Secrets, and CAN delete pods — the positive control is deliberate, because without it a blanket deny would make every negative pass vacuously (it earned its place immediately, catching a malformed verb in the first draft that would otherwise have shipped a gate asserting nothing). CR deletion now proves collection AND retention: owned objects garbage-collected, every volume kept. controller.rs went from 304 lines and ZERO tests to covering owner_reference (uid is what GC matches on — a name alone would let a recreated CR adopt the old objects) and every_rendered_kind_has_a_gvk, which reproduces the T7 ServiceAccount bug exactly when the mapping is removed. READINESS FLOOR: corrected per the T9 note above. RESILIENCE: fence-once is enforced by CLUSTER state (the PVC label) not operator memory — proven by deleting the operator mid-incident and asserting the same pod uid, unchanged quarantine count, verdict still raised; leader failover runs the Lease path with a contender for the first time and requires the survivor to actually RECONCILE, not merely take the lease; and brownout — a remediation with plan() unit tests and zero runtime coverage — is detected end to end, with expansion asserted ONLY where the StorageClass can expand and the run printing which case it took (kind's local-path reports allowVolumeExpansion empty, and a green tick from a cluster incapable of the operation would be exactly the hollow proof this exercise removes). It also answered an open question: a browned-out node stays READY. The e2e went from 3 assertions to 9; nightly timeout 45 -> 60."
---

# 0055 — Kubernetes operator: delivery

**Decision:** [ADR 0055](../adr/0055-kubernetes-operator.md). One-line story: the
ADR 0054 signals get their reconciler — a kube-rs `MqttdCluster` controller that
wraps the existing contracts (drain, `/readyz`, `/statusz`, TOML config), with every
destructive remediation opt-in and alert-only by default; the Helm chart remains the
fully-supported no-operator path, pinned to the operator by a render-parity test.

<!-- status-table:0055 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0055-T1 | ✅ done | 2026-08-05 | "crates/mqttd-operator: kube 4 / k8s-openapi 0.28 (jiff time), rustls-tls (single stack, ring ban still holds — cargo tree -i ring empty, deny all four gates ok with the kube tree); MqttdCluster v1alpha1 spec/status with alert-only remediation defaults (unit-tested), printcolumns, shortname mqc; CRD manifest generated as JSON (deploy/crds/, kubectl-appliable, avoids the unmaintained serde_yaml) and golden-tested against the Rust types (regenerate: cargo run -p mqttd-operator --bin gen_crd); reconcile skeleton stamps observedGeneration + Reconciled condition via patch_status; Lease-based leader gate (coordination.k8s.io, no third-party election crate). CI = workspace membership (fmt/clippy/deny/test) + the golden test; kubeconform lacks a CRD meta-schema, so live validation is the T7 kind e2e's kubectl apply. NOTE deviation from the T1 title: CRD committed as JSON not YAML, and the helm-job kubeconform idea replaced by the golden test — both recorded here" |
| 0055-T2 | ✅ done | 2026-08-05 | "crates/mqttd-operator/src/render.rs renders the chart's five objects from a CR as serde_json::Value (server-side apply takes JSON directly, so this is the production shape, not a test fixture); scripts/k8s/render-parity.sh renders both sides for equivalent inputs (shared deploy/helm/mqttd/parity-config.toml so the config block cannot diverge), normalizes ONLY by-construction differences (provenance labels helm.sh/chart + managed-by + version, metadata.namespace, chart-optional ServiceAccount/ServiceMonitor) and diffs the rest — RENDER PARITY OK on 5/5 objects. Gate proven to work: its first run caught three real drifts (missing checksum/config roll-trigger annotation — an actual operator feature gap, a dropped comment block in the init script, and a trailing-newline difference in the config), and an injected maxUnavailable change is caught with a precise unified diff. Wired into the CI helm job (+ rust-cache); render unit tests cover the object set, the operational contracts (OrderedReady, preStop decommission, PVC retention, per-pod render env), and by-path secret wiring" |
| 0055-T3 | ✅ done | 2026-08-05 | "probe.rs reads each pod's /statusz over plain HTTP (the ADR 0054 contract — no new control surface, no admin API; body parsed via serde_json since the workspace pins reqwest with default-features off); observe.rs is PURE (gather → conditions_for/events_for, no I/O or clock) so every judgement is unit-tested. Status carries phase/readyReplicas/members/clusterId/brownout/observedGeneration; conditions Reconciled/SplitBrain/Converged/RotationInProgress/Brownout/Draining; Warning Events for SplitBrain, Brownout, ConfigDivergence. The load-bearing invariant — an unreachable pod is ABSENT EVIDENCE, never bad news — has its own regression test (a pod mid-restart must not read as a split brain or lost quorum), as does a joiner that has not adopted an identity yet (silent, not dissenting). 13 operator tests green, clippy clean, render parity still 5/5. Alert-only by design: T3 decides what is TRUE, T4's opt-in remediations decide what to do. |
| 0055-T4 | ✅ done | 2026-08-05 | "remediate.rs: PURE plan() + thin execute(). Four safety properties, each unit-tested: (1) nothing acts unless the CR opts in — defaults_never_act; (2) NO ACTION DELETES DATA — a fenced node's PVC is labelled mqttd.io/quarantined, never deleted, and the destructive wipe stays manual; (3) ambiguous evidence never acts — a_tie_is_never_fenced and unreachable_pods_that_could_flip_the_verdict_block_the_fence (the rolling-restart safety property, resting on T3's absent-evidence rule); (4) one destructive act per reconcile, everything idempotent — a_fence_preempts_an_expansion_in_the_same_pass. Fence targets the minority FOUNDER (the re-founder, not a symptom node). Expansion steps 50% bounded by expansionMaxSize, refuses entirely when no ceiling is declared (no blank cheque), clamps an overshoot; quantity parse/format refuses unknown units rather than guessing. 21 operator tests, clippy clean. ALSO caught here: kube 4.2 needs rustc 1.89 while the workspace declares 1.88 — the operator now carries its own rust-version so the BROKER's MSRV promise stays 1.88 (verified: cargo +1.88.0 check --workspace --exclude mqttd-operator). NOTE deviation from the original T4 title, recorded 2026-08-06: it also named \"seeds-override recovery\" and \"founder-PVC-loss guard before pod-0 recreation\", and NEITHER was delivered — execute() only labels the PVC and deletes the pod, and no pod-0 guard exists. The title above is corrected to what shipped; the missing work is owned by 0055-T9, and ADR 0055 §3.5 turned out to be unimplementable as written (the operator does not create pods, so there is no race-free hook before pod-0 starts — the guarantee has to be render-time). SECOND deviation note, recorded 2026-08-07 in a delivery-record audit: the original title ALSO claimed the brownout remediation raises the watermark via config roll, matching ADR 0055 section 3.2 (patch PVC sizes AND raise store_max_bytes accordingly through the config contract). That half was never built. Action::ExpandPvcs patches only spec.resources.requests.storage; store_max_bytes lives in cr.spec.config and the operator never edits config. This is a FUNCTIONAL consequence, not just wording: a user who opts into ExpandPvc gets a LARGER VOLUME AND A BROKER STILL REFUSING WRITES, because the brownout the remediation exists to clear does not clear. Exposure is bounded — remediation defaults to Alert, so this is opt-in. Title narrowed to what shipped; the config-roll half is owned by 0055-T10. |
| 0055-T9 | ✅ done | 2026-08-06 | "Founding stops being an emergent property of an empty seed list and becomes a decision. The init script (byte-identical in chart and operator) gains CLUSTER_ESTABLISHED: when set, ordinal 0 renders seeds pointing at its PEERS, so a pod-0 whose volume is lost REJOINS and back-fills (ADR 0043) instead of minting a second identity beside the survivors. Chart: clusterEstablished (default false — a first install must still bootstrap). Operator: latched from status.bootstrapped the first time exactly one identity is observed from a REACHABLE pod, monotonic after (zero identities is absent evidence; a split brain is no evidence either; neither re-arms founding — latch_bootstrapped is pure and unit-tested for all of it). spec.bootstrapPolicy: AllowRebootstrap is the break-glass for total volume loss: it CLEARS the latch rather than leaving it stale, and warns every reconcile. Carve-outs, both load-bearing: replicaCount < 2 still founds (no survivor to split from), and READY_MIN. CORRECTED 2026-08-07 (0055-T11): this originally read that the floor stays 1 for ordinal 0 because 'with seeds the node mints nothing and bootstraps no lease group', so readiness gates on real admission. That holds ONLY with durable sessions enabled — without a durable plane lease_group_ready is None and cannot gate, and member_count() counts self, so an isolated pod-0 reported members=1 against a floor of 1 and served an empty store. An armed ordinal 0 now takes the joiner's MAJORITY floor; an unarmed one keeps 1, so a founder and a single-node deployment still come Ready alone. The unstated precondition is the same defect shape this audit keeps finding. NO new SplitBrainAction variant: the guard must hold under the DEFAULT Alert-only posture, and a 'rejoin' remediation would imply the operator wiping a data dir, which the missing PVC-delete verb forbids. ADR 0055 §3.5 recorded as unimplementable as written — the operator does not create pods, so there is no race-free hook before pod-0 starts; the guarantee has to be render-time. TESTED WHERE IT WAS NOT: the render script decides which pods may found a cluster and nothing exercised it outside kind — a shell matrix now runs the real script over (ordinal, guard, replicas) and asserts the rendered seeds line, including that an armed pod-0 seeds to its peers and NEVER to itself (which would still found); its paths are overridable (TMPL_DIR/OUT_DIR) so it runs in a temp dir. Render parity runs in BOTH guard states, so the armed branch is a per-PR gate rather than nightly-only — verified it fails, naming CLUSTER_ESTABLISHED, when only one side emits it. e2e GREEN 5/5 on kind: 'founder guard held: pod-0 rejoined the SAME cluster, no split brain, no fence', and with the break-glass set 'fenced once, self-quarantined (Ready=False), data intact'." |
| 0055-T5 | ⬜ planned | — |  |
| 0055-T6 | ⬜ planned | — |  |
| 0055-T7 | ✅ done | 2026-08-05 | "controller.rs now OWNS what it renders: server-side apply of every object with an ownerReference to the CR (deleting the MqttdCluster garbage-collects the cluster), applied before observation so the thing being observed exists. render.rs gained the ServiceAccount the StatefulSet references — the operator rendered serviceAccountName for an SA nobody created, so the API server refused every pod; render-parity is correspondingly STRICTER (ServiceAccount out of IGNORED_KINDS: 6 objects compared, not 5). scripts/k8s/operator-e2e.sh is the runtime proof, wired as a nightly job (45 min), GREEN locally on arm64: (1) CR -> StatefulSet -> 3/3 Ready -> phase=Ready with one clusterId and objects owned; (2) a routine ROLLING RESTART provokes NO fence — the safety property, asserted BEFORE the fence is shown to work at all; (3) an induced split brain (founder PVC wiped so pod-0 re-founds) is DETECTED and FENCED with the PVC quarantined by label and still present, and the verdict HOLDS (20/20 samples over 100s). Four runtime defects only a live API server could surface, each fixed with a regression guard where one is possible: missing aws-lc-rs process CryptoProvider; Lease renewTime rejected (MicroTime needs microsecond precision — unit-tested now); reqwest::Client::new() panicking in a CA-less image (probe client built with an EMPTY root store — plaintext by ADR 0020 design, fails closed on https); and a 403 storm from a cluster-scoped watch, fixed by narrowing the watch to one namespace to match the ADR's namespaced-RBAC promise rather than widening RBAC to match the code. deploy/operator/operator.yaml is the RBAC actually proven sufficient (no cluster-admin, no Secret access, and NO delete on PVCs — the absence is the never-destroy-data guarantee). BEHAVIOUR CHANGE the e2e forced (ADR §3.1 amended): the fence now fires AT MOST ONCE per node. The first build re-fenced every 30s forever — the pod returns with the same volume, re-founds, and each delete removed it from the observation, so SplitBrain flickered True for ~25ms per cycle and no alert or human would ever have seen the incident. plan() takes the already-quarantined PVCs; test an_already_quarantined_node_is_not_fenced_again. 23 operator tests, clippy clean. TWO findings outside the operator: a full ephemeral-storage volume makes the render init container write a 0-BYTE config and exit 0, after which the broker boots on pure defaults (no listeners, node id "node-local") and only fails its probes — the render script (chart and operator, byte-identical) now fails the init container on an empty render; and issue #92, a broker-side membership flap after ANY roll that leaves a pod NotReady forever. DEVIATION from the T7 title: the scale up/down step was written and briefly WITHHELD because it reproduced #92; it is asserted again now that #92 is fixed (ADR 0016 generation amendment). The drain assertion on a scale belongs to T6 regardless." |
| 0055-T8 | ✅ done | 2026-08-14 | "Issue #252 (review panel: 'built but you cannot use it'). RELEASE PIPELINE: mqttd-operator joins scripts/release/build-repro.sh's package set and release.yml end to end, mirroring the bridge exactly — reproducible static musl binary both arches (in the byte-identical rebuild verification), staged + checksummed + SLSA-attested release asset, Dockerfile.operator (distroless/static:nonroot, binary copied in, never rebuilt in-image), per-arch push by digest, multi-arch manifest, cosign keyless image signature, provenance + SBOM attestations pushed to the registry, sbom-operator-X.Y.Z.cdx.json in the per-crate SBOM loop, and the release notes name the image + chart. CHART: deploy/helm/mqttd-operator — CRD in crds/ as a BYTE-IDENTICAL copy of the generated schema (JSON is valid YAML; a new golden test, the_chart_crd_is_the_same_bytes_as_the_generated_schema, makes drift impossible), the namespaced RBAC proven by the nightly e2e (copied from deploy/operator/operator.yaml with lockstep comments both ways; no cluster-admin, no Secret access, no PVC delete), leader-gated single-replica Deployment (downward-API POD_NAME/POD_NAMESPACE, nonroot, read-only rootfs, caps dropped), NOTES.txt with the CRD-upgrade caveat (helm never upgrades crds/), and an annotated SECURE example MqttdCluster mirroring the mqttd chart's default config (TLS + ACL + durable + majority floor — not the e2e's allow_anonymous shape, per the #254 lesson). Verified locally: helm lint clean, helm template --include-crds renders the object set, kubeconform valid. CI: the helm job lints/templates/kubeconform-validates the operator chart per PR. FORWARD PIN, gate-proven (the #263 discipline): appVersion 0.9.1 — no operator image predates that tag — and check-deploy-image-pin.sh gains property 4: the operator chart and the compose default must pin the SAME forward tag (mutation-proven: 0.9.2 in the chart fails naming both pins). FIXING THAT GATE SURFACED A LATENT #263 RESIDUE, closed here: the compose pin shipped in GIT-tag form (:v0.9.1) but the release pipeline publishes REGISTRY-form tags (VERSION strips the v; verified against GHCR — 0.9.0 is 200, v0.9.0 is 404), so on release day the reader's default would have 404'd and compose-smoke's default-image lane (which keys its skip on docker manifest inspect of that ref) would have skipped FOREVER instead of engaging. compose.yaml + bootstrap.sh + the compose README now pin :0.9.1, and the gate enforces the registry form and maps it to git tag v<tag> for the released-branch check (mutation-proven: re-pinning the v-form fails). DOCS: README limitations bullet flipped + workspace table row; COMPARISON Kubernetes row + changelog; deploy/README.md operator row now points at the chart; OPERATIONS.md gains 'Running the operator' (install, upgrade story, alert-only posture, CRD stability); RELEASING.md lists the third image + operator binary + SBOM and states the no-v image-tag form; operator.yaml's header now names itself dev/e2e and points at the chart. ADR 0055 amended (2026-08-14) including the CRD stability posture the issue's acceptance asked for: v1alpha1, schema-pinned within a release, changeable between pre-1.0 releases, chart-only path stability-conservative. RESIDUAL, stated: the acceptance closes fully at the next release — pushing v0.9.1 publishes the image; until then the chart is complete but the image pull will 404 (same posture as 0047-T12's compose pin). cargo test -p mqttd-operator green (34 tests incl. both CRD pins); render-parity untouched (the operator chart is a NEW chart, not a second render of the broker's objects)." |
| 0055-T10 | ✅ done | 2026-08-13 | "Delivered through the config contract exactly as titled (issue #228). The shared per-pod render script (operator render.rs + chart statefulset.yaml, byte-identical under the parity gate) gains a __STORE_MAX_BYTES__ placeholder, substituted from a STORE_MAX_BYTES env (absent -> 0, so templates that do not use the placeholder are unaffected). The OPERATOR computes the env per reconcile: persistence.storeMaxBytesPercent (new CRD field, opt-in) of the LARGEST observed data-PVC storage request — the same observation the #203 expansion baseline uses, fetched by data-<sts>- prefix BEFORE apply_owned — floored by persistence.size so a fresh cluster starts from the spec and the value never regresses (k8s never shrinks a PVC). An env change rolls the StatefulSet; the init container re-renders; the broker starts with the raised watermark: brownout -> ExpandPvc grows the volume -> next reconcile observes it -> watermark rises -> roll -> brownout clears, no manual config roll. The CHART takes a declared values.storeMaxBytes instead (no controller to observe with). CRD schema regenerated (gen_crd); the stale crd.rs/BrownoutAction docstrings that promised the manual roll are rewritten to describe the delivered loop. Tests: the_watermark_env_follows_the_observed_pvc_size (spec-floor fresh, rises with observed 20Gi, never lowered by a smaller observation, absent knob = absent env); the CRD schema pin; render parity green in both founder-guard states (CI). Residual: the e2e brownout-clears assertion rides the existing T11 harness's brownout case on an expandable StorageClass." |
| 0055-T12 | ✅ done | 2026-08-12 | "plan_expansion computed the step baseline from cr.spec.persistence.size, which nothing updates: every reconcile recomputed the same spec*1.5 target, so a PVC took exactly one step (and was then re-patched to that same size forever) and the declared expansionMaxSize above it could never be reached — the audit's 'unreachable ceiling', moot only while T10 keeps the watermark unmoved but a correctness gap regardless. The controller now reads the data PVCs' current storage REQUESTS (one namespaced list per reconcile, parsed by the same bounded quantity parser the planner emits with) and plan() takes the observed max as the baseline, floored by the spec size so a fresh or foreign-unit PVC can never plan a shrink. Steps now ladder observed*1.5 up to the ceiling, stop exactly there (no perpetual no-op re-patching at the cap), and an operator's manual grow becomes the new baseline instead of being fought. Tests: expansion_steps_from_the_observed_size_up_to_the_ceiling (15Gi observed under a 20Gi cap plans 20Gi; 20Gi observed plans nothing; 5Gi observed is floored by the 10Gi spec), plus the pre-existing step/clamp/no-ceiling tests unchanged with observed=None (spec fallback). The crd.rs ExpandPvc docstring's 'grow PVC sizes up to persistence.expansionMaxSize' is now true as written." |
| 0055-T11 | ✅ done | 2026-08-07 | "Shipped in 21d30bd (#101), c9c6ff9 (#102) and 96b6fbc (#105); recorded 2026-08-07 in a delivery-record audit. An audit found several properties CLAIMED but never verified — the same class as this ADR's own T4 title. The most load-bearing: the absence of a PVC delete verb, the never-destroy-data guarantee, lived only in a COMMENT. Now gates: auth can-i asserts the operator cannot delete PVCs and cannot read or delete Secrets, and CAN delete pods — the positive control is deliberate, because without it a blanket deny would make every negative pass vacuously (it earned its place immediately, catching a malformed verb in the first draft that would otherwise have shipped a gate asserting nothing). CR deletion now proves collection AND retention: owned objects garbage-collected, every volume kept. controller.rs went from 304 lines and ZERO tests to covering owner_reference (uid is what GC matches on — a name alone would let a recreated CR adopt the old objects) and every_rendered_kind_has_a_gvk, which reproduces the T7 ServiceAccount bug exactly when the mapping is removed. READINESS FLOOR: corrected per the T9 note above. RESILIENCE: fence-once is enforced by CLUSTER state (the PVC label) not operator memory — proven by deleting the operator mid-incident and asserting the same pod uid, unchanged quarantine count, verdict still raised; leader failover runs the Lease path with a contender for the first time and requires the survivor to actually RECONCILE, not merely take the lease; and brownout — a remediation with plan() unit tests and zero runtime coverage — is detected end to end, with expansion asserted ONLY where the StorageClass can expand and the run printing which case it took (kind's local-path reports allowVolumeExpansion empty, and a green tick from a cluster incapable of the operation would be exactly the hollow proof this exercise removes). It also answered an open question: a browned-out node stays READY. The e2e went from 3 assertions to 9; nightly timeout 45 -> 60." |
<!-- /status-table:0055 -->

## Notes

- 2026-08-14 — **T8 done (issue #252): the operator ships.** Image in the ADR 0045
  release pipeline (signed/reproducible/SBOM, both arches), install chart
  `deploy/helm/mqttd-operator` with the CRD packaged byte-identically to the generated
  schema (a second golden test pins the copy), the e2e-proven namespaced RBAC, and a
  secure annotated example CR. Forward-pinned to `v0.9.1` and gate-held to the compose
  pin — which surfaced and closed a latent #263 residue (the compose default pinned the
  git-tag form `:v0.9.1`; the registry publishes `:0.9.1`, so release day would have
  404'd and the nightly default-image lane would have skipped forever). Docs flipped
  everywhere the old truth was stated (README ×2, COMPARISON, deploy/README,
  OPERATIONS "Running the operator", RELEASING, operator.yaml header); ADR amended with
  the CRD stability posture. Repository side complete; pushing `v0.9.1` makes the
  chart installable as-is.
- 2026-08-05 — Planned as the follow-on cycle to ADR 0054 (signals-first sequencing
  per the ADR 0047 amendment). Suggested implementation order is T1→T8 as numbered;
  T1–T3 make the operator *useful* (status + alerting), T4–T6 make it *act*, T7–T8
  make it *shippable*. Each task is one reviewable PR against the standing
  fmt/clippy/deny/test gates.
