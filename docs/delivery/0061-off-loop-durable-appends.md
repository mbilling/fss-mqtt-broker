---
adr: "0061"
title: "Off-loop durable appends: per-session lanes for the publish path"
adr_status: Accepted
tasks:
  - id: 0061-T1
    title: "RED acceptance test: a stalled group-A append must not delay a group-B publish, nor a CONNECT"
    status: done
    date: 2026-08-14
    evidence: "crates/mqttd/src/hub.rs tests — a_stalled_group_a_append_does_not_delay_a_group_b_publish, with a ParkingStore double (per-client watch-gated enqueue, ordered op log). Observed RED against the inline code first: 'a group-B publish must not wait behind group A's stalled append (#242): Elapsed(())' at hub.rs:14375 — b's ack sat in the hub queue behind a's parked await. GREEN after the lanes; the released publish still completes Accepted."
    notes: "Red-before-fix is this test's mutation proof: the reverted line is the whole inline await."
  - id: 0061-T2
    title: "Per-session append lanes: on-loop plan+submit freeze, off-loop store call, AppendDone completion, every invariant preserved"
    status: done
    date: 2026-08-14
    evidence: "crates/mqttd/src/hub.rs — submit_append/submit_passthrough/submit_lane_job (brownout arm + #238 debug_assert moved verbatim to the submit freeze point; try_send reject-newest at LANE_QUEUE_CAP=256 with publish_dropped{append-backlog-full}), append_lane_worker/run_lane_job (free fns holding only store+self_tx+metrics; LaneOutcome has no Refused variant by construction), HubCommand::AppendDone handler (durable_writes + stored bookkeeping, post-durable live send with planned-conn/replay-high-water fence, appends_outstanding gate, (node,seq) peer-verdict aggregate so a peer never hears Stored early), LaneJob::Discard for clean-start, passthrough guard for QoS0 behind a busy lane, refuse_pending withhold-while-outstanding guard, try_complete_pending appends_outstanding conjunct. All 259 mqttd lib tests green unmodified — the completion-driven rewrite changed no observable behavior the suite pins."
    notes: "Reconciliation decisions and the freeze-point argument are in the ADR; the deliberate residuals (ack-path writes, replay reads, local retained.set, the bounded-replay ordering corner) are named there with the dispatch histogram as watchdog."
  - id: 0061-T3
    title: "Time-on-loop instrumentation: mqttd_hub_dispatch_seconds{command} + mqttd_append_lane_jobs, alert rows"
    status: done
    date: 2026-08-14
    evidence: "crates/mqtt-observability/src/metrics.rs — hub_dispatch_seconds labelled histogram (coarse 7-value command class, exponential buckets 100µs→~13s so the 5s repl RPC bound is on-scale) with observe_hub_dispatch rustdoc naming the alert; append_lane_jobs gauge; append-backlog-full documented in the publish_dropped reason table; exposition test pins names, label, and population. hub.rs run() wraps dispatch and the sweep arm. docs/OPERATIONS.md gains two monitoring rows (hub loop held: p99 > 0.1s sustained = page; append lane saturating: gauge growth = warn, append-backlog-full rate = page)."
  - id: 0061-T4
    title: "Invariant test battery, each mutation-proven (revert named line, run named test, observe failure, restore)"
    status: done
    date: 2026-08-14
    evidence: "hub.rs tests, all green, each reddened by its named mutation: per-session ordering (two_publishes_to_one_offline_subscriber_append_in_arrival_order; mutation: fresh lane per job in submit_lane_job → observed ['s second','s first']); brownout flip mid-append (brownout_flipped_mid_append_neither_refuses_the_committed_publish_nor_admits_the_next; mutation: plan_refusal never decides → P2 not refused AND the submit_append debug_assert tripwire fired: 'an answerable fan-out that OWED a durable append reached submit_append's brownout arm: the plan/commit invariant behind an effect-free refusal is broken (#238)' — cite the message, not a line number, which drifts with edits); wire-after-durable (an_online_persistent_subscriber_receives_the_wire_send_only_after_the_append_resolves; mutation: live send at submit time → packet on wire while parked); backpressure (a_full_append_lane_withholds_the_publisher_and_reorders_nothing; mutation: 1024x lane cap → reds at the loop-liveness barrier, 'the loop must not be parked with the appends: Elapsed(())' — the ~262k submissions exceed the 1s ping barrier before the withhold assert is reached); reconnect fence (attach_during_inflight_append_delivers_the_message_exactly_once; mutation: fence removed → zero deliveries); publisher disconnect (publisher_disconnect_mid_append_resolves_the_obligation_without_panic; mutation: unwrap on the done send → hub panicked at hub.rs:6134); shutdown drain (a_dead_hub_mid_append_still_runs_the_admitted_job_to_a_store_outcome; mutation: select! abandoning the store call on hub death → admitted write lost); QoS0 overtake (a_qos0_send_behind_a_busy_lane_does_not_overtake_the_pending_qos1_delivery; mutation: passthrough guard disabled → QoS0 overtook); metric export (hub_dispatch_time_is_exported_and_stays_flat_while_a_store_is_parked; mutation: observation call removed → count assert failed; the flat assert pins the whole publish-class distribution under 100ms while a 30s-equivalent park is live)."
  - id: 0061-T5
    title: "Docs: ADR 0061 + ADR 0017 as-delivered note, SIZING bounded-behavior math, OPERATIONS alert rows, README hot-path claim"
    status: done
    date: 2026-08-14
    evidence: "docs/adr/0061-off-loop-durable-appends.md (decision, freeze-point argument, named residuals); docs/adr/0017-durable-attach-readiness.md as-delivered note pointing here; docs/SIZING.md (lane bound in the internal-bounds table + 'where a slow write is felt' paragraph: 5s/append per session FIFO, other groups/connects unaffected); docs/OPERATIONS.md monitoring rows; README durable-sessions paragraph states the isolation and the metric. gen-status and check-readme-facts green."
  - id: 0061-T6
    title: "Round-2 remediation of the #242 verification findings: online-delivery stalls (A), refused-will ordering (B), spill/discard races (C), doc scoping (D)"
    status: done
    date: 2026-08-14
    evidence: "crates/mqttd/src/hub.rs — the second lane stage (LaneWork::RecordOutbound, OutState::AwaitingIdRecord, records_pending gate, outbound_record_done as the only QoS2-with-offset send site), reserve-at-spent packet-id blocks (HubCommand::PkidBlockReserved, single-flight), unanswerable-refused sends routed through the busy-lane passthrough, detach spill as LaneWork::Spill, discard serialized as LaneJob::Remove, LANE_CONTROL_HEADROOM=16; send chain is fn not async fn (compiler-enforced zero awaits). Mutation-proven, each (message text verbatim, line numbers omitted deliberately — they drift with edits/fmt; re-verify against the message): refused-will ordering (a_brownout_refused_will_does_not_overtake_an_inflight_qos1_append; mutation: Refused-arm send reverted to direct → 'the refused will overtook an in-flight QoS 1 append to the same subscriber (#242 finding B)'); detach spill (a_detach_spill_rides_the_lane_keeping_the_loop_live_and_replay_order; mutation: flush reverted to the inline await loop → 'the detach spill parked the hub loop (#242 finding C): Elapsed(())'); discard ghost (a_discard_with_an_inflight_append_leaves_no_ghost_queue; mutation: lane serialization disabled → 'the discard overtook an in-flight append — ghost-queue window (#242 finding C): [("remove", "s"), ("enqueue", "s ghost")]'); record-stall isolation (a_stalled_record_outbound_does_not_delay_a_group_b_publish; mutation: inline record_outbound await restored at the send site → 'a group-B publish must not wait behind group A's stalled outbound-id record (#242 finding A): Elapsed(())'); durable-before-wire (a_qos2_packet_reaches_the_wire_only_after_its_outbound_id_is_durable; mutation: tx.send moved to stage time → 'the PUBLISH went on the wire while its outbound-id record was still pending (ADR 0057 broken by the motion)'); record ordering (a_qos1_send_does_not_overtake_a_pending_qos2_record_to_the_same_client; mutation: records_pending conjunct deleted from must_queue → 'a QoS 1 send overtook a staged QoS 2 record to the same client (#242 finding A)'); record failure arm (a_failed_outbound_id_write_is_counted_and_the_next_drain_retries; mutation: push_front deleted from the Failed arm → assertion left == right failed: left: "fresh", right: "deferred" — the deferred delivery lost); reserve stall (a_stalled_packet_id_reservation_does_not_park_the_loop; mutation: inline reserve_packet_ids await restored → 'the publisher's ack gates on the append, not the reserve: Elapsed(())'); reconnect fence (a_reconnect_mid_record_never_sends_to_the_new_connection; mutation: planned-conn equality deleted from the fence → 'exactly once: the stale completion (planned for conn 1) must not also send (#242 finding A)'); QoS0 park (a_qos0_passthrough_completion_does_not_overtake_a_staged_record; mutation: QoS0 re-check deleted → 'nothing may reach the wire while the first record is parked — least of all the QoS 0 (#242 finding A)'); forged-PUBCOMP guard (a_pubcomp_for_a_staged_unsent_id_is_ignored; mutation: pub_comp early-return deleted → 'a PUBCOMP for an unsent staged id reached clear_outbound — it races the in-flight record for the same id (#242 finding A): [("enqueue", "p staged"), ("reserve", "p"), ("clear", "p 1")]'). Finding D: ADR 0061 residuals re-classed per dispatch class + named trades; OPERATIONS Hub-loop-held/lane rows re-scoped; SIZING per-session QoS2 bound + cap-sharing; README isolation claim scoped by the ack-class residuals. Gates: fmt, clippy -D warnings, mqttd lib 270/270, brownout_ack 5, durable_sessions 10, cluster_stress 11, storage/cluster/observability suites, gen-status, check-readme-facts — all green."
    notes: "The pre-round-2 code live-sent QoS2 deliveries after an ON-LOOP record_outbound await and live-sent unanswerable refusals directly; both are lane-routed now. The spill/discard fallbacks beyond cap+headroom stay loud and are named in the ADR residuals."
---

# Delivery — ADR 0061

Tracking issue: #242 (review panel, reviewer 4 — "the 3am problem"). The decision text
is [the ADR](../adr/0061-off-loop-durable-appends.md); this file records what is
actually built, task by task, with evidence.

<!-- status-table:0061 -->
| Task | Status | When | Evidence / notes |
|------|--------|------|------------------|
| 0061-T1 | ✅ done | 2026-08-14 | "crates/mqttd/src/hub.rs tests — a_stalled_group_a_append_does_not_delay_a_group_b_publish, with a ParkingStore double (per-client watch-gated enqueue, ordered op log). Observed RED against the inline code first: 'a group-B publish must not wait behind group A's stalled append (#242): Elapsed(())' at hub.rs:14375 — b's ack sat in the hub queue behind a's parked await. GREEN after the lanes; the released publish still completes Accepted." |
| 0061-T2 | ✅ done | 2026-08-14 | "crates/mqttd/src/hub.rs — submit_append/submit_passthrough/submit_lane_job (brownout arm + #238 debug_assert moved verbatim to the submit freeze point; try_send reject-newest at LANE_QUEUE_CAP=256 with publish_dropped{append-backlog-full}), append_lane_worker/run_lane_job (free fns holding only store+self_tx+metrics; LaneOutcome has no Refused variant by construction), HubCommand::AppendDone handler (durable_writes + stored bookkeeping, post-durable live send with planned-conn/replay-high-water fence, appends_outstanding gate, (node,seq) peer-verdict aggregate so a peer never hears Stored early), LaneJob::Discard for clean-start, passthrough guard for QoS0 behind a busy lane, refuse_pending withhold-while-outstanding guard, try_complete_pending appends_outstanding conjunct. All 259 mqttd lib tests green unmodified — the completion-driven rewrite changed no observable behavior the suite pins." |
| 0061-T3 | ✅ done | 2026-08-14 | "crates/mqtt-observability/src/metrics.rs — hub_dispatch_seconds labelled histogram (coarse 7-value command class, exponential buckets 100µs→~13s so the 5s repl RPC bound is on-scale) with observe_hub_dispatch rustdoc naming the alert; append_lane_jobs gauge; append-backlog-full documented in the publish_dropped reason table; exposition test pins names, label, and population. hub.rs run() wraps dispatch and the sweep arm. docs/OPERATIONS.md gains two monitoring rows (hub loop held: p99 > 0.1s sustained = page; append lane saturating: gauge growth = warn, append-backlog-full rate = page)." |
| 0061-T4 | ✅ done | 2026-08-14 | "hub.rs tests, all green, each reddened by its named mutation: per-session ordering (two_publishes_to_one_offline_subscriber_append_in_arrival_order; mutation: fresh lane per job in submit_lane_job → observed ['s second','s first']); brownout flip mid-append (brownout_flipped_mid_append_neither_refuses_the_committed_publish_nor_admits_the_next; mutation: plan_refusal never decides → P2 not refused AND the submit_append debug_assert tripwire fired: 'an answerable fan-out that OWED a durable append reached submit_append's brownout arm: the plan/commit invariant behind an effect-free refusal is broken (#238)' — cite the message, not a line number, which drifts with edits); wire-after-durable (an_online_persistent_subscriber_receives_the_wire_send_only_after_the_append_resolves; mutation: live send at submit time → packet on wire while parked); backpressure (a_full_append_lane_withholds_the_publisher_and_reorders_nothing; mutation: 1024x lane cap → reds at the loop-liveness barrier, 'the loop must not be parked with the appends: Elapsed(())' — the ~262k submissions exceed the 1s ping barrier before the withhold assert is reached); reconnect fence (attach_during_inflight_append_delivers_the_message_exactly_once; mutation: fence removed → zero deliveries); publisher disconnect (publisher_disconnect_mid_append_resolves_the_obligation_without_panic; mutation: unwrap on the done send → hub panicked at hub.rs:6134); shutdown drain (a_dead_hub_mid_append_still_runs_the_admitted_job_to_a_store_outcome; mutation: select! abandoning the store call on hub death → admitted write lost); QoS0 overtake (a_qos0_send_behind_a_busy_lane_does_not_overtake_the_pending_qos1_delivery; mutation: passthrough guard disabled → QoS0 overtook); metric export (hub_dispatch_time_is_exported_and_stays_flat_while_a_store_is_parked; mutation: observation call removed → count assert failed; the flat assert pins the whole publish-class distribution under 100ms while a 30s-equivalent park is live)." |
| 0061-T5 | ✅ done | 2026-08-14 | "docs/adr/0061-off-loop-durable-appends.md (decision, freeze-point argument, named residuals); docs/adr/0017-durable-attach-readiness.md as-delivered note pointing here; docs/SIZING.md (lane bound in the internal-bounds table + 'where a slow write is felt' paragraph: 5s/append per session FIFO, other groups/connects unaffected); docs/OPERATIONS.md monitoring rows; README durable-sessions paragraph states the isolation and the metric. gen-status and check-readme-facts green." |
| 0061-T6 | ✅ done | 2026-08-14 | "crates/mqttd/src/hub.rs — the second lane stage (LaneWork::RecordOutbound, OutState::AwaitingIdRecord, records_pending gate, outbound_record_done as the only QoS2-with-offset send site), reserve-at-spent packet-id blocks (HubCommand::PkidBlockReserved, single-flight), unanswerable-refused sends routed through the busy-lane passthrough, detach spill as LaneWork::Spill, discard serialized as LaneJob::Remove, LANE_CONTROL_HEADROOM=16; send chain is fn not async fn (compiler-enforced zero awaits). Mutation-proven, each (message text verbatim, line numbers omitted deliberately — they drift with edits/fmt; re-verify against the message): refused-will ordering (a_brownout_refused_will_does_not_overtake_an_inflight_qos1_append; mutation: Refused-arm send reverted to direct → 'the refused will overtook an in-flight QoS 1 append to the same subscriber (#242 finding B)'); detach spill (a_detach_spill_rides_the_lane_keeping_the_loop_live_and_replay_order; mutation: flush reverted to the inline await loop → 'the detach spill parked the hub loop (#242 finding C): Elapsed(())'); discard ghost (a_discard_with_an_inflight_append_leaves_no_ghost_queue; mutation: lane serialization disabled → 'the discard overtook an in-flight append — ghost-queue window (#242 finding C): [("remove", "s"), ("enqueue", "s ghost")]'); record-stall isolation (a_stalled_record_outbound_does_not_delay_a_group_b_publish; mutation: inline record_outbound await restored at the send site → 'a group-B publish must not wait behind group A's stalled outbound-id record (#242 finding A): Elapsed(())'); durable-before-wire (a_qos2_packet_reaches_the_wire_only_after_its_outbound_id_is_durable; mutation: tx.send moved to stage time → 'the PUBLISH went on the wire while its outbound-id record was still pending (ADR 0057 broken by the motion)'); record ordering (a_qos1_send_does_not_overtake_a_pending_qos2_record_to_the_same_client; mutation: records_pending conjunct deleted from must_queue → 'a QoS 1 send overtook a staged QoS 2 record to the same client (#242 finding A)'); record failure arm (a_failed_outbound_id_write_is_counted_and_the_next_drain_retries; mutation: push_front deleted from the Failed arm → assertion left == right failed: left: "fresh", right: "deferred" — the deferred delivery lost); reserve stall (a_stalled_packet_id_reservation_does_not_park_the_loop; mutation: inline reserve_packet_ids await restored → 'the publisher's ack gates on the append, not the reserve: Elapsed(())'); reconnect fence (a_reconnect_mid_record_never_sends_to_the_new_connection; mutation: planned-conn equality deleted from the fence → 'exactly once: the stale completion (planned for conn 1) must not also send (#242 finding A)'); QoS0 park (a_qos0_passthrough_completion_does_not_overtake_a_staged_record; mutation: QoS0 re-check deleted → 'nothing may reach the wire while the first record is parked — least of all the QoS 0 (#242 finding A)'); forged-PUBCOMP guard (a_pubcomp_for_a_staged_unsent_id_is_ignored; mutation: pub_comp early-return deleted → 'a PUBCOMP for an unsent staged id reached clear_outbound — it races the in-flight record for the same id (#242 finding A): [("enqueue", "p staged"), ("reserve", "p"), ("clear", "p 1")]'). Finding D: ADR 0061 residuals re-classed per dispatch class + named trades; OPERATIONS Hub-loop-held/lane rows re-scoped; SIZING per-session QoS2 bound + cap-sharing; README isolation claim scoped by the ack-class residuals. Gates: fmt, clippy -D warnings, mqttd lib 270/270, brownout_ack 5, durable_sessions 10, cluster_stress 11, storage/cluster/observability suites, gen-status, check-readme-facts — all green." |
<!-- /status-table:0061 -->

## Changelog

- 2026-08-14 (CI round) — THE ESCAPE THAT ONLY CI CAUGHT, and the one worth remembering
  from this slice: `cluster_stress::a_full_cluster_stop_start_recovers_every_acked_fact`
  failed with `lease-store … Database already open. Cannot acquire lock.` while every
  local run of the same suite passed. Root cause: a lane worker holds an `Arc` of the
  session store, whose redb handle is an exclusive lock and which transitively owns the
  cluster log and the lease store. The harness stops a node by aborting the tasks it
  spawned — its stand-in for the OS reclaiming a crashed process's handles — but the lane
  workers were spawned bare *inside* the hub, so the abort never reached them; at a
  full-cluster stop their in-flight append cannot reach quorum, so it parks for the 5 s
  replication bound holding the lock, and the restart arrives first. Before this ADR the
  append was awaited inline on the loop, where the abort killed it for free: **moving work
  off-loop silently converted an implicit teardown guarantee into one nobody owned.**
  Fixed by ownership — a `JoinSet` (`owned_tasks`) plus `Hub::spawn_owned`, through which
  EVERY store-capturing task now spawns, including three that predate this ADR
  (clean-start discard, session recovery, inherited-session scan) and were latently
  exposed. Confidence: the failing test went from 1-in-3 red to 6/6 green, and unmodified
  main measured 4/4 green first, which is what established this as a regression rather
  than a pre-existing flake. Pinned deterministically at the unit tier by
  `a_crashed_hub_releases_the_store_so_the_node_can_restart`, mutation-proven against the
  bare-spawn shape (mutation → `a lane worker outlived the crashed hub and still holds the
  session store…`). Two claims were withdrawn in the same pass rather than left standing:
  a graceful shutdown drain I wrote first was DELETED as unreachable code — the hub holds
  a clone of its own command sender, so the loop's `None` arm cannot fire and every real
  stop is an abort — and ADR 0061 §8's "a worker always runs an admitted job to a real
  store outcome even when the hub is gone" was rewritten, because at a crash stop the
  write is now abandoned (honest: the publisher's ack was withheld, so nothing was falsely
  promised, and a crash is the event the durable plane already recovers from).
- 2026-08-14 (pre-commit audit) — PROCESS SCAR, recorded because it nearly shipped: the
  pre-commit check found ONE mutation from T6's table still live in the tree — the
  `records_pending` conjunct was missing from `must_queue`, so
  `a_qos1_send_does_not_overtake_a_pending_qos2_record_to_the_same_client` failed with
  its own mutation's message (269/270). A mutate-test-restore cycle was interrupted
  between mutate and restore, and no marker comment was left behind to grep for.
  Restored (270/270). The lesson is that the paired test IS the detector — the suite
  caught it the moment it ran green-to-red — so the gate before any commit of a
  mutation-audited change is a FULL suite run plus a grep for markers, never one or the
  other. Two of T6's mutations (the QoS 0 re-check and this one) were then re-applied
  and re-observed independently, and every metric selector and dispatch-class label the
  new OPERATIONS/README/SIZING rows cite was checked to exist in code. The README
  parenthetical also gained the one publish-class residual it had omitted (the
  eviction truncate past a 10 000-entry backlog), which OPERATIONS and the ADR named.
- 2026-08-14 (round 2) — verification findings A–D remediated before commit: the
  online-delivery half joined the lanes (outbound-id records as a second lane stage,
  reserve-at-spent packet-id blocks), the unanswerable-refused send and the detach
  spill/discard paths were lane-serialized, and the ADR/OPERATIONS/SIZING/README
  claims were re-scoped to what the code does (residuals per dispatch class). Eleven
  more mutation-proven tests (T6); mqttd lib 270/270.
- 2026-08-14 — the whole slice landed in one PR: RED acceptance test first (failure
  line recorded), then the per-session lane motion, the instrumentation, the
  mutation-proven invariant battery, and the docs. Gates: fmt, clippy -D warnings,
  mqttd lib (259), brownout_ack (5), durable_sessions (10), cluster_stress (11),
  mqtt-storage + mqtt-cluster suites — all green.
