# Logistics / asset tracking — MOBILE, INTERMITTENT, STORE-AND-FORWARD.
#
# NOT RUNNABLE YET. This workload needs a rig lane that does not exist: a
# connect / publish / DISCONNECT / reconnect-and-drain cycle with queue-depth
# and resumption verification. Every other workload here is a configuration of
# an existing lane; this one is real work (its own PR).
#
# What it would measure: offline queue durability, session resumption time,
# message expiry sweeps, and cross-node session ownership (HRW) as sessions
# reconnect to a DIFFERENT node than they left.
# Tuning levers when built: MQTTD_MAX_QUEUED_MESSAGES, MQTTD_QUEUE_OVERFLOW,
#   message-expiry, MQTTD_LEASE_VOTERS.
WORKLOAD_NOT_IMPLEMENTED=1
